version: v2
modules:
  - path: proto
lint:
  use:
    - DEFAULT
  except:
    - RPC_REQUEST_RESPONSE_UNIQUE
    - ENUM_VALUE_PREFIX
  ignore:
    - deps
breaking:
  use:
    - WIRE
  ignore:
    - deps