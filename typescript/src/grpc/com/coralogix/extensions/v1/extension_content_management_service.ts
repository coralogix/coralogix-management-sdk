// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.176.1
//   protoc               v3.21.12
// source: com/coralogix/extensions/v1/extension_content_management_service.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { Observable } from "rxjs";
import { map } from "rxjs/operators";
import { ExtensionData } from "./extension";

export const protobufPackage = "com.coralogix.extensions.v1";

export interface ImportAndReleaseExtensionsResponse {
}

function createBaseImportAndReleaseExtensionsResponse(): ImportAndReleaseExtensionsResponse {
  return {};
}

export const ImportAndReleaseExtensionsResponse = {
  encode(_: ImportAndReleaseExtensionsResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ImportAndReleaseExtensionsResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImportAndReleaseExtensionsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ImportAndReleaseExtensionsResponse {
    return {};
  },

  toJSON(_: ImportAndReleaseExtensionsResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<ImportAndReleaseExtensionsResponse>, I>>(
    base?: I,
  ): ImportAndReleaseExtensionsResponse {
    return ImportAndReleaseExtensionsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ImportAndReleaseExtensionsResponse>, I>>(
    _: I,
  ): ImportAndReleaseExtensionsResponse {
    const message = createBaseImportAndReleaseExtensionsResponse();
    return message;
  },
};

export interface ExtensionContentManagementService {
  ImportAndReleaseExtensions(request: Observable<ExtensionData>): Promise<ImportAndReleaseExtensionsResponse>;
}

export const ExtensionContentManagementServiceServiceName =
  "com.coralogix.extensions.v1.ExtensionContentManagementService";
export class ExtensionContentManagementServiceClientImpl implements ExtensionContentManagementService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || ExtensionContentManagementServiceServiceName;
    this.rpc = rpc;
    this.ImportAndReleaseExtensions = this.ImportAndReleaseExtensions.bind(this);
  }
  ImportAndReleaseExtensions(request: Observable<ExtensionData>): Promise<ImportAndReleaseExtensionsResponse> {
    const data = request.pipe(map((request) => ExtensionData.encode(request).finish()));
    const promise = this.rpc.clientStreamingRequest(this.service, "ImportAndReleaseExtensions", data);
    return promise.then((data) => ImportAndReleaseExtensionsResponse.decode(_m0.Reader.create(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
  clientStreamingRequest(service: string, method: string, data: Observable<Uint8Array>): Promise<Uint8Array>;
  serverStreamingRequest(service: string, method: string, data: Uint8Array): Observable<Uint8Array>;
  bidirectionalStreamingRequest(service: string, method: string, data: Observable<Uint8Array>): Observable<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };
