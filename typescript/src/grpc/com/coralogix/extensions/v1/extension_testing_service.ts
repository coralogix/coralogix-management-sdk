// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.176.1
//   protoc               v3.21.12
// source: com/coralogix/extensions/v1/extension_testing_service.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { BoolValue, StringValue } from "../../../../google/protobuf/wrappers";
import { ExtensionData } from "./extension";

export const protobufPackage = "com.coralogix.extensions.v1";

export interface InitializeTestingRevisionRequest {
  extensionData: ExtensionData | undefined;
}

export interface InitializeTestingRevisionResponse {
}

export interface CleanupTestingRevisionRequest {
  id: string | undefined;
}

export interface CleanupTestingRevisionResponse {
}

export interface TestExtensionRevisionRequest {
  extensionData: ExtensionData | undefined;
  cleanupAfterTest: boolean | undefined;
}

export interface TestExtensionRevisionResponse {
}

function createBaseInitializeTestingRevisionRequest(): InitializeTestingRevisionRequest {
  return { extensionData: undefined };
}

export const InitializeTestingRevisionRequest = {
  encode(message: InitializeTestingRevisionRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.extensionData !== undefined) {
      ExtensionData.encode(message.extensionData, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): InitializeTestingRevisionRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInitializeTestingRevisionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.extensionData = ExtensionData.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): InitializeTestingRevisionRequest {
    return { extensionData: isSet(object.extensionData) ? ExtensionData.fromJSON(object.extensionData) : undefined };
  },

  toJSON(message: InitializeTestingRevisionRequest): unknown {
    const obj: any = {};
    if (message.extensionData !== undefined) {
      obj.extensionData = ExtensionData.toJSON(message.extensionData);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<InitializeTestingRevisionRequest>, I>>(
    base?: I,
  ): InitializeTestingRevisionRequest {
    return InitializeTestingRevisionRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InitializeTestingRevisionRequest>, I>>(
    object: I,
  ): InitializeTestingRevisionRequest {
    const message = createBaseInitializeTestingRevisionRequest();
    message.extensionData = (object.extensionData !== undefined && object.extensionData !== null)
      ? ExtensionData.fromPartial(object.extensionData)
      : undefined;
    return message;
  },
};

function createBaseInitializeTestingRevisionResponse(): InitializeTestingRevisionResponse {
  return {};
}

export const InitializeTestingRevisionResponse = {
  encode(_: InitializeTestingRevisionResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): InitializeTestingRevisionResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInitializeTestingRevisionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): InitializeTestingRevisionResponse {
    return {};
  },

  toJSON(_: InitializeTestingRevisionResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<InitializeTestingRevisionResponse>, I>>(
    base?: I,
  ): InitializeTestingRevisionResponse {
    return InitializeTestingRevisionResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InitializeTestingRevisionResponse>, I>>(
    _: I,
  ): InitializeTestingRevisionResponse {
    const message = createBaseInitializeTestingRevisionResponse();
    return message;
  },
};

function createBaseCleanupTestingRevisionRequest(): CleanupTestingRevisionRequest {
  return { id: undefined };
}

export const CleanupTestingRevisionRequest = {
  encode(message: CleanupTestingRevisionRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== undefined) {
      StringValue.encode({ value: message.id! }, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): CleanupTestingRevisionRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCleanupTestingRevisionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = StringValue.decode(reader, reader.uint32()).value;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CleanupTestingRevisionRequest {
    return { id: isSet(object.id) ? String(object.id) : undefined };
  },

  toJSON(message: CleanupTestingRevisionRequest): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = message.id;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CleanupTestingRevisionRequest>, I>>(base?: I): CleanupTestingRevisionRequest {
    return CleanupTestingRevisionRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CleanupTestingRevisionRequest>, I>>(
    object: I,
  ): CleanupTestingRevisionRequest {
    const message = createBaseCleanupTestingRevisionRequest();
    message.id = object.id ?? undefined;
    return message;
  },
};

function createBaseCleanupTestingRevisionResponse(): CleanupTestingRevisionResponse {
  return {};
}

export const CleanupTestingRevisionResponse = {
  encode(_: CleanupTestingRevisionResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): CleanupTestingRevisionResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCleanupTestingRevisionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): CleanupTestingRevisionResponse {
    return {};
  },

  toJSON(_: CleanupTestingRevisionResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<CleanupTestingRevisionResponse>, I>>(base?: I): CleanupTestingRevisionResponse {
    return CleanupTestingRevisionResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CleanupTestingRevisionResponse>, I>>(_: I): CleanupTestingRevisionResponse {
    const message = createBaseCleanupTestingRevisionResponse();
    return message;
  },
};

function createBaseTestExtensionRevisionRequest(): TestExtensionRevisionRequest {
  return { extensionData: undefined, cleanupAfterTest: undefined };
}

export const TestExtensionRevisionRequest = {
  encode(message: TestExtensionRevisionRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.extensionData !== undefined) {
      ExtensionData.encode(message.extensionData, writer.uint32(10).fork()).ldelim();
    }
    if (message.cleanupAfterTest !== undefined) {
      BoolValue.encode({ value: message.cleanupAfterTest! }, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): TestExtensionRevisionRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTestExtensionRevisionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.extensionData = ExtensionData.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.cleanupAfterTest = BoolValue.decode(reader, reader.uint32()).value;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TestExtensionRevisionRequest {
    return {
      extensionData: isSet(object.extensionData) ? ExtensionData.fromJSON(object.extensionData) : undefined,
      cleanupAfterTest: isSet(object.cleanupAfterTest) ? Boolean(object.cleanupAfterTest) : undefined,
    };
  },

  toJSON(message: TestExtensionRevisionRequest): unknown {
    const obj: any = {};
    if (message.extensionData !== undefined) {
      obj.extensionData = ExtensionData.toJSON(message.extensionData);
    }
    if (message.cleanupAfterTest !== undefined) {
      obj.cleanupAfterTest = message.cleanupAfterTest;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TestExtensionRevisionRequest>, I>>(base?: I): TestExtensionRevisionRequest {
    return TestExtensionRevisionRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TestExtensionRevisionRequest>, I>>(object: I): TestExtensionRevisionRequest {
    const message = createBaseTestExtensionRevisionRequest();
    message.extensionData = (object.extensionData !== undefined && object.extensionData !== null)
      ? ExtensionData.fromPartial(object.extensionData)
      : undefined;
    message.cleanupAfterTest = object.cleanupAfterTest ?? undefined;
    return message;
  },
};

function createBaseTestExtensionRevisionResponse(): TestExtensionRevisionResponse {
  return {};
}

export const TestExtensionRevisionResponse = {
  encode(_: TestExtensionRevisionResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): TestExtensionRevisionResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTestExtensionRevisionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): TestExtensionRevisionResponse {
    return {};
  },

  toJSON(_: TestExtensionRevisionResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<TestExtensionRevisionResponse>, I>>(base?: I): TestExtensionRevisionResponse {
    return TestExtensionRevisionResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TestExtensionRevisionResponse>, I>>(_: I): TestExtensionRevisionResponse {
    const message = createBaseTestExtensionRevisionResponse();
    return message;
  },
};

export interface ExtensionTestingService {
  InitializeTestingRevision(request: InitializeTestingRevisionRequest): Promise<InitializeTestingRevisionResponse>;
  CleanupTestingRevision(request: CleanupTestingRevisionRequest): Promise<CleanupTestingRevisionResponse>;
  TestExtensionRevision(request: TestExtensionRevisionRequest): Promise<TestExtensionRevisionResponse>;
}

export const ExtensionTestingServiceServiceName = "com.coralogix.extensions.v1.ExtensionTestingService";
export class ExtensionTestingServiceClientImpl implements ExtensionTestingService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || ExtensionTestingServiceServiceName;
    this.rpc = rpc;
    this.InitializeTestingRevision = this.InitializeTestingRevision.bind(this);
    this.CleanupTestingRevision = this.CleanupTestingRevision.bind(this);
    this.TestExtensionRevision = this.TestExtensionRevision.bind(this);
  }
  InitializeTestingRevision(request: InitializeTestingRevisionRequest): Promise<InitializeTestingRevisionResponse> {
    const data = InitializeTestingRevisionRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "InitializeTestingRevision", data);
    return promise.then((data) => InitializeTestingRevisionResponse.decode(_m0.Reader.create(data)));
  }

  CleanupTestingRevision(request: CleanupTestingRevisionRequest): Promise<CleanupTestingRevisionResponse> {
    const data = CleanupTestingRevisionRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "CleanupTestingRevision", data);
    return promise.then((data) => CleanupTestingRevisionResponse.decode(_m0.Reader.create(data)));
  }

  TestExtensionRevision(request: TestExtensionRevisionRequest): Promise<TestExtensionRevisionResponse> {
    const data = TestExtensionRevisionRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "TestExtensionRevision", data);
    return promise.then((data) => TestExtensionRevisionResponse.decode(_m0.Reader.create(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
