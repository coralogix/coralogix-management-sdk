// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.176.1
//   protoc               v3.21.12
// source: com/coralogix/integrations/v1/external_token_service.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { StringValue } from "../../../../google/protobuf/wrappers";
import { PushBasedPlatform, pushBasedPlatformFromJSON, pushBasedPlatformToJSON } from "./push_based_platform";

export const protobufPackage = "com.coralogix.integrations.v1";

export interface GenerateNewTokenRequest {
  platform: PushBasedPlatform;
}

export interface GenerateNewTokenResponse {
  token: string | undefined;
  id: string | undefined;
}

export interface UpdateTokenRequest {
  id: string | undefined;
}

export interface UpdateTokenResponse {
  token: string | undefined;
}

function createBaseGenerateNewTokenRequest(): GenerateNewTokenRequest {
  return { platform: 0 };
}

export const GenerateNewTokenRequest = {
  encode(message: GenerateNewTokenRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.platform !== 0) {
      writer.uint32(8).int32(message.platform);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GenerateNewTokenRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenerateNewTokenRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.platform = reader.int32() as any;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenerateNewTokenRequest {
    return { platform: isSet(object.platform) ? pushBasedPlatformFromJSON(object.platform) : 0 };
  },

  toJSON(message: GenerateNewTokenRequest): unknown {
    const obj: any = {};
    if (message.platform !== 0) {
      obj.platform = pushBasedPlatformToJSON(message.platform);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GenerateNewTokenRequest>, I>>(base?: I): GenerateNewTokenRequest {
    return GenerateNewTokenRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GenerateNewTokenRequest>, I>>(object: I): GenerateNewTokenRequest {
    const message = createBaseGenerateNewTokenRequest();
    message.platform = object.platform ?? 0;
    return message;
  },
};

function createBaseGenerateNewTokenResponse(): GenerateNewTokenResponse {
  return { token: undefined, id: undefined };
}

export const GenerateNewTokenResponse = {
  encode(message: GenerateNewTokenResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.token !== undefined) {
      StringValue.encode({ value: message.token! }, writer.uint32(10).fork()).ldelim();
    }
    if (message.id !== undefined) {
      StringValue.encode({ value: message.id! }, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GenerateNewTokenResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenerateNewTokenResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.token = StringValue.decode(reader, reader.uint32()).value;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.id = StringValue.decode(reader, reader.uint32()).value;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenerateNewTokenResponse {
    return {
      token: isSet(object.token) ? String(object.token) : undefined,
      id: isSet(object.id) ? String(object.id) : undefined,
    };
  },

  toJSON(message: GenerateNewTokenResponse): unknown {
    const obj: any = {};
    if (message.token !== undefined) {
      obj.token = message.token;
    }
    if (message.id !== undefined) {
      obj.id = message.id;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GenerateNewTokenResponse>, I>>(base?: I): GenerateNewTokenResponse {
    return GenerateNewTokenResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GenerateNewTokenResponse>, I>>(object: I): GenerateNewTokenResponse {
    const message = createBaseGenerateNewTokenResponse();
    message.token = object.token ?? undefined;
    message.id = object.id ?? undefined;
    return message;
  },
};

function createBaseUpdateTokenRequest(): UpdateTokenRequest {
  return { id: undefined };
}

export const UpdateTokenRequest = {
  encode(message: UpdateTokenRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== undefined) {
      StringValue.encode({ value: message.id! }, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): UpdateTokenRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateTokenRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = StringValue.decode(reader, reader.uint32()).value;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateTokenRequest {
    return { id: isSet(object.id) ? String(object.id) : undefined };
  },

  toJSON(message: UpdateTokenRequest): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = message.id;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateTokenRequest>, I>>(base?: I): UpdateTokenRequest {
    return UpdateTokenRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateTokenRequest>, I>>(object: I): UpdateTokenRequest {
    const message = createBaseUpdateTokenRequest();
    message.id = object.id ?? undefined;
    return message;
  },
};

function createBaseUpdateTokenResponse(): UpdateTokenResponse {
  return { token: undefined };
}

export const UpdateTokenResponse = {
  encode(message: UpdateTokenResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.token !== undefined) {
      StringValue.encode({ value: message.token! }, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): UpdateTokenResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateTokenResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.token = StringValue.decode(reader, reader.uint32()).value;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateTokenResponse {
    return { token: isSet(object.token) ? String(object.token) : undefined };
  },

  toJSON(message: UpdateTokenResponse): unknown {
    const obj: any = {};
    if (message.token !== undefined) {
      obj.token = message.token;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateTokenResponse>, I>>(base?: I): UpdateTokenResponse {
    return UpdateTokenResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateTokenResponse>, I>>(object: I): UpdateTokenResponse {
    const message = createBaseUpdateTokenResponse();
    message.token = object.token ?? undefined;
    return message;
  },
};

export interface ExternalTokenService {
  GenerateNewToken(request: GenerateNewTokenRequest): Promise<GenerateNewTokenResponse>;
  UpdateToken(request: UpdateTokenRequest): Promise<UpdateTokenResponse>;
}

export const ExternalTokenServiceServiceName = "com.coralogix.integrations.v1.ExternalTokenService";
export class ExternalTokenServiceClientImpl implements ExternalTokenService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || ExternalTokenServiceServiceName;
    this.rpc = rpc;
    this.GenerateNewToken = this.GenerateNewToken.bind(this);
    this.UpdateToken = this.UpdateToken.bind(this);
  }
  GenerateNewToken(request: GenerateNewTokenRequest): Promise<GenerateNewTokenResponse> {
    const data = GenerateNewTokenRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "GenerateNewToken", data);
    return promise.then((data) => GenerateNewTokenResponse.decode(_m0.Reader.create(data)));
  }

  UpdateToken(request: UpdateTokenRequest): Promise<UpdateTokenResponse> {
    const data = UpdateTokenRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "UpdateToken", data);
    return promise.then((data) => UpdateTokenResponse.decode(_m0.Reader.create(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
