// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.176.1
//   protoc               v3.21.12
// source: com/coralogix/integrations/v1/webhook.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { StringValue } from "../../../../google/protobuf/wrappers";

export const protobufPackage = "com.coralogix.integrations.v1";

export interface TimestampSourceGenerate {
}

export interface TimestampFormatAutomatic {
}

export interface TimestampFormatCustom {
  format: string | undefined;
}

export interface TimestampSourceJsonPath {
  jsonPath: string | undefined;
  automatic?: TimestampFormatAutomatic | undefined;
  custom?: TimestampFormatCustom | undefined;
}

export interface TimestampSourceRegex {
  regex: string | undefined;
  automatic?: TimestampFormatAutomatic | undefined;
  custom?: TimestampFormatCustom | undefined;
}

export interface JsonContentType {
  generate?: TimestampSourceGenerate | undefined;
  jsonPath?: TimestampSourceJsonPath | undefined;
}

export interface TextContentType {
  generate?: TimestampSourceGenerate | undefined;
  regex?: TimestampSourceRegex | undefined;
}

function createBaseTimestampSourceGenerate(): TimestampSourceGenerate {
  return {};
}

export const TimestampSourceGenerate = {
  encode(_: TimestampSourceGenerate, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): TimestampSourceGenerate {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTimestampSourceGenerate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): TimestampSourceGenerate {
    return {};
  },

  toJSON(_: TimestampSourceGenerate): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<TimestampSourceGenerate>, I>>(base?: I): TimestampSourceGenerate {
    return TimestampSourceGenerate.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TimestampSourceGenerate>, I>>(_: I): TimestampSourceGenerate {
    const message = createBaseTimestampSourceGenerate();
    return message;
  },
};

function createBaseTimestampFormatAutomatic(): TimestampFormatAutomatic {
  return {};
}

export const TimestampFormatAutomatic = {
  encode(_: TimestampFormatAutomatic, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): TimestampFormatAutomatic {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTimestampFormatAutomatic();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): TimestampFormatAutomatic {
    return {};
  },

  toJSON(_: TimestampFormatAutomatic): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<TimestampFormatAutomatic>, I>>(base?: I): TimestampFormatAutomatic {
    return TimestampFormatAutomatic.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TimestampFormatAutomatic>, I>>(_: I): TimestampFormatAutomatic {
    const message = createBaseTimestampFormatAutomatic();
    return message;
  },
};

function createBaseTimestampFormatCustom(): TimestampFormatCustom {
  return { format: undefined };
}

export const TimestampFormatCustom = {
  encode(message: TimestampFormatCustom, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.format !== undefined) {
      StringValue.encode({ value: message.format! }, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): TimestampFormatCustom {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTimestampFormatCustom();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.format = StringValue.decode(reader, reader.uint32()).value;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TimestampFormatCustom {
    return { format: isSet(object.format) ? String(object.format) : undefined };
  },

  toJSON(message: TimestampFormatCustom): unknown {
    const obj: any = {};
    if (message.format !== undefined) {
      obj.format = message.format;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TimestampFormatCustom>, I>>(base?: I): TimestampFormatCustom {
    return TimestampFormatCustom.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TimestampFormatCustom>, I>>(object: I): TimestampFormatCustom {
    const message = createBaseTimestampFormatCustom();
    message.format = object.format ?? undefined;
    return message;
  },
};

function createBaseTimestampSourceJsonPath(): TimestampSourceJsonPath {
  return { jsonPath: undefined, automatic: undefined, custom: undefined };
}

export const TimestampSourceJsonPath = {
  encode(message: TimestampSourceJsonPath, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.jsonPath !== undefined) {
      StringValue.encode({ value: message.jsonPath! }, writer.uint32(10).fork()).ldelim();
    }
    if (message.automatic !== undefined) {
      TimestampFormatAutomatic.encode(message.automatic, writer.uint32(810).fork()).ldelim();
    }
    if (message.custom !== undefined) {
      TimestampFormatCustom.encode(message.custom, writer.uint32(818).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): TimestampSourceJsonPath {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTimestampSourceJsonPath();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.jsonPath = StringValue.decode(reader, reader.uint32()).value;
          continue;
        case 101:
          if (tag !== 810) {
            break;
          }

          message.automatic = TimestampFormatAutomatic.decode(reader, reader.uint32());
          continue;
        case 102:
          if (tag !== 818) {
            break;
          }

          message.custom = TimestampFormatCustom.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TimestampSourceJsonPath {
    return {
      jsonPath: isSet(object.jsonPath) ? String(object.jsonPath) : undefined,
      automatic: isSet(object.automatic) ? TimestampFormatAutomatic.fromJSON(object.automatic) : undefined,
      custom: isSet(object.custom) ? TimestampFormatCustom.fromJSON(object.custom) : undefined,
    };
  },

  toJSON(message: TimestampSourceJsonPath): unknown {
    const obj: any = {};
    if (message.jsonPath !== undefined) {
      obj.jsonPath = message.jsonPath;
    }
    if (message.automatic !== undefined) {
      obj.automatic = TimestampFormatAutomatic.toJSON(message.automatic);
    }
    if (message.custom !== undefined) {
      obj.custom = TimestampFormatCustom.toJSON(message.custom);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TimestampSourceJsonPath>, I>>(base?: I): TimestampSourceJsonPath {
    return TimestampSourceJsonPath.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TimestampSourceJsonPath>, I>>(object: I): TimestampSourceJsonPath {
    const message = createBaseTimestampSourceJsonPath();
    message.jsonPath = object.jsonPath ?? undefined;
    message.automatic = (object.automatic !== undefined && object.automatic !== null)
      ? TimestampFormatAutomatic.fromPartial(object.automatic)
      : undefined;
    message.custom = (object.custom !== undefined && object.custom !== null)
      ? TimestampFormatCustom.fromPartial(object.custom)
      : undefined;
    return message;
  },
};

function createBaseTimestampSourceRegex(): TimestampSourceRegex {
  return { regex: undefined, automatic: undefined, custom: undefined };
}

export const TimestampSourceRegex = {
  encode(message: TimestampSourceRegex, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.regex !== undefined) {
      StringValue.encode({ value: message.regex! }, writer.uint32(10).fork()).ldelim();
    }
    if (message.automatic !== undefined) {
      TimestampFormatAutomatic.encode(message.automatic, writer.uint32(810).fork()).ldelim();
    }
    if (message.custom !== undefined) {
      TimestampFormatCustom.encode(message.custom, writer.uint32(818).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): TimestampSourceRegex {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTimestampSourceRegex();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.regex = StringValue.decode(reader, reader.uint32()).value;
          continue;
        case 101:
          if (tag !== 810) {
            break;
          }

          message.automatic = TimestampFormatAutomatic.decode(reader, reader.uint32());
          continue;
        case 102:
          if (tag !== 818) {
            break;
          }

          message.custom = TimestampFormatCustom.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TimestampSourceRegex {
    return {
      regex: isSet(object.regex) ? String(object.regex) : undefined,
      automatic: isSet(object.automatic) ? TimestampFormatAutomatic.fromJSON(object.automatic) : undefined,
      custom: isSet(object.custom) ? TimestampFormatCustom.fromJSON(object.custom) : undefined,
    };
  },

  toJSON(message: TimestampSourceRegex): unknown {
    const obj: any = {};
    if (message.regex !== undefined) {
      obj.regex = message.regex;
    }
    if (message.automatic !== undefined) {
      obj.automatic = TimestampFormatAutomatic.toJSON(message.automatic);
    }
    if (message.custom !== undefined) {
      obj.custom = TimestampFormatCustom.toJSON(message.custom);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TimestampSourceRegex>, I>>(base?: I): TimestampSourceRegex {
    return TimestampSourceRegex.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TimestampSourceRegex>, I>>(object: I): TimestampSourceRegex {
    const message = createBaseTimestampSourceRegex();
    message.regex = object.regex ?? undefined;
    message.automatic = (object.automatic !== undefined && object.automatic !== null)
      ? TimestampFormatAutomatic.fromPartial(object.automatic)
      : undefined;
    message.custom = (object.custom !== undefined && object.custom !== null)
      ? TimestampFormatCustom.fromPartial(object.custom)
      : undefined;
    return message;
  },
};

function createBaseJsonContentType(): JsonContentType {
  return { generate: undefined, jsonPath: undefined };
}

export const JsonContentType = {
  encode(message: JsonContentType, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.generate !== undefined) {
      TimestampSourceGenerate.encode(message.generate, writer.uint32(10).fork()).ldelim();
    }
    if (message.jsonPath !== undefined) {
      TimestampSourceJsonPath.encode(message.jsonPath, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): JsonContentType {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJsonContentType();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.generate = TimestampSourceGenerate.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.jsonPath = TimestampSourceJsonPath.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): JsonContentType {
    return {
      generate: isSet(object.generate) ? TimestampSourceGenerate.fromJSON(object.generate) : undefined,
      jsonPath: isSet(object.jsonPath) ? TimestampSourceJsonPath.fromJSON(object.jsonPath) : undefined,
    };
  },

  toJSON(message: JsonContentType): unknown {
    const obj: any = {};
    if (message.generate !== undefined) {
      obj.generate = TimestampSourceGenerate.toJSON(message.generate);
    }
    if (message.jsonPath !== undefined) {
      obj.jsonPath = TimestampSourceJsonPath.toJSON(message.jsonPath);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<JsonContentType>, I>>(base?: I): JsonContentType {
    return JsonContentType.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<JsonContentType>, I>>(object: I): JsonContentType {
    const message = createBaseJsonContentType();
    message.generate = (object.generate !== undefined && object.generate !== null)
      ? TimestampSourceGenerate.fromPartial(object.generate)
      : undefined;
    message.jsonPath = (object.jsonPath !== undefined && object.jsonPath !== null)
      ? TimestampSourceJsonPath.fromPartial(object.jsonPath)
      : undefined;
    return message;
  },
};

function createBaseTextContentType(): TextContentType {
  return { generate: undefined, regex: undefined };
}

export const TextContentType = {
  encode(message: TextContentType, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.generate !== undefined) {
      TimestampSourceGenerate.encode(message.generate, writer.uint32(10).fork()).ldelim();
    }
    if (message.regex !== undefined) {
      TimestampSourceRegex.encode(message.regex, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): TextContentType {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTextContentType();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.generate = TimestampSourceGenerate.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.regex = TimestampSourceRegex.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TextContentType {
    return {
      generate: isSet(object.generate) ? TimestampSourceGenerate.fromJSON(object.generate) : undefined,
      regex: isSet(object.regex) ? TimestampSourceRegex.fromJSON(object.regex) : undefined,
    };
  },

  toJSON(message: TextContentType): unknown {
    const obj: any = {};
    if (message.generate !== undefined) {
      obj.generate = TimestampSourceGenerate.toJSON(message.generate);
    }
    if (message.regex !== undefined) {
      obj.regex = TimestampSourceRegex.toJSON(message.regex);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TextContentType>, I>>(base?: I): TextContentType {
    return TextContentType.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TextContentType>, I>>(object: I): TextContentType {
    const message = createBaseTextContentType();
    message.generate = (object.generate !== undefined && object.generate !== null)
      ? TimestampSourceGenerate.fromPartial(object.generate)
      : undefined;
    message.regex = (object.regex !== undefined && object.regex !== null)
      ? TimestampSourceRegex.fromPartial(object.regex)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
