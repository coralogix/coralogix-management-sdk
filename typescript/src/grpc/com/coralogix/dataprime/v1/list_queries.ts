// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.176.1
//   protoc               v3.21.12
// source: com/coralogix/dataprime/v1/list_queries.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import Long = require("long");

export const protobufPackage = "com.coralogix.dataprime.v1";

export interface ListQueryRequest {
}

export interface RunningQuery {
  id: string;
  /** how long the query has been running (in wall clock time) */
  durationsMs: number;
  /** total aggregated duration of all completed tasks */
  totalTaskDurationMs: number;
  /** How many tasks total in the query */
  totalTasks: number;
  /** How many tasks are completed */
  completedTasks: number;
}

export interface ListQueryResponse {
  runningQueries: RunningQuery[];
}

function createBaseListQueryRequest(): ListQueryRequest {
  return {};
}

export const ListQueryRequest = {
  encode(_: ListQueryRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ListQueryRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListQueryRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ListQueryRequest {
    return {};
  },

  toJSON(_: ListQueryRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<ListQueryRequest>, I>>(base?: I): ListQueryRequest {
    return ListQueryRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListQueryRequest>, I>>(_: I): ListQueryRequest {
    const message = createBaseListQueryRequest();
    return message;
  },
};

function createBaseRunningQuery(): RunningQuery {
  return { id: "", durationsMs: 0, totalTaskDurationMs: 0, totalTasks: 0, completedTasks: 0 };
}

export const RunningQuery = {
  encode(message: RunningQuery, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.durationsMs !== 0) {
      writer.uint32(16).uint64(message.durationsMs);
    }
    if (message.totalTaskDurationMs !== 0) {
      writer.uint32(24).uint64(message.totalTaskDurationMs);
    }
    if (message.totalTasks !== 0) {
      writer.uint32(32).uint32(message.totalTasks);
    }
    if (message.completedTasks !== 0) {
      writer.uint32(40).uint32(message.completedTasks);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): RunningQuery {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRunningQuery();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.durationsMs = longToNumber(reader.uint64() as Long);
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.totalTaskDurationMs = longToNumber(reader.uint64() as Long);
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.totalTasks = reader.uint32();
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.completedTasks = reader.uint32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RunningQuery {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      durationsMs: isSet(object.durationsMs) ? globalThis.Number(object.durationsMs) : 0,
      totalTaskDurationMs: isSet(object.totalTaskDurationMs) ? globalThis.Number(object.totalTaskDurationMs) : 0,
      totalTasks: isSet(object.totalTasks) ? globalThis.Number(object.totalTasks) : 0,
      completedTasks: isSet(object.completedTasks) ? globalThis.Number(object.completedTasks) : 0,
    };
  },

  toJSON(message: RunningQuery): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.durationsMs !== 0) {
      obj.durationsMs = Math.round(message.durationsMs);
    }
    if (message.totalTaskDurationMs !== 0) {
      obj.totalTaskDurationMs = Math.round(message.totalTaskDurationMs);
    }
    if (message.totalTasks !== 0) {
      obj.totalTasks = Math.round(message.totalTasks);
    }
    if (message.completedTasks !== 0) {
      obj.completedTasks = Math.round(message.completedTasks);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RunningQuery>, I>>(base?: I): RunningQuery {
    return RunningQuery.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RunningQuery>, I>>(object: I): RunningQuery {
    const message = createBaseRunningQuery();
    message.id = object.id ?? "";
    message.durationsMs = object.durationsMs ?? 0;
    message.totalTaskDurationMs = object.totalTaskDurationMs ?? 0;
    message.totalTasks = object.totalTasks ?? 0;
    message.completedTasks = object.completedTasks ?? 0;
    return message;
  },
};

function createBaseListQueryResponse(): ListQueryResponse {
  return { runningQueries: [] };
}

export const ListQueryResponse = {
  encode(message: ListQueryResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.runningQueries) {
      RunningQuery.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ListQueryResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListQueryResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.runningQueries.push(RunningQuery.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListQueryResponse {
    return {
      runningQueries: globalThis.Array.isArray(object?.runningQueries)
        ? object.runningQueries.map((e: any) => RunningQuery.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ListQueryResponse): unknown {
    const obj: any = {};
    if (message.runningQueries?.length) {
      obj.runningQueries = message.runningQueries.map((e) => RunningQuery.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListQueryResponse>, I>>(base?: I): ListQueryResponse {
    return ListQueryResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListQueryResponse>, I>>(object: I): ListQueryResponse {
    const message = createBaseListQueryResponse();
    message.runningQueries = object.runningQueries?.map((e) => RunningQuery.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(long: Long): number {
  if (long.gt(globalThis.Number.MAX_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  return long.toNumber();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
