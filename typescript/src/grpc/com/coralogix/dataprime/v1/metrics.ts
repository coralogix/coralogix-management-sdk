// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.176.1
//   protoc               v3.21.12
// source: com/coralogix/dataprime/v1/metrics.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import Long = require("long");

export const protobufPackage = "com.coralogix.dataprime.v1";

export enum MetricType {
  METRIC_TYPE_UNSPECIFIED = 0,
  METRIC_TYPE_TIMESTAMP = 1,
  METRIC_TYPE_COUNT = 2,
  METRIC_TYPE_GAUGE = 3,
  METRIC_TYPE_TIME = 4,
  UNRECOGNIZED = -1,
}

export function metricTypeFromJSON(object: any): MetricType {
  switch (object) {
    case 0:
    case "METRIC_TYPE_UNSPECIFIED":
      return MetricType.METRIC_TYPE_UNSPECIFIED;
    case 1:
    case "METRIC_TYPE_TIMESTAMP":
      return MetricType.METRIC_TYPE_TIMESTAMP;
    case 2:
    case "METRIC_TYPE_COUNT":
      return MetricType.METRIC_TYPE_COUNT;
    case 3:
    case "METRIC_TYPE_GAUGE":
      return MetricType.METRIC_TYPE_GAUGE;
    case 4:
    case "METRIC_TYPE_TIME":
      return MetricType.METRIC_TYPE_TIME;
    case -1:
    case "UNRECOGNIZED":
    default:
      return MetricType.UNRECOGNIZED;
  }
}

export function metricTypeToJSON(object: MetricType): string {
  switch (object) {
    case MetricType.METRIC_TYPE_UNSPECIFIED:
      return "METRIC_TYPE_UNSPECIFIED";
    case MetricType.METRIC_TYPE_TIMESTAMP:
      return "METRIC_TYPE_TIMESTAMP";
    case MetricType.METRIC_TYPE_COUNT:
      return "METRIC_TYPE_COUNT";
    case MetricType.METRIC_TYPE_GAUGE:
      return "METRIC_TYPE_GAUGE";
    case MetricType.METRIC_TYPE_TIME:
      return "METRIC_TYPE_TIME";
    case MetricType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface QueryMetrics {
  queryId: string;
  stageMetrics: StageMetrics[];
}

export interface StageMetrics {
  stageId: number;
  partitionId: number;
  operatorMetrics: OperatorMetrics[];
}

export interface OperatorMetrics {
  operatorName: string;
  metrics: Metric[];
}

export interface MetricLabel {
  name: string;
  value: string;
}

export interface Metric {
  value: number;
  name: string;
  metricType: MetricType;
  labels: string[];
}

export interface GetMetricsRequest {
  queryId: string;
}

export interface GetMetricsResponse {
  metrics: QueryMetrics | undefined;
}

function createBaseQueryMetrics(): QueryMetrics {
  return { queryId: "", stageMetrics: [] };
}

export const QueryMetrics = {
  encode(message: QueryMetrics, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.queryId !== "") {
      writer.uint32(10).string(message.queryId);
    }
    for (const v of message.stageMetrics) {
      StageMetrics.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryMetrics {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryMetrics();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.queryId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.stageMetrics.push(StageMetrics.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryMetrics {
    return {
      queryId: isSet(object.queryId) ? globalThis.String(object.queryId) : "",
      stageMetrics: globalThis.Array.isArray(object?.stageMetrics)
        ? object.stageMetrics.map((e: any) => StageMetrics.fromJSON(e))
        : [],
    };
  },

  toJSON(message: QueryMetrics): unknown {
    const obj: any = {};
    if (message.queryId !== "") {
      obj.queryId = message.queryId;
    }
    if (message.stageMetrics?.length) {
      obj.stageMetrics = message.stageMetrics.map((e) => StageMetrics.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryMetrics>, I>>(base?: I): QueryMetrics {
    return QueryMetrics.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryMetrics>, I>>(object: I): QueryMetrics {
    const message = createBaseQueryMetrics();
    message.queryId = object.queryId ?? "";
    message.stageMetrics = object.stageMetrics?.map((e) => StageMetrics.fromPartial(e)) || [];
    return message;
  },
};

function createBaseStageMetrics(): StageMetrics {
  return { stageId: 0, partitionId: 0, operatorMetrics: [] };
}

export const StageMetrics = {
  encode(message: StageMetrics, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.stageId !== 0) {
      writer.uint32(8).int64(message.stageId);
    }
    if (message.partitionId !== 0) {
      writer.uint32(16).int64(message.partitionId);
    }
    for (const v of message.operatorMetrics) {
      OperatorMetrics.encode(v!, writer.uint32(26).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): StageMetrics {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStageMetrics();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.stageId = longToNumber(reader.int64() as Long);
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.partitionId = longToNumber(reader.int64() as Long);
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.operatorMetrics.push(OperatorMetrics.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StageMetrics {
    return {
      stageId: isSet(object.stageId) ? globalThis.Number(object.stageId) : 0,
      partitionId: isSet(object.partitionId) ? globalThis.Number(object.partitionId) : 0,
      operatorMetrics: globalThis.Array.isArray(object?.operatorMetrics)
        ? object.operatorMetrics.map((e: any) => OperatorMetrics.fromJSON(e))
        : [],
    };
  },

  toJSON(message: StageMetrics): unknown {
    const obj: any = {};
    if (message.stageId !== 0) {
      obj.stageId = Math.round(message.stageId);
    }
    if (message.partitionId !== 0) {
      obj.partitionId = Math.round(message.partitionId);
    }
    if (message.operatorMetrics?.length) {
      obj.operatorMetrics = message.operatorMetrics.map((e) => OperatorMetrics.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StageMetrics>, I>>(base?: I): StageMetrics {
    return StageMetrics.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StageMetrics>, I>>(object: I): StageMetrics {
    const message = createBaseStageMetrics();
    message.stageId = object.stageId ?? 0;
    message.partitionId = object.partitionId ?? 0;
    message.operatorMetrics = object.operatorMetrics?.map((e) => OperatorMetrics.fromPartial(e)) || [];
    return message;
  },
};

function createBaseOperatorMetrics(): OperatorMetrics {
  return { operatorName: "", metrics: [] };
}

export const OperatorMetrics = {
  encode(message: OperatorMetrics, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.operatorName !== "") {
      writer.uint32(10).string(message.operatorName);
    }
    for (const v of message.metrics) {
      Metric.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): OperatorMetrics {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOperatorMetrics();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.operatorName = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.metrics.push(Metric.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OperatorMetrics {
    return {
      operatorName: isSet(object.operatorName) ? globalThis.String(object.operatorName) : "",
      metrics: globalThis.Array.isArray(object?.metrics) ? object.metrics.map((e: any) => Metric.fromJSON(e)) : [],
    };
  },

  toJSON(message: OperatorMetrics): unknown {
    const obj: any = {};
    if (message.operatorName !== "") {
      obj.operatorName = message.operatorName;
    }
    if (message.metrics?.length) {
      obj.metrics = message.metrics.map((e) => Metric.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<OperatorMetrics>, I>>(base?: I): OperatorMetrics {
    return OperatorMetrics.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<OperatorMetrics>, I>>(object: I): OperatorMetrics {
    const message = createBaseOperatorMetrics();
    message.operatorName = object.operatorName ?? "";
    message.metrics = object.metrics?.map((e) => Metric.fromPartial(e)) || [];
    return message;
  },
};

function createBaseMetricLabel(): MetricLabel {
  return { name: "", value: "" };
}

export const MetricLabel = {
  encode(message: MetricLabel, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MetricLabel {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMetricLabel();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MetricLabel {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: MetricLabel): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MetricLabel>, I>>(base?: I): MetricLabel {
    return MetricLabel.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MetricLabel>, I>>(object: I): MetricLabel {
    const message = createBaseMetricLabel();
    message.name = object.name ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseMetric(): Metric {
  return { value: 0, name: "", metricType: 0, labels: [] };
}

export const Metric = {
  encode(message: Metric, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.value !== 0) {
      writer.uint32(8).int64(message.value);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.metricType !== 0) {
      writer.uint32(24).int32(message.metricType);
    }
    for (const v of message.labels) {
      writer.uint32(34).string(v!);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Metric {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMetric();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.value = longToNumber(reader.int64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.metricType = reader.int32() as any;
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.labels.push(reader.string());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Metric {
    return {
      value: isSet(object.value) ? globalThis.Number(object.value) : 0,
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      metricType: isSet(object.metricType) ? metricTypeFromJSON(object.metricType) : 0,
      labels: globalThis.Array.isArray(object?.labels) ? object.labels.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: Metric): unknown {
    const obj: any = {};
    if (message.value !== 0) {
      obj.value = Math.round(message.value);
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.metricType !== 0) {
      obj.metricType = metricTypeToJSON(message.metricType);
    }
    if (message.labels?.length) {
      obj.labels = message.labels;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Metric>, I>>(base?: I): Metric {
    return Metric.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Metric>, I>>(object: I): Metric {
    const message = createBaseMetric();
    message.value = object.value ?? 0;
    message.name = object.name ?? "";
    message.metricType = object.metricType ?? 0;
    message.labels = object.labels?.map((e) => e) || [];
    return message;
  },
};

function createBaseGetMetricsRequest(): GetMetricsRequest {
  return { queryId: "" };
}

export const GetMetricsRequest = {
  encode(message: GetMetricsRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.queryId !== "") {
      writer.uint32(10).string(message.queryId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetMetricsRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetMetricsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.queryId = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetMetricsRequest {
    return { queryId: isSet(object.queryId) ? globalThis.String(object.queryId) : "" };
  },

  toJSON(message: GetMetricsRequest): unknown {
    const obj: any = {};
    if (message.queryId !== "") {
      obj.queryId = message.queryId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetMetricsRequest>, I>>(base?: I): GetMetricsRequest {
    return GetMetricsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetMetricsRequest>, I>>(object: I): GetMetricsRequest {
    const message = createBaseGetMetricsRequest();
    message.queryId = object.queryId ?? "";
    return message;
  },
};

function createBaseGetMetricsResponse(): GetMetricsResponse {
  return { metrics: undefined };
}

export const GetMetricsResponse = {
  encode(message: GetMetricsResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.metrics !== undefined) {
      QueryMetrics.encode(message.metrics, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetMetricsResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetMetricsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.metrics = QueryMetrics.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetMetricsResponse {
    return { metrics: isSet(object.metrics) ? QueryMetrics.fromJSON(object.metrics) : undefined };
  },

  toJSON(message: GetMetricsResponse): unknown {
    const obj: any = {};
    if (message.metrics !== undefined) {
      obj.metrics = QueryMetrics.toJSON(message.metrics);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetMetricsResponse>, I>>(base?: I): GetMetricsResponse {
    return GetMetricsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetMetricsResponse>, I>>(object: I): GetMetricsResponse {
    const message = createBaseGetMetricsResponse();
    message.metrics = (object.metrics !== undefined && object.metrics !== null)
      ? QueryMetrics.fromPartial(object.metrics)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(long: Long): number {
  if (long.gt(globalThis.Number.MAX_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  return long.toNumber();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
