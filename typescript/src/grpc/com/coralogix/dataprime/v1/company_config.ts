// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.176.1
//   protoc               v3.21.12
// source: com/coralogix/dataprime/v1/company_config.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { Timestamp } from "../../../../google/protobuf/timestamp";

export const protobufPackage = "com.coralogix.dataprime.v1";

export interface GetCompanyConfigRequest {
}

export interface GetCompanyConfigResponse {
  config: CompanyConfig | undefined;
}

export interface CompanyConfig {
  migratedCompanies: MigratedCompany[];
  maxTeamsPerQuery: number;
}

export interface MigratedCompany {
  companyId: number;
  cutoff: Date | undefined;
}

function createBaseGetCompanyConfigRequest(): GetCompanyConfigRequest {
  return {};
}

export const GetCompanyConfigRequest = {
  encode(_: GetCompanyConfigRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetCompanyConfigRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetCompanyConfigRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): GetCompanyConfigRequest {
    return {};
  },

  toJSON(_: GetCompanyConfigRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<GetCompanyConfigRequest>, I>>(base?: I): GetCompanyConfigRequest {
    return GetCompanyConfigRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetCompanyConfigRequest>, I>>(_: I): GetCompanyConfigRequest {
    const message = createBaseGetCompanyConfigRequest();
    return message;
  },
};

function createBaseGetCompanyConfigResponse(): GetCompanyConfigResponse {
  return { config: undefined };
}

export const GetCompanyConfigResponse = {
  encode(message: GetCompanyConfigResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.config !== undefined) {
      CompanyConfig.encode(message.config, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetCompanyConfigResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetCompanyConfigResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.config = CompanyConfig.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetCompanyConfigResponse {
    return { config: isSet(object.config) ? CompanyConfig.fromJSON(object.config) : undefined };
  },

  toJSON(message: GetCompanyConfigResponse): unknown {
    const obj: any = {};
    if (message.config !== undefined) {
      obj.config = CompanyConfig.toJSON(message.config);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetCompanyConfigResponse>, I>>(base?: I): GetCompanyConfigResponse {
    return GetCompanyConfigResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetCompanyConfigResponse>, I>>(object: I): GetCompanyConfigResponse {
    const message = createBaseGetCompanyConfigResponse();
    message.config = (object.config !== undefined && object.config !== null)
      ? CompanyConfig.fromPartial(object.config)
      : undefined;
    return message;
  },
};

function createBaseCompanyConfig(): CompanyConfig {
  return { migratedCompanies: [], maxTeamsPerQuery: 0 };
}

export const CompanyConfig = {
  encode(message: CompanyConfig, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.migratedCompanies) {
      MigratedCompany.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    if (message.maxTeamsPerQuery !== 0) {
      writer.uint32(16).int32(message.maxTeamsPerQuery);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): CompanyConfig {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCompanyConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.migratedCompanies.push(MigratedCompany.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.maxTeamsPerQuery = reader.int32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CompanyConfig {
    return {
      migratedCompanies: globalThis.Array.isArray(object?.migratedCompanies)
        ? object.migratedCompanies.map((e: any) => MigratedCompany.fromJSON(e))
        : [],
      maxTeamsPerQuery: isSet(object.maxTeamsPerQuery) ? globalThis.Number(object.maxTeamsPerQuery) : 0,
    };
  },

  toJSON(message: CompanyConfig): unknown {
    const obj: any = {};
    if (message.migratedCompanies?.length) {
      obj.migratedCompanies = message.migratedCompanies.map((e) => MigratedCompany.toJSON(e));
    }
    if (message.maxTeamsPerQuery !== 0) {
      obj.maxTeamsPerQuery = Math.round(message.maxTeamsPerQuery);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CompanyConfig>, I>>(base?: I): CompanyConfig {
    return CompanyConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CompanyConfig>, I>>(object: I): CompanyConfig {
    const message = createBaseCompanyConfig();
    message.migratedCompanies = object.migratedCompanies?.map((e) => MigratedCompany.fromPartial(e)) || [];
    message.maxTeamsPerQuery = object.maxTeamsPerQuery ?? 0;
    return message;
  },
};

function createBaseMigratedCompany(): MigratedCompany {
  return { companyId: 0, cutoff: undefined };
}

export const MigratedCompany = {
  encode(message: MigratedCompany, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.companyId !== 0) {
      writer.uint32(8).int32(message.companyId);
    }
    if (message.cutoff !== undefined) {
      Timestamp.encode(toTimestamp(message.cutoff), writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MigratedCompany {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMigratedCompany();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.companyId = reader.int32();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.cutoff = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MigratedCompany {
    return {
      companyId: isSet(object.companyId) ? globalThis.Number(object.companyId) : 0,
      cutoff: isSet(object.cutoff) ? fromJsonTimestamp(object.cutoff) : undefined,
    };
  },

  toJSON(message: MigratedCompany): unknown {
    const obj: any = {};
    if (message.companyId !== 0) {
      obj.companyId = Math.round(message.companyId);
    }
    if (message.cutoff !== undefined) {
      obj.cutoff = message.cutoff.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MigratedCompany>, I>>(base?: I): MigratedCompany {
    return MigratedCompany.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MigratedCompany>, I>>(object: I): MigratedCompany {
    const message = createBaseMigratedCompany();
    message.companyId = object.companyId ?? 0;
    message.cutoff = object.cutoff ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
