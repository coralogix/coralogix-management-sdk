// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.176.1
//   protoc               v3.21.12
// source: com/coralogix/dataprime/v1/run_response.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { Timestamp } from "../../../../google/protobuf/timestamp";
import { Int64Value, StringValue } from "../../../../google/protobuf/wrappers";
import { KeyValue } from "./common";
import { HistogramSlice } from "./histogram";
import { Error } from "./run_error";
import { DataprimeWarning } from "./warnings";
import Long = require("long");

export const protobufPackage = "com.coralogix.dataprime.v1";

export interface RunDataprimeQueryResponse {
  error?: Error | undefined;
  logResult?:
    | LogResult
    | undefined;
  /** Might be replaced with generic histogram in the future */
  severityHistogram?: SeverityHistogram | undefined;
  count?: Count | undefined;
  keyDistribution?: KeyDistribution | undefined;
  histogram?: Histogram | undefined;
  comparedHistogram?: Histogram | undefined;
  sidebarFilters?: SideBarFilters | undefined;
  requestStatistics?: RequestStatistics | undefined;
  teamIdCounts?: TeamIdCounts | undefined;
}

export interface LogResult {
  results: LogEntry[];
  warnings: DataprimeWarning[];
  skipped: number | undefined;
}

export interface SeverityCounts {
  debugCount: number | undefined;
  infoCount: number | undefined;
  warningCount: number | undefined;
  errorCount: number | undefined;
  criticalCount: number | undefined;
  verboseCount: number | undefined;
  warnings: DataprimeWarning[];
}

export interface SeverityHistogram {
  buckets: SeverityHistogram_Bucket[];
  warnings: DataprimeWarning[];
}

export interface SeverityHistogram_Bucket {
  bucket: Date | undefined;
  severityCounts: SeverityCounts | undefined;
}

export interface DefaultTermCounts {
  termCounts: DefaultTermCounts_TermCount[];
  warnings: DataprimeWarning[];
}

export interface DefaultTermCounts_TermCount {
  term: string | undefined;
  count: number | undefined;
}

export interface SideBarFilters {
  sidebarFilter: SideBarFilter[];
  warnings: DataprimeWarning[];
}

export interface SideBarFilter {
  filterName: string | undefined;
  term: DefaultTermCounts | undefined;
}

export interface Count {
  totalCount: number | undefined;
  resultCount: number | undefined;
  warnings: DataprimeWarning[];
}

export interface KeyDistribution {
  key: string;
  distribution: { [key: string]: number };
  warnings: DataprimeWarning[];
}

export interface KeyDistribution_DistributionEntry {
  key: string;
  value: number;
}

export interface Histogram {
  values: string[];
  timestamps: HistogramSlice[];
  warnings: DataprimeWarning[];
}

export interface RequestStatistics {
  bytesScanned: number | undefined;
}

export interface TeamIdCounts {
  teamIdCounts: TeamIdCount[];
}

export interface TeamIdCount {
  term: string | undefined;
  count: number | undefined;
}

/** ------- Response entities */
export interface LogEntry {
  index: number | undefined;
  labels: KeyValue[];
  metadata: KeyValue[];
  data: string;
}

export interface Result {
  index: number | undefined;
  labels: KeyValue[];
  metadata: KeyValue[];
  data: string;
}

function createBaseRunDataprimeQueryResponse(): RunDataprimeQueryResponse {
  return {
    error: undefined,
    logResult: undefined,
    severityHistogram: undefined,
    count: undefined,
    keyDistribution: undefined,
    histogram: undefined,
    comparedHistogram: undefined,
    sidebarFilters: undefined,
    requestStatistics: undefined,
    teamIdCounts: undefined,
  };
}

export const RunDataprimeQueryResponse = {
  encode(message: RunDataprimeQueryResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.error !== undefined) {
      Error.encode(message.error, writer.uint32(10).fork()).ldelim();
    }
    if (message.logResult !== undefined) {
      LogResult.encode(message.logResult, writer.uint32(18).fork()).ldelim();
    }
    if (message.severityHistogram !== undefined) {
      SeverityHistogram.encode(message.severityHistogram, writer.uint32(26).fork()).ldelim();
    }
    if (message.count !== undefined) {
      Count.encode(message.count, writer.uint32(58).fork()).ldelim();
    }
    if (message.keyDistribution !== undefined) {
      KeyDistribution.encode(message.keyDistribution, writer.uint32(66).fork()).ldelim();
    }
    if (message.histogram !== undefined) {
      Histogram.encode(message.histogram, writer.uint32(74).fork()).ldelim();
    }
    if (message.comparedHistogram !== undefined) {
      Histogram.encode(message.comparedHistogram, writer.uint32(82).fork()).ldelim();
    }
    if (message.sidebarFilters !== undefined) {
      SideBarFilters.encode(message.sidebarFilters, writer.uint32(90).fork()).ldelim();
    }
    if (message.requestStatistics !== undefined) {
      RequestStatistics.encode(message.requestStatistics, writer.uint32(98).fork()).ldelim();
    }
    if (message.teamIdCounts !== undefined) {
      TeamIdCounts.encode(message.teamIdCounts, writer.uint32(106).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): RunDataprimeQueryResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRunDataprimeQueryResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.error = Error.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.logResult = LogResult.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.severityHistogram = SeverityHistogram.decode(reader, reader.uint32());
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.count = Count.decode(reader, reader.uint32());
          continue;
        case 8:
          if (tag !== 66) {
            break;
          }

          message.keyDistribution = KeyDistribution.decode(reader, reader.uint32());
          continue;
        case 9:
          if (tag !== 74) {
            break;
          }

          message.histogram = Histogram.decode(reader, reader.uint32());
          continue;
        case 10:
          if (tag !== 82) {
            break;
          }

          message.comparedHistogram = Histogram.decode(reader, reader.uint32());
          continue;
        case 11:
          if (tag !== 90) {
            break;
          }

          message.sidebarFilters = SideBarFilters.decode(reader, reader.uint32());
          continue;
        case 12:
          if (tag !== 98) {
            break;
          }

          message.requestStatistics = RequestStatistics.decode(reader, reader.uint32());
          continue;
        case 13:
          if (tag !== 106) {
            break;
          }

          message.teamIdCounts = TeamIdCounts.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RunDataprimeQueryResponse {
    return {
      error: isSet(object.error) ? Error.fromJSON(object.error) : undefined,
      logResult: isSet(object.logResult) ? LogResult.fromJSON(object.logResult) : undefined,
      severityHistogram: isSet(object.severityHistogram)
        ? SeverityHistogram.fromJSON(object.severityHistogram)
        : undefined,
      count: isSet(object.count) ? Count.fromJSON(object.count) : undefined,
      keyDistribution: isSet(object.keyDistribution) ? KeyDistribution.fromJSON(object.keyDistribution) : undefined,
      histogram: isSet(object.histogram) ? Histogram.fromJSON(object.histogram) : undefined,
      comparedHistogram: isSet(object.comparedHistogram) ? Histogram.fromJSON(object.comparedHistogram) : undefined,
      sidebarFilters: isSet(object.sidebarFilters) ? SideBarFilters.fromJSON(object.sidebarFilters) : undefined,
      requestStatistics: isSet(object.requestStatistics)
        ? RequestStatistics.fromJSON(object.requestStatistics)
        : undefined,
      teamIdCounts: isSet(object.teamIdCounts) ? TeamIdCounts.fromJSON(object.teamIdCounts) : undefined,
    };
  },

  toJSON(message: RunDataprimeQueryResponse): unknown {
    const obj: any = {};
    if (message.error !== undefined) {
      obj.error = Error.toJSON(message.error);
    }
    if (message.logResult !== undefined) {
      obj.logResult = LogResult.toJSON(message.logResult);
    }
    if (message.severityHistogram !== undefined) {
      obj.severityHistogram = SeverityHistogram.toJSON(message.severityHistogram);
    }
    if (message.count !== undefined) {
      obj.count = Count.toJSON(message.count);
    }
    if (message.keyDistribution !== undefined) {
      obj.keyDistribution = KeyDistribution.toJSON(message.keyDistribution);
    }
    if (message.histogram !== undefined) {
      obj.histogram = Histogram.toJSON(message.histogram);
    }
    if (message.comparedHistogram !== undefined) {
      obj.comparedHistogram = Histogram.toJSON(message.comparedHistogram);
    }
    if (message.sidebarFilters !== undefined) {
      obj.sidebarFilters = SideBarFilters.toJSON(message.sidebarFilters);
    }
    if (message.requestStatistics !== undefined) {
      obj.requestStatistics = RequestStatistics.toJSON(message.requestStatistics);
    }
    if (message.teamIdCounts !== undefined) {
      obj.teamIdCounts = TeamIdCounts.toJSON(message.teamIdCounts);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RunDataprimeQueryResponse>, I>>(base?: I): RunDataprimeQueryResponse {
    return RunDataprimeQueryResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RunDataprimeQueryResponse>, I>>(object: I): RunDataprimeQueryResponse {
    const message = createBaseRunDataprimeQueryResponse();
    message.error = (object.error !== undefined && object.error !== null) ? Error.fromPartial(object.error) : undefined;
    message.logResult = (object.logResult !== undefined && object.logResult !== null)
      ? LogResult.fromPartial(object.logResult)
      : undefined;
    message.severityHistogram = (object.severityHistogram !== undefined && object.severityHistogram !== null)
      ? SeverityHistogram.fromPartial(object.severityHistogram)
      : undefined;
    message.count = (object.count !== undefined && object.count !== null) ? Count.fromPartial(object.count) : undefined;
    message.keyDistribution = (object.keyDistribution !== undefined && object.keyDistribution !== null)
      ? KeyDistribution.fromPartial(object.keyDistribution)
      : undefined;
    message.histogram = (object.histogram !== undefined && object.histogram !== null)
      ? Histogram.fromPartial(object.histogram)
      : undefined;
    message.comparedHistogram = (object.comparedHistogram !== undefined && object.comparedHistogram !== null)
      ? Histogram.fromPartial(object.comparedHistogram)
      : undefined;
    message.sidebarFilters = (object.sidebarFilters !== undefined && object.sidebarFilters !== null)
      ? SideBarFilters.fromPartial(object.sidebarFilters)
      : undefined;
    message.requestStatistics = (object.requestStatistics !== undefined && object.requestStatistics !== null)
      ? RequestStatistics.fromPartial(object.requestStatistics)
      : undefined;
    message.teamIdCounts = (object.teamIdCounts !== undefined && object.teamIdCounts !== null)
      ? TeamIdCounts.fromPartial(object.teamIdCounts)
      : undefined;
    return message;
  },
};

function createBaseLogResult(): LogResult {
  return { results: [], warnings: [], skipped: undefined };
}

export const LogResult = {
  encode(message: LogResult, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.results) {
      LogEntry.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    for (const v of message.warnings) {
      DataprimeWarning.encode(v!, writer.uint32(26).fork()).ldelim();
    }
    if (message.skipped !== undefined) {
      Int64Value.encode({ value: message.skipped! }, writer.uint32(34).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): LogResult {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLogResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.results.push(LogEntry.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.warnings.push(DataprimeWarning.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.skipped = Int64Value.decode(reader, reader.uint32()).value;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LogResult {
    return {
      results: globalThis.Array.isArray(object?.results) ? object.results.map((e: any) => LogEntry.fromJSON(e)) : [],
      warnings: globalThis.Array.isArray(object?.warnings)
        ? object.warnings.map((e: any) => DataprimeWarning.fromJSON(e))
        : [],
      skipped: isSet(object.skipped) ? Number(object.skipped) : undefined,
    };
  },

  toJSON(message: LogResult): unknown {
    const obj: any = {};
    if (message.results?.length) {
      obj.results = message.results.map((e) => LogEntry.toJSON(e));
    }
    if (message.warnings?.length) {
      obj.warnings = message.warnings.map((e) => DataprimeWarning.toJSON(e));
    }
    if (message.skipped !== undefined) {
      obj.skipped = message.skipped;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LogResult>, I>>(base?: I): LogResult {
    return LogResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LogResult>, I>>(object: I): LogResult {
    const message = createBaseLogResult();
    message.results = object.results?.map((e) => LogEntry.fromPartial(e)) || [];
    message.warnings = object.warnings?.map((e) => DataprimeWarning.fromPartial(e)) || [];
    message.skipped = object.skipped ?? undefined;
    return message;
  },
};

function createBaseSeverityCounts(): SeverityCounts {
  return {
    debugCount: undefined,
    infoCount: undefined,
    warningCount: undefined,
    errorCount: undefined,
    criticalCount: undefined,
    verboseCount: undefined,
    warnings: [],
  };
}

export const SeverityCounts = {
  encode(message: SeverityCounts, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.debugCount !== undefined) {
      Int64Value.encode({ value: message.debugCount! }, writer.uint32(10).fork()).ldelim();
    }
    if (message.infoCount !== undefined) {
      Int64Value.encode({ value: message.infoCount! }, writer.uint32(18).fork()).ldelim();
    }
    if (message.warningCount !== undefined) {
      Int64Value.encode({ value: message.warningCount! }, writer.uint32(26).fork()).ldelim();
    }
    if (message.errorCount !== undefined) {
      Int64Value.encode({ value: message.errorCount! }, writer.uint32(34).fork()).ldelim();
    }
    if (message.criticalCount !== undefined) {
      Int64Value.encode({ value: message.criticalCount! }, writer.uint32(42).fork()).ldelim();
    }
    if (message.verboseCount !== undefined) {
      Int64Value.encode({ value: message.verboseCount! }, writer.uint32(50).fork()).ldelim();
    }
    for (const v of message.warnings) {
      DataprimeWarning.encode(v!, writer.uint32(58).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SeverityCounts {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSeverityCounts();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.debugCount = Int64Value.decode(reader, reader.uint32()).value;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.infoCount = Int64Value.decode(reader, reader.uint32()).value;
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.warningCount = Int64Value.decode(reader, reader.uint32()).value;
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.errorCount = Int64Value.decode(reader, reader.uint32()).value;
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.criticalCount = Int64Value.decode(reader, reader.uint32()).value;
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.verboseCount = Int64Value.decode(reader, reader.uint32()).value;
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.warnings.push(DataprimeWarning.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SeverityCounts {
    return {
      debugCount: isSet(object.debugCount) ? Number(object.debugCount) : undefined,
      infoCount: isSet(object.infoCount) ? Number(object.infoCount) : undefined,
      warningCount: isSet(object.warningCount) ? Number(object.warningCount) : undefined,
      errorCount: isSet(object.errorCount) ? Number(object.errorCount) : undefined,
      criticalCount: isSet(object.criticalCount) ? Number(object.criticalCount) : undefined,
      verboseCount: isSet(object.verboseCount) ? Number(object.verboseCount) : undefined,
      warnings: globalThis.Array.isArray(object?.warnings)
        ? object.warnings.map((e: any) => DataprimeWarning.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SeverityCounts): unknown {
    const obj: any = {};
    if (message.debugCount !== undefined) {
      obj.debugCount = message.debugCount;
    }
    if (message.infoCount !== undefined) {
      obj.infoCount = message.infoCount;
    }
    if (message.warningCount !== undefined) {
      obj.warningCount = message.warningCount;
    }
    if (message.errorCount !== undefined) {
      obj.errorCount = message.errorCount;
    }
    if (message.criticalCount !== undefined) {
      obj.criticalCount = message.criticalCount;
    }
    if (message.verboseCount !== undefined) {
      obj.verboseCount = message.verboseCount;
    }
    if (message.warnings?.length) {
      obj.warnings = message.warnings.map((e) => DataprimeWarning.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SeverityCounts>, I>>(base?: I): SeverityCounts {
    return SeverityCounts.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SeverityCounts>, I>>(object: I): SeverityCounts {
    const message = createBaseSeverityCounts();
    message.debugCount = object.debugCount ?? undefined;
    message.infoCount = object.infoCount ?? undefined;
    message.warningCount = object.warningCount ?? undefined;
    message.errorCount = object.errorCount ?? undefined;
    message.criticalCount = object.criticalCount ?? undefined;
    message.verboseCount = object.verboseCount ?? undefined;
    message.warnings = object.warnings?.map((e) => DataprimeWarning.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSeverityHistogram(): SeverityHistogram {
  return { buckets: [], warnings: [] };
}

export const SeverityHistogram = {
  encode(message: SeverityHistogram, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.buckets) {
      SeverityHistogram_Bucket.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    for (const v of message.warnings) {
      DataprimeWarning.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SeverityHistogram {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSeverityHistogram();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.buckets.push(SeverityHistogram_Bucket.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.warnings.push(DataprimeWarning.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SeverityHistogram {
    return {
      buckets: globalThis.Array.isArray(object?.buckets)
        ? object.buckets.map((e: any) => SeverityHistogram_Bucket.fromJSON(e))
        : [],
      warnings: globalThis.Array.isArray(object?.warnings)
        ? object.warnings.map((e: any) => DataprimeWarning.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SeverityHistogram): unknown {
    const obj: any = {};
    if (message.buckets?.length) {
      obj.buckets = message.buckets.map((e) => SeverityHistogram_Bucket.toJSON(e));
    }
    if (message.warnings?.length) {
      obj.warnings = message.warnings.map((e) => DataprimeWarning.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SeverityHistogram>, I>>(base?: I): SeverityHistogram {
    return SeverityHistogram.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SeverityHistogram>, I>>(object: I): SeverityHistogram {
    const message = createBaseSeverityHistogram();
    message.buckets = object.buckets?.map((e) => SeverityHistogram_Bucket.fromPartial(e)) || [];
    message.warnings = object.warnings?.map((e) => DataprimeWarning.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSeverityHistogram_Bucket(): SeverityHistogram_Bucket {
  return { bucket: undefined, severityCounts: undefined };
}

export const SeverityHistogram_Bucket = {
  encode(message: SeverityHistogram_Bucket, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.bucket !== undefined) {
      Timestamp.encode(toTimestamp(message.bucket), writer.uint32(10).fork()).ldelim();
    }
    if (message.severityCounts !== undefined) {
      SeverityCounts.encode(message.severityCounts, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SeverityHistogram_Bucket {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSeverityHistogram_Bucket();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.bucket = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.severityCounts = SeverityCounts.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SeverityHistogram_Bucket {
    return {
      bucket: isSet(object.bucket) ? fromJsonTimestamp(object.bucket) : undefined,
      severityCounts: isSet(object.severityCounts) ? SeverityCounts.fromJSON(object.severityCounts) : undefined,
    };
  },

  toJSON(message: SeverityHistogram_Bucket): unknown {
    const obj: any = {};
    if (message.bucket !== undefined) {
      obj.bucket = message.bucket.toISOString();
    }
    if (message.severityCounts !== undefined) {
      obj.severityCounts = SeverityCounts.toJSON(message.severityCounts);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SeverityHistogram_Bucket>, I>>(base?: I): SeverityHistogram_Bucket {
    return SeverityHistogram_Bucket.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SeverityHistogram_Bucket>, I>>(object: I): SeverityHistogram_Bucket {
    const message = createBaseSeverityHistogram_Bucket();
    message.bucket = object.bucket ?? undefined;
    message.severityCounts = (object.severityCounts !== undefined && object.severityCounts !== null)
      ? SeverityCounts.fromPartial(object.severityCounts)
      : undefined;
    return message;
  },
};

function createBaseDefaultTermCounts(): DefaultTermCounts {
  return { termCounts: [], warnings: [] };
}

export const DefaultTermCounts = {
  encode(message: DefaultTermCounts, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.termCounts) {
      DefaultTermCounts_TermCount.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    for (const v of message.warnings) {
      DataprimeWarning.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): DefaultTermCounts {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDefaultTermCounts();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.termCounts.push(DefaultTermCounts_TermCount.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.warnings.push(DataprimeWarning.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DefaultTermCounts {
    return {
      termCounts: globalThis.Array.isArray(object?.termCounts)
        ? object.termCounts.map((e: any) => DefaultTermCounts_TermCount.fromJSON(e))
        : [],
      warnings: globalThis.Array.isArray(object?.warnings)
        ? object.warnings.map((e: any) => DataprimeWarning.fromJSON(e))
        : [],
    };
  },

  toJSON(message: DefaultTermCounts): unknown {
    const obj: any = {};
    if (message.termCounts?.length) {
      obj.termCounts = message.termCounts.map((e) => DefaultTermCounts_TermCount.toJSON(e));
    }
    if (message.warnings?.length) {
      obj.warnings = message.warnings.map((e) => DataprimeWarning.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DefaultTermCounts>, I>>(base?: I): DefaultTermCounts {
    return DefaultTermCounts.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DefaultTermCounts>, I>>(object: I): DefaultTermCounts {
    const message = createBaseDefaultTermCounts();
    message.termCounts = object.termCounts?.map((e) => DefaultTermCounts_TermCount.fromPartial(e)) || [];
    message.warnings = object.warnings?.map((e) => DataprimeWarning.fromPartial(e)) || [];
    return message;
  },
};

function createBaseDefaultTermCounts_TermCount(): DefaultTermCounts_TermCount {
  return { term: undefined, count: undefined };
}

export const DefaultTermCounts_TermCount = {
  encode(message: DefaultTermCounts_TermCount, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.term !== undefined) {
      StringValue.encode({ value: message.term! }, writer.uint32(10).fork()).ldelim();
    }
    if (message.count !== undefined) {
      Int64Value.encode({ value: message.count! }, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): DefaultTermCounts_TermCount {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDefaultTermCounts_TermCount();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.term = StringValue.decode(reader, reader.uint32()).value;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.count = Int64Value.decode(reader, reader.uint32()).value;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DefaultTermCounts_TermCount {
    return {
      term: isSet(object.term) ? String(object.term) : undefined,
      count: isSet(object.count) ? Number(object.count) : undefined,
    };
  },

  toJSON(message: DefaultTermCounts_TermCount): unknown {
    const obj: any = {};
    if (message.term !== undefined) {
      obj.term = message.term;
    }
    if (message.count !== undefined) {
      obj.count = message.count;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DefaultTermCounts_TermCount>, I>>(base?: I): DefaultTermCounts_TermCount {
    return DefaultTermCounts_TermCount.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DefaultTermCounts_TermCount>, I>>(object: I): DefaultTermCounts_TermCount {
    const message = createBaseDefaultTermCounts_TermCount();
    message.term = object.term ?? undefined;
    message.count = object.count ?? undefined;
    return message;
  },
};

function createBaseSideBarFilters(): SideBarFilters {
  return { sidebarFilter: [], warnings: [] };
}

export const SideBarFilters = {
  encode(message: SideBarFilters, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.sidebarFilter) {
      SideBarFilter.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    for (const v of message.warnings) {
      DataprimeWarning.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SideBarFilters {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSideBarFilters();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.sidebarFilter.push(SideBarFilter.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.warnings.push(DataprimeWarning.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SideBarFilters {
    return {
      sidebarFilter: globalThis.Array.isArray(object?.sidebarFilter)
        ? object.sidebarFilter.map((e: any) => SideBarFilter.fromJSON(e))
        : [],
      warnings: globalThis.Array.isArray(object?.warnings)
        ? object.warnings.map((e: any) => DataprimeWarning.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SideBarFilters): unknown {
    const obj: any = {};
    if (message.sidebarFilter?.length) {
      obj.sidebarFilter = message.sidebarFilter.map((e) => SideBarFilter.toJSON(e));
    }
    if (message.warnings?.length) {
      obj.warnings = message.warnings.map((e) => DataprimeWarning.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SideBarFilters>, I>>(base?: I): SideBarFilters {
    return SideBarFilters.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SideBarFilters>, I>>(object: I): SideBarFilters {
    const message = createBaseSideBarFilters();
    message.sidebarFilter = object.sidebarFilter?.map((e) => SideBarFilter.fromPartial(e)) || [];
    message.warnings = object.warnings?.map((e) => DataprimeWarning.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSideBarFilter(): SideBarFilter {
  return { filterName: undefined, term: undefined };
}

export const SideBarFilter = {
  encode(message: SideBarFilter, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.filterName !== undefined) {
      StringValue.encode({ value: message.filterName! }, writer.uint32(10).fork()).ldelim();
    }
    if (message.term !== undefined) {
      DefaultTermCounts.encode(message.term, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SideBarFilter {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSideBarFilter();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.filterName = StringValue.decode(reader, reader.uint32()).value;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.term = DefaultTermCounts.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SideBarFilter {
    return {
      filterName: isSet(object.filterName) ? String(object.filterName) : undefined,
      term: isSet(object.term) ? DefaultTermCounts.fromJSON(object.term) : undefined,
    };
  },

  toJSON(message: SideBarFilter): unknown {
    const obj: any = {};
    if (message.filterName !== undefined) {
      obj.filterName = message.filterName;
    }
    if (message.term !== undefined) {
      obj.term = DefaultTermCounts.toJSON(message.term);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SideBarFilter>, I>>(base?: I): SideBarFilter {
    return SideBarFilter.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SideBarFilter>, I>>(object: I): SideBarFilter {
    const message = createBaseSideBarFilter();
    message.filterName = object.filterName ?? undefined;
    message.term = (object.term !== undefined && object.term !== null)
      ? DefaultTermCounts.fromPartial(object.term)
      : undefined;
    return message;
  },
};

function createBaseCount(): Count {
  return { totalCount: undefined, resultCount: undefined, warnings: [] };
}

export const Count = {
  encode(message: Count, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.totalCount !== undefined) {
      Int64Value.encode({ value: message.totalCount! }, writer.uint32(10).fork()).ldelim();
    }
    if (message.resultCount !== undefined) {
      Int64Value.encode({ value: message.resultCount! }, writer.uint32(18).fork()).ldelim();
    }
    for (const v of message.warnings) {
      DataprimeWarning.encode(v!, writer.uint32(26).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Count {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCount();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.totalCount = Int64Value.decode(reader, reader.uint32()).value;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.resultCount = Int64Value.decode(reader, reader.uint32()).value;
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.warnings.push(DataprimeWarning.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Count {
    return {
      totalCount: isSet(object.totalCount) ? Number(object.totalCount) : undefined,
      resultCount: isSet(object.resultCount) ? Number(object.resultCount) : undefined,
      warnings: globalThis.Array.isArray(object?.warnings)
        ? object.warnings.map((e: any) => DataprimeWarning.fromJSON(e))
        : [],
    };
  },

  toJSON(message: Count): unknown {
    const obj: any = {};
    if (message.totalCount !== undefined) {
      obj.totalCount = message.totalCount;
    }
    if (message.resultCount !== undefined) {
      obj.resultCount = message.resultCount;
    }
    if (message.warnings?.length) {
      obj.warnings = message.warnings.map((e) => DataprimeWarning.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Count>, I>>(base?: I): Count {
    return Count.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Count>, I>>(object: I): Count {
    const message = createBaseCount();
    message.totalCount = object.totalCount ?? undefined;
    message.resultCount = object.resultCount ?? undefined;
    message.warnings = object.warnings?.map((e) => DataprimeWarning.fromPartial(e)) || [];
    return message;
  },
};

function createBaseKeyDistribution(): KeyDistribution {
  return { key: "", distribution: {}, warnings: [] };
}

export const KeyDistribution = {
  encode(message: KeyDistribution, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    Object.entries(message.distribution).forEach(([key, value]) => {
      KeyDistribution_DistributionEntry.encode({ key: key as any, value }, writer.uint32(18).fork()).ldelim();
    });
    for (const v of message.warnings) {
      DataprimeWarning.encode(v!, writer.uint32(26).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): KeyDistribution {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKeyDistribution();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          const entry2 = KeyDistribution_DistributionEntry.decode(reader, reader.uint32());
          if (entry2.value !== undefined) {
            message.distribution[entry2.key] = entry2.value;
          }
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.warnings.push(DataprimeWarning.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KeyDistribution {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      distribution: isObject(object.distribution)
        ? Object.entries(object.distribution).reduce<{ [key: string]: number }>((acc, [key, value]) => {
          acc[key] = Number(value);
          return acc;
        }, {})
        : {},
      warnings: globalThis.Array.isArray(object?.warnings)
        ? object.warnings.map((e: any) => DataprimeWarning.fromJSON(e))
        : [],
    };
  },

  toJSON(message: KeyDistribution): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.distribution) {
      const entries = Object.entries(message.distribution);
      if (entries.length > 0) {
        obj.distribution = {};
        entries.forEach(([k, v]) => {
          obj.distribution[k] = Math.round(v);
        });
      }
    }
    if (message.warnings?.length) {
      obj.warnings = message.warnings.map((e) => DataprimeWarning.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KeyDistribution>, I>>(base?: I): KeyDistribution {
    return KeyDistribution.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KeyDistribution>, I>>(object: I): KeyDistribution {
    const message = createBaseKeyDistribution();
    message.key = object.key ?? "";
    message.distribution = Object.entries(object.distribution ?? {}).reduce<{ [key: string]: number }>(
      (acc, [key, value]) => {
        if (value !== undefined) {
          acc[key] = globalThis.Number(value);
        }
        return acc;
      },
      {},
    );
    message.warnings = object.warnings?.map((e) => DataprimeWarning.fromPartial(e)) || [];
    return message;
  },
};

function createBaseKeyDistribution_DistributionEntry(): KeyDistribution_DistributionEntry {
  return { key: "", value: 0 };
}

export const KeyDistribution_DistributionEntry = {
  encode(message: KeyDistribution_DistributionEntry, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== 0) {
      writer.uint32(16).int64(message.value);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): KeyDistribution_DistributionEntry {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKeyDistribution_DistributionEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.value = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KeyDistribution_DistributionEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.Number(object.value) : 0,
    };
  },

  toJSON(message: KeyDistribution_DistributionEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== 0) {
      obj.value = Math.round(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KeyDistribution_DistributionEntry>, I>>(
    base?: I,
  ): KeyDistribution_DistributionEntry {
    return KeyDistribution_DistributionEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KeyDistribution_DistributionEntry>, I>>(
    object: I,
  ): KeyDistribution_DistributionEntry {
    const message = createBaseKeyDistribution_DistributionEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? 0;
    return message;
  },
};

function createBaseHistogram(): Histogram {
  return { values: [], timestamps: [], warnings: [] };
}

export const Histogram = {
  encode(message: Histogram, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.values) {
      StringValue.encode({ value: v!! }, writer.uint32(10).fork()).ldelim();
    }
    for (const v of message.timestamps) {
      HistogramSlice.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    for (const v of message.warnings) {
      DataprimeWarning.encode(v!, writer.uint32(26).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Histogram {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHistogram();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.values.push(StringValue.decode(reader, reader.uint32()).value);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.timestamps.push(HistogramSlice.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.warnings.push(DataprimeWarning.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Histogram {
    return {
      values: globalThis.Array.isArray(object?.values) ? object.values.map((e: any) => String(e)) : [],
      timestamps: globalThis.Array.isArray(object?.timestamps)
        ? object.timestamps.map((e: any) => HistogramSlice.fromJSON(e))
        : [],
      warnings: globalThis.Array.isArray(object?.warnings)
        ? object.warnings.map((e: any) => DataprimeWarning.fromJSON(e))
        : [],
    };
  },

  toJSON(message: Histogram): unknown {
    const obj: any = {};
    if (message.values?.length) {
      obj.values = message.values;
    }
    if (message.timestamps?.length) {
      obj.timestamps = message.timestamps.map((e) => HistogramSlice.toJSON(e));
    }
    if (message.warnings?.length) {
      obj.warnings = message.warnings.map((e) => DataprimeWarning.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Histogram>, I>>(base?: I): Histogram {
    return Histogram.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Histogram>, I>>(object: I): Histogram {
    const message = createBaseHistogram();
    message.values = object.values?.map((e) => e) || [];
    message.timestamps = object.timestamps?.map((e) => HistogramSlice.fromPartial(e)) || [];
    message.warnings = object.warnings?.map((e) => DataprimeWarning.fromPartial(e)) || [];
    return message;
  },
};

function createBaseRequestStatistics(): RequestStatistics {
  return { bytesScanned: undefined };
}

export const RequestStatistics = {
  encode(message: RequestStatistics, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.bytesScanned !== undefined) {
      Int64Value.encode({ value: message.bytesScanned! }, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): RequestStatistics {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRequestStatistics();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.bytesScanned = Int64Value.decode(reader, reader.uint32()).value;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RequestStatistics {
    return { bytesScanned: isSet(object.bytesScanned) ? Number(object.bytesScanned) : undefined };
  },

  toJSON(message: RequestStatistics): unknown {
    const obj: any = {};
    if (message.bytesScanned !== undefined) {
      obj.bytesScanned = message.bytesScanned;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RequestStatistics>, I>>(base?: I): RequestStatistics {
    return RequestStatistics.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RequestStatistics>, I>>(object: I): RequestStatistics {
    const message = createBaseRequestStatistics();
    message.bytesScanned = object.bytesScanned ?? undefined;
    return message;
  },
};

function createBaseTeamIdCounts(): TeamIdCounts {
  return { teamIdCounts: [] };
}

export const TeamIdCounts = {
  encode(message: TeamIdCounts, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.teamIdCounts) {
      TeamIdCount.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): TeamIdCounts {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTeamIdCounts();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.teamIdCounts.push(TeamIdCount.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TeamIdCounts {
    return {
      teamIdCounts: globalThis.Array.isArray(object?.teamIdCounts)
        ? object.teamIdCounts.map((e: any) => TeamIdCount.fromJSON(e))
        : [],
    };
  },

  toJSON(message: TeamIdCounts): unknown {
    const obj: any = {};
    if (message.teamIdCounts?.length) {
      obj.teamIdCounts = message.teamIdCounts.map((e) => TeamIdCount.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TeamIdCounts>, I>>(base?: I): TeamIdCounts {
    return TeamIdCounts.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TeamIdCounts>, I>>(object: I): TeamIdCounts {
    const message = createBaseTeamIdCounts();
    message.teamIdCounts = object.teamIdCounts?.map((e) => TeamIdCount.fromPartial(e)) || [];
    return message;
  },
};

function createBaseTeamIdCount(): TeamIdCount {
  return { term: undefined, count: undefined };
}

export const TeamIdCount = {
  encode(message: TeamIdCount, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.term !== undefined) {
      StringValue.encode({ value: message.term! }, writer.uint32(10).fork()).ldelim();
    }
    if (message.count !== undefined) {
      Int64Value.encode({ value: message.count! }, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): TeamIdCount {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTeamIdCount();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.term = StringValue.decode(reader, reader.uint32()).value;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.count = Int64Value.decode(reader, reader.uint32()).value;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TeamIdCount {
    return {
      term: isSet(object.term) ? String(object.term) : undefined,
      count: isSet(object.count) ? Number(object.count) : undefined,
    };
  },

  toJSON(message: TeamIdCount): unknown {
    const obj: any = {};
    if (message.term !== undefined) {
      obj.term = message.term;
    }
    if (message.count !== undefined) {
      obj.count = message.count;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TeamIdCount>, I>>(base?: I): TeamIdCount {
    return TeamIdCount.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TeamIdCount>, I>>(object: I): TeamIdCount {
    const message = createBaseTeamIdCount();
    message.term = object.term ?? undefined;
    message.count = object.count ?? undefined;
    return message;
  },
};

function createBaseLogEntry(): LogEntry {
  return { index: undefined, labels: [], metadata: [], data: "" };
}

export const LogEntry = {
  encode(message: LogEntry, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.index !== undefined) {
      Int64Value.encode({ value: message.index! }, writer.uint32(10).fork()).ldelim();
    }
    for (const v of message.labels) {
      KeyValue.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    for (const v of message.metadata) {
      KeyValue.encode(v!, writer.uint32(26).fork()).ldelim();
    }
    if (message.data !== "") {
      writer.uint32(34).string(message.data);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): LogEntry {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLogEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.index = Int64Value.decode(reader, reader.uint32()).value;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.labels.push(KeyValue.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.metadata.push(KeyValue.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.data = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LogEntry {
    return {
      index: isSet(object.index) ? Number(object.index) : undefined,
      labels: globalThis.Array.isArray(object?.labels) ? object.labels.map((e: any) => KeyValue.fromJSON(e)) : [],
      metadata: globalThis.Array.isArray(object?.metadata) ? object.metadata.map((e: any) => KeyValue.fromJSON(e)) : [],
      data: isSet(object.data) ? globalThis.String(object.data) : "",
    };
  },

  toJSON(message: LogEntry): unknown {
    const obj: any = {};
    if (message.index !== undefined) {
      obj.index = message.index;
    }
    if (message.labels?.length) {
      obj.labels = message.labels.map((e) => KeyValue.toJSON(e));
    }
    if (message.metadata?.length) {
      obj.metadata = message.metadata.map((e) => KeyValue.toJSON(e));
    }
    if (message.data !== "") {
      obj.data = message.data;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LogEntry>, I>>(base?: I): LogEntry {
    return LogEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LogEntry>, I>>(object: I): LogEntry {
    const message = createBaseLogEntry();
    message.index = object.index ?? undefined;
    message.labels = object.labels?.map((e) => KeyValue.fromPartial(e)) || [];
    message.metadata = object.metadata?.map((e) => KeyValue.fromPartial(e)) || [];
    message.data = object.data ?? "";
    return message;
  },
};

function createBaseResult(): Result {
  return { index: undefined, labels: [], metadata: [], data: "" };
}

export const Result = {
  encode(message: Result, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.index !== undefined) {
      Int64Value.encode({ value: message.index! }, writer.uint32(10).fork()).ldelim();
    }
    for (const v of message.labels) {
      KeyValue.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    for (const v of message.metadata) {
      KeyValue.encode(v!, writer.uint32(26).fork()).ldelim();
    }
    if (message.data !== "") {
      writer.uint32(34).string(message.data);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Result {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.index = Int64Value.decode(reader, reader.uint32()).value;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.labels.push(KeyValue.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.metadata.push(KeyValue.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.data = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Result {
    return {
      index: isSet(object.index) ? Number(object.index) : undefined,
      labels: globalThis.Array.isArray(object?.labels) ? object.labels.map((e: any) => KeyValue.fromJSON(e)) : [],
      metadata: globalThis.Array.isArray(object?.metadata) ? object.metadata.map((e: any) => KeyValue.fromJSON(e)) : [],
      data: isSet(object.data) ? globalThis.String(object.data) : "",
    };
  },

  toJSON(message: Result): unknown {
    const obj: any = {};
    if (message.index !== undefined) {
      obj.index = message.index;
    }
    if (message.labels?.length) {
      obj.labels = message.labels.map((e) => KeyValue.toJSON(e));
    }
    if (message.metadata?.length) {
      obj.metadata = message.metadata.map((e) => KeyValue.toJSON(e));
    }
    if (message.data !== "") {
      obj.data = message.data;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Result>, I>>(base?: I): Result {
    return Result.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Result>, I>>(object: I): Result {
    const message = createBaseResult();
    message.index = object.index ?? undefined;
    message.labels = object.labels?.map((e) => KeyValue.fromPartial(e)) || [];
    message.metadata = object.metadata?.map((e) => KeyValue.fromPartial(e)) || [];
    message.data = object.data ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(long: Long): number {
  if (long.gt(globalThis.Number.MAX_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  return long.toNumber();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
