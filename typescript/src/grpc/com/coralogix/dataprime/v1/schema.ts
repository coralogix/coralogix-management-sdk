// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.176.1
//   protoc               v3.21.12
// source: com/coralogix/dataprime/v1/schema.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { Source } from "../ast/v1/query";
import { Datatype } from "../ast/v1/types";

export const protobufPackage = "com.coralogix.dataprime.v1";

export interface SchemaLookup {
  schemas: NamedSchema[];
}

export interface NamedSchema {
  source: Source | undefined;
  schema: Schema | undefined;
}

export interface Schema {
  userdataFields: SchemaField[];
  metadataFields: SchemaField[];
  labelsFields: SchemaField[];
}

export interface SchemaField {
  path: string[];
  lucenePath?: LucenePath | undefined;
  datatype?: Datatype | undefined;
}

export interface LucenePath {
  path: string[];
}

function createBaseSchemaLookup(): SchemaLookup {
  return { schemas: [] };
}

export const SchemaLookup = {
  encode(message: SchemaLookup, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.schemas) {
      NamedSchema.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SchemaLookup {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchemaLookup();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.schemas.push(NamedSchema.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchemaLookup {
    return {
      schemas: globalThis.Array.isArray(object?.schemas) ? object.schemas.map((e: any) => NamedSchema.fromJSON(e)) : [],
    };
  },

  toJSON(message: SchemaLookup): unknown {
    const obj: any = {};
    if (message.schemas?.length) {
      obj.schemas = message.schemas.map((e) => NamedSchema.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchemaLookup>, I>>(base?: I): SchemaLookup {
    return SchemaLookup.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchemaLookup>, I>>(object: I): SchemaLookup {
    const message = createBaseSchemaLookup();
    message.schemas = object.schemas?.map((e) => NamedSchema.fromPartial(e)) || [];
    return message;
  },
};

function createBaseNamedSchema(): NamedSchema {
  return { source: undefined, schema: undefined };
}

export const NamedSchema = {
  encode(message: NamedSchema, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.source !== undefined) {
      Source.encode(message.source, writer.uint32(10).fork()).ldelim();
    }
    if (message.schema !== undefined) {
      Schema.encode(message.schema, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): NamedSchema {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNamedSchema();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.source = Source.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.schema = Schema.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NamedSchema {
    return {
      source: isSet(object.source) ? Source.fromJSON(object.source) : undefined,
      schema: isSet(object.schema) ? Schema.fromJSON(object.schema) : undefined,
    };
  },

  toJSON(message: NamedSchema): unknown {
    const obj: any = {};
    if (message.source !== undefined) {
      obj.source = Source.toJSON(message.source);
    }
    if (message.schema !== undefined) {
      obj.schema = Schema.toJSON(message.schema);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<NamedSchema>, I>>(base?: I): NamedSchema {
    return NamedSchema.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<NamedSchema>, I>>(object: I): NamedSchema {
    const message = createBaseNamedSchema();
    message.source = (object.source !== undefined && object.source !== null)
      ? Source.fromPartial(object.source)
      : undefined;
    message.schema = (object.schema !== undefined && object.schema !== null)
      ? Schema.fromPartial(object.schema)
      : undefined;
    return message;
  },
};

function createBaseSchema(): Schema {
  return { userdataFields: [], metadataFields: [], labelsFields: [] };
}

export const Schema = {
  encode(message: Schema, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.userdataFields) {
      SchemaField.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    for (const v of message.metadataFields) {
      SchemaField.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    for (const v of message.labelsFields) {
      SchemaField.encode(v!, writer.uint32(26).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Schema {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchema();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.userdataFields.push(SchemaField.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.metadataFields.push(SchemaField.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.labelsFields.push(SchemaField.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Schema {
    return {
      userdataFields: globalThis.Array.isArray(object?.userdataFields)
        ? object.userdataFields.map((e: any) => SchemaField.fromJSON(e))
        : [],
      metadataFields: globalThis.Array.isArray(object?.metadataFields)
        ? object.metadataFields.map((e: any) => SchemaField.fromJSON(e))
        : [],
      labelsFields: globalThis.Array.isArray(object?.labelsFields)
        ? object.labelsFields.map((e: any) => SchemaField.fromJSON(e))
        : [],
    };
  },

  toJSON(message: Schema): unknown {
    const obj: any = {};
    if (message.userdataFields?.length) {
      obj.userdataFields = message.userdataFields.map((e) => SchemaField.toJSON(e));
    }
    if (message.metadataFields?.length) {
      obj.metadataFields = message.metadataFields.map((e) => SchemaField.toJSON(e));
    }
    if (message.labelsFields?.length) {
      obj.labelsFields = message.labelsFields.map((e) => SchemaField.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Schema>, I>>(base?: I): Schema {
    return Schema.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Schema>, I>>(object: I): Schema {
    const message = createBaseSchema();
    message.userdataFields = object.userdataFields?.map((e) => SchemaField.fromPartial(e)) || [];
    message.metadataFields = object.metadataFields?.map((e) => SchemaField.fromPartial(e)) || [];
    message.labelsFields = object.labelsFields?.map((e) => SchemaField.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSchemaField(): SchemaField {
  return { path: [], lucenePath: undefined, datatype: undefined };
}

export const SchemaField = {
  encode(message: SchemaField, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.path) {
      writer.uint32(10).string(v!);
    }
    if (message.lucenePath !== undefined) {
      LucenePath.encode(message.lucenePath, writer.uint32(18).fork()).ldelim();
    }
    if (message.datatype !== undefined) {
      Datatype.encode(message.datatype, writer.uint32(26).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SchemaField {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchemaField();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.path.push(reader.string());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.lucenePath = LucenePath.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.datatype = Datatype.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchemaField {
    return {
      path: globalThis.Array.isArray(object?.path) ? object.path.map((e: any) => globalThis.String(e)) : [],
      lucenePath: isSet(object.lucenePath) ? LucenePath.fromJSON(object.lucenePath) : undefined,
      datatype: isSet(object.datatype) ? Datatype.fromJSON(object.datatype) : undefined,
    };
  },

  toJSON(message: SchemaField): unknown {
    const obj: any = {};
    if (message.path?.length) {
      obj.path = message.path;
    }
    if (message.lucenePath !== undefined) {
      obj.lucenePath = LucenePath.toJSON(message.lucenePath);
    }
    if (message.datatype !== undefined) {
      obj.datatype = Datatype.toJSON(message.datatype);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchemaField>, I>>(base?: I): SchemaField {
    return SchemaField.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchemaField>, I>>(object: I): SchemaField {
    const message = createBaseSchemaField();
    message.path = object.path?.map((e) => e) || [];
    message.lucenePath = (object.lucenePath !== undefined && object.lucenePath !== null)
      ? LucenePath.fromPartial(object.lucenePath)
      : undefined;
    message.datatype = (object.datatype !== undefined && object.datatype !== null)
      ? Datatype.fromPartial(object.datatype)
      : undefined;
    return message;
  },
};

function createBaseLucenePath(): LucenePath {
  return { path: [] };
}

export const LucenePath = {
  encode(message: LucenePath, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.path) {
      writer.uint32(10).string(v!);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): LucenePath {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLucenePath();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.path.push(reader.string());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LucenePath {
    return { path: globalThis.Array.isArray(object?.path) ? object.path.map((e: any) => globalThis.String(e)) : [] };
  },

  toJSON(message: LucenePath): unknown {
    const obj: any = {};
    if (message.path?.length) {
      obj.path = message.path;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LucenePath>, I>>(base?: I): LucenePath {
    return LucenePath.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LucenePath>, I>>(object: I): LucenePath {
    const message = createBaseLucenePath();
    message.path = object.path?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
