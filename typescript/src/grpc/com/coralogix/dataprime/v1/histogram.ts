// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.176.1
//   protoc               v3.21.12
// source: com/coralogix/dataprime/v1/histogram.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { DoubleValue, Int64Value, StringValue } from "../../../../google/protobuf/wrappers";
import { Operator, operatorFromJSON, operatorToJSON } from "./aggregation_operators";
import { UntypedKeypath } from "./common";

export const protobufPackage = "com.coralogix.dataprime.v1";

export interface Aggregation {
  field: UntypedKeypath | undefined;
  type: Operator;
}

export interface Data {
  key: string | undefined;
  docCount: number | undefined;
  aggVal: number | undefined;
  compared?: ComparedData | undefined;
}

export interface ComparedData {
  historicalDocCount: number | undefined;
  historicalAggVal: number | undefined;
}

export interface HistogramSlice {
  value: number | undefined;
  data: Data[];
}

function createBaseAggregation(): Aggregation {
  return { field: undefined, type: 0 };
}

export const Aggregation = {
  encode(message: Aggregation, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.field !== undefined) {
      UntypedKeypath.encode(message.field, writer.uint32(10).fork()).ldelim();
    }
    if (message.type !== 0) {
      writer.uint32(16).int32(message.type);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Aggregation {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAggregation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.field = UntypedKeypath.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Aggregation {
    return {
      field: isSet(object.field) ? UntypedKeypath.fromJSON(object.field) : undefined,
      type: isSet(object.type) ? operatorFromJSON(object.type) : 0,
    };
  },

  toJSON(message: Aggregation): unknown {
    const obj: any = {};
    if (message.field !== undefined) {
      obj.field = UntypedKeypath.toJSON(message.field);
    }
    if (message.type !== 0) {
      obj.type = operatorToJSON(message.type);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Aggregation>, I>>(base?: I): Aggregation {
    return Aggregation.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Aggregation>, I>>(object: I): Aggregation {
    const message = createBaseAggregation();
    message.field = (object.field !== undefined && object.field !== null)
      ? UntypedKeypath.fromPartial(object.field)
      : undefined;
    message.type = object.type ?? 0;
    return message;
  },
};

function createBaseData(): Data {
  return { key: undefined, docCount: undefined, aggVal: undefined, compared: undefined };
}

export const Data = {
  encode(message: Data, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.key !== undefined) {
      StringValue.encode({ value: message.key! }, writer.uint32(10).fork()).ldelim();
    }
    if (message.docCount !== undefined) {
      Int64Value.encode({ value: message.docCount! }, writer.uint32(18).fork()).ldelim();
    }
    if (message.aggVal !== undefined) {
      DoubleValue.encode({ value: message.aggVal! }, writer.uint32(26).fork()).ldelim();
    }
    if (message.compared !== undefined) {
      ComparedData.encode(message.compared, writer.uint32(34).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Data {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.key = StringValue.decode(reader, reader.uint32()).value;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.docCount = Int64Value.decode(reader, reader.uint32()).value;
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.aggVal = DoubleValue.decode(reader, reader.uint32()).value;
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.compared = ComparedData.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Data {
    return {
      key: isSet(object.key) ? String(object.key) : undefined,
      docCount: isSet(object.docCount) ? Number(object.docCount) : undefined,
      aggVal: isSet(object.aggVal) ? Number(object.aggVal) : undefined,
      compared: isSet(object.compared) ? ComparedData.fromJSON(object.compared) : undefined,
    };
  },

  toJSON(message: Data): unknown {
    const obj: any = {};
    if (message.key !== undefined) {
      obj.key = message.key;
    }
    if (message.docCount !== undefined) {
      obj.docCount = message.docCount;
    }
    if (message.aggVal !== undefined) {
      obj.aggVal = message.aggVal;
    }
    if (message.compared !== undefined) {
      obj.compared = ComparedData.toJSON(message.compared);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Data>, I>>(base?: I): Data {
    return Data.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Data>, I>>(object: I): Data {
    const message = createBaseData();
    message.key = object.key ?? undefined;
    message.docCount = object.docCount ?? undefined;
    message.aggVal = object.aggVal ?? undefined;
    message.compared = (object.compared !== undefined && object.compared !== null)
      ? ComparedData.fromPartial(object.compared)
      : undefined;
    return message;
  },
};

function createBaseComparedData(): ComparedData {
  return { historicalDocCount: undefined, historicalAggVal: undefined };
}

export const ComparedData = {
  encode(message: ComparedData, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.historicalDocCount !== undefined) {
      Int64Value.encode({ value: message.historicalDocCount! }, writer.uint32(10).fork()).ldelim();
    }
    if (message.historicalAggVal !== undefined) {
      DoubleValue.encode({ value: message.historicalAggVal! }, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ComparedData {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseComparedData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.historicalDocCount = Int64Value.decode(reader, reader.uint32()).value;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.historicalAggVal = DoubleValue.decode(reader, reader.uint32()).value;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ComparedData {
    return {
      historicalDocCount: isSet(object.historicalDocCount) ? Number(object.historicalDocCount) : undefined,
      historicalAggVal: isSet(object.historicalAggVal) ? Number(object.historicalAggVal) : undefined,
    };
  },

  toJSON(message: ComparedData): unknown {
    const obj: any = {};
    if (message.historicalDocCount !== undefined) {
      obj.historicalDocCount = message.historicalDocCount;
    }
    if (message.historicalAggVal !== undefined) {
      obj.historicalAggVal = message.historicalAggVal;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ComparedData>, I>>(base?: I): ComparedData {
    return ComparedData.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ComparedData>, I>>(object: I): ComparedData {
    const message = createBaseComparedData();
    message.historicalDocCount = object.historicalDocCount ?? undefined;
    message.historicalAggVal = object.historicalAggVal ?? undefined;
    return message;
  },
};

function createBaseHistogramSlice(): HistogramSlice {
  return { value: undefined, data: [] };
}

export const HistogramSlice = {
  encode(message: HistogramSlice, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.value !== undefined) {
      Int64Value.encode({ value: message.value! }, writer.uint32(10).fork()).ldelim();
    }
    for (const v of message.data) {
      Data.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): HistogramSlice {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHistogramSlice();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.value = Int64Value.decode(reader, reader.uint32()).value;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.data.push(Data.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HistogramSlice {
    return {
      value: isSet(object.value) ? Number(object.value) : undefined,
      data: globalThis.Array.isArray(object?.data) ? object.data.map((e: any) => Data.fromJSON(e)) : [],
    };
  },

  toJSON(message: HistogramSlice): unknown {
    const obj: any = {};
    if (message.value !== undefined) {
      obj.value = message.value;
    }
    if (message.data?.length) {
      obj.data = message.data.map((e) => Data.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HistogramSlice>, I>>(base?: I): HistogramSlice {
    return HistogramSlice.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HistogramSlice>, I>>(object: I): HistogramSlice {
    const message = createBaseHistogramSlice();
    message.value = object.value ?? undefined;
    message.data = object.data?.map((e) => Data.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
