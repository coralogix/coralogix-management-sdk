// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.176.1
//   protoc               v3.21.12
// source: com/coralogix/dataprime/v1/internal_metadata.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { Timestamp } from "../../../../google/protobuf/timestamp";
import { StringValue } from "../../../../google/protobuf/wrappers";

export const protobufPackage = "com.coralogix.dataprime.v1";

export interface MetadataEnums {
}

export enum MetadataEnums_Tier {
  TIER_UNSPECIFIED = 0,
  TIER_ARCHIVE = 1,
  TIER_FREQUENT_SEARCH = 2,
  UNRECOGNIZED = -1,
}

export function metadataEnums_TierFromJSON(object: any): MetadataEnums_Tier {
  switch (object) {
    case 0:
    case "TIER_UNSPECIFIED":
      return MetadataEnums_Tier.TIER_UNSPECIFIED;
    case 1:
    case "TIER_ARCHIVE":
      return MetadataEnums_Tier.TIER_ARCHIVE;
    case 2:
    case "TIER_FREQUENT_SEARCH":
      return MetadataEnums_Tier.TIER_FREQUENT_SEARCH;
    case -1:
    case "UNRECOGNIZED":
    default:
      return MetadataEnums_Tier.UNRECOGNIZED;
  }
}

export function metadataEnums_TierToJSON(object: MetadataEnums_Tier): string {
  switch (object) {
    case MetadataEnums_Tier.TIER_UNSPECIFIED:
      return "TIER_UNSPECIFIED";
    case MetadataEnums_Tier.TIER_ARCHIVE:
      return "TIER_ARCHIVE";
    case MetadataEnums_Tier.TIER_FREQUENT_SEARCH:
      return "TIER_FREQUENT_SEARCH";
    case MetadataEnums_Tier.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum MetadataEnums_QuerySyntax {
  QUERY_SYNTAX_UNSPECIFIED = 0,
  QUERY_SYNTAX_LUCENE = 1,
  QUERY_SYNTAX_DATAPRIME = 2,
  UNRECOGNIZED = -1,
}

export function metadataEnums_QuerySyntaxFromJSON(object: any): MetadataEnums_QuerySyntax {
  switch (object) {
    case 0:
    case "QUERY_SYNTAX_UNSPECIFIED":
      return MetadataEnums_QuerySyntax.QUERY_SYNTAX_UNSPECIFIED;
    case 1:
    case "QUERY_SYNTAX_LUCENE":
      return MetadataEnums_QuerySyntax.QUERY_SYNTAX_LUCENE;
    case 2:
    case "QUERY_SYNTAX_DATAPRIME":
      return MetadataEnums_QuerySyntax.QUERY_SYNTAX_DATAPRIME;
    case -1:
    case "UNRECOGNIZED":
    default:
      return MetadataEnums_QuerySyntax.UNRECOGNIZED;
  }
}

export function metadataEnums_QuerySyntaxToJSON(object: MetadataEnums_QuerySyntax): string {
  switch (object) {
    case MetadataEnums_QuerySyntax.QUERY_SYNTAX_UNSPECIFIED:
      return "QUERY_SYNTAX_UNSPECIFIED";
    case MetadataEnums_QuerySyntax.QUERY_SYNTAX_LUCENE:
      return "QUERY_SYNTAX_LUCENE";
    case MetadataEnums_QuerySyntax.QUERY_SYNTAX_DATAPRIME:
      return "QUERY_SYNTAX_DATAPRIME";
    case MetadataEnums_QuerySyntax.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface QueryMetadata {
  /** beginning of the time range for the query */
  startDate?:
    | Date
    | undefined;
  /** end of the time range for the query */
  endDate?:
    | Date
    | undefined;
  /** tier on which query runs */
  tier?:
    | MetadataEnums_Tier
    | undefined;
  /** limit number of results */
  limit?:
    | number
    | undefined;
  /** dataprime strict fields validation in parser */
  strictFieldsValidation?:
    | boolean
    | undefined;
  /**
   * skip application and subsystem filters based on user scopes from RBAC.
   * WARNING: if you set this flag, you are responsible for managing access to the data.
   */
  skipRbacFilters?: boolean | undefined;
}

/** only text related metadata */
export interface TextQueryMetadata {
  /** text query syntax */
  syntax?:
    | MetadataEnums_QuerySyntax
    | undefined;
  /** default value for source to be used when source is omitted in a query */
  defaultSource?: string | undefined;
}

function createBaseMetadataEnums(): MetadataEnums {
  return {};
}

export const MetadataEnums = {
  encode(_: MetadataEnums, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MetadataEnums {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMetadataEnums();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MetadataEnums {
    return {};
  },

  toJSON(_: MetadataEnums): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<MetadataEnums>, I>>(base?: I): MetadataEnums {
    return MetadataEnums.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MetadataEnums>, I>>(_: I): MetadataEnums {
    const message = createBaseMetadataEnums();
    return message;
  },
};

function createBaseQueryMetadata(): QueryMetadata {
  return {
    startDate: undefined,
    endDate: undefined,
    tier: undefined,
    limit: undefined,
    strictFieldsValidation: undefined,
    skipRbacFilters: undefined,
  };
}

export const QueryMetadata = {
  encode(message: QueryMetadata, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.startDate !== undefined) {
      Timestamp.encode(toTimestamp(message.startDate), writer.uint32(10).fork()).ldelim();
    }
    if (message.endDate !== undefined) {
      Timestamp.encode(toTimestamp(message.endDate), writer.uint32(18).fork()).ldelim();
    }
    if (message.tier !== undefined) {
      writer.uint32(24).int32(message.tier);
    }
    if (message.limit !== undefined) {
      writer.uint32(32).int32(message.limit);
    }
    if (message.strictFieldsValidation !== undefined) {
      writer.uint32(40).bool(message.strictFieldsValidation);
    }
    if (message.skipRbacFilters !== undefined) {
      writer.uint32(48).bool(message.skipRbacFilters);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryMetadata {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryMetadata();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.startDate = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.endDate = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.tier = reader.int32() as any;
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.limit = reader.int32();
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.strictFieldsValidation = reader.bool();
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.skipRbacFilters = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryMetadata {
    return {
      startDate: isSet(object.startDate) ? fromJsonTimestamp(object.startDate) : undefined,
      endDate: isSet(object.endDate) ? fromJsonTimestamp(object.endDate) : undefined,
      tier: isSet(object.tier) ? metadataEnums_TierFromJSON(object.tier) : undefined,
      limit: isSet(object.limit) ? globalThis.Number(object.limit) : undefined,
      strictFieldsValidation: isSet(object.strictFieldsValidation)
        ? globalThis.Boolean(object.strictFieldsValidation)
        : undefined,
      skipRbacFilters: isSet(object.skipRbacFilters) ? globalThis.Boolean(object.skipRbacFilters) : undefined,
    };
  },

  toJSON(message: QueryMetadata): unknown {
    const obj: any = {};
    if (message.startDate !== undefined) {
      obj.startDate = message.startDate.toISOString();
    }
    if (message.endDate !== undefined) {
      obj.endDate = message.endDate.toISOString();
    }
    if (message.tier !== undefined) {
      obj.tier = metadataEnums_TierToJSON(message.tier);
    }
    if (message.limit !== undefined) {
      obj.limit = Math.round(message.limit);
    }
    if (message.strictFieldsValidation !== undefined) {
      obj.strictFieldsValidation = message.strictFieldsValidation;
    }
    if (message.skipRbacFilters !== undefined) {
      obj.skipRbacFilters = message.skipRbacFilters;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryMetadata>, I>>(base?: I): QueryMetadata {
    return QueryMetadata.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryMetadata>, I>>(object: I): QueryMetadata {
    const message = createBaseQueryMetadata();
    message.startDate = object.startDate ?? undefined;
    message.endDate = object.endDate ?? undefined;
    message.tier = object.tier ?? undefined;
    message.limit = object.limit ?? undefined;
    message.strictFieldsValidation = object.strictFieldsValidation ?? undefined;
    message.skipRbacFilters = object.skipRbacFilters ?? undefined;
    return message;
  },
};

function createBaseTextQueryMetadata(): TextQueryMetadata {
  return { syntax: undefined, defaultSource: undefined };
}

export const TextQueryMetadata = {
  encode(message: TextQueryMetadata, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.syntax !== undefined) {
      writer.uint32(8).int32(message.syntax);
    }
    if (message.defaultSource !== undefined) {
      StringValue.encode({ value: message.defaultSource! }, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): TextQueryMetadata {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTextQueryMetadata();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.syntax = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.defaultSource = StringValue.decode(reader, reader.uint32()).value;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TextQueryMetadata {
    return {
      syntax: isSet(object.syntax) ? metadataEnums_QuerySyntaxFromJSON(object.syntax) : undefined,
      defaultSource: isSet(object.defaultSource) ? String(object.defaultSource) : undefined,
    };
  },

  toJSON(message: TextQueryMetadata): unknown {
    const obj: any = {};
    if (message.syntax !== undefined) {
      obj.syntax = metadataEnums_QuerySyntaxToJSON(message.syntax);
    }
    if (message.defaultSource !== undefined) {
      obj.defaultSource = message.defaultSource;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TextQueryMetadata>, I>>(base?: I): TextQueryMetadata {
    return TextQueryMetadata.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TextQueryMetadata>, I>>(object: I): TextQueryMetadata {
    const message = createBaseTextQueryMetadata();
    message.syntax = object.syntax ?? undefined;
    message.defaultSource = object.defaultSource ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
