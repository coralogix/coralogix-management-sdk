// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.176.1
//   protoc               v3.21.12
// source: com/coralogix/datausage/v1/common.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { Timestamp } from "../../../../google/protobuf/timestamp";
import { FloatValue, UInt64Value } from "../../../../google/protobuf/wrappers";

export const protobufPackage = "com.coralogix.datausage.v1";

/** @deprecated */
export enum TcoTier {
  TCO_TIER_UNSPECIFIED = 0,
  TCO_TIER_LOW = 1,
  TCO_TIER_MEDIUM = 2,
  TCO_TIER_HIGH = 3,
  TCO_TIER_BLOCKED = 4,
  UNRECOGNIZED = -1,
}

export function tcoTierFromJSON(object: any): TcoTier {
  switch (object) {
    case 0:
    case "TCO_TIER_UNSPECIFIED":
      return TcoTier.TCO_TIER_UNSPECIFIED;
    case 1:
    case "TCO_TIER_LOW":
      return TcoTier.TCO_TIER_LOW;
    case 2:
    case "TCO_TIER_MEDIUM":
      return TcoTier.TCO_TIER_MEDIUM;
    case 3:
    case "TCO_TIER_HIGH":
      return TcoTier.TCO_TIER_HIGH;
    case 4:
    case "TCO_TIER_BLOCKED":
      return TcoTier.TCO_TIER_BLOCKED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return TcoTier.UNRECOGNIZED;
  }
}

export function tcoTierToJSON(object: TcoTier): string {
  switch (object) {
    case TcoTier.TCO_TIER_UNSPECIFIED:
      return "TCO_TIER_UNSPECIFIED";
    case TcoTier.TCO_TIER_LOW:
      return "TCO_TIER_LOW";
    case TcoTier.TCO_TIER_MEDIUM:
      return "TCO_TIER_MEDIUM";
    case TcoTier.TCO_TIER_HIGH:
      return "TCO_TIER_HIGH";
    case TcoTier.TCO_TIER_BLOCKED:
      return "TCO_TIER_BLOCKED";
    case TcoTier.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum Priority {
  PRIORITY_UNSPECIFIED = 0,
  PRIORITY_LOW = 1,
  PRIORITY_MEDIUM = 2,
  PRIORITY_HIGH = 3,
  PRIORITY_BLOCKED = 4,
  UNRECOGNIZED = -1,
}

export function priorityFromJSON(object: any): Priority {
  switch (object) {
    case 0:
    case "PRIORITY_UNSPECIFIED":
      return Priority.PRIORITY_UNSPECIFIED;
    case 1:
    case "PRIORITY_LOW":
      return Priority.PRIORITY_LOW;
    case 2:
    case "PRIORITY_MEDIUM":
      return Priority.PRIORITY_MEDIUM;
    case 3:
    case "PRIORITY_HIGH":
      return Priority.PRIORITY_HIGH;
    case 4:
    case "PRIORITY_BLOCKED":
      return Priority.PRIORITY_BLOCKED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return Priority.UNRECOGNIZED;
  }
}

export function priorityToJSON(object: Priority): string {
  switch (object) {
    case Priority.PRIORITY_UNSPECIFIED:
      return "PRIORITY_UNSPECIFIED";
    case Priority.PRIORITY_LOW:
      return "PRIORITY_LOW";
    case Priority.PRIORITY_MEDIUM:
      return "PRIORITY_MEDIUM";
    case Priority.PRIORITY_HIGH:
      return "PRIORITY_HIGH";
    case Priority.PRIORITY_BLOCKED:
      return "PRIORITY_BLOCKED";
    case Priority.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum DataType {
  DATA_TYPE_UNSPECIFIED = 0,
  DATA_TYPE_LOGS = 1,
  DATA_TYPE_METRICS = 2,
  DATA_TYPE_TRACING = 3,
  UNRECOGNIZED = -1,
}

export function dataTypeFromJSON(object: any): DataType {
  switch (object) {
    case 0:
    case "DATA_TYPE_UNSPECIFIED":
      return DataType.DATA_TYPE_UNSPECIFIED;
    case 1:
    case "DATA_TYPE_LOGS":
      return DataType.DATA_TYPE_LOGS;
    case 2:
    case "DATA_TYPE_METRICS":
      return DataType.DATA_TYPE_METRICS;
    case 3:
    case "DATA_TYPE_TRACING":
      return DataType.DATA_TYPE_TRACING;
    case -1:
    case "UNRECOGNIZED":
    default:
      return DataType.UNRECOGNIZED;
  }
}

export function dataTypeToJSON(object: DataType): string {
  switch (object) {
    case DataType.DATA_TYPE_UNSPECIFIED:
      return "DATA_TYPE_UNSPECIFIED";
    case DataType.DATA_TYPE_LOGS:
      return "DATA_TYPE_LOGS";
    case DataType.DATA_TYPE_METRICS:
      return "DATA_TYPE_METRICS";
    case DataType.DATA_TYPE_TRACING:
      return "DATA_TYPE_TRACING";
    case DataType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum Severity {
  SEVERITY_UNSPECIFIED = 0,
  SEVERITY_DEBUG = 1,
  SEVERITY_VERBOSE = 2,
  SEVERITY_INFO = 3,
  SEVERITY_WARNING = 4,
  SEVERITY_ERROR = 5,
  SEVERITY_CRITICAL = 6,
  UNRECOGNIZED = -1,
}

export function severityFromJSON(object: any): Severity {
  switch (object) {
    case 0:
    case "SEVERITY_UNSPECIFIED":
      return Severity.SEVERITY_UNSPECIFIED;
    case 1:
    case "SEVERITY_DEBUG":
      return Severity.SEVERITY_DEBUG;
    case 2:
    case "SEVERITY_VERBOSE":
      return Severity.SEVERITY_VERBOSE;
    case 3:
    case "SEVERITY_INFO":
      return Severity.SEVERITY_INFO;
    case 4:
    case "SEVERITY_WARNING":
      return Severity.SEVERITY_WARNING;
    case 5:
    case "SEVERITY_ERROR":
      return Severity.SEVERITY_ERROR;
    case 6:
    case "SEVERITY_CRITICAL":
      return Severity.SEVERITY_CRITICAL;
    case -1:
    case "UNRECOGNIZED":
    default:
      return Severity.UNRECOGNIZED;
  }
}

export function severityToJSON(object: Severity): string {
  switch (object) {
    case Severity.SEVERITY_UNSPECIFIED:
      return "SEVERITY_UNSPECIFIED";
    case Severity.SEVERITY_DEBUG:
      return "SEVERITY_DEBUG";
    case Severity.SEVERITY_VERBOSE:
      return "SEVERITY_VERBOSE";
    case Severity.SEVERITY_INFO:
      return "SEVERITY_INFO";
    case Severity.SEVERITY_WARNING:
      return "SEVERITY_WARNING";
    case Severity.SEVERITY_ERROR:
      return "SEVERITY_ERROR";
    case Severity.SEVERITY_CRITICAL:
      return "SEVERITY_CRITICAL";
    case Severity.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum Range {
  RANGE_UNSPECIFIED = 0,
  RANGE_CURRENT_MONTH = 1,
  RANGE_LAST_30_DAYS = 2,
  RANGE_LAST_90_DAYS = 3,
  RANGE_LAST_WEEK = 4,
  UNRECOGNIZED = -1,
}

export function rangeFromJSON(object: any): Range {
  switch (object) {
    case 0:
    case "RANGE_UNSPECIFIED":
      return Range.RANGE_UNSPECIFIED;
    case 1:
    case "RANGE_CURRENT_MONTH":
      return Range.RANGE_CURRENT_MONTH;
    case 2:
    case "RANGE_LAST_30_DAYS":
      return Range.RANGE_LAST_30_DAYS;
    case 3:
    case "RANGE_LAST_90_DAYS":
      return Range.RANGE_LAST_90_DAYS;
    case 4:
    case "RANGE_LAST_WEEK":
      return Range.RANGE_LAST_WEEK;
    case -1:
    case "UNRECOGNIZED":
    default:
      return Range.UNRECOGNIZED;
  }
}

export function rangeToJSON(object: Range): string {
  switch (object) {
    case Range.RANGE_UNSPECIFIED:
      return "RANGE_UNSPECIFIED";
    case Range.RANGE_CURRENT_MONTH:
      return "RANGE_CURRENT_MONTH";
    case Range.RANGE_LAST_30_DAYS:
      return "RANGE_LAST_30_DAYS";
    case Range.RANGE_LAST_90_DAYS:
      return "RANGE_LAST_90_DAYS";
    case Range.RANGE_LAST_WEEK:
      return "RANGE_LAST_WEEK";
    case Range.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum Resolution {
  RESOLUTION_UNSPECIFIED = 0,
  RESOLUTION_ONE_MINUTE = 1,
  RESOLUTION_FIVE_MINUTES = 2,
  RESOLUTION_FIFTEEN_MINUTES = 3,
  RESOLUTION_ONE_HOUR = 4,
  RESOLUTION_SIX_HOURS = 5,
  RESOLUTION_ONE_DAY = 6,
  RESOLUTION_ONE_WEEK = 7,
  UNRECOGNIZED = -1,
}

export function resolutionFromJSON(object: any): Resolution {
  switch (object) {
    case 0:
    case "RESOLUTION_UNSPECIFIED":
      return Resolution.RESOLUTION_UNSPECIFIED;
    case 1:
    case "RESOLUTION_ONE_MINUTE":
      return Resolution.RESOLUTION_ONE_MINUTE;
    case 2:
    case "RESOLUTION_FIVE_MINUTES":
      return Resolution.RESOLUTION_FIVE_MINUTES;
    case 3:
    case "RESOLUTION_FIFTEEN_MINUTES":
      return Resolution.RESOLUTION_FIFTEEN_MINUTES;
    case 4:
    case "RESOLUTION_ONE_HOUR":
      return Resolution.RESOLUTION_ONE_HOUR;
    case 5:
    case "RESOLUTION_SIX_HOURS":
      return Resolution.RESOLUTION_SIX_HOURS;
    case 6:
    case "RESOLUTION_ONE_DAY":
      return Resolution.RESOLUTION_ONE_DAY;
    case 7:
    case "RESOLUTION_ONE_WEEK":
      return Resolution.RESOLUTION_ONE_WEEK;
    case -1:
    case "UNRECOGNIZED":
    default:
      return Resolution.UNRECOGNIZED;
  }
}

export function resolutionToJSON(object: Resolution): string {
  switch (object) {
    case Resolution.RESOLUTION_UNSPECIFIED:
      return "RESOLUTION_UNSPECIFIED";
    case Resolution.RESOLUTION_ONE_MINUTE:
      return "RESOLUTION_ONE_MINUTE";
    case Resolution.RESOLUTION_FIVE_MINUTES:
      return "RESOLUTION_FIVE_MINUTES";
    case Resolution.RESOLUTION_FIFTEEN_MINUTES:
      return "RESOLUTION_FIFTEEN_MINUTES";
    case Resolution.RESOLUTION_ONE_HOUR:
      return "RESOLUTION_ONE_HOUR";
    case Resolution.RESOLUTION_SIX_HOURS:
      return "RESOLUTION_SIX_HOURS";
    case Resolution.RESOLUTION_ONE_DAY:
      return "RESOLUTION_ONE_DAY";
    case Resolution.RESOLUTION_ONE_WEEK:
      return "RESOLUTION_ONE_WEEK";
    case Resolution.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface Team {
  id: number | undefined;
}

export interface Unit {
  value: number | undefined;
}

export interface GB {
  value: number | undefined;
}

export interface Retention {
  value: number | undefined;
}

export interface DateRange {
  fromDate: Date | undefined;
  toDate: Date | undefined;
}

function createBaseTeam(): Team {
  return { id: undefined };
}

export const Team = {
  encode(message: Team, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== undefined) {
      UInt64Value.encode({ value: message.id! }, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Team {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTeam();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 2:
          if (tag !== 18) {
            break;
          }

          message.id = UInt64Value.decode(reader, reader.uint32()).value;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Team {
    return { id: isSet(object.id) ? Number(object.id) : undefined };
  },

  toJSON(message: Team): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = message.id;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Team>, I>>(base?: I): Team {
    return Team.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Team>, I>>(object: I): Team {
    const message = createBaseTeam();
    message.id = object.id ?? undefined;
    return message;
  },
};

function createBaseUnit(): Unit {
  return { value: undefined };
}

export const Unit = {
  encode(message: Unit, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.value !== undefined) {
      FloatValue.encode({ value: message.value! }, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Unit {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUnit();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.value = FloatValue.decode(reader, reader.uint32()).value;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Unit {
    return { value: isSet(object.value) ? Number(object.value) : undefined };
  },

  toJSON(message: Unit): unknown {
    const obj: any = {};
    if (message.value !== undefined) {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Unit>, I>>(base?: I): Unit {
    return Unit.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Unit>, I>>(object: I): Unit {
    const message = createBaseUnit();
    message.value = object.value ?? undefined;
    return message;
  },
};

function createBaseGB(): GB {
  return { value: undefined };
}

export const GB = {
  encode(message: GB, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.value !== undefined) {
      FloatValue.encode({ value: message.value! }, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GB {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGB();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.value = FloatValue.decode(reader, reader.uint32()).value;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GB {
    return { value: isSet(object.value) ? Number(object.value) : undefined };
  },

  toJSON(message: GB): unknown {
    const obj: any = {};
    if (message.value !== undefined) {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GB>, I>>(base?: I): GB {
    return GB.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GB>, I>>(object: I): GB {
    const message = createBaseGB();
    message.value = object.value ?? undefined;
    return message;
  },
};

function createBaseRetention(): Retention {
  return { value: undefined };
}

export const Retention = {
  encode(message: Retention, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.value !== undefined) {
      UInt64Value.encode({ value: message.value! }, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Retention {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRetention();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.value = UInt64Value.decode(reader, reader.uint32()).value;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Retention {
    return { value: isSet(object.value) ? Number(object.value) : undefined };
  },

  toJSON(message: Retention): unknown {
    const obj: any = {};
    if (message.value !== undefined) {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Retention>, I>>(base?: I): Retention {
    return Retention.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Retention>, I>>(object: I): Retention {
    const message = createBaseRetention();
    message.value = object.value ?? undefined;
    return message;
  },
};

function createBaseDateRange(): DateRange {
  return { fromDate: undefined, toDate: undefined };
}

export const DateRange = {
  encode(message: DateRange, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.fromDate !== undefined) {
      Timestamp.encode(toTimestamp(message.fromDate), writer.uint32(10).fork()).ldelim();
    }
    if (message.toDate !== undefined) {
      Timestamp.encode(toTimestamp(message.toDate), writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): DateRange {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDateRange();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.fromDate = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.toDate = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DateRange {
    return {
      fromDate: isSet(object.fromDate) ? fromJsonTimestamp(object.fromDate) : undefined,
      toDate: isSet(object.toDate) ? fromJsonTimestamp(object.toDate) : undefined,
    };
  },

  toJSON(message: DateRange): unknown {
    const obj: any = {};
    if (message.fromDate !== undefined) {
      obj.fromDate = message.fromDate.toISOString();
    }
    if (message.toDate !== undefined) {
      obj.toDate = message.toDate.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DateRange>, I>>(base?: I): DateRange {
    return DateRange.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DateRange>, I>>(object: I): DateRange {
    const message = createBaseDateRange();
    message.fromDate = object.fromDate ?? undefined;
    message.toDate = object.toDate ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
