// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.176.1
//   protoc               v3.21.12
// source: com/coralogix/datausage/v1/daily_usage_service.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { DateRange, Range, rangeFromJSON, rangeToJSON } from "./common";
import { DetailedDailyUsage, OverageDailyReport } from "./daily_usage";

export const protobufPackage = "com.coralogix.datausage.v1";

export interface DailyUsageRequest {
  range?: Range | undefined;
  dateRange?: DateRange | undefined;
}

export interface DailyUsageResponse {
  response: DetailedDailyUsage[];
}

export interface OverageDailyUsageRequest {
  range?: Range | undefined;
  dateRange?: DateRange | undefined;
}

export interface OverageDailyUsageResponse {
  overageDetail: OverageDailyReport[];
}

function createBaseDailyUsageRequest(): DailyUsageRequest {
  return { range: undefined, dateRange: undefined };
}

export const DailyUsageRequest = {
  encode(message: DailyUsageRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.range !== undefined) {
      writer.uint32(8).int32(message.range);
    }
    if (message.dateRange !== undefined) {
      DateRange.encode(message.dateRange, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): DailyUsageRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDailyUsageRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.range = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.dateRange = DateRange.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DailyUsageRequest {
    return {
      range: isSet(object.range) ? rangeFromJSON(object.range) : undefined,
      dateRange: isSet(object.dateRange) ? DateRange.fromJSON(object.dateRange) : undefined,
    };
  },

  toJSON(message: DailyUsageRequest): unknown {
    const obj: any = {};
    if (message.range !== undefined) {
      obj.range = rangeToJSON(message.range);
    }
    if (message.dateRange !== undefined) {
      obj.dateRange = DateRange.toJSON(message.dateRange);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DailyUsageRequest>, I>>(base?: I): DailyUsageRequest {
    return DailyUsageRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DailyUsageRequest>, I>>(object: I): DailyUsageRequest {
    const message = createBaseDailyUsageRequest();
    message.range = object.range ?? undefined;
    message.dateRange = (object.dateRange !== undefined && object.dateRange !== null)
      ? DateRange.fromPartial(object.dateRange)
      : undefined;
    return message;
  },
};

function createBaseDailyUsageResponse(): DailyUsageResponse {
  return { response: [] };
}

export const DailyUsageResponse = {
  encode(message: DailyUsageResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.response) {
      DetailedDailyUsage.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): DailyUsageResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDailyUsageResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.response.push(DetailedDailyUsage.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DailyUsageResponse {
    return {
      response: globalThis.Array.isArray(object?.response)
        ? object.response.map((e: any) => DetailedDailyUsage.fromJSON(e))
        : [],
    };
  },

  toJSON(message: DailyUsageResponse): unknown {
    const obj: any = {};
    if (message.response?.length) {
      obj.response = message.response.map((e) => DetailedDailyUsage.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DailyUsageResponse>, I>>(base?: I): DailyUsageResponse {
    return DailyUsageResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DailyUsageResponse>, I>>(object: I): DailyUsageResponse {
    const message = createBaseDailyUsageResponse();
    message.response = object.response?.map((e) => DetailedDailyUsage.fromPartial(e)) || [];
    return message;
  },
};

function createBaseOverageDailyUsageRequest(): OverageDailyUsageRequest {
  return { range: undefined, dateRange: undefined };
}

export const OverageDailyUsageRequest = {
  encode(message: OverageDailyUsageRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.range !== undefined) {
      writer.uint32(8).int32(message.range);
    }
    if (message.dateRange !== undefined) {
      DateRange.encode(message.dateRange, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): OverageDailyUsageRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOverageDailyUsageRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.range = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.dateRange = DateRange.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OverageDailyUsageRequest {
    return {
      range: isSet(object.range) ? rangeFromJSON(object.range) : undefined,
      dateRange: isSet(object.dateRange) ? DateRange.fromJSON(object.dateRange) : undefined,
    };
  },

  toJSON(message: OverageDailyUsageRequest): unknown {
    const obj: any = {};
    if (message.range !== undefined) {
      obj.range = rangeToJSON(message.range);
    }
    if (message.dateRange !== undefined) {
      obj.dateRange = DateRange.toJSON(message.dateRange);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<OverageDailyUsageRequest>, I>>(base?: I): OverageDailyUsageRequest {
    return OverageDailyUsageRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<OverageDailyUsageRequest>, I>>(object: I): OverageDailyUsageRequest {
    const message = createBaseOverageDailyUsageRequest();
    message.range = object.range ?? undefined;
    message.dateRange = (object.dateRange !== undefined && object.dateRange !== null)
      ? DateRange.fromPartial(object.dateRange)
      : undefined;
    return message;
  },
};

function createBaseOverageDailyUsageResponse(): OverageDailyUsageResponse {
  return { overageDetail: [] };
}

export const OverageDailyUsageResponse = {
  encode(message: OverageDailyUsageResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.overageDetail) {
      OverageDailyReport.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): OverageDailyUsageResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOverageDailyUsageResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.overageDetail.push(OverageDailyReport.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OverageDailyUsageResponse {
    return {
      overageDetail: globalThis.Array.isArray(object?.overageDetail)
        ? object.overageDetail.map((e: any) => OverageDailyReport.fromJSON(e))
        : [],
    };
  },

  toJSON(message: OverageDailyUsageResponse): unknown {
    const obj: any = {};
    if (message.overageDetail?.length) {
      obj.overageDetail = message.overageDetail.map((e) => OverageDailyReport.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<OverageDailyUsageResponse>, I>>(base?: I): OverageDailyUsageResponse {
    return OverageDailyUsageResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<OverageDailyUsageResponse>, I>>(object: I): OverageDailyUsageResponse {
    const message = createBaseOverageDailyUsageResponse();
    message.overageDetail = object.overageDetail?.map((e) => OverageDailyReport.fromPartial(e)) || [];
    return message;
  },
};

/** / Legacy data usage API for backwards compatibility with web-api */
export interface DailyUsageService {
  /** /Endpoint to fetch daily usage */
  DailyUsage(request: DailyUsageRequest): Promise<DailyUsageResponse>;
  /** /Endpoint to fetch how many units "pay as you go" customers have went over their plan */
  OverageDailyUsage(request: OverageDailyUsageRequest): Promise<OverageDailyUsageResponse>;
}

export const DailyUsageServiceServiceName = "com.coralogix.datausage.v1.DailyUsageService";
export class DailyUsageServiceClientImpl implements DailyUsageService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || DailyUsageServiceServiceName;
    this.rpc = rpc;
    this.DailyUsage = this.DailyUsage.bind(this);
    this.OverageDailyUsage = this.OverageDailyUsage.bind(this);
  }
  DailyUsage(request: DailyUsageRequest): Promise<DailyUsageResponse> {
    const data = DailyUsageRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "DailyUsage", data);
    return promise.then((data) => DailyUsageResponse.decode(_m0.Reader.create(data)));
  }

  OverageDailyUsage(request: OverageDailyUsageRequest): Promise<OverageDailyUsageResponse> {
    const data = OverageDailyUsageRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "OverageDailyUsage", data);
    return promise.then((data) => OverageDailyUsageResponse.decode(_m0.Reader.create(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
