// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.176.1
//   protoc               v3.21.12
// source: com/coralogix/datausage/v2/internal_data_usage_service.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { Observable } from "rxjs";
import { map } from "rxjs/operators";
import { Duration } from "../../../../google/protobuf/duration";
import { Timestamp } from "../../../../google/protobuf/timestamp";
import { FloatValue } from "../../../../google/protobuf/wrappers";
import { DateRange, Dimension } from "./data_usage";

export const protobufPackage = "com.coralogix.datausage.v2";

export interface ExportTeamsDetailedUsageRequest {
  /** Date range must be between 1 hour and 24 hours. */
  dateRange:
    | DateRange
    | undefined;
  /**
   * Aggregation resolution with minute precision.
   *
   * Do note that to avoid overloading the service we enforce resolution limits
   * The maximum is 24 data points for 24 hours date range and 1 hour resolution.
   * Depending on how much data there is, exporting detailed data usage for 24 hours can take significant time (~2 minutes).
   *
   * Resolution must be between 1 hour and 24 hour. It also must not be bigger than the total DateRange.
   */
  resolution: Duration | undefined;
}

export interface DataUsage {
  timestamp: Date | undefined;
  sizeGb:
    | number
    | undefined;
  /**
   * / Describes this data usage
   * / i.e. [Pillar.PILLAR_LOGS, Priority.PRIORITY_HIGH], [Pillar.PILLAR_METRICS, Priority.PRIORITY_MEDIUM]
   */
  dimensions: Dimension[];
}

export interface TeamDetailedUsage {
  teamId: number;
  externalId?: string | undefined;
  retention: number;
  dataUsage: DataUsage[];
}

export interface ExportTeamsDetailedUsageResponse {
  /**
   * / It is possible that if a team has many records they will be split into multiple messages
   * / Client is expected to read stream and combine all records for each team.
   */
  teamUsage: TeamDetailedUsage | undefined;
}

function createBaseExportTeamsDetailedUsageRequest(): ExportTeamsDetailedUsageRequest {
  return { dateRange: undefined, resolution: undefined };
}

export const ExportTeamsDetailedUsageRequest = {
  encode(message: ExportTeamsDetailedUsageRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.dateRange !== undefined) {
      DateRange.encode(message.dateRange, writer.uint32(10).fork()).ldelim();
    }
    if (message.resolution !== undefined) {
      Duration.encode(message.resolution, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ExportTeamsDetailedUsageRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExportTeamsDetailedUsageRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.dateRange = DateRange.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.resolution = Duration.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExportTeamsDetailedUsageRequest {
    return {
      dateRange: isSet(object.dateRange) ? DateRange.fromJSON(object.dateRange) : undefined,
      resolution: isSet(object.resolution) ? Duration.fromJSON(object.resolution) : undefined,
    };
  },

  toJSON(message: ExportTeamsDetailedUsageRequest): unknown {
    const obj: any = {};
    if (message.dateRange !== undefined) {
      obj.dateRange = DateRange.toJSON(message.dateRange);
    }
    if (message.resolution !== undefined) {
      obj.resolution = Duration.toJSON(message.resolution);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ExportTeamsDetailedUsageRequest>, I>>(base?: I): ExportTeamsDetailedUsageRequest {
    return ExportTeamsDetailedUsageRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExportTeamsDetailedUsageRequest>, I>>(
    object: I,
  ): ExportTeamsDetailedUsageRequest {
    const message = createBaseExportTeamsDetailedUsageRequest();
    message.dateRange = (object.dateRange !== undefined && object.dateRange !== null)
      ? DateRange.fromPartial(object.dateRange)
      : undefined;
    message.resolution = (object.resolution !== undefined && object.resolution !== null)
      ? Duration.fromPartial(object.resolution)
      : undefined;
    return message;
  },
};

function createBaseDataUsage(): DataUsage {
  return { timestamp: undefined, sizeGb: undefined, dimensions: [] };
}

export const DataUsage = {
  encode(message: DataUsage, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.timestamp !== undefined) {
      Timestamp.encode(toTimestamp(message.timestamp), writer.uint32(10).fork()).ldelim();
    }
    if (message.sizeGb !== undefined) {
      FloatValue.encode({ value: message.sizeGb! }, writer.uint32(18).fork()).ldelim();
    }
    for (const v of message.dimensions) {
      Dimension.encode(v!, writer.uint32(26).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): DataUsage {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDataUsage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.timestamp = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.sizeGb = FloatValue.decode(reader, reader.uint32()).value;
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.dimensions.push(Dimension.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DataUsage {
    return {
      timestamp: isSet(object.timestamp) ? fromJsonTimestamp(object.timestamp) : undefined,
      sizeGb: isSet(object.sizeGb) ? Number(object.sizeGb) : undefined,
      dimensions: globalThis.Array.isArray(object?.dimensions)
        ? object.dimensions.map((e: any) => Dimension.fromJSON(e))
        : [],
    };
  },

  toJSON(message: DataUsage): unknown {
    const obj: any = {};
    if (message.timestamp !== undefined) {
      obj.timestamp = message.timestamp.toISOString();
    }
    if (message.sizeGb !== undefined) {
      obj.sizeGb = message.sizeGb;
    }
    if (message.dimensions?.length) {
      obj.dimensions = message.dimensions.map((e) => Dimension.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DataUsage>, I>>(base?: I): DataUsage {
    return DataUsage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DataUsage>, I>>(object: I): DataUsage {
    const message = createBaseDataUsage();
    message.timestamp = object.timestamp ?? undefined;
    message.sizeGb = object.sizeGb ?? undefined;
    message.dimensions = object.dimensions?.map((e) => Dimension.fromPartial(e)) || [];
    return message;
  },
};

function createBaseTeamDetailedUsage(): TeamDetailedUsage {
  return { teamId: 0, externalId: undefined, retention: 0, dataUsage: [] };
}

export const TeamDetailedUsage = {
  encode(message: TeamDetailedUsage, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.teamId !== 0) {
      writer.uint32(8).uint32(message.teamId);
    }
    if (message.externalId !== undefined) {
      writer.uint32(18).string(message.externalId);
    }
    if (message.retention !== 0) {
      writer.uint32(24).uint32(message.retention);
    }
    for (const v of message.dataUsage) {
      DataUsage.encode(v!, writer.uint32(34).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): TeamDetailedUsage {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTeamDetailedUsage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.teamId = reader.uint32();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.externalId = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.retention = reader.uint32();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.dataUsage.push(DataUsage.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TeamDetailedUsage {
    return {
      teamId: isSet(object.teamId) ? globalThis.Number(object.teamId) : 0,
      externalId: isSet(object.externalId) ? globalThis.String(object.externalId) : undefined,
      retention: isSet(object.retention) ? globalThis.Number(object.retention) : 0,
      dataUsage: globalThis.Array.isArray(object?.dataUsage)
        ? object.dataUsage.map((e: any) => DataUsage.fromJSON(e))
        : [],
    };
  },

  toJSON(message: TeamDetailedUsage): unknown {
    const obj: any = {};
    if (message.teamId !== 0) {
      obj.teamId = Math.round(message.teamId);
    }
    if (message.externalId !== undefined) {
      obj.externalId = message.externalId;
    }
    if (message.retention !== 0) {
      obj.retention = Math.round(message.retention);
    }
    if (message.dataUsage?.length) {
      obj.dataUsage = message.dataUsage.map((e) => DataUsage.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TeamDetailedUsage>, I>>(base?: I): TeamDetailedUsage {
    return TeamDetailedUsage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TeamDetailedUsage>, I>>(object: I): TeamDetailedUsage {
    const message = createBaseTeamDetailedUsage();
    message.teamId = object.teamId ?? 0;
    message.externalId = object.externalId ?? undefined;
    message.retention = object.retention ?? 0;
    message.dataUsage = object.dataUsage?.map((e) => DataUsage.fromPartial(e)) || [];
    return message;
  },
};

function createBaseExportTeamsDetailedUsageResponse(): ExportTeamsDetailedUsageResponse {
  return { teamUsage: undefined };
}

export const ExportTeamsDetailedUsageResponse = {
  encode(message: ExportTeamsDetailedUsageResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.teamUsage !== undefined) {
      TeamDetailedUsage.encode(message.teamUsage, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ExportTeamsDetailedUsageResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExportTeamsDetailedUsageResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.teamUsage = TeamDetailedUsage.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExportTeamsDetailedUsageResponse {
    return { teamUsage: isSet(object.teamUsage) ? TeamDetailedUsage.fromJSON(object.teamUsage) : undefined };
  },

  toJSON(message: ExportTeamsDetailedUsageResponse): unknown {
    const obj: any = {};
    if (message.teamUsage !== undefined) {
      obj.teamUsage = TeamDetailedUsage.toJSON(message.teamUsage);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ExportTeamsDetailedUsageResponse>, I>>(
    base?: I,
  ): ExportTeamsDetailedUsageResponse {
    return ExportTeamsDetailedUsageResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExportTeamsDetailedUsageResponse>, I>>(
    object: I,
  ): ExportTeamsDetailedUsageResponse {
    const message = createBaseExportTeamsDetailedUsageResponse();
    message.teamUsage = (object.teamUsage !== undefined && object.teamUsage !== null)
      ? TeamDetailedUsage.fromPartial(object.teamUsage)
      : undefined;
    return message;
  },
};

export interface InternalDataUsageService {
  /**
   * / Streams entries containing detailed data usage for all teams given a date range and resolution interval.
   * / Results are aggregated by pillar and priority
   * / Pillars i.e. [Logs, Metrics, Tracing, ... ]
   * / Priority i.e. [High, Medium, Low, ... ]
   */
  ExportTeamsDetailedUsage(request: ExportTeamsDetailedUsageRequest): Observable<ExportTeamsDetailedUsageResponse>;
}

export const InternalDataUsageServiceServiceName = "com.coralogix.datausage.v2.InternalDataUsageService";
export class InternalDataUsageServiceClientImpl implements InternalDataUsageService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || InternalDataUsageServiceServiceName;
    this.rpc = rpc;
    this.ExportTeamsDetailedUsage = this.ExportTeamsDetailedUsage.bind(this);
  }
  ExportTeamsDetailedUsage(request: ExportTeamsDetailedUsageRequest): Observable<ExportTeamsDetailedUsageResponse> {
    const data = ExportTeamsDetailedUsageRequest.encode(request).finish();
    const result = this.rpc.serverStreamingRequest(this.service, "ExportTeamsDetailedUsage", data);
    return result.pipe(map((data) => ExportTeamsDetailedUsageResponse.decode(_m0.Reader.create(data))));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
  clientStreamingRequest(service: string, method: string, data: Observable<Uint8Array>): Promise<Uint8Array>;
  serverStreamingRequest(service: string, method: string, data: Uint8Array): Observable<Uint8Array>;
  bidirectionalStreamingRequest(service: string, method: string, data: Observable<Uint8Array>): Observable<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
