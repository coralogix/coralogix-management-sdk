// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.176.1
//   protoc               v3.21.12
// source: com/coralogix/alerts/v1/tracing_alert.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";

export const protobufPackage = "com.coralogix.alerts.v1";

export interface TracingAlert {
  conditionLatency: number;
  fieldFilters: FilterData[];
  tagFilters: FilterData[];
}

export interface FilterData {
  field: string;
  filters: Filters[];
}

export interface Filters {
  values: string[];
  operator: string;
}

function createBaseTracingAlert(): TracingAlert {
  return { conditionLatency: 0, fieldFilters: [], tagFilters: [] };
}

export const TracingAlert = {
  encode(message: TracingAlert, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.conditionLatency !== 0) {
      writer.uint32(8).uint32(message.conditionLatency);
    }
    for (const v of message.fieldFilters) {
      FilterData.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    for (const v of message.tagFilters) {
      FilterData.encode(v!, writer.uint32(26).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): TracingAlert {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTracingAlert();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.conditionLatency = reader.uint32();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.fieldFilters.push(FilterData.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.tagFilters.push(FilterData.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TracingAlert {
    return {
      conditionLatency: isSet(object.conditionLatency) ? globalThis.Number(object.conditionLatency) : 0,
      fieldFilters: globalThis.Array.isArray(object?.fieldFilters)
        ? object.fieldFilters.map((e: any) => FilterData.fromJSON(e))
        : [],
      tagFilters: globalThis.Array.isArray(object?.tagFilters)
        ? object.tagFilters.map((e: any) => FilterData.fromJSON(e))
        : [],
    };
  },

  toJSON(message: TracingAlert): unknown {
    const obj: any = {};
    if (message.conditionLatency !== 0) {
      obj.conditionLatency = Math.round(message.conditionLatency);
    }
    if (message.fieldFilters?.length) {
      obj.fieldFilters = message.fieldFilters.map((e) => FilterData.toJSON(e));
    }
    if (message.tagFilters?.length) {
      obj.tagFilters = message.tagFilters.map((e) => FilterData.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TracingAlert>, I>>(base?: I): TracingAlert {
    return TracingAlert.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TracingAlert>, I>>(object: I): TracingAlert {
    const message = createBaseTracingAlert();
    message.conditionLatency = object.conditionLatency ?? 0;
    message.fieldFilters = object.fieldFilters?.map((e) => FilterData.fromPartial(e)) || [];
    message.tagFilters = object.tagFilters?.map((e) => FilterData.fromPartial(e)) || [];
    return message;
  },
};

function createBaseFilterData(): FilterData {
  return { field: "", filters: [] };
}

export const FilterData = {
  encode(message: FilterData, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.field !== "") {
      writer.uint32(10).string(message.field);
    }
    for (const v of message.filters) {
      Filters.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): FilterData {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFilterData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.field = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.filters.push(Filters.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FilterData {
    return {
      field: isSet(object.field) ? globalThis.String(object.field) : "",
      filters: globalThis.Array.isArray(object?.filters) ? object.filters.map((e: any) => Filters.fromJSON(e)) : [],
    };
  },

  toJSON(message: FilterData): unknown {
    const obj: any = {};
    if (message.field !== "") {
      obj.field = message.field;
    }
    if (message.filters?.length) {
      obj.filters = message.filters.map((e) => Filters.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FilterData>, I>>(base?: I): FilterData {
    return FilterData.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FilterData>, I>>(object: I): FilterData {
    const message = createBaseFilterData();
    message.field = object.field ?? "";
    message.filters = object.filters?.map((e) => Filters.fromPartial(e)) || [];
    return message;
  },
};

function createBaseFilters(): Filters {
  return { values: [], operator: "" };
}

export const Filters = {
  encode(message: Filters, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.values) {
      writer.uint32(10).string(v!);
    }
    if (message.operator !== "") {
      writer.uint32(18).string(message.operator);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Filters {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFilters();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.values.push(reader.string());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.operator = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Filters {
    return {
      values: globalThis.Array.isArray(object?.values) ? object.values.map((e: any) => globalThis.String(e)) : [],
      operator: isSet(object.operator) ? globalThis.String(object.operator) : "",
    };
  },

  toJSON(message: Filters): unknown {
    const obj: any = {};
    if (message.values?.length) {
      obj.values = message.values;
    }
    if (message.operator !== "") {
      obj.operator = message.operator;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Filters>, I>>(base?: I): Filters {
    return Filters.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Filters>, I>>(object: I): Filters {
    const message = createBaseFilters();
    message.values = object.values?.map((e) => e) || [];
    message.operator = object.operator ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
