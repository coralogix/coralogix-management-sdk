// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.176.1
//   protoc               v3.21.12
// source: com/coralogix/alerts/v1/alert_active_when.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { Time } from "./date_time";

export const protobufPackage = "com.coralogix.alerts.v1";

export enum DayOfWeek {
  DAY_OF_WEEK_MONDAY_OR_UNSPECIFIED = 0,
  DAY_OF_WEEK_TUESDAY = 1,
  DAY_OF_WEEK_WEDNESDAY = 2,
  DAY_OF_WEEK_THURSDAY = 3,
  DAY_OF_WEEK_FRIDAY = 4,
  DAY_OF_WEEK_SATURDAY = 5,
  DAY_OF_WEEK_SUNDAY = 6,
  UNRECOGNIZED = -1,
}

export function dayOfWeekFromJSON(object: any): DayOfWeek {
  switch (object) {
    case 0:
    case "DAY_OF_WEEK_MONDAY_OR_UNSPECIFIED":
      return DayOfWeek.DAY_OF_WEEK_MONDAY_OR_UNSPECIFIED;
    case 1:
    case "DAY_OF_WEEK_TUESDAY":
      return DayOfWeek.DAY_OF_WEEK_TUESDAY;
    case 2:
    case "DAY_OF_WEEK_WEDNESDAY":
      return DayOfWeek.DAY_OF_WEEK_WEDNESDAY;
    case 3:
    case "DAY_OF_WEEK_THURSDAY":
      return DayOfWeek.DAY_OF_WEEK_THURSDAY;
    case 4:
    case "DAY_OF_WEEK_FRIDAY":
      return DayOfWeek.DAY_OF_WEEK_FRIDAY;
    case 5:
    case "DAY_OF_WEEK_SATURDAY":
      return DayOfWeek.DAY_OF_WEEK_SATURDAY;
    case 6:
    case "DAY_OF_WEEK_SUNDAY":
      return DayOfWeek.DAY_OF_WEEK_SUNDAY;
    case -1:
    case "UNRECOGNIZED":
    default:
      return DayOfWeek.UNRECOGNIZED;
  }
}

export function dayOfWeekToJSON(object: DayOfWeek): string {
  switch (object) {
    case DayOfWeek.DAY_OF_WEEK_MONDAY_OR_UNSPECIFIED:
      return "DAY_OF_WEEK_MONDAY_OR_UNSPECIFIED";
    case DayOfWeek.DAY_OF_WEEK_TUESDAY:
      return "DAY_OF_WEEK_TUESDAY";
    case DayOfWeek.DAY_OF_WEEK_WEDNESDAY:
      return "DAY_OF_WEEK_WEDNESDAY";
    case DayOfWeek.DAY_OF_WEEK_THURSDAY:
      return "DAY_OF_WEEK_THURSDAY";
    case DayOfWeek.DAY_OF_WEEK_FRIDAY:
      return "DAY_OF_WEEK_FRIDAY";
    case DayOfWeek.DAY_OF_WEEK_SATURDAY:
      return "DAY_OF_WEEK_SATURDAY";
    case DayOfWeek.DAY_OF_WEEK_SUNDAY:
      return "DAY_OF_WEEK_SUNDAY";
    case DayOfWeek.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface AlertActiveWhen {
  timeframes: AlertActiveTimeframe[];
}

export interface AlertActiveTimeframe {
  daysOfWeek: DayOfWeek[];
  range: TimeRange | undefined;
}

export interface TimeRange {
  start: Time | undefined;
  end: Time | undefined;
}

function createBaseAlertActiveWhen(): AlertActiveWhen {
  return { timeframes: [] };
}

export const AlertActiveWhen = {
  encode(message: AlertActiveWhen, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.timeframes) {
      AlertActiveTimeframe.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): AlertActiveWhen {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAlertActiveWhen();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.timeframes.push(AlertActiveTimeframe.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AlertActiveWhen {
    return {
      timeframes: globalThis.Array.isArray(object?.timeframes)
        ? object.timeframes.map((e: any) => AlertActiveTimeframe.fromJSON(e))
        : [],
    };
  },

  toJSON(message: AlertActiveWhen): unknown {
    const obj: any = {};
    if (message.timeframes?.length) {
      obj.timeframes = message.timeframes.map((e) => AlertActiveTimeframe.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AlertActiveWhen>, I>>(base?: I): AlertActiveWhen {
    return AlertActiveWhen.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AlertActiveWhen>, I>>(object: I): AlertActiveWhen {
    const message = createBaseAlertActiveWhen();
    message.timeframes = object.timeframes?.map((e) => AlertActiveTimeframe.fromPartial(e)) || [];
    return message;
  },
};

function createBaseAlertActiveTimeframe(): AlertActiveTimeframe {
  return { daysOfWeek: [], range: undefined };
}

export const AlertActiveTimeframe = {
  encode(message: AlertActiveTimeframe, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    writer.uint32(10).fork();
    for (const v of message.daysOfWeek) {
      writer.int32(v);
    }
    writer.ldelim();
    if (message.range !== undefined) {
      TimeRange.encode(message.range, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): AlertActiveTimeframe {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAlertActiveTimeframe();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag === 8) {
            message.daysOfWeek.push(reader.int32() as any);

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.daysOfWeek.push(reader.int32() as any);
            }

            continue;
          }

          break;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.range = TimeRange.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AlertActiveTimeframe {
    return {
      daysOfWeek: globalThis.Array.isArray(object?.daysOfWeek)
        ? object.daysOfWeek.map((e: any) => dayOfWeekFromJSON(e))
        : [],
      range: isSet(object.range) ? TimeRange.fromJSON(object.range) : undefined,
    };
  },

  toJSON(message: AlertActiveTimeframe): unknown {
    const obj: any = {};
    if (message.daysOfWeek?.length) {
      obj.daysOfWeek = message.daysOfWeek.map((e) => dayOfWeekToJSON(e));
    }
    if (message.range !== undefined) {
      obj.range = TimeRange.toJSON(message.range);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AlertActiveTimeframe>, I>>(base?: I): AlertActiveTimeframe {
    return AlertActiveTimeframe.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AlertActiveTimeframe>, I>>(object: I): AlertActiveTimeframe {
    const message = createBaseAlertActiveTimeframe();
    message.daysOfWeek = object.daysOfWeek?.map((e) => e) || [];
    message.range = (object.range !== undefined && object.range !== null)
      ? TimeRange.fromPartial(object.range)
      : undefined;
    return message;
  },
};

function createBaseTimeRange(): TimeRange {
  return { start: undefined, end: undefined };
}

export const TimeRange = {
  encode(message: TimeRange, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.start !== undefined) {
      Time.encode(message.start, writer.uint32(10).fork()).ldelim();
    }
    if (message.end !== undefined) {
      Time.encode(message.end, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): TimeRange {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTimeRange();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.start = Time.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.end = Time.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TimeRange {
    return {
      start: isSet(object.start) ? Time.fromJSON(object.start) : undefined,
      end: isSet(object.end) ? Time.fromJSON(object.end) : undefined,
    };
  },

  toJSON(message: TimeRange): unknown {
    const obj: any = {};
    if (message.start !== undefined) {
      obj.start = Time.toJSON(message.start);
    }
    if (message.end !== undefined) {
      obj.end = Time.toJSON(message.end);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TimeRange>, I>>(base?: I): TimeRange {
    return TimeRange.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TimeRange>, I>>(object: I): TimeRange {
    const message = createBaseTimeRange();
    message.start = (object.start !== undefined && object.start !== null) ? Time.fromPartial(object.start) : undefined;
    message.end = (object.end !== undefined && object.end !== null) ? Time.fromPartial(object.end) : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
