// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.176.1
//   protoc               v3.21.12
// source: com/coralogix/alerts/v1/flow_alert.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { BoolValue, StringValue, UInt32Value } from "../../../../google/protobuf/wrappers";

export const protobufPackage = "com.coralogix.alerts.v1";

export enum FlowOperator {
  AND = 0,
  OR = 1,
  UNRECOGNIZED = -1,
}

export function flowOperatorFromJSON(object: any): FlowOperator {
  switch (object) {
    case 0:
    case "AND":
      return FlowOperator.AND;
    case 1:
    case "OR":
      return FlowOperator.OR;
    case -1:
    case "UNRECOGNIZED":
    default:
      return FlowOperator.UNRECOGNIZED;
  }
}

export function flowOperatorToJSON(object: FlowOperator): string {
  switch (object) {
    case FlowOperator.AND:
      return "AND";
    case FlowOperator.OR:
      return "OR";
    case FlowOperator.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface FlowStage {
  groups: FlowGroup[];
  timeframe: FlowTimeframe | undefined;
}

export interface FlowAlert {
  id: string | undefined;
  not: boolean | undefined;
}

export interface FlowAlerts {
  op: FlowOperator;
  values: FlowAlert[];
}

export interface FlowGroup {
  alerts: FlowAlerts | undefined;
  nextOp: FlowOperator;
}

export interface FlowTimeframe {
  ms: number | undefined;
}

function createBaseFlowStage(): FlowStage {
  return { groups: [], timeframe: undefined };
}

export const FlowStage = {
  encode(message: FlowStage, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.groups) {
      FlowGroup.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    if (message.timeframe !== undefined) {
      FlowTimeframe.encode(message.timeframe, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): FlowStage {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFlowStage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.groups.push(FlowGroup.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.timeframe = FlowTimeframe.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FlowStage {
    return {
      groups: globalThis.Array.isArray(object?.groups) ? object.groups.map((e: any) => FlowGroup.fromJSON(e)) : [],
      timeframe: isSet(object.timeframe) ? FlowTimeframe.fromJSON(object.timeframe) : undefined,
    };
  },

  toJSON(message: FlowStage): unknown {
    const obj: any = {};
    if (message.groups?.length) {
      obj.groups = message.groups.map((e) => FlowGroup.toJSON(e));
    }
    if (message.timeframe !== undefined) {
      obj.timeframe = FlowTimeframe.toJSON(message.timeframe);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FlowStage>, I>>(base?: I): FlowStage {
    return FlowStage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FlowStage>, I>>(object: I): FlowStage {
    const message = createBaseFlowStage();
    message.groups = object.groups?.map((e) => FlowGroup.fromPartial(e)) || [];
    message.timeframe = (object.timeframe !== undefined && object.timeframe !== null)
      ? FlowTimeframe.fromPartial(object.timeframe)
      : undefined;
    return message;
  },
};

function createBaseFlowAlert(): FlowAlert {
  return { id: undefined, not: undefined };
}

export const FlowAlert = {
  encode(message: FlowAlert, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== undefined) {
      StringValue.encode({ value: message.id! }, writer.uint32(10).fork()).ldelim();
    }
    if (message.not !== undefined) {
      BoolValue.encode({ value: message.not! }, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): FlowAlert {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFlowAlert();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = StringValue.decode(reader, reader.uint32()).value;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.not = BoolValue.decode(reader, reader.uint32()).value;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FlowAlert {
    return {
      id: isSet(object.id) ? String(object.id) : undefined,
      not: isSet(object.not) ? Boolean(object.not) : undefined,
    };
  },

  toJSON(message: FlowAlert): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = message.id;
    }
    if (message.not !== undefined) {
      obj.not = message.not;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FlowAlert>, I>>(base?: I): FlowAlert {
    return FlowAlert.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FlowAlert>, I>>(object: I): FlowAlert {
    const message = createBaseFlowAlert();
    message.id = object.id ?? undefined;
    message.not = object.not ?? undefined;
    return message;
  },
};

function createBaseFlowAlerts(): FlowAlerts {
  return { op: 0, values: [] };
}

export const FlowAlerts = {
  encode(message: FlowAlerts, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.op !== 0) {
      writer.uint32(8).int32(message.op);
    }
    for (const v of message.values) {
      FlowAlert.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): FlowAlerts {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFlowAlerts();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.op = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.values.push(FlowAlert.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FlowAlerts {
    return {
      op: isSet(object.op) ? flowOperatorFromJSON(object.op) : 0,
      values: globalThis.Array.isArray(object?.values) ? object.values.map((e: any) => FlowAlert.fromJSON(e)) : [],
    };
  },

  toJSON(message: FlowAlerts): unknown {
    const obj: any = {};
    if (message.op !== 0) {
      obj.op = flowOperatorToJSON(message.op);
    }
    if (message.values?.length) {
      obj.values = message.values.map((e) => FlowAlert.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FlowAlerts>, I>>(base?: I): FlowAlerts {
    return FlowAlerts.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FlowAlerts>, I>>(object: I): FlowAlerts {
    const message = createBaseFlowAlerts();
    message.op = object.op ?? 0;
    message.values = object.values?.map((e) => FlowAlert.fromPartial(e)) || [];
    return message;
  },
};

function createBaseFlowGroup(): FlowGroup {
  return { alerts: undefined, nextOp: 0 };
}

export const FlowGroup = {
  encode(message: FlowGroup, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.alerts !== undefined) {
      FlowAlerts.encode(message.alerts, writer.uint32(10).fork()).ldelim();
    }
    if (message.nextOp !== 0) {
      writer.uint32(16).int32(message.nextOp);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): FlowGroup {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFlowGroup();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.alerts = FlowAlerts.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.nextOp = reader.int32() as any;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FlowGroup {
    return {
      alerts: isSet(object.alerts) ? FlowAlerts.fromJSON(object.alerts) : undefined,
      nextOp: isSet(object.nextOp) ? flowOperatorFromJSON(object.nextOp) : 0,
    };
  },

  toJSON(message: FlowGroup): unknown {
    const obj: any = {};
    if (message.alerts !== undefined) {
      obj.alerts = FlowAlerts.toJSON(message.alerts);
    }
    if (message.nextOp !== 0) {
      obj.nextOp = flowOperatorToJSON(message.nextOp);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FlowGroup>, I>>(base?: I): FlowGroup {
    return FlowGroup.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FlowGroup>, I>>(object: I): FlowGroup {
    const message = createBaseFlowGroup();
    message.alerts = (object.alerts !== undefined && object.alerts !== null)
      ? FlowAlerts.fromPartial(object.alerts)
      : undefined;
    message.nextOp = object.nextOp ?? 0;
    return message;
  },
};

function createBaseFlowTimeframe(): FlowTimeframe {
  return { ms: undefined };
}

export const FlowTimeframe = {
  encode(message: FlowTimeframe, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.ms !== undefined) {
      UInt32Value.encode({ value: message.ms! }, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): FlowTimeframe {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFlowTimeframe();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.ms = UInt32Value.decode(reader, reader.uint32()).value;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FlowTimeframe {
    return { ms: isSet(object.ms) ? Number(object.ms) : undefined };
  },

  toJSON(message: FlowTimeframe): unknown {
    const obj: any = {};
    if (message.ms !== undefined) {
      obj.ms = message.ms;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FlowTimeframe>, I>>(base?: I): FlowTimeframe {
    return FlowTimeframe.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FlowTimeframe>, I>>(object: I): FlowTimeframe {
    const message = createBaseFlowTimeframe();
    message.ms = object.ms ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
