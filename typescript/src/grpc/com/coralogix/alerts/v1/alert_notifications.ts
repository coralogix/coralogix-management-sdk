// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.176.1
//   protoc               v3.21.12
// source: com/coralogix/alerts/v1/alert_notifications.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { StringValue } from "../../../../google/protobuf/wrappers";

export const protobufPackage = "com.coralogix.alerts.v1";

export interface AlertNotifications {
  emails: string[];
  integrations: string[];
}

function createBaseAlertNotifications(): AlertNotifications {
  return { emails: [], integrations: [] };
}

export const AlertNotifications = {
  encode(message: AlertNotifications, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.emails) {
      StringValue.encode({ value: v!! }, writer.uint32(10).fork()).ldelim();
    }
    for (const v of message.integrations) {
      StringValue.encode({ value: v!! }, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): AlertNotifications {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAlertNotifications();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.emails.push(StringValue.decode(reader, reader.uint32()).value);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.integrations.push(StringValue.decode(reader, reader.uint32()).value);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AlertNotifications {
    return {
      emails: globalThis.Array.isArray(object?.emails) ? object.emails.map((e: any) => String(e)) : [],
      integrations: globalThis.Array.isArray(object?.integrations)
        ? object.integrations.map((e: any) => String(e))
        : [],
    };
  },

  toJSON(message: AlertNotifications): unknown {
    const obj: any = {};
    if (message.emails?.length) {
      obj.emails = message.emails;
    }
    if (message.integrations?.length) {
      obj.integrations = message.integrations;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AlertNotifications>, I>>(base?: I): AlertNotifications {
    return AlertNotifications.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AlertNotifications>, I>>(object: I): AlertNotifications {
    const message = createBaseAlertNotifications();
    message.emails = object.emails?.map((e) => e) || [];
    message.integrations = object.integrations?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };
