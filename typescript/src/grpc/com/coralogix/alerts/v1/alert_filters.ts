// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.176.1
//   protoc               v3.21.12
// source: com/coralogix/alerts/v1/alert_filters.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { StringValue } from "../../../../google/protobuf/wrappers";

export const protobufPackage = "com.coralogix.alerts.v1";

export interface AlertFilters {
  severities: AlertFilters_LogSeverity[];
  metadata: AlertFilters_MetadataFilters | undefined;
  alias: string | undefined;
  text: string | undefined;
  ratioAlerts: AlertFilters_RatioAlert[];
  filterType: AlertFilters_FilterType;
}

export enum AlertFilters_LogSeverity {
  LOG_SEVERITY_DEBUG_OR_UNSPECIFIED = 0,
  LOG_SEVERITY_VERBOSE = 1,
  LOG_SEVERITY_INFO = 2,
  LOG_SEVERITY_WARNING = 3,
  LOG_SEVERITY_ERROR = 4,
  LOG_SEVERITY_CRITICAL = 5,
  UNRECOGNIZED = -1,
}

export function alertFilters_LogSeverityFromJSON(object: any): AlertFilters_LogSeverity {
  switch (object) {
    case 0:
    case "LOG_SEVERITY_DEBUG_OR_UNSPECIFIED":
      return AlertFilters_LogSeverity.LOG_SEVERITY_DEBUG_OR_UNSPECIFIED;
    case 1:
    case "LOG_SEVERITY_VERBOSE":
      return AlertFilters_LogSeverity.LOG_SEVERITY_VERBOSE;
    case 2:
    case "LOG_SEVERITY_INFO":
      return AlertFilters_LogSeverity.LOG_SEVERITY_INFO;
    case 3:
    case "LOG_SEVERITY_WARNING":
      return AlertFilters_LogSeverity.LOG_SEVERITY_WARNING;
    case 4:
    case "LOG_SEVERITY_ERROR":
      return AlertFilters_LogSeverity.LOG_SEVERITY_ERROR;
    case 5:
    case "LOG_SEVERITY_CRITICAL":
      return AlertFilters_LogSeverity.LOG_SEVERITY_CRITICAL;
    case -1:
    case "UNRECOGNIZED":
    default:
      return AlertFilters_LogSeverity.UNRECOGNIZED;
  }
}

export function alertFilters_LogSeverityToJSON(object: AlertFilters_LogSeverity): string {
  switch (object) {
    case AlertFilters_LogSeverity.LOG_SEVERITY_DEBUG_OR_UNSPECIFIED:
      return "LOG_SEVERITY_DEBUG_OR_UNSPECIFIED";
    case AlertFilters_LogSeverity.LOG_SEVERITY_VERBOSE:
      return "LOG_SEVERITY_VERBOSE";
    case AlertFilters_LogSeverity.LOG_SEVERITY_INFO:
      return "LOG_SEVERITY_INFO";
    case AlertFilters_LogSeverity.LOG_SEVERITY_WARNING:
      return "LOG_SEVERITY_WARNING";
    case AlertFilters_LogSeverity.LOG_SEVERITY_ERROR:
      return "LOG_SEVERITY_ERROR";
    case AlertFilters_LogSeverity.LOG_SEVERITY_CRITICAL:
      return "LOG_SEVERITY_CRITICAL";
    case AlertFilters_LogSeverity.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum AlertFilters_FilterType {
  FILTER_TYPE_TEXT_OR_UNSPECIFIED = 0,
  FILTER_TYPE_TEMPLATE = 1,
  FILTER_TYPE_RATIO = 2,
  FILTER_TYPE_UNIQUE_COUNT = 3,
  FILTER_TYPE_TIME_RELATIVE = 4,
  FILTER_TYPE_METRIC = 5,
  FILTER_TYPE_TRACING = 6,
  FILTER_TYPE_FLOW = 7,
  UNRECOGNIZED = -1,
}

export function alertFilters_FilterTypeFromJSON(object: any): AlertFilters_FilterType {
  switch (object) {
    case 0:
    case "FILTER_TYPE_TEXT_OR_UNSPECIFIED":
      return AlertFilters_FilterType.FILTER_TYPE_TEXT_OR_UNSPECIFIED;
    case 1:
    case "FILTER_TYPE_TEMPLATE":
      return AlertFilters_FilterType.FILTER_TYPE_TEMPLATE;
    case 2:
    case "FILTER_TYPE_RATIO":
      return AlertFilters_FilterType.FILTER_TYPE_RATIO;
    case 3:
    case "FILTER_TYPE_UNIQUE_COUNT":
      return AlertFilters_FilterType.FILTER_TYPE_UNIQUE_COUNT;
    case 4:
    case "FILTER_TYPE_TIME_RELATIVE":
      return AlertFilters_FilterType.FILTER_TYPE_TIME_RELATIVE;
    case 5:
    case "FILTER_TYPE_METRIC":
      return AlertFilters_FilterType.FILTER_TYPE_METRIC;
    case 6:
    case "FILTER_TYPE_TRACING":
      return AlertFilters_FilterType.FILTER_TYPE_TRACING;
    case 7:
    case "FILTER_TYPE_FLOW":
      return AlertFilters_FilterType.FILTER_TYPE_FLOW;
    case -1:
    case "UNRECOGNIZED":
    default:
      return AlertFilters_FilterType.UNRECOGNIZED;
  }
}

export function alertFilters_FilterTypeToJSON(object: AlertFilters_FilterType): string {
  switch (object) {
    case AlertFilters_FilterType.FILTER_TYPE_TEXT_OR_UNSPECIFIED:
      return "FILTER_TYPE_TEXT_OR_UNSPECIFIED";
    case AlertFilters_FilterType.FILTER_TYPE_TEMPLATE:
      return "FILTER_TYPE_TEMPLATE";
    case AlertFilters_FilterType.FILTER_TYPE_RATIO:
      return "FILTER_TYPE_RATIO";
    case AlertFilters_FilterType.FILTER_TYPE_UNIQUE_COUNT:
      return "FILTER_TYPE_UNIQUE_COUNT";
    case AlertFilters_FilterType.FILTER_TYPE_TIME_RELATIVE:
      return "FILTER_TYPE_TIME_RELATIVE";
    case AlertFilters_FilterType.FILTER_TYPE_METRIC:
      return "FILTER_TYPE_METRIC";
    case AlertFilters_FilterType.FILTER_TYPE_TRACING:
      return "FILTER_TYPE_TRACING";
    case AlertFilters_FilterType.FILTER_TYPE_FLOW:
      return "FILTER_TYPE_FLOW";
    case AlertFilters_FilterType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface AlertFilters_MetadataFilters {
  categories: string[];
  applications: string[];
  subsystems: string[];
  computers: string[];
  classes: string[];
  methods: string[];
  ipAddresses: string[];
}

export interface AlertFilters_RatioAlert {
  alias: string | undefined;
  text: string | undefined;
  severities: AlertFilters_LogSeverity[];
  applications: string[];
  subsystems: string[];
  groupBy: string[];
}

function createBaseAlertFilters(): AlertFilters {
  return { severities: [], metadata: undefined, alias: undefined, text: undefined, ratioAlerts: [], filterType: 0 };
}

export const AlertFilters = {
  encode(message: AlertFilters, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    writer.uint32(10).fork();
    for (const v of message.severities) {
      writer.int32(v);
    }
    writer.ldelim();
    if (message.metadata !== undefined) {
      AlertFilters_MetadataFilters.encode(message.metadata, writer.uint32(18).fork()).ldelim();
    }
    if (message.alias !== undefined) {
      StringValue.encode({ value: message.alias! }, writer.uint32(26).fork()).ldelim();
    }
    if (message.text !== undefined) {
      StringValue.encode({ value: message.text! }, writer.uint32(34).fork()).ldelim();
    }
    for (const v of message.ratioAlerts) {
      AlertFilters_RatioAlert.encode(v!, writer.uint32(42).fork()).ldelim();
    }
    if (message.filterType !== 0) {
      writer.uint32(48).int32(message.filterType);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): AlertFilters {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAlertFilters();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag === 8) {
            message.severities.push(reader.int32() as any);

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.severities.push(reader.int32() as any);
            }

            continue;
          }

          break;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.metadata = AlertFilters_MetadataFilters.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.alias = StringValue.decode(reader, reader.uint32()).value;
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.text = StringValue.decode(reader, reader.uint32()).value;
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.ratioAlerts.push(AlertFilters_RatioAlert.decode(reader, reader.uint32()));
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.filterType = reader.int32() as any;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AlertFilters {
    return {
      severities: globalThis.Array.isArray(object?.severities)
        ? object.severities.map((e: any) => alertFilters_LogSeverityFromJSON(e))
        : [],
      metadata: isSet(object.metadata) ? AlertFilters_MetadataFilters.fromJSON(object.metadata) : undefined,
      alias: isSet(object.alias) ? String(object.alias) : undefined,
      text: isSet(object.text) ? String(object.text) : undefined,
      ratioAlerts: globalThis.Array.isArray(object?.ratioAlerts)
        ? object.ratioAlerts.map((e: any) => AlertFilters_RatioAlert.fromJSON(e))
        : [],
      filterType: isSet(object.filterType) ? alertFilters_FilterTypeFromJSON(object.filterType) : 0,
    };
  },

  toJSON(message: AlertFilters): unknown {
    const obj: any = {};
    if (message.severities?.length) {
      obj.severities = message.severities.map((e) => alertFilters_LogSeverityToJSON(e));
    }
    if (message.metadata !== undefined) {
      obj.metadata = AlertFilters_MetadataFilters.toJSON(message.metadata);
    }
    if (message.alias !== undefined) {
      obj.alias = message.alias;
    }
    if (message.text !== undefined) {
      obj.text = message.text;
    }
    if (message.ratioAlerts?.length) {
      obj.ratioAlerts = message.ratioAlerts.map((e) => AlertFilters_RatioAlert.toJSON(e));
    }
    if (message.filterType !== 0) {
      obj.filterType = alertFilters_FilterTypeToJSON(message.filterType);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AlertFilters>, I>>(base?: I): AlertFilters {
    return AlertFilters.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AlertFilters>, I>>(object: I): AlertFilters {
    const message = createBaseAlertFilters();
    message.severities = object.severities?.map((e) => e) || [];
    message.metadata = (object.metadata !== undefined && object.metadata !== null)
      ? AlertFilters_MetadataFilters.fromPartial(object.metadata)
      : undefined;
    message.alias = object.alias ?? undefined;
    message.text = object.text ?? undefined;
    message.ratioAlerts = object.ratioAlerts?.map((e) => AlertFilters_RatioAlert.fromPartial(e)) || [];
    message.filterType = object.filterType ?? 0;
    return message;
  },
};

function createBaseAlertFilters_MetadataFilters(): AlertFilters_MetadataFilters {
  return { categories: [], applications: [], subsystems: [], computers: [], classes: [], methods: [], ipAddresses: [] };
}

export const AlertFilters_MetadataFilters = {
  encode(message: AlertFilters_MetadataFilters, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.categories) {
      StringValue.encode({ value: v!! }, writer.uint32(10).fork()).ldelim();
    }
    for (const v of message.applications) {
      StringValue.encode({ value: v!! }, writer.uint32(18).fork()).ldelim();
    }
    for (const v of message.subsystems) {
      StringValue.encode({ value: v!! }, writer.uint32(26).fork()).ldelim();
    }
    for (const v of message.computers) {
      StringValue.encode({ value: v!! }, writer.uint32(34).fork()).ldelim();
    }
    for (const v of message.classes) {
      StringValue.encode({ value: v!! }, writer.uint32(42).fork()).ldelim();
    }
    for (const v of message.methods) {
      StringValue.encode({ value: v!! }, writer.uint32(50).fork()).ldelim();
    }
    for (const v of message.ipAddresses) {
      StringValue.encode({ value: v!! }, writer.uint32(58).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): AlertFilters_MetadataFilters {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAlertFilters_MetadataFilters();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.categories.push(StringValue.decode(reader, reader.uint32()).value);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.applications.push(StringValue.decode(reader, reader.uint32()).value);
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.subsystems.push(StringValue.decode(reader, reader.uint32()).value);
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.computers.push(StringValue.decode(reader, reader.uint32()).value);
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.classes.push(StringValue.decode(reader, reader.uint32()).value);
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.methods.push(StringValue.decode(reader, reader.uint32()).value);
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.ipAddresses.push(StringValue.decode(reader, reader.uint32()).value);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AlertFilters_MetadataFilters {
    return {
      categories: globalThis.Array.isArray(object?.categories) ? object.categories.map((e: any) => String(e)) : [],
      applications: globalThis.Array.isArray(object?.applications)
        ? object.applications.map((e: any) => String(e))
        : [],
      subsystems: globalThis.Array.isArray(object?.subsystems) ? object.subsystems.map((e: any) => String(e)) : [],
      computers: globalThis.Array.isArray(object?.computers) ? object.computers.map((e: any) => String(e)) : [],
      classes: globalThis.Array.isArray(object?.classes) ? object.classes.map((e: any) => String(e)) : [],
      methods: globalThis.Array.isArray(object?.methods) ? object.methods.map((e: any) => String(e)) : [],
      ipAddresses: globalThis.Array.isArray(object?.ipAddresses) ? object.ipAddresses.map((e: any) => String(e)) : [],
    };
  },

  toJSON(message: AlertFilters_MetadataFilters): unknown {
    const obj: any = {};
    if (message.categories?.length) {
      obj.categories = message.categories;
    }
    if (message.applications?.length) {
      obj.applications = message.applications;
    }
    if (message.subsystems?.length) {
      obj.subsystems = message.subsystems;
    }
    if (message.computers?.length) {
      obj.computers = message.computers;
    }
    if (message.classes?.length) {
      obj.classes = message.classes;
    }
    if (message.methods?.length) {
      obj.methods = message.methods;
    }
    if (message.ipAddresses?.length) {
      obj.ipAddresses = message.ipAddresses;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AlertFilters_MetadataFilters>, I>>(base?: I): AlertFilters_MetadataFilters {
    return AlertFilters_MetadataFilters.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AlertFilters_MetadataFilters>, I>>(object: I): AlertFilters_MetadataFilters {
    const message = createBaseAlertFilters_MetadataFilters();
    message.categories = object.categories?.map((e) => e) || [];
    message.applications = object.applications?.map((e) => e) || [];
    message.subsystems = object.subsystems?.map((e) => e) || [];
    message.computers = object.computers?.map((e) => e) || [];
    message.classes = object.classes?.map((e) => e) || [];
    message.methods = object.methods?.map((e) => e) || [];
    message.ipAddresses = object.ipAddresses?.map((e) => e) || [];
    return message;
  },
};

function createBaseAlertFilters_RatioAlert(): AlertFilters_RatioAlert {
  return { alias: undefined, text: undefined, severities: [], applications: [], subsystems: [], groupBy: [] };
}

export const AlertFilters_RatioAlert = {
  encode(message: AlertFilters_RatioAlert, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.alias !== undefined) {
      StringValue.encode({ value: message.alias! }, writer.uint32(10).fork()).ldelim();
    }
    if (message.text !== undefined) {
      StringValue.encode({ value: message.text! }, writer.uint32(18).fork()).ldelim();
    }
    writer.uint32(26).fork();
    for (const v of message.severities) {
      writer.int32(v);
    }
    writer.ldelim();
    for (const v of message.applications) {
      StringValue.encode({ value: v!! }, writer.uint32(34).fork()).ldelim();
    }
    for (const v of message.subsystems) {
      StringValue.encode({ value: v!! }, writer.uint32(42).fork()).ldelim();
    }
    for (const v of message.groupBy) {
      StringValue.encode({ value: v!! }, writer.uint32(50).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): AlertFilters_RatioAlert {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAlertFilters_RatioAlert();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.alias = StringValue.decode(reader, reader.uint32()).value;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.text = StringValue.decode(reader, reader.uint32()).value;
          continue;
        case 3:
          if (tag === 24) {
            message.severities.push(reader.int32() as any);

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.severities.push(reader.int32() as any);
            }

            continue;
          }

          break;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.applications.push(StringValue.decode(reader, reader.uint32()).value);
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.subsystems.push(StringValue.decode(reader, reader.uint32()).value);
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.groupBy.push(StringValue.decode(reader, reader.uint32()).value);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AlertFilters_RatioAlert {
    return {
      alias: isSet(object.alias) ? String(object.alias) : undefined,
      text: isSet(object.text) ? String(object.text) : undefined,
      severities: globalThis.Array.isArray(object?.severities)
        ? object.severities.map((e: any) => alertFilters_LogSeverityFromJSON(e))
        : [],
      applications: globalThis.Array.isArray(object?.applications)
        ? object.applications.map((e: any) => String(e))
        : [],
      subsystems: globalThis.Array.isArray(object?.subsystems) ? object.subsystems.map((e: any) => String(e)) : [],
      groupBy: globalThis.Array.isArray(object?.groupBy) ? object.groupBy.map((e: any) => String(e)) : [],
    };
  },

  toJSON(message: AlertFilters_RatioAlert): unknown {
    const obj: any = {};
    if (message.alias !== undefined) {
      obj.alias = message.alias;
    }
    if (message.text !== undefined) {
      obj.text = message.text;
    }
    if (message.severities?.length) {
      obj.severities = message.severities.map((e) => alertFilters_LogSeverityToJSON(e));
    }
    if (message.applications?.length) {
      obj.applications = message.applications;
    }
    if (message.subsystems?.length) {
      obj.subsystems = message.subsystems;
    }
    if (message.groupBy?.length) {
      obj.groupBy = message.groupBy;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AlertFilters_RatioAlert>, I>>(base?: I): AlertFilters_RatioAlert {
    return AlertFilters_RatioAlert.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AlertFilters_RatioAlert>, I>>(object: I): AlertFilters_RatioAlert {
    const message = createBaseAlertFilters_RatioAlert();
    message.alias = object.alias ?? undefined;
    message.text = object.text ?? undefined;
    message.severities = object.severities?.map((e) => e) || [];
    message.applications = object.applications?.map((e) => e) || [];
    message.subsystems = object.subsystems?.map((e) => e) || [];
    message.groupBy = object.groupBy?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
