// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.176.1
//   protoc               v3.21.12
// source: com/coralogix/tags/v1/tag.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { Timestamp } from "../../../../google/protobuf/timestamp";
import { StringValue, UInt32Value, UInt64Value } from "../../../../google/protobuf/wrappers";
import { TagType } from "./tag_type";

export const protobufPackage = "com.coralogix.tags.v1";

export enum Status {
  STATUS_UNSPECIFIED = 0,
  STATUS_SUCCESSFUL = 1,
  UNRECOGNIZED = -1,
}

export function statusFromJSON(object: any): Status {
  switch (object) {
    case 0:
    case "STATUS_UNSPECIFIED":
      return Status.STATUS_UNSPECIFIED;
    case 1:
    case "STATUS_SUCCESSFUL":
      return Status.STATUS_SUCCESSFUL;
    case -1:
    case "UNRECOGNIZED":
    default:
      return Status.UNRECOGNIZED;
  }
}

export function statusToJSON(object: Status): string {
  switch (object) {
    case Status.STATUS_UNSPECIFIED:
      return "STATUS_UNSPECIFIED";
    case Status.STATUS_SUCCESSFUL:
      return "STATUS_SUCCESSFUL";
    case Status.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface Tag {
  id: number | undefined;
  key: string | undefined;
  name: string | undefined;
  companyId: number | undefined;
  status: Status;
  iconUrl: string | undefined;
  timestamp: Date | undefined;
  application: string[];
  subsystem: string[];
  updatedAt: Date | undefined;
  createdAt: Date | undefined;
  type: TagType | undefined;
}

function createBaseTag(): Tag {
  return {
    id: undefined,
    key: undefined,
    name: undefined,
    companyId: undefined,
    status: 0,
    iconUrl: undefined,
    timestamp: undefined,
    application: [],
    subsystem: [],
    updatedAt: undefined,
    createdAt: undefined,
    type: undefined,
  };
}

export const Tag = {
  encode(message: Tag, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== undefined) {
      UInt64Value.encode({ value: message.id! }, writer.uint32(10).fork()).ldelim();
    }
    if (message.key !== undefined) {
      StringValue.encode({ value: message.key! }, writer.uint32(18).fork()).ldelim();
    }
    if (message.name !== undefined) {
      StringValue.encode({ value: message.name! }, writer.uint32(26).fork()).ldelim();
    }
    if (message.companyId !== undefined) {
      UInt32Value.encode({ value: message.companyId! }, writer.uint32(34).fork()).ldelim();
    }
    if (message.status !== 0) {
      writer.uint32(40).int32(message.status);
    }
    if (message.iconUrl !== undefined) {
      StringValue.encode({ value: message.iconUrl! }, writer.uint32(50).fork()).ldelim();
    }
    if (message.timestamp !== undefined) {
      Timestamp.encode(toTimestamp(message.timestamp), writer.uint32(58).fork()).ldelim();
    }
    for (const v of message.application) {
      StringValue.encode({ value: v!! }, writer.uint32(66).fork()).ldelim();
    }
    for (const v of message.subsystem) {
      StringValue.encode({ value: v!! }, writer.uint32(74).fork()).ldelim();
    }
    if (message.updatedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.updatedAt), writer.uint32(82).fork()).ldelim();
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(90).fork()).ldelim();
    }
    if (message.type !== undefined) {
      TagType.encode(message.type, writer.uint32(106).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Tag {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTag();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = UInt64Value.decode(reader, reader.uint32()).value;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.key = StringValue.decode(reader, reader.uint32()).value;
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.name = StringValue.decode(reader, reader.uint32()).value;
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.companyId = UInt32Value.decode(reader, reader.uint32()).value;
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.iconUrl = StringValue.decode(reader, reader.uint32()).value;
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.timestamp = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 8:
          if (tag !== 66) {
            break;
          }

          message.application.push(StringValue.decode(reader, reader.uint32()).value);
          continue;
        case 9:
          if (tag !== 74) {
            break;
          }

          message.subsystem.push(StringValue.decode(reader, reader.uint32()).value);
          continue;
        case 10:
          if (tag !== 82) {
            break;
          }

          message.updatedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 11:
          if (tag !== 90) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 13:
          if (tag !== 106) {
            break;
          }

          message.type = TagType.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Tag {
    return {
      id: isSet(object.id) ? Number(object.id) : undefined,
      key: isSet(object.key) ? String(object.key) : undefined,
      name: isSet(object.name) ? String(object.name) : undefined,
      companyId: isSet(object.companyId) ? Number(object.companyId) : undefined,
      status: isSet(object.status) ? statusFromJSON(object.status) : 0,
      iconUrl: isSet(object.iconUrl) ? String(object.iconUrl) : undefined,
      timestamp: isSet(object.timestamp) ? fromJsonTimestamp(object.timestamp) : undefined,
      application: globalThis.Array.isArray(object?.application) ? object.application.map((e: any) => String(e)) : [],
      subsystem: globalThis.Array.isArray(object?.subsystem) ? object.subsystem.map((e: any) => String(e)) : [],
      updatedAt: isSet(object.updatedAt) ? fromJsonTimestamp(object.updatedAt) : undefined,
      createdAt: isSet(object.createdAt) ? fromJsonTimestamp(object.createdAt) : undefined,
      type: isSet(object.type) ? TagType.fromJSON(object.type) : undefined,
    };
  },

  toJSON(message: Tag): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = message.id;
    }
    if (message.key !== undefined) {
      obj.key = message.key;
    }
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    if (message.companyId !== undefined) {
      obj.companyId = message.companyId;
    }
    if (message.status !== 0) {
      obj.status = statusToJSON(message.status);
    }
    if (message.iconUrl !== undefined) {
      obj.iconUrl = message.iconUrl;
    }
    if (message.timestamp !== undefined) {
      obj.timestamp = message.timestamp.toISOString();
    }
    if (message.application?.length) {
      obj.application = message.application;
    }
    if (message.subsystem?.length) {
      obj.subsystem = message.subsystem;
    }
    if (message.updatedAt !== undefined) {
      obj.updatedAt = message.updatedAt.toISOString();
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.type !== undefined) {
      obj.type = TagType.toJSON(message.type);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Tag>, I>>(base?: I): Tag {
    return Tag.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Tag>, I>>(object: I): Tag {
    const message = createBaseTag();
    message.id = object.id ?? undefined;
    message.key = object.key ?? undefined;
    message.name = object.name ?? undefined;
    message.companyId = object.companyId ?? undefined;
    message.status = object.status ?? 0;
    message.iconUrl = object.iconUrl ?? undefined;
    message.timestamp = object.timestamp ?? undefined;
    message.application = object.application?.map((e) => e) || [];
    message.subsystem = object.subsystem?.map((e) => e) || [];
    message.updatedAt = object.updatedAt ?? undefined;
    message.createdAt = object.createdAt ?? undefined;
    message.type = (object.type !== undefined && object.type !== null) ? TagType.fromPartial(object.type) : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
