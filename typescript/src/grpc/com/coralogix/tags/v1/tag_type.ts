// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.176.1
//   protoc               v3.21.12
// source: com/coralogix/tags/v1/tag_type.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { Int64Value } from "../../../../google/protobuf/wrappers";

export const protobufPackage = "com.coralogix.tags.v1";

export enum Type {
  TYPE_UNSPECIFIED = 0,
  TYPE_CUSTOM_EVENT = 1,
  TYPE_BITBUCKET = 2,
  TYPE_GITLAB = 3,
  TYPE_TFS = 4,
  TYPE_HEROKU = 5,
  UNRECOGNIZED = -1,
}

export function typeFromJSON(object: any): Type {
  switch (object) {
    case 0:
    case "TYPE_UNSPECIFIED":
      return Type.TYPE_UNSPECIFIED;
    case 1:
    case "TYPE_CUSTOM_EVENT":
      return Type.TYPE_CUSTOM_EVENT;
    case 2:
    case "TYPE_BITBUCKET":
      return Type.TYPE_BITBUCKET;
    case 3:
    case "TYPE_GITLAB":
      return Type.TYPE_GITLAB;
    case 4:
    case "TYPE_TFS":
      return Type.TYPE_TFS;
    case 5:
    case "TYPE_HEROKU":
      return Type.TYPE_HEROKU;
    case -1:
    case "UNRECOGNIZED":
    default:
      return Type.UNRECOGNIZED;
  }
}

export function typeToJSON(object: Type): string {
  switch (object) {
    case Type.TYPE_UNSPECIFIED:
      return "TYPE_UNSPECIFIED";
    case Type.TYPE_CUSTOM_EVENT:
      return "TYPE_CUSTOM_EVENT";
    case Type.TYPE_BITBUCKET:
      return "TYPE_BITBUCKET";
    case Type.TYPE_GITLAB:
      return "TYPE_GITLAB";
    case Type.TYPE_TFS:
      return "TYPE_TFS";
    case Type.TYPE_HEROKU:
      return "TYPE_HEROKU";
    case Type.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface TagType {
  id: number | undefined;
  type: Type;
}

function createBaseTagType(): TagType {
  return { id: undefined, type: 0 };
}

export const TagType = {
  encode(message: TagType, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== undefined) {
      Int64Value.encode({ value: message.id! }, writer.uint32(10).fork()).ldelim();
    }
    if (message.type !== 0) {
      writer.uint32(16).int32(message.type);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): TagType {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTagType();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = Int64Value.decode(reader, reader.uint32()).value;
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TagType {
    return {
      id: isSet(object.id) ? Number(object.id) : undefined,
      type: isSet(object.type) ? typeFromJSON(object.type) : 0,
    };
  },

  toJSON(message: TagType): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = message.id;
    }
    if (message.type !== 0) {
      obj.type = typeToJSON(message.type);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TagType>, I>>(base?: I): TagType {
    return TagType.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TagType>, I>>(object: I): TagType {
    const message = createBaseTagType();
    message.id = object.id ?? undefined;
    message.type = object.type ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
