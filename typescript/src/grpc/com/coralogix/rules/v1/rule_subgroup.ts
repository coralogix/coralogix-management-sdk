// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.176.1
//   protoc               v3.21.12
// source: com/coralogix/rules/v1/rule_subgroup.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { BoolValue, StringValue, UInt32Value } from "../../../../google/protobuf/wrappers";
import { Rule } from "./rule";

export const protobufPackage = "com.coralogix.rules.v1";

export interface RuleSubgroup {
  id: string | undefined;
  rules: Rule[];
  enabled: boolean | undefined;
  order: number | undefined;
}

function createBaseRuleSubgroup(): RuleSubgroup {
  return { id: undefined, rules: [], enabled: undefined, order: undefined };
}

export const RuleSubgroup = {
  encode(message: RuleSubgroup, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== undefined) {
      StringValue.encode({ value: message.id! }, writer.uint32(10).fork()).ldelim();
    }
    for (const v of message.rules) {
      Rule.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    if (message.enabled !== undefined) {
      BoolValue.encode({ value: message.enabled! }, writer.uint32(26).fork()).ldelim();
    }
    if (message.order !== undefined) {
      UInt32Value.encode({ value: message.order! }, writer.uint32(34).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): RuleSubgroup {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRuleSubgroup();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = StringValue.decode(reader, reader.uint32()).value;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.rules.push(Rule.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.enabled = BoolValue.decode(reader, reader.uint32()).value;
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.order = UInt32Value.decode(reader, reader.uint32()).value;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RuleSubgroup {
    return {
      id: isSet(object.id) ? String(object.id) : undefined,
      rules: globalThis.Array.isArray(object?.rules) ? object.rules.map((e: any) => Rule.fromJSON(e)) : [],
      enabled: isSet(object.enabled) ? Boolean(object.enabled) : undefined,
      order: isSet(object.order) ? Number(object.order) : undefined,
    };
  },

  toJSON(message: RuleSubgroup): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = message.id;
    }
    if (message.rules?.length) {
      obj.rules = message.rules.map((e) => Rule.toJSON(e));
    }
    if (message.enabled !== undefined) {
      obj.enabled = message.enabled;
    }
    if (message.order !== undefined) {
      obj.order = message.order;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RuleSubgroup>, I>>(base?: I): RuleSubgroup {
    return RuleSubgroup.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RuleSubgroup>, I>>(object: I): RuleSubgroup {
    const message = createBaseRuleSubgroup();
    message.id = object.id ?? undefined;
    message.rules = object.rules?.map((e) => Rule.fromPartial(e)) || [];
    message.enabled = object.enabled ?? undefined;
    message.order = object.order ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
