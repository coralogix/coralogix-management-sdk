// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.176.1
//   protoc               v3.21.12
// source: com/coralogix/dashboards/v1/widgets/alerts_service.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { Int32Value } from "../../../../../google/protobuf/wrappers";
import { SearchFilter } from "../common/search_filter";
import { TimeFrame } from "../common/time_frame";

export const protobufPackage = "com.coralogix.dashboards.v1.widgets";

export enum AlertGroupSeverity {
  ALERT_GROUP_SEVERITY_UNSPECIFIED = 0,
  ALERT_GROUP_SEVERITY_INFO = 1,
  ALERT_GROUP_SEVERITY_WARNING = 2,
  ALERT_GROUP_SEVERITY_CRITICAL = 3,
  ALERT_GROUP_SEVERITY_ERROR = 4,
  UNRECOGNIZED = -1,
}

export function alertGroupSeverityFromJSON(object: any): AlertGroupSeverity {
  switch (object) {
    case 0:
    case "ALERT_GROUP_SEVERITY_UNSPECIFIED":
      return AlertGroupSeverity.ALERT_GROUP_SEVERITY_UNSPECIFIED;
    case 1:
    case "ALERT_GROUP_SEVERITY_INFO":
      return AlertGroupSeverity.ALERT_GROUP_SEVERITY_INFO;
    case 2:
    case "ALERT_GROUP_SEVERITY_WARNING":
      return AlertGroupSeverity.ALERT_GROUP_SEVERITY_WARNING;
    case 3:
    case "ALERT_GROUP_SEVERITY_CRITICAL":
      return AlertGroupSeverity.ALERT_GROUP_SEVERITY_CRITICAL;
    case 4:
    case "ALERT_GROUP_SEVERITY_ERROR":
      return AlertGroupSeverity.ALERT_GROUP_SEVERITY_ERROR;
    case -1:
    case "UNRECOGNIZED":
    default:
      return AlertGroupSeverity.UNRECOGNIZED;
  }
}

export function alertGroupSeverityToJSON(object: AlertGroupSeverity): string {
  switch (object) {
    case AlertGroupSeverity.ALERT_GROUP_SEVERITY_UNSPECIFIED:
      return "ALERT_GROUP_SEVERITY_UNSPECIFIED";
    case AlertGroupSeverity.ALERT_GROUP_SEVERITY_INFO:
      return "ALERT_GROUP_SEVERITY_INFO";
    case AlertGroupSeverity.ALERT_GROUP_SEVERITY_WARNING:
      return "ALERT_GROUP_SEVERITY_WARNING";
    case AlertGroupSeverity.ALERT_GROUP_SEVERITY_CRITICAL:
      return "ALERT_GROUP_SEVERITY_CRITICAL";
    case AlertGroupSeverity.ALERT_GROUP_SEVERITY_ERROR:
      return "ALERT_GROUP_SEVERITY_ERROR";
    case AlertGroupSeverity.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface SearchAlertsGroupedBySeverityRequest {
  filters: SearchFilter[];
  timeFrame: TimeFrame | undefined;
}

export interface SearchAlertsGroupedBySeverityResponse {
  alerts: AlertGroup[];
}

export interface AlertGroup {
  severity: AlertGroupSeverity;
  amount: number | undefined;
}

function createBaseSearchAlertsGroupedBySeverityRequest(): SearchAlertsGroupedBySeverityRequest {
  return { filters: [], timeFrame: undefined };
}

export const SearchAlertsGroupedBySeverityRequest = {
  encode(message: SearchAlertsGroupedBySeverityRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.filters) {
      SearchFilter.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    if (message.timeFrame !== undefined) {
      TimeFrame.encode(message.timeFrame, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SearchAlertsGroupedBySeverityRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchAlertsGroupedBySeverityRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.filters.push(SearchFilter.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.timeFrame = TimeFrame.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchAlertsGroupedBySeverityRequest {
    return {
      filters: globalThis.Array.isArray(object?.filters)
        ? object.filters.map((e: any) => SearchFilter.fromJSON(e))
        : [],
      timeFrame: isSet(object.timeFrame) ? TimeFrame.fromJSON(object.timeFrame) : undefined,
    };
  },

  toJSON(message: SearchAlertsGroupedBySeverityRequest): unknown {
    const obj: any = {};
    if (message.filters?.length) {
      obj.filters = message.filters.map((e) => SearchFilter.toJSON(e));
    }
    if (message.timeFrame !== undefined) {
      obj.timeFrame = TimeFrame.toJSON(message.timeFrame);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SearchAlertsGroupedBySeverityRequest>, I>>(
    base?: I,
  ): SearchAlertsGroupedBySeverityRequest {
    return SearchAlertsGroupedBySeverityRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchAlertsGroupedBySeverityRequest>, I>>(
    object: I,
  ): SearchAlertsGroupedBySeverityRequest {
    const message = createBaseSearchAlertsGroupedBySeverityRequest();
    message.filters = object.filters?.map((e) => SearchFilter.fromPartial(e)) || [];
    message.timeFrame = (object.timeFrame !== undefined && object.timeFrame !== null)
      ? TimeFrame.fromPartial(object.timeFrame)
      : undefined;
    return message;
  },
};

function createBaseSearchAlertsGroupedBySeverityResponse(): SearchAlertsGroupedBySeverityResponse {
  return { alerts: [] };
}

export const SearchAlertsGroupedBySeverityResponse = {
  encode(message: SearchAlertsGroupedBySeverityResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.alerts) {
      AlertGroup.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SearchAlertsGroupedBySeverityResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchAlertsGroupedBySeverityResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.alerts.push(AlertGroup.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchAlertsGroupedBySeverityResponse {
    return {
      alerts: globalThis.Array.isArray(object?.alerts) ? object.alerts.map((e: any) => AlertGroup.fromJSON(e)) : [],
    };
  },

  toJSON(message: SearchAlertsGroupedBySeverityResponse): unknown {
    const obj: any = {};
    if (message.alerts?.length) {
      obj.alerts = message.alerts.map((e) => AlertGroup.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SearchAlertsGroupedBySeverityResponse>, I>>(
    base?: I,
  ): SearchAlertsGroupedBySeverityResponse {
    return SearchAlertsGroupedBySeverityResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchAlertsGroupedBySeverityResponse>, I>>(
    object: I,
  ): SearchAlertsGroupedBySeverityResponse {
    const message = createBaseSearchAlertsGroupedBySeverityResponse();
    message.alerts = object.alerts?.map((e) => AlertGroup.fromPartial(e)) || [];
    return message;
  },
};

function createBaseAlertGroup(): AlertGroup {
  return { severity: 0, amount: undefined };
}

export const AlertGroup = {
  encode(message: AlertGroup, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.severity !== 0) {
      writer.uint32(8).int32(message.severity);
    }
    if (message.amount !== undefined) {
      Int32Value.encode({ value: message.amount! }, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): AlertGroup {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAlertGroup();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.severity = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.amount = Int32Value.decode(reader, reader.uint32()).value;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AlertGroup {
    return {
      severity: isSet(object.severity) ? alertGroupSeverityFromJSON(object.severity) : 0,
      amount: isSet(object.amount) ? Number(object.amount) : undefined,
    };
  },

  toJSON(message: AlertGroup): unknown {
    const obj: any = {};
    if (message.severity !== 0) {
      obj.severity = alertGroupSeverityToJSON(message.severity);
    }
    if (message.amount !== undefined) {
      obj.amount = message.amount;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AlertGroup>, I>>(base?: I): AlertGroup {
    return AlertGroup.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AlertGroup>, I>>(object: I): AlertGroup {
    const message = createBaseAlertGroup();
    message.severity = object.severity ?? 0;
    message.amount = object.amount ?? undefined;
    return message;
  },
};

export interface AlertsService {
  SearchAlertsGroupedBySeverity(
    request: SearchAlertsGroupedBySeverityRequest,
  ): Promise<SearchAlertsGroupedBySeverityResponse>;
}

export const AlertsServiceServiceName = "com.coralogix.dashboards.v1.widgets.AlertsService";
export class AlertsServiceClientImpl implements AlertsService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || AlertsServiceServiceName;
    this.rpc = rpc;
    this.SearchAlertsGroupedBySeverity = this.SearchAlertsGroupedBySeverity.bind(this);
  }
  SearchAlertsGroupedBySeverity(
    request: SearchAlertsGroupedBySeverityRequest,
  ): Promise<SearchAlertsGroupedBySeverityResponse> {
    const data = SearchAlertsGroupedBySeverityRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "SearchAlertsGroupedBySeverity", data);
    return promise.then((data) => SearchAlertsGroupedBySeverityResponse.decode(_m0.Reader.create(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
