// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.176.1
//   protoc               v3.21.12
// source: com/coralogix/dashboards/v1/widgets/platform_overview_service.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { Duration } from "../../../../../google/protobuf/duration";
import { Timestamp } from "../../../../../google/protobuf/timestamp";
import { DoubleValue, Int32Value, Int64Value } from "../../../../../google/protobuf/wrappers";
import { SearchFilter } from "../common/search_filter";
import { TimeFrame } from "../common/time_frame";

export const protobufPackage = "com.coralogix.dashboards.v1.widgets";

export interface CountLogsRequest {
  filters: SearchFilter[];
  timeFrame: TimeFrame | undefined;
  interval: Duration | undefined;
  severities: number[];
}

export interface CountLogsResponse {
  timeSeries: HistogramTimeSeries | undefined;
  totalCount: number | undefined;
}

export interface GetMetricsStatsRequest {
  filters: SearchFilter[];
  timeFrame: TimeFrame | undefined;
  interval: Duration | undefined;
}

export interface GetMetricsStatsResponse {
  timeSeries: HistogramTimeSeries | undefined;
}

export interface CountSpansRequest {
  filters: SearchFilter[];
  timeFrame: TimeFrame | undefined;
  interval: Duration | undefined;
}

export interface CountSpansResponse {
  timeSeries: HistogramTimeSeries | undefined;
  totalCount: number | undefined;
}

export interface CountFailedXDRTestsRequest {
  filters: SearchFilter[];
  timeFrame: TimeFrame | undefined;
  interval: Duration | undefined;
}

export interface CountFailedXDRTestsResponse {
  timeSeries: HistogramTimeSeries | undefined;
  totalCount: number | undefined;
}

export interface HistogramTimeSeries {
  values: HistogramDataPoint[];
}

export interface HistogramDataPoint {
  timestamp: Date | undefined;
  values: { [key: string]: number | undefined };
}

export interface HistogramDataPoint_ValuesEntry {
  key: string;
  value: number | undefined;
}

function createBaseCountLogsRequest(): CountLogsRequest {
  return { filters: [], timeFrame: undefined, interval: undefined, severities: [] };
}

export const CountLogsRequest = {
  encode(message: CountLogsRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.filters) {
      SearchFilter.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    if (message.timeFrame !== undefined) {
      TimeFrame.encode(message.timeFrame, writer.uint32(18).fork()).ldelim();
    }
    if (message.interval !== undefined) {
      Duration.encode(message.interval, writer.uint32(26).fork()).ldelim();
    }
    for (const v of message.severities) {
      Int32Value.encode({ value: v!! }, writer.uint32(34).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): CountLogsRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCountLogsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.filters.push(SearchFilter.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.timeFrame = TimeFrame.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.interval = Duration.decode(reader, reader.uint32());
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.severities.push(Int32Value.decode(reader, reader.uint32()).value);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CountLogsRequest {
    return {
      filters: globalThis.Array.isArray(object?.filters)
        ? object.filters.map((e: any) => SearchFilter.fromJSON(e))
        : [],
      timeFrame: isSet(object.timeFrame) ? TimeFrame.fromJSON(object.timeFrame) : undefined,
      interval: isSet(object.interval) ? Duration.fromJSON(object.interval) : undefined,
      severities: globalThis.Array.isArray(object?.severities) ? object.severities.map((e: any) => Number(e)) : [],
    };
  },

  toJSON(message: CountLogsRequest): unknown {
    const obj: any = {};
    if (message.filters?.length) {
      obj.filters = message.filters.map((e) => SearchFilter.toJSON(e));
    }
    if (message.timeFrame !== undefined) {
      obj.timeFrame = TimeFrame.toJSON(message.timeFrame);
    }
    if (message.interval !== undefined) {
      obj.interval = Duration.toJSON(message.interval);
    }
    if (message.severities?.length) {
      obj.severities = message.severities;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CountLogsRequest>, I>>(base?: I): CountLogsRequest {
    return CountLogsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CountLogsRequest>, I>>(object: I): CountLogsRequest {
    const message = createBaseCountLogsRequest();
    message.filters = object.filters?.map((e) => SearchFilter.fromPartial(e)) || [];
    message.timeFrame = (object.timeFrame !== undefined && object.timeFrame !== null)
      ? TimeFrame.fromPartial(object.timeFrame)
      : undefined;
    message.interval = (object.interval !== undefined && object.interval !== null)
      ? Duration.fromPartial(object.interval)
      : undefined;
    message.severities = object.severities?.map((e) => e) || [];
    return message;
  },
};

function createBaseCountLogsResponse(): CountLogsResponse {
  return { timeSeries: undefined, totalCount: undefined };
}

export const CountLogsResponse = {
  encode(message: CountLogsResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.timeSeries !== undefined) {
      HistogramTimeSeries.encode(message.timeSeries, writer.uint32(10).fork()).ldelim();
    }
    if (message.totalCount !== undefined) {
      Int64Value.encode({ value: message.totalCount! }, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): CountLogsResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCountLogsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.timeSeries = HistogramTimeSeries.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.totalCount = Int64Value.decode(reader, reader.uint32()).value;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CountLogsResponse {
    return {
      timeSeries: isSet(object.timeSeries) ? HistogramTimeSeries.fromJSON(object.timeSeries) : undefined,
      totalCount: isSet(object.totalCount) ? Number(object.totalCount) : undefined,
    };
  },

  toJSON(message: CountLogsResponse): unknown {
    const obj: any = {};
    if (message.timeSeries !== undefined) {
      obj.timeSeries = HistogramTimeSeries.toJSON(message.timeSeries);
    }
    if (message.totalCount !== undefined) {
      obj.totalCount = message.totalCount;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CountLogsResponse>, I>>(base?: I): CountLogsResponse {
    return CountLogsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CountLogsResponse>, I>>(object: I): CountLogsResponse {
    const message = createBaseCountLogsResponse();
    message.timeSeries = (object.timeSeries !== undefined && object.timeSeries !== null)
      ? HistogramTimeSeries.fromPartial(object.timeSeries)
      : undefined;
    message.totalCount = object.totalCount ?? undefined;
    return message;
  },
};

function createBaseGetMetricsStatsRequest(): GetMetricsStatsRequest {
  return { filters: [], timeFrame: undefined, interval: undefined };
}

export const GetMetricsStatsRequest = {
  encode(message: GetMetricsStatsRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.filters) {
      SearchFilter.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    if (message.timeFrame !== undefined) {
      TimeFrame.encode(message.timeFrame, writer.uint32(18).fork()).ldelim();
    }
    if (message.interval !== undefined) {
      Duration.encode(message.interval, writer.uint32(26).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetMetricsStatsRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetMetricsStatsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.filters.push(SearchFilter.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.timeFrame = TimeFrame.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.interval = Duration.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetMetricsStatsRequest {
    return {
      filters: globalThis.Array.isArray(object?.filters)
        ? object.filters.map((e: any) => SearchFilter.fromJSON(e))
        : [],
      timeFrame: isSet(object.timeFrame) ? TimeFrame.fromJSON(object.timeFrame) : undefined,
      interval: isSet(object.interval) ? Duration.fromJSON(object.interval) : undefined,
    };
  },

  toJSON(message: GetMetricsStatsRequest): unknown {
    const obj: any = {};
    if (message.filters?.length) {
      obj.filters = message.filters.map((e) => SearchFilter.toJSON(e));
    }
    if (message.timeFrame !== undefined) {
      obj.timeFrame = TimeFrame.toJSON(message.timeFrame);
    }
    if (message.interval !== undefined) {
      obj.interval = Duration.toJSON(message.interval);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetMetricsStatsRequest>, I>>(base?: I): GetMetricsStatsRequest {
    return GetMetricsStatsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetMetricsStatsRequest>, I>>(object: I): GetMetricsStatsRequest {
    const message = createBaseGetMetricsStatsRequest();
    message.filters = object.filters?.map((e) => SearchFilter.fromPartial(e)) || [];
    message.timeFrame = (object.timeFrame !== undefined && object.timeFrame !== null)
      ? TimeFrame.fromPartial(object.timeFrame)
      : undefined;
    message.interval = (object.interval !== undefined && object.interval !== null)
      ? Duration.fromPartial(object.interval)
      : undefined;
    return message;
  },
};

function createBaseGetMetricsStatsResponse(): GetMetricsStatsResponse {
  return { timeSeries: undefined };
}

export const GetMetricsStatsResponse = {
  encode(message: GetMetricsStatsResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.timeSeries !== undefined) {
      HistogramTimeSeries.encode(message.timeSeries, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetMetricsStatsResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetMetricsStatsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.timeSeries = HistogramTimeSeries.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetMetricsStatsResponse {
    return { timeSeries: isSet(object.timeSeries) ? HistogramTimeSeries.fromJSON(object.timeSeries) : undefined };
  },

  toJSON(message: GetMetricsStatsResponse): unknown {
    const obj: any = {};
    if (message.timeSeries !== undefined) {
      obj.timeSeries = HistogramTimeSeries.toJSON(message.timeSeries);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetMetricsStatsResponse>, I>>(base?: I): GetMetricsStatsResponse {
    return GetMetricsStatsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetMetricsStatsResponse>, I>>(object: I): GetMetricsStatsResponse {
    const message = createBaseGetMetricsStatsResponse();
    message.timeSeries = (object.timeSeries !== undefined && object.timeSeries !== null)
      ? HistogramTimeSeries.fromPartial(object.timeSeries)
      : undefined;
    return message;
  },
};

function createBaseCountSpansRequest(): CountSpansRequest {
  return { filters: [], timeFrame: undefined, interval: undefined };
}

export const CountSpansRequest = {
  encode(message: CountSpansRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.filters) {
      SearchFilter.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    if (message.timeFrame !== undefined) {
      TimeFrame.encode(message.timeFrame, writer.uint32(18).fork()).ldelim();
    }
    if (message.interval !== undefined) {
      Duration.encode(message.interval, writer.uint32(26).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): CountSpansRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCountSpansRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.filters.push(SearchFilter.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.timeFrame = TimeFrame.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.interval = Duration.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CountSpansRequest {
    return {
      filters: globalThis.Array.isArray(object?.filters)
        ? object.filters.map((e: any) => SearchFilter.fromJSON(e))
        : [],
      timeFrame: isSet(object.timeFrame) ? TimeFrame.fromJSON(object.timeFrame) : undefined,
      interval: isSet(object.interval) ? Duration.fromJSON(object.interval) : undefined,
    };
  },

  toJSON(message: CountSpansRequest): unknown {
    const obj: any = {};
    if (message.filters?.length) {
      obj.filters = message.filters.map((e) => SearchFilter.toJSON(e));
    }
    if (message.timeFrame !== undefined) {
      obj.timeFrame = TimeFrame.toJSON(message.timeFrame);
    }
    if (message.interval !== undefined) {
      obj.interval = Duration.toJSON(message.interval);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CountSpansRequest>, I>>(base?: I): CountSpansRequest {
    return CountSpansRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CountSpansRequest>, I>>(object: I): CountSpansRequest {
    const message = createBaseCountSpansRequest();
    message.filters = object.filters?.map((e) => SearchFilter.fromPartial(e)) || [];
    message.timeFrame = (object.timeFrame !== undefined && object.timeFrame !== null)
      ? TimeFrame.fromPartial(object.timeFrame)
      : undefined;
    message.interval = (object.interval !== undefined && object.interval !== null)
      ? Duration.fromPartial(object.interval)
      : undefined;
    return message;
  },
};

function createBaseCountSpansResponse(): CountSpansResponse {
  return { timeSeries: undefined, totalCount: undefined };
}

export const CountSpansResponse = {
  encode(message: CountSpansResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.timeSeries !== undefined) {
      HistogramTimeSeries.encode(message.timeSeries, writer.uint32(10).fork()).ldelim();
    }
    if (message.totalCount !== undefined) {
      Int64Value.encode({ value: message.totalCount! }, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): CountSpansResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCountSpansResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.timeSeries = HistogramTimeSeries.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.totalCount = Int64Value.decode(reader, reader.uint32()).value;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CountSpansResponse {
    return {
      timeSeries: isSet(object.timeSeries) ? HistogramTimeSeries.fromJSON(object.timeSeries) : undefined,
      totalCount: isSet(object.totalCount) ? Number(object.totalCount) : undefined,
    };
  },

  toJSON(message: CountSpansResponse): unknown {
    const obj: any = {};
    if (message.timeSeries !== undefined) {
      obj.timeSeries = HistogramTimeSeries.toJSON(message.timeSeries);
    }
    if (message.totalCount !== undefined) {
      obj.totalCount = message.totalCount;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CountSpansResponse>, I>>(base?: I): CountSpansResponse {
    return CountSpansResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CountSpansResponse>, I>>(object: I): CountSpansResponse {
    const message = createBaseCountSpansResponse();
    message.timeSeries = (object.timeSeries !== undefined && object.timeSeries !== null)
      ? HistogramTimeSeries.fromPartial(object.timeSeries)
      : undefined;
    message.totalCount = object.totalCount ?? undefined;
    return message;
  },
};

function createBaseCountFailedXDRTestsRequest(): CountFailedXDRTestsRequest {
  return { filters: [], timeFrame: undefined, interval: undefined };
}

export const CountFailedXDRTestsRequest = {
  encode(message: CountFailedXDRTestsRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.filters) {
      SearchFilter.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    if (message.timeFrame !== undefined) {
      TimeFrame.encode(message.timeFrame, writer.uint32(18).fork()).ldelim();
    }
    if (message.interval !== undefined) {
      Duration.encode(message.interval, writer.uint32(26).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): CountFailedXDRTestsRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCountFailedXDRTestsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.filters.push(SearchFilter.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.timeFrame = TimeFrame.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.interval = Duration.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CountFailedXDRTestsRequest {
    return {
      filters: globalThis.Array.isArray(object?.filters)
        ? object.filters.map((e: any) => SearchFilter.fromJSON(e))
        : [],
      timeFrame: isSet(object.timeFrame) ? TimeFrame.fromJSON(object.timeFrame) : undefined,
      interval: isSet(object.interval) ? Duration.fromJSON(object.interval) : undefined,
    };
  },

  toJSON(message: CountFailedXDRTestsRequest): unknown {
    const obj: any = {};
    if (message.filters?.length) {
      obj.filters = message.filters.map((e) => SearchFilter.toJSON(e));
    }
    if (message.timeFrame !== undefined) {
      obj.timeFrame = TimeFrame.toJSON(message.timeFrame);
    }
    if (message.interval !== undefined) {
      obj.interval = Duration.toJSON(message.interval);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CountFailedXDRTestsRequest>, I>>(base?: I): CountFailedXDRTestsRequest {
    return CountFailedXDRTestsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CountFailedXDRTestsRequest>, I>>(object: I): CountFailedXDRTestsRequest {
    const message = createBaseCountFailedXDRTestsRequest();
    message.filters = object.filters?.map((e) => SearchFilter.fromPartial(e)) || [];
    message.timeFrame = (object.timeFrame !== undefined && object.timeFrame !== null)
      ? TimeFrame.fromPartial(object.timeFrame)
      : undefined;
    message.interval = (object.interval !== undefined && object.interval !== null)
      ? Duration.fromPartial(object.interval)
      : undefined;
    return message;
  },
};

function createBaseCountFailedXDRTestsResponse(): CountFailedXDRTestsResponse {
  return { timeSeries: undefined, totalCount: undefined };
}

export const CountFailedXDRTestsResponse = {
  encode(message: CountFailedXDRTestsResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.timeSeries !== undefined) {
      HistogramTimeSeries.encode(message.timeSeries, writer.uint32(10).fork()).ldelim();
    }
    if (message.totalCount !== undefined) {
      Int64Value.encode({ value: message.totalCount! }, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): CountFailedXDRTestsResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCountFailedXDRTestsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.timeSeries = HistogramTimeSeries.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.totalCount = Int64Value.decode(reader, reader.uint32()).value;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CountFailedXDRTestsResponse {
    return {
      timeSeries: isSet(object.timeSeries) ? HistogramTimeSeries.fromJSON(object.timeSeries) : undefined,
      totalCount: isSet(object.totalCount) ? Number(object.totalCount) : undefined,
    };
  },

  toJSON(message: CountFailedXDRTestsResponse): unknown {
    const obj: any = {};
    if (message.timeSeries !== undefined) {
      obj.timeSeries = HistogramTimeSeries.toJSON(message.timeSeries);
    }
    if (message.totalCount !== undefined) {
      obj.totalCount = message.totalCount;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CountFailedXDRTestsResponse>, I>>(base?: I): CountFailedXDRTestsResponse {
    return CountFailedXDRTestsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CountFailedXDRTestsResponse>, I>>(object: I): CountFailedXDRTestsResponse {
    const message = createBaseCountFailedXDRTestsResponse();
    message.timeSeries = (object.timeSeries !== undefined && object.timeSeries !== null)
      ? HistogramTimeSeries.fromPartial(object.timeSeries)
      : undefined;
    message.totalCount = object.totalCount ?? undefined;
    return message;
  },
};

function createBaseHistogramTimeSeries(): HistogramTimeSeries {
  return { values: [] };
}

export const HistogramTimeSeries = {
  encode(message: HistogramTimeSeries, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.values) {
      HistogramDataPoint.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): HistogramTimeSeries {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHistogramTimeSeries();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.values.push(HistogramDataPoint.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HistogramTimeSeries {
    return {
      values: globalThis.Array.isArray(object?.values)
        ? object.values.map((e: any) => HistogramDataPoint.fromJSON(e))
        : [],
    };
  },

  toJSON(message: HistogramTimeSeries): unknown {
    const obj: any = {};
    if (message.values?.length) {
      obj.values = message.values.map((e) => HistogramDataPoint.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HistogramTimeSeries>, I>>(base?: I): HistogramTimeSeries {
    return HistogramTimeSeries.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HistogramTimeSeries>, I>>(object: I): HistogramTimeSeries {
    const message = createBaseHistogramTimeSeries();
    message.values = object.values?.map((e) => HistogramDataPoint.fromPartial(e)) || [];
    return message;
  },
};

function createBaseHistogramDataPoint(): HistogramDataPoint {
  return { timestamp: undefined, values: {} };
}

export const HistogramDataPoint = {
  encode(message: HistogramDataPoint, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.timestamp !== undefined) {
      Timestamp.encode(toTimestamp(message.timestamp), writer.uint32(10).fork()).ldelim();
    }
    Object.entries(message.values).forEach(([key, value]) => {
      if (value !== undefined) {
        HistogramDataPoint_ValuesEntry.encode({ key: key as any, value }, writer.uint32(18).fork()).ldelim();
      }
    });
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): HistogramDataPoint {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHistogramDataPoint();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.timestamp = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          const entry2 = HistogramDataPoint_ValuesEntry.decode(reader, reader.uint32());
          if (entry2.value !== undefined) {
            message.values[entry2.key] = entry2.value;
          }
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HistogramDataPoint {
    return {
      timestamp: isSet(object.timestamp) ? fromJsonTimestamp(object.timestamp) : undefined,
      values: isObject(object.values)
        ? Object.entries(object.values).reduce<{ [key: string]: number | undefined }>((acc, [key, value]) => {
          acc[key] = value as number | undefined;
          return acc;
        }, {})
        : {},
    };
  },

  toJSON(message: HistogramDataPoint): unknown {
    const obj: any = {};
    if (message.timestamp !== undefined) {
      obj.timestamp = message.timestamp.toISOString();
    }
    if (message.values) {
      const entries = Object.entries(message.values);
      if (entries.length > 0) {
        obj.values = {};
        entries.forEach(([k, v]) => {
          obj.values[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HistogramDataPoint>, I>>(base?: I): HistogramDataPoint {
    return HistogramDataPoint.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HistogramDataPoint>, I>>(object: I): HistogramDataPoint {
    const message = createBaseHistogramDataPoint();
    message.timestamp = object.timestamp ?? undefined;
    message.values = Object.entries(object.values ?? {}).reduce<{ [key: string]: number | undefined }>(
      (acc, [key, value]) => {
        if (value !== undefined) {
          acc[key] = value;
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseHistogramDataPoint_ValuesEntry(): HistogramDataPoint_ValuesEntry {
  return { key: "", value: undefined };
}

export const HistogramDataPoint_ValuesEntry = {
  encode(message: HistogramDataPoint_ValuesEntry, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      DoubleValue.encode({ value: message.value! }, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): HistogramDataPoint_ValuesEntry {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHistogramDataPoint_ValuesEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.value = DoubleValue.decode(reader, reader.uint32()).value;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HistogramDataPoint_ValuesEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? Number(object.value) : undefined,
    };
  },

  toJSON(message: HistogramDataPoint_ValuesEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HistogramDataPoint_ValuesEntry>, I>>(base?: I): HistogramDataPoint_ValuesEntry {
    return HistogramDataPoint_ValuesEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HistogramDataPoint_ValuesEntry>, I>>(
    object: I,
  ): HistogramDataPoint_ValuesEntry {
    const message = createBaseHistogramDataPoint_ValuesEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? undefined;
    return message;
  },
};

export interface PlatformOverviewService {
  CountLogs(request: CountLogsRequest): Promise<CountLogsResponse>;
  GetMetricsStats(request: GetMetricsStatsRequest): Promise<GetMetricsStatsResponse>;
  CountSpans(request: CountSpansRequest): Promise<CountSpansResponse>;
  CountFailedXDRTests(request: CountFailedXDRTestsRequest): Promise<CountFailedXDRTestsResponse>;
}

export const PlatformOverviewServiceServiceName = "com.coralogix.dashboards.v1.widgets.PlatformOverviewService";
export class PlatformOverviewServiceClientImpl implements PlatformOverviewService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || PlatformOverviewServiceServiceName;
    this.rpc = rpc;
    this.CountLogs = this.CountLogs.bind(this);
    this.GetMetricsStats = this.GetMetricsStats.bind(this);
    this.CountSpans = this.CountSpans.bind(this);
    this.CountFailedXDRTests = this.CountFailedXDRTests.bind(this);
  }
  CountLogs(request: CountLogsRequest): Promise<CountLogsResponse> {
    const data = CountLogsRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "CountLogs", data);
    return promise.then((data) => CountLogsResponse.decode(_m0.Reader.create(data)));
  }

  GetMetricsStats(request: GetMetricsStatsRequest): Promise<GetMetricsStatsResponse> {
    const data = GetMetricsStatsRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetMetricsStats", data);
    return promise.then((data) => GetMetricsStatsResponse.decode(_m0.Reader.create(data)));
  }

  CountSpans(request: CountSpansRequest): Promise<CountSpansResponse> {
    const data = CountSpansRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "CountSpans", data);
    return promise.then((data) => CountSpansResponse.decode(_m0.Reader.create(data)));
  }

  CountFailedXDRTests(request: CountFailedXDRTestsRequest): Promise<CountFailedXDRTestsResponse> {
    const data = CountFailedXDRTestsRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "CountFailedXDRTests", data);
    return promise.then((data) => CountFailedXDRTestsResponse.decode(_m0.Reader.create(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
