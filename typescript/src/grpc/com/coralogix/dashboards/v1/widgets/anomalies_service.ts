// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.176.1
//   protoc               v3.21.12
// source: com/coralogix/dashboards/v1/widgets/anomalies_service.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { Int32Value } from "../../../../../google/protobuf/wrappers";
import { SearchFilter } from "../common/search_filter";
import { TimeFrame } from "../common/time_frame";

export const protobufPackage = "com.coralogix.dashboards.v1.widgets";

export enum AnomalyGroupType {
  ANOMALY_GROUP_TYPE_UNSPECIFIED = 0,
  ANOMALY_GROUP_TYPE_VOLUME = 1,
  ANOMALY_GROUP_TYPE_FLOW = 2,
  UNRECOGNIZED = -1,
}

export function anomalyGroupTypeFromJSON(object: any): AnomalyGroupType {
  switch (object) {
    case 0:
    case "ANOMALY_GROUP_TYPE_UNSPECIFIED":
      return AnomalyGroupType.ANOMALY_GROUP_TYPE_UNSPECIFIED;
    case 1:
    case "ANOMALY_GROUP_TYPE_VOLUME":
      return AnomalyGroupType.ANOMALY_GROUP_TYPE_VOLUME;
    case 2:
    case "ANOMALY_GROUP_TYPE_FLOW":
      return AnomalyGroupType.ANOMALY_GROUP_TYPE_FLOW;
    case -1:
    case "UNRECOGNIZED":
    default:
      return AnomalyGroupType.UNRECOGNIZED;
  }
}

export function anomalyGroupTypeToJSON(object: AnomalyGroupType): string {
  switch (object) {
    case AnomalyGroupType.ANOMALY_GROUP_TYPE_UNSPECIFIED:
      return "ANOMALY_GROUP_TYPE_UNSPECIFIED";
    case AnomalyGroupType.ANOMALY_GROUP_TYPE_VOLUME:
      return "ANOMALY_GROUP_TYPE_VOLUME";
    case AnomalyGroupType.ANOMALY_GROUP_TYPE_FLOW:
      return "ANOMALY_GROUP_TYPE_FLOW";
    case AnomalyGroupType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface SearchAnomaliesGroupedByTypeRequest {
  filters: SearchFilter[];
  timeFrame: TimeFrame | undefined;
}

export interface SearchAnomaliesGroupedByTypeResponse {
  anomalies: AnomalyGroupByType[];
}

export interface AnomalyGroupByType {
  type: AnomalyGroupType;
  amount: number | undefined;
}

function createBaseSearchAnomaliesGroupedByTypeRequest(): SearchAnomaliesGroupedByTypeRequest {
  return { filters: [], timeFrame: undefined };
}

export const SearchAnomaliesGroupedByTypeRequest = {
  encode(message: SearchAnomaliesGroupedByTypeRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.filters) {
      SearchFilter.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    if (message.timeFrame !== undefined) {
      TimeFrame.encode(message.timeFrame, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SearchAnomaliesGroupedByTypeRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchAnomaliesGroupedByTypeRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.filters.push(SearchFilter.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.timeFrame = TimeFrame.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchAnomaliesGroupedByTypeRequest {
    return {
      filters: globalThis.Array.isArray(object?.filters)
        ? object.filters.map((e: any) => SearchFilter.fromJSON(e))
        : [],
      timeFrame: isSet(object.timeFrame) ? TimeFrame.fromJSON(object.timeFrame) : undefined,
    };
  },

  toJSON(message: SearchAnomaliesGroupedByTypeRequest): unknown {
    const obj: any = {};
    if (message.filters?.length) {
      obj.filters = message.filters.map((e) => SearchFilter.toJSON(e));
    }
    if (message.timeFrame !== undefined) {
      obj.timeFrame = TimeFrame.toJSON(message.timeFrame);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SearchAnomaliesGroupedByTypeRequest>, I>>(
    base?: I,
  ): SearchAnomaliesGroupedByTypeRequest {
    return SearchAnomaliesGroupedByTypeRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchAnomaliesGroupedByTypeRequest>, I>>(
    object: I,
  ): SearchAnomaliesGroupedByTypeRequest {
    const message = createBaseSearchAnomaliesGroupedByTypeRequest();
    message.filters = object.filters?.map((e) => SearchFilter.fromPartial(e)) || [];
    message.timeFrame = (object.timeFrame !== undefined && object.timeFrame !== null)
      ? TimeFrame.fromPartial(object.timeFrame)
      : undefined;
    return message;
  },
};

function createBaseSearchAnomaliesGroupedByTypeResponse(): SearchAnomaliesGroupedByTypeResponse {
  return { anomalies: [] };
}

export const SearchAnomaliesGroupedByTypeResponse = {
  encode(message: SearchAnomaliesGroupedByTypeResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.anomalies) {
      AnomalyGroupByType.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SearchAnomaliesGroupedByTypeResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchAnomaliesGroupedByTypeResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.anomalies.push(AnomalyGroupByType.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchAnomaliesGroupedByTypeResponse {
    return {
      anomalies: globalThis.Array.isArray(object?.anomalies)
        ? object.anomalies.map((e: any) => AnomalyGroupByType.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SearchAnomaliesGroupedByTypeResponse): unknown {
    const obj: any = {};
    if (message.anomalies?.length) {
      obj.anomalies = message.anomalies.map((e) => AnomalyGroupByType.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SearchAnomaliesGroupedByTypeResponse>, I>>(
    base?: I,
  ): SearchAnomaliesGroupedByTypeResponse {
    return SearchAnomaliesGroupedByTypeResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchAnomaliesGroupedByTypeResponse>, I>>(
    object: I,
  ): SearchAnomaliesGroupedByTypeResponse {
    const message = createBaseSearchAnomaliesGroupedByTypeResponse();
    message.anomalies = object.anomalies?.map((e) => AnomalyGroupByType.fromPartial(e)) || [];
    return message;
  },
};

function createBaseAnomalyGroupByType(): AnomalyGroupByType {
  return { type: 0, amount: undefined };
}

export const AnomalyGroupByType = {
  encode(message: AnomalyGroupByType, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.type !== 0) {
      writer.uint32(8).int32(message.type);
    }
    if (message.amount !== undefined) {
      Int32Value.encode({ value: message.amount! }, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): AnomalyGroupByType {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAnomalyGroupByType();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.amount = Int32Value.decode(reader, reader.uint32()).value;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AnomalyGroupByType {
    return {
      type: isSet(object.type) ? anomalyGroupTypeFromJSON(object.type) : 0,
      amount: isSet(object.amount) ? Number(object.amount) : undefined,
    };
  },

  toJSON(message: AnomalyGroupByType): unknown {
    const obj: any = {};
    if (message.type !== 0) {
      obj.type = anomalyGroupTypeToJSON(message.type);
    }
    if (message.amount !== undefined) {
      obj.amount = message.amount;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AnomalyGroupByType>, I>>(base?: I): AnomalyGroupByType {
    return AnomalyGroupByType.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AnomalyGroupByType>, I>>(object: I): AnomalyGroupByType {
    const message = createBaseAnomalyGroupByType();
    message.type = object.type ?? 0;
    message.amount = object.amount ?? undefined;
    return message;
  },
};

export interface AnomaliesService {
  SearchAnomaliesGroupedByType(
    request: SearchAnomaliesGroupedByTypeRequest,
  ): Promise<SearchAnomaliesGroupedByTypeResponse>;
}

export const AnomaliesServiceServiceName = "com.coralogix.dashboards.v1.widgets.AnomaliesService";
export class AnomaliesServiceClientImpl implements AnomaliesService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || AnomaliesServiceServiceName;
    this.rpc = rpc;
    this.SearchAnomaliesGroupedByType = this.SearchAnomaliesGroupedByType.bind(this);
  }
  SearchAnomaliesGroupedByType(
    request: SearchAnomaliesGroupedByTypeRequest,
  ): Promise<SearchAnomaliesGroupedByTypeResponse> {
    const data = SearchAnomaliesGroupedByTypeRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "SearchAnomaliesGroupedByType", data);
    return promise.then((data) => SearchAnomaliesGroupedByTypeResponse.decode(_m0.Reader.create(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
