// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.176.1
//   protoc               v3.21.12
// source: com/coralogix/dashboards/v1/widgets/logs_activity_service.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { Duration } from "../../../../../google/protobuf/duration";
import { Timestamp } from "../../../../../google/protobuf/timestamp";
import { Int32Value, StringValue } from "../../../../../google/protobuf/wrappers";
import { SearchFilter } from "../common/search_filter";
import { TimeFrame } from "../common/time_frame";
import { TimeSeries } from "../common/time_series";

export const protobufPackage = "com.coralogix.dashboards.v1.widgets";

export interface SearchLogsActivityRequest {
  filters: SearchFilter[];
  timeFrame: TimeFrame | undefined;
  interval: Duration | undefined;
}

export interface SearchLogsActivityResponse {
  timeSeries: TimeSeries[];
  tags: Tag[];
}

export interface Tag {
  id: number | undefined;
  occurredOn: Date | undefined;
  avatar: string | undefined;
  title: string | undefined;
  description: string | undefined;
}

function createBaseSearchLogsActivityRequest(): SearchLogsActivityRequest {
  return { filters: [], timeFrame: undefined, interval: undefined };
}

export const SearchLogsActivityRequest = {
  encode(message: SearchLogsActivityRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.filters) {
      SearchFilter.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    if (message.timeFrame !== undefined) {
      TimeFrame.encode(message.timeFrame, writer.uint32(18).fork()).ldelim();
    }
    if (message.interval !== undefined) {
      Duration.encode(message.interval, writer.uint32(26).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SearchLogsActivityRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchLogsActivityRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.filters.push(SearchFilter.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.timeFrame = TimeFrame.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.interval = Duration.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchLogsActivityRequest {
    return {
      filters: globalThis.Array.isArray(object?.filters)
        ? object.filters.map((e: any) => SearchFilter.fromJSON(e))
        : [],
      timeFrame: isSet(object.timeFrame) ? TimeFrame.fromJSON(object.timeFrame) : undefined,
      interval: isSet(object.interval) ? Duration.fromJSON(object.interval) : undefined,
    };
  },

  toJSON(message: SearchLogsActivityRequest): unknown {
    const obj: any = {};
    if (message.filters?.length) {
      obj.filters = message.filters.map((e) => SearchFilter.toJSON(e));
    }
    if (message.timeFrame !== undefined) {
      obj.timeFrame = TimeFrame.toJSON(message.timeFrame);
    }
    if (message.interval !== undefined) {
      obj.interval = Duration.toJSON(message.interval);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SearchLogsActivityRequest>, I>>(base?: I): SearchLogsActivityRequest {
    return SearchLogsActivityRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchLogsActivityRequest>, I>>(object: I): SearchLogsActivityRequest {
    const message = createBaseSearchLogsActivityRequest();
    message.filters = object.filters?.map((e) => SearchFilter.fromPartial(e)) || [];
    message.timeFrame = (object.timeFrame !== undefined && object.timeFrame !== null)
      ? TimeFrame.fromPartial(object.timeFrame)
      : undefined;
    message.interval = (object.interval !== undefined && object.interval !== null)
      ? Duration.fromPartial(object.interval)
      : undefined;
    return message;
  },
};

function createBaseSearchLogsActivityResponse(): SearchLogsActivityResponse {
  return { timeSeries: [], tags: [] };
}

export const SearchLogsActivityResponse = {
  encode(message: SearchLogsActivityResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.timeSeries) {
      TimeSeries.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    for (const v of message.tags) {
      Tag.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SearchLogsActivityResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchLogsActivityResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.timeSeries.push(TimeSeries.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.tags.push(Tag.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchLogsActivityResponse {
    return {
      timeSeries: globalThis.Array.isArray(object?.timeSeries)
        ? object.timeSeries.map((e: any) => TimeSeries.fromJSON(e))
        : [],
      tags: globalThis.Array.isArray(object?.tags) ? object.tags.map((e: any) => Tag.fromJSON(e)) : [],
    };
  },

  toJSON(message: SearchLogsActivityResponse): unknown {
    const obj: any = {};
    if (message.timeSeries?.length) {
      obj.timeSeries = message.timeSeries.map((e) => TimeSeries.toJSON(e));
    }
    if (message.tags?.length) {
      obj.tags = message.tags.map((e) => Tag.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SearchLogsActivityResponse>, I>>(base?: I): SearchLogsActivityResponse {
    return SearchLogsActivityResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchLogsActivityResponse>, I>>(object: I): SearchLogsActivityResponse {
    const message = createBaseSearchLogsActivityResponse();
    message.timeSeries = object.timeSeries?.map((e) => TimeSeries.fromPartial(e)) || [];
    message.tags = object.tags?.map((e) => Tag.fromPartial(e)) || [];
    return message;
  },
};

function createBaseTag(): Tag {
  return { id: undefined, occurredOn: undefined, avatar: undefined, title: undefined, description: undefined };
}

export const Tag = {
  encode(message: Tag, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== undefined) {
      Int32Value.encode({ value: message.id! }, writer.uint32(10).fork()).ldelim();
    }
    if (message.occurredOn !== undefined) {
      Timestamp.encode(toTimestamp(message.occurredOn), writer.uint32(18).fork()).ldelim();
    }
    if (message.avatar !== undefined) {
      StringValue.encode({ value: message.avatar! }, writer.uint32(26).fork()).ldelim();
    }
    if (message.title !== undefined) {
      StringValue.encode({ value: message.title! }, writer.uint32(34).fork()).ldelim();
    }
    if (message.description !== undefined) {
      StringValue.encode({ value: message.description! }, writer.uint32(42).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Tag {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTag();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = Int32Value.decode(reader, reader.uint32()).value;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.occurredOn = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.avatar = StringValue.decode(reader, reader.uint32()).value;
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.title = StringValue.decode(reader, reader.uint32()).value;
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.description = StringValue.decode(reader, reader.uint32()).value;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Tag {
    return {
      id: isSet(object.id) ? Number(object.id) : undefined,
      occurredOn: isSet(object.occurredOn) ? fromJsonTimestamp(object.occurredOn) : undefined,
      avatar: isSet(object.avatar) ? String(object.avatar) : undefined,
      title: isSet(object.title) ? String(object.title) : undefined,
      description: isSet(object.description) ? String(object.description) : undefined,
    };
  },

  toJSON(message: Tag): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = message.id;
    }
    if (message.occurredOn !== undefined) {
      obj.occurredOn = message.occurredOn.toISOString();
    }
    if (message.avatar !== undefined) {
      obj.avatar = message.avatar;
    }
    if (message.title !== undefined) {
      obj.title = message.title;
    }
    if (message.description !== undefined) {
      obj.description = message.description;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Tag>, I>>(base?: I): Tag {
    return Tag.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Tag>, I>>(object: I): Tag {
    const message = createBaseTag();
    message.id = object.id ?? undefined;
    message.occurredOn = object.occurredOn ?? undefined;
    message.avatar = object.avatar ?? undefined;
    message.title = object.title ?? undefined;
    message.description = object.description ?? undefined;
    return message;
  },
};

export interface LogsActivityService {
  SearchLogsActivity(request: SearchLogsActivityRequest): Promise<SearchLogsActivityResponse>;
}

export const LogsActivityServiceServiceName = "com.coralogix.dashboards.v1.widgets.LogsActivityService";
export class LogsActivityServiceClientImpl implements LogsActivityService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || LogsActivityServiceServiceName;
    this.rpc = rpc;
    this.SearchLogsActivity = this.SearchLogsActivity.bind(this);
  }
  SearchLogsActivity(request: SearchLogsActivityRequest): Promise<SearchLogsActivityResponse> {
    const data = SearchLogsActivityRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "SearchLogsActivity", data);
    return promise.then((data) => SearchLogsActivityResponse.decode(_m0.Reader.create(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
