// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.176.1
//   protoc               v3.21.12
// source: com/coralogix/dashboards/v1/widgets/insights_service.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { Timestamp } from "../../../../../google/protobuf/timestamp";
import { Int32Value, StringValue } from "../../../../../google/protobuf/wrappers";
import { SearchFilter } from "../common/search_filter";
import { TimeFrame } from "../common/time_frame";

export const protobufPackage = "com.coralogix.dashboards.v1.widgets";

export enum InsightSeverity {
  INSIGHT_SEVERITY_UNSPECIFIED = 0,
  INSIGHT_SEVERITY_INFO = 1,
  INSIGHT_SEVERITY_WARNING = 2,
  INSIGHT_SEVERITY_CRITICAL = 3,
  INSIGHT_SEVERITY_ERROR = 4,
  UNRECOGNIZED = -1,
}

export function insightSeverityFromJSON(object: any): InsightSeverity {
  switch (object) {
    case 0:
    case "INSIGHT_SEVERITY_UNSPECIFIED":
      return InsightSeverity.INSIGHT_SEVERITY_UNSPECIFIED;
    case 1:
    case "INSIGHT_SEVERITY_INFO":
      return InsightSeverity.INSIGHT_SEVERITY_INFO;
    case 2:
    case "INSIGHT_SEVERITY_WARNING":
      return InsightSeverity.INSIGHT_SEVERITY_WARNING;
    case 3:
    case "INSIGHT_SEVERITY_CRITICAL":
      return InsightSeverity.INSIGHT_SEVERITY_CRITICAL;
    case 4:
    case "INSIGHT_SEVERITY_ERROR":
      return InsightSeverity.INSIGHT_SEVERITY_ERROR;
    case -1:
    case "UNRECOGNIZED":
    default:
      return InsightSeverity.UNRECOGNIZED;
  }
}

export function insightSeverityToJSON(object: InsightSeverity): string {
  switch (object) {
    case InsightSeverity.INSIGHT_SEVERITY_UNSPECIFIED:
      return "INSIGHT_SEVERITY_UNSPECIFIED";
    case InsightSeverity.INSIGHT_SEVERITY_INFO:
      return "INSIGHT_SEVERITY_INFO";
    case InsightSeverity.INSIGHT_SEVERITY_WARNING:
      return "INSIGHT_SEVERITY_WARNING";
    case InsightSeverity.INSIGHT_SEVERITY_CRITICAL:
      return "INSIGHT_SEVERITY_CRITICAL";
    case InsightSeverity.INSIGHT_SEVERITY_ERROR:
      return "INSIGHT_SEVERITY_ERROR";
    case InsightSeverity.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum InsightType {
  INSIGHT_TYPE_UNSPECIFIED = 0,
  INSIGHT_TYPE_USER_ALERT = 1,
  INSIGHT_TYPE_ANOMALY = 2,
  INSIGHT_TYPE_RATIO = 3,
  INSIGHT_TYPE_NEW_VALUE = 4,
  INSIGHT_TYPE_UNIQUE_COUNT = 5,
  INSIGHT_TYPE_TIME_RELATIVE = 6,
  INSIGHT_TYPE_METRIC = 7,
  INSIGHT_TYPE_FLOW = 8,
  UNRECOGNIZED = -1,
}

export function insightTypeFromJSON(object: any): InsightType {
  switch (object) {
    case 0:
    case "INSIGHT_TYPE_UNSPECIFIED":
      return InsightType.INSIGHT_TYPE_UNSPECIFIED;
    case 1:
    case "INSIGHT_TYPE_USER_ALERT":
      return InsightType.INSIGHT_TYPE_USER_ALERT;
    case 2:
    case "INSIGHT_TYPE_ANOMALY":
      return InsightType.INSIGHT_TYPE_ANOMALY;
    case 3:
    case "INSIGHT_TYPE_RATIO":
      return InsightType.INSIGHT_TYPE_RATIO;
    case 4:
    case "INSIGHT_TYPE_NEW_VALUE":
      return InsightType.INSIGHT_TYPE_NEW_VALUE;
    case 5:
    case "INSIGHT_TYPE_UNIQUE_COUNT":
      return InsightType.INSIGHT_TYPE_UNIQUE_COUNT;
    case 6:
    case "INSIGHT_TYPE_TIME_RELATIVE":
      return InsightType.INSIGHT_TYPE_TIME_RELATIVE;
    case 7:
    case "INSIGHT_TYPE_METRIC":
      return InsightType.INSIGHT_TYPE_METRIC;
    case 8:
    case "INSIGHT_TYPE_FLOW":
      return InsightType.INSIGHT_TYPE_FLOW;
    case -1:
    case "UNRECOGNIZED":
    default:
      return InsightType.UNRECOGNIZED;
  }
}

export function insightTypeToJSON(object: InsightType): string {
  switch (object) {
    case InsightType.INSIGHT_TYPE_UNSPECIFIED:
      return "INSIGHT_TYPE_UNSPECIFIED";
    case InsightType.INSIGHT_TYPE_USER_ALERT:
      return "INSIGHT_TYPE_USER_ALERT";
    case InsightType.INSIGHT_TYPE_ANOMALY:
      return "INSIGHT_TYPE_ANOMALY";
    case InsightType.INSIGHT_TYPE_RATIO:
      return "INSIGHT_TYPE_RATIO";
    case InsightType.INSIGHT_TYPE_NEW_VALUE:
      return "INSIGHT_TYPE_NEW_VALUE";
    case InsightType.INSIGHT_TYPE_UNIQUE_COUNT:
      return "INSIGHT_TYPE_UNIQUE_COUNT";
    case InsightType.INSIGHT_TYPE_TIME_RELATIVE:
      return "INSIGHT_TYPE_TIME_RELATIVE";
    case InsightType.INSIGHT_TYPE_METRIC:
      return "INSIGHT_TYPE_METRIC";
    case InsightType.INSIGHT_TYPE_FLOW:
      return "INSIGHT_TYPE_FLOW";
    case InsightType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface SearchInsightsRequest {
  filters: SearchFilter[];
  timeFrame: TimeFrame | undefined;
}

export interface SearchInsightsResponse {
  insights: Insight[];
}

export interface Insight {
  id: string | undefined;
  alertId: string | undefined;
  name: string | undefined;
  severity: InsightSeverity;
  application: string | undefined;
  subsystem: string | undefined;
  repetitions: number | undefined;
  type: InsightType;
  lastOccurrence: Date | undefined;
  alertSnoozed: AlertSnoozed | undefined;
}

export interface AlertSnoozed {
  userId: string | undefined;
  until: Date | undefined;
}

function createBaseSearchInsightsRequest(): SearchInsightsRequest {
  return { filters: [], timeFrame: undefined };
}

export const SearchInsightsRequest = {
  encode(message: SearchInsightsRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.filters) {
      SearchFilter.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    if (message.timeFrame !== undefined) {
      TimeFrame.encode(message.timeFrame, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SearchInsightsRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchInsightsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.filters.push(SearchFilter.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.timeFrame = TimeFrame.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchInsightsRequest {
    return {
      filters: globalThis.Array.isArray(object?.filters)
        ? object.filters.map((e: any) => SearchFilter.fromJSON(e))
        : [],
      timeFrame: isSet(object.timeFrame) ? TimeFrame.fromJSON(object.timeFrame) : undefined,
    };
  },

  toJSON(message: SearchInsightsRequest): unknown {
    const obj: any = {};
    if (message.filters?.length) {
      obj.filters = message.filters.map((e) => SearchFilter.toJSON(e));
    }
    if (message.timeFrame !== undefined) {
      obj.timeFrame = TimeFrame.toJSON(message.timeFrame);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SearchInsightsRequest>, I>>(base?: I): SearchInsightsRequest {
    return SearchInsightsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchInsightsRequest>, I>>(object: I): SearchInsightsRequest {
    const message = createBaseSearchInsightsRequest();
    message.filters = object.filters?.map((e) => SearchFilter.fromPartial(e)) || [];
    message.timeFrame = (object.timeFrame !== undefined && object.timeFrame !== null)
      ? TimeFrame.fromPartial(object.timeFrame)
      : undefined;
    return message;
  },
};

function createBaseSearchInsightsResponse(): SearchInsightsResponse {
  return { insights: [] };
}

export const SearchInsightsResponse = {
  encode(message: SearchInsightsResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.insights) {
      Insight.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SearchInsightsResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchInsightsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.insights.push(Insight.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchInsightsResponse {
    return {
      insights: globalThis.Array.isArray(object?.insights) ? object.insights.map((e: any) => Insight.fromJSON(e)) : [],
    };
  },

  toJSON(message: SearchInsightsResponse): unknown {
    const obj: any = {};
    if (message.insights?.length) {
      obj.insights = message.insights.map((e) => Insight.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SearchInsightsResponse>, I>>(base?: I): SearchInsightsResponse {
    return SearchInsightsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchInsightsResponse>, I>>(object: I): SearchInsightsResponse {
    const message = createBaseSearchInsightsResponse();
    message.insights = object.insights?.map((e) => Insight.fromPartial(e)) || [];
    return message;
  },
};

function createBaseInsight(): Insight {
  return {
    id: undefined,
    alertId: undefined,
    name: undefined,
    severity: 0,
    application: undefined,
    subsystem: undefined,
    repetitions: undefined,
    type: 0,
    lastOccurrence: undefined,
    alertSnoozed: undefined,
  };
}

export const Insight = {
  encode(message: Insight, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== undefined) {
      StringValue.encode({ value: message.id! }, writer.uint32(10).fork()).ldelim();
    }
    if (message.alertId !== undefined) {
      StringValue.encode({ value: message.alertId! }, writer.uint32(18).fork()).ldelim();
    }
    if (message.name !== undefined) {
      StringValue.encode({ value: message.name! }, writer.uint32(26).fork()).ldelim();
    }
    if (message.severity !== 0) {
      writer.uint32(32).int32(message.severity);
    }
    if (message.application !== undefined) {
      StringValue.encode({ value: message.application! }, writer.uint32(42).fork()).ldelim();
    }
    if (message.subsystem !== undefined) {
      StringValue.encode({ value: message.subsystem! }, writer.uint32(50).fork()).ldelim();
    }
    if (message.repetitions !== undefined) {
      Int32Value.encode({ value: message.repetitions! }, writer.uint32(58).fork()).ldelim();
    }
    if (message.type !== 0) {
      writer.uint32(64).int32(message.type);
    }
    if (message.lastOccurrence !== undefined) {
      Timestamp.encode(toTimestamp(message.lastOccurrence), writer.uint32(74).fork()).ldelim();
    }
    if (message.alertSnoozed !== undefined) {
      AlertSnoozed.encode(message.alertSnoozed, writer.uint32(82).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Insight {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInsight();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = StringValue.decode(reader, reader.uint32()).value;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.alertId = StringValue.decode(reader, reader.uint32()).value;
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.name = StringValue.decode(reader, reader.uint32()).value;
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.severity = reader.int32() as any;
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.application = StringValue.decode(reader, reader.uint32()).value;
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.subsystem = StringValue.decode(reader, reader.uint32()).value;
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.repetitions = Int32Value.decode(reader, reader.uint32()).value;
          continue;
        case 8:
          if (tag !== 64) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        case 9:
          if (tag !== 74) {
            break;
          }

          message.lastOccurrence = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 10:
          if (tag !== 82) {
            break;
          }

          message.alertSnoozed = AlertSnoozed.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Insight {
    return {
      id: isSet(object.id) ? String(object.id) : undefined,
      alertId: isSet(object.alertId) ? String(object.alertId) : undefined,
      name: isSet(object.name) ? String(object.name) : undefined,
      severity: isSet(object.severity) ? insightSeverityFromJSON(object.severity) : 0,
      application: isSet(object.application) ? String(object.application) : undefined,
      subsystem: isSet(object.subsystem) ? String(object.subsystem) : undefined,
      repetitions: isSet(object.repetitions) ? Number(object.repetitions) : undefined,
      type: isSet(object.type) ? insightTypeFromJSON(object.type) : 0,
      lastOccurrence: isSet(object.lastOccurrence) ? fromJsonTimestamp(object.lastOccurrence) : undefined,
      alertSnoozed: isSet(object.alertSnoozed) ? AlertSnoozed.fromJSON(object.alertSnoozed) : undefined,
    };
  },

  toJSON(message: Insight): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = message.id;
    }
    if (message.alertId !== undefined) {
      obj.alertId = message.alertId;
    }
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    if (message.severity !== 0) {
      obj.severity = insightSeverityToJSON(message.severity);
    }
    if (message.application !== undefined) {
      obj.application = message.application;
    }
    if (message.subsystem !== undefined) {
      obj.subsystem = message.subsystem;
    }
    if (message.repetitions !== undefined) {
      obj.repetitions = message.repetitions;
    }
    if (message.type !== 0) {
      obj.type = insightTypeToJSON(message.type);
    }
    if (message.lastOccurrence !== undefined) {
      obj.lastOccurrence = message.lastOccurrence.toISOString();
    }
    if (message.alertSnoozed !== undefined) {
      obj.alertSnoozed = AlertSnoozed.toJSON(message.alertSnoozed);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Insight>, I>>(base?: I): Insight {
    return Insight.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Insight>, I>>(object: I): Insight {
    const message = createBaseInsight();
    message.id = object.id ?? undefined;
    message.alertId = object.alertId ?? undefined;
    message.name = object.name ?? undefined;
    message.severity = object.severity ?? 0;
    message.application = object.application ?? undefined;
    message.subsystem = object.subsystem ?? undefined;
    message.repetitions = object.repetitions ?? undefined;
    message.type = object.type ?? 0;
    message.lastOccurrence = object.lastOccurrence ?? undefined;
    message.alertSnoozed = (object.alertSnoozed !== undefined && object.alertSnoozed !== null)
      ? AlertSnoozed.fromPartial(object.alertSnoozed)
      : undefined;
    return message;
  },
};

function createBaseAlertSnoozed(): AlertSnoozed {
  return { userId: undefined, until: undefined };
}

export const AlertSnoozed = {
  encode(message: AlertSnoozed, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.userId !== undefined) {
      StringValue.encode({ value: message.userId! }, writer.uint32(10).fork()).ldelim();
    }
    if (message.until !== undefined) {
      Timestamp.encode(toTimestamp(message.until), writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): AlertSnoozed {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAlertSnoozed();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.userId = StringValue.decode(reader, reader.uint32()).value;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.until = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AlertSnoozed {
    return {
      userId: isSet(object.userId) ? String(object.userId) : undefined,
      until: isSet(object.until) ? fromJsonTimestamp(object.until) : undefined,
    };
  },

  toJSON(message: AlertSnoozed): unknown {
    const obj: any = {};
    if (message.userId !== undefined) {
      obj.userId = message.userId;
    }
    if (message.until !== undefined) {
      obj.until = message.until.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AlertSnoozed>, I>>(base?: I): AlertSnoozed {
    return AlertSnoozed.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AlertSnoozed>, I>>(object: I): AlertSnoozed {
    const message = createBaseAlertSnoozed();
    message.userId = object.userId ?? undefined;
    message.until = object.until ?? undefined;
    return message;
  },
};

export interface InsightsService {
  SearchInsights(request: SearchInsightsRequest): Promise<SearchInsightsResponse>;
}

export const InsightsServiceServiceName = "com.coralogix.dashboards.v1.widgets.InsightsService";
export class InsightsServiceClientImpl implements InsightsService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || InsightsServiceServiceName;
    this.rpc = rpc;
    this.SearchInsights = this.SearchInsights.bind(this);
  }
  SearchInsights(request: SearchInsightsRequest): Promise<SearchInsightsResponse> {
    const data = SearchInsightsRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "SearchInsights", data);
    return promise.then((data) => SearchInsightsResponse.decode(_m0.Reader.create(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
