// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.176.1
//   protoc               v3.21.12
// source: com/coralogix/enrichment/v1/enrichment_internal_service.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { UInt32Value } from "../../../../google/protobuf/wrappers";
import { Enrichment } from "./enrichment";
import { EnrichmentRequestModel } from "./enrichment_request_model";

export const protobufPackage = "com.coralogix.enrichment.v1";

export interface GetInternalEnrichmentsRequest {
}

export interface GetInternalEnrichmentsResponse {
  enrichments: Enrichment[];
}

export interface AddInternalEnrichmentsRequest {
  requestEnrichments: EnrichmentRequestModel[];
}

export interface AddInternalEnrichmentsResponse {
  enrichments: Enrichment[];
}

export interface RemoveInternalEnrichmentsRequest {
  enrichmentIds: number[];
}

export interface RemoveInternalEnrichmentsResponse {
  remainingEnrichments: Enrichment[];
}

function createBaseGetInternalEnrichmentsRequest(): GetInternalEnrichmentsRequest {
  return {};
}

export const GetInternalEnrichmentsRequest = {
  encode(_: GetInternalEnrichmentsRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetInternalEnrichmentsRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetInternalEnrichmentsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): GetInternalEnrichmentsRequest {
    return {};
  },

  toJSON(_: GetInternalEnrichmentsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<GetInternalEnrichmentsRequest>, I>>(base?: I): GetInternalEnrichmentsRequest {
    return GetInternalEnrichmentsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetInternalEnrichmentsRequest>, I>>(_: I): GetInternalEnrichmentsRequest {
    const message = createBaseGetInternalEnrichmentsRequest();
    return message;
  },
};

function createBaseGetInternalEnrichmentsResponse(): GetInternalEnrichmentsResponse {
  return { enrichments: [] };
}

export const GetInternalEnrichmentsResponse = {
  encode(message: GetInternalEnrichmentsResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.enrichments) {
      Enrichment.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetInternalEnrichmentsResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetInternalEnrichmentsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.enrichments.push(Enrichment.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetInternalEnrichmentsResponse {
    return {
      enrichments: globalThis.Array.isArray(object?.enrichments)
        ? object.enrichments.map((e: any) => Enrichment.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GetInternalEnrichmentsResponse): unknown {
    const obj: any = {};
    if (message.enrichments?.length) {
      obj.enrichments = message.enrichments.map((e) => Enrichment.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetInternalEnrichmentsResponse>, I>>(base?: I): GetInternalEnrichmentsResponse {
    return GetInternalEnrichmentsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetInternalEnrichmentsResponse>, I>>(
    object: I,
  ): GetInternalEnrichmentsResponse {
    const message = createBaseGetInternalEnrichmentsResponse();
    message.enrichments = object.enrichments?.map((e) => Enrichment.fromPartial(e)) || [];
    return message;
  },
};

function createBaseAddInternalEnrichmentsRequest(): AddInternalEnrichmentsRequest {
  return { requestEnrichments: [] };
}

export const AddInternalEnrichmentsRequest = {
  encode(message: AddInternalEnrichmentsRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.requestEnrichments) {
      EnrichmentRequestModel.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): AddInternalEnrichmentsRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAddInternalEnrichmentsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.requestEnrichments.push(EnrichmentRequestModel.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AddInternalEnrichmentsRequest {
    return {
      requestEnrichments: globalThis.Array.isArray(object?.requestEnrichments)
        ? object.requestEnrichments.map((e: any) => EnrichmentRequestModel.fromJSON(e))
        : [],
    };
  },

  toJSON(message: AddInternalEnrichmentsRequest): unknown {
    const obj: any = {};
    if (message.requestEnrichments?.length) {
      obj.requestEnrichments = message.requestEnrichments.map((e) => EnrichmentRequestModel.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AddInternalEnrichmentsRequest>, I>>(base?: I): AddInternalEnrichmentsRequest {
    return AddInternalEnrichmentsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AddInternalEnrichmentsRequest>, I>>(
    object: I,
  ): AddInternalEnrichmentsRequest {
    const message = createBaseAddInternalEnrichmentsRequest();
    message.requestEnrichments = object.requestEnrichments?.map((e) => EnrichmentRequestModel.fromPartial(e)) || [];
    return message;
  },
};

function createBaseAddInternalEnrichmentsResponse(): AddInternalEnrichmentsResponse {
  return { enrichments: [] };
}

export const AddInternalEnrichmentsResponse = {
  encode(message: AddInternalEnrichmentsResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.enrichments) {
      Enrichment.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): AddInternalEnrichmentsResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAddInternalEnrichmentsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.enrichments.push(Enrichment.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AddInternalEnrichmentsResponse {
    return {
      enrichments: globalThis.Array.isArray(object?.enrichments)
        ? object.enrichments.map((e: any) => Enrichment.fromJSON(e))
        : [],
    };
  },

  toJSON(message: AddInternalEnrichmentsResponse): unknown {
    const obj: any = {};
    if (message.enrichments?.length) {
      obj.enrichments = message.enrichments.map((e) => Enrichment.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AddInternalEnrichmentsResponse>, I>>(base?: I): AddInternalEnrichmentsResponse {
    return AddInternalEnrichmentsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AddInternalEnrichmentsResponse>, I>>(
    object: I,
  ): AddInternalEnrichmentsResponse {
    const message = createBaseAddInternalEnrichmentsResponse();
    message.enrichments = object.enrichments?.map((e) => Enrichment.fromPartial(e)) || [];
    return message;
  },
};

function createBaseRemoveInternalEnrichmentsRequest(): RemoveInternalEnrichmentsRequest {
  return { enrichmentIds: [] };
}

export const RemoveInternalEnrichmentsRequest = {
  encode(message: RemoveInternalEnrichmentsRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.enrichmentIds) {
      UInt32Value.encode({ value: v!! }, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): RemoveInternalEnrichmentsRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRemoveInternalEnrichmentsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.enrichmentIds.push(UInt32Value.decode(reader, reader.uint32()).value);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RemoveInternalEnrichmentsRequest {
    return {
      enrichmentIds: globalThis.Array.isArray(object?.enrichmentIds)
        ? object.enrichmentIds.map((e: any) => Number(e))
        : [],
    };
  },

  toJSON(message: RemoveInternalEnrichmentsRequest): unknown {
    const obj: any = {};
    if (message.enrichmentIds?.length) {
      obj.enrichmentIds = message.enrichmentIds;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RemoveInternalEnrichmentsRequest>, I>>(
    base?: I,
  ): RemoveInternalEnrichmentsRequest {
    return RemoveInternalEnrichmentsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RemoveInternalEnrichmentsRequest>, I>>(
    object: I,
  ): RemoveInternalEnrichmentsRequest {
    const message = createBaseRemoveInternalEnrichmentsRequest();
    message.enrichmentIds = object.enrichmentIds?.map((e) => e) || [];
    return message;
  },
};

function createBaseRemoveInternalEnrichmentsResponse(): RemoveInternalEnrichmentsResponse {
  return { remainingEnrichments: [] };
}

export const RemoveInternalEnrichmentsResponse = {
  encode(message: RemoveInternalEnrichmentsResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.remainingEnrichments) {
      Enrichment.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): RemoveInternalEnrichmentsResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRemoveInternalEnrichmentsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.remainingEnrichments.push(Enrichment.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RemoveInternalEnrichmentsResponse {
    return {
      remainingEnrichments: globalThis.Array.isArray(object?.remainingEnrichments)
        ? object.remainingEnrichments.map((e: any) => Enrichment.fromJSON(e))
        : [],
    };
  },

  toJSON(message: RemoveInternalEnrichmentsResponse): unknown {
    const obj: any = {};
    if (message.remainingEnrichments?.length) {
      obj.remainingEnrichments = message.remainingEnrichments.map((e) => Enrichment.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RemoveInternalEnrichmentsResponse>, I>>(
    base?: I,
  ): RemoveInternalEnrichmentsResponse {
    return RemoveInternalEnrichmentsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RemoveInternalEnrichmentsResponse>, I>>(
    object: I,
  ): RemoveInternalEnrichmentsResponse {
    const message = createBaseRemoveInternalEnrichmentsResponse();
    message.remainingEnrichments = object.remainingEnrichments?.map((e) => Enrichment.fromPartial(e)) || [];
    return message;
  },
};

export interface EnrichmentInternalService {
  GetInternalEnrichments(request: GetInternalEnrichmentsRequest): Promise<GetInternalEnrichmentsResponse>;
  AddInternalEnrichments(request: AddInternalEnrichmentsRequest): Promise<AddInternalEnrichmentsResponse>;
  RemoveInternalEnrichments(request: RemoveInternalEnrichmentsRequest): Promise<RemoveInternalEnrichmentsResponse>;
}

export const EnrichmentInternalServiceServiceName = "com.coralogix.enrichment.v1.EnrichmentInternalService";
export class EnrichmentInternalServiceClientImpl implements EnrichmentInternalService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || EnrichmentInternalServiceServiceName;
    this.rpc = rpc;
    this.GetInternalEnrichments = this.GetInternalEnrichments.bind(this);
    this.AddInternalEnrichments = this.AddInternalEnrichments.bind(this);
    this.RemoveInternalEnrichments = this.RemoveInternalEnrichments.bind(this);
  }
  GetInternalEnrichments(request: GetInternalEnrichmentsRequest): Promise<GetInternalEnrichmentsResponse> {
    const data = GetInternalEnrichmentsRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetInternalEnrichments", data);
    return promise.then((data) => GetInternalEnrichmentsResponse.decode(_m0.Reader.create(data)));
  }

  AddInternalEnrichments(request: AddInternalEnrichmentsRequest): Promise<AddInternalEnrichmentsResponse> {
    const data = AddInternalEnrichmentsRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "AddInternalEnrichments", data);
    return promise.then((data) => AddInternalEnrichmentsResponse.decode(_m0.Reader.create(data)));
  }

  RemoveInternalEnrichments(request: RemoveInternalEnrichmentsRequest): Promise<RemoveInternalEnrichmentsResponse> {
    const data = RemoveInternalEnrichmentsRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "RemoveInternalEnrichments", data);
    return promise.then((data) => RemoveInternalEnrichmentsResponse.decode(_m0.Reader.create(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };
