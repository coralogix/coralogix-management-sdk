// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.176.1
//   protoc               v3.21.12
// source: com/coralogixapis/spans2metrics/v2/spans_query.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { StringValue } from "../../../../google/protobuf/wrappers";

export const protobufPackage = "com.coralogixapis.spans2metrics.v2";

export interface SpansQuery {
  /** lucene query */
  lucene:
    | string
    | undefined;
  /** application name filters */
  applicationnameFilters: string[];
  /** subsystem name filters */
  subsystemnameFilters: string[];
  /** action filters */
  actionFilters: string[];
  /** service filters */
  serviceFilters: string[];
}

function createBaseSpansQuery(): SpansQuery {
  return {
    lucene: undefined,
    applicationnameFilters: [],
    subsystemnameFilters: [],
    actionFilters: [],
    serviceFilters: [],
  };
}

export const SpansQuery = {
  encode(message: SpansQuery, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.lucene !== undefined) {
      StringValue.encode({ value: message.lucene! }, writer.uint32(10).fork()).ldelim();
    }
    for (const v of message.applicationnameFilters) {
      StringValue.encode({ value: v!! }, writer.uint32(18).fork()).ldelim();
    }
    for (const v of message.subsystemnameFilters) {
      StringValue.encode({ value: v!! }, writer.uint32(26).fork()).ldelim();
    }
    for (const v of message.actionFilters) {
      StringValue.encode({ value: v!! }, writer.uint32(34).fork()).ldelim();
    }
    for (const v of message.serviceFilters) {
      StringValue.encode({ value: v!! }, writer.uint32(42).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SpansQuery {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSpansQuery();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.lucene = StringValue.decode(reader, reader.uint32()).value;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.applicationnameFilters.push(StringValue.decode(reader, reader.uint32()).value);
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.subsystemnameFilters.push(StringValue.decode(reader, reader.uint32()).value);
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.actionFilters.push(StringValue.decode(reader, reader.uint32()).value);
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.serviceFilters.push(StringValue.decode(reader, reader.uint32()).value);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SpansQuery {
    return {
      lucene: isSet(object.lucene) ? String(object.lucene) : undefined,
      applicationnameFilters: globalThis.Array.isArray(object?.applicationnameFilters)
        ? object.applicationnameFilters.map((e: any) => String(e))
        : [],
      subsystemnameFilters: globalThis.Array.isArray(object?.subsystemnameFilters)
        ? object.subsystemnameFilters.map((e: any) => String(e))
        : [],
      actionFilters: globalThis.Array.isArray(object?.actionFilters)
        ? object.actionFilters.map((e: any) => String(e))
        : [],
      serviceFilters: globalThis.Array.isArray(object?.serviceFilters)
        ? object.serviceFilters.map((e: any) => String(e))
        : [],
    };
  },

  toJSON(message: SpansQuery): unknown {
    const obj: any = {};
    if (message.lucene !== undefined) {
      obj.lucene = message.lucene;
    }
    if (message.applicationnameFilters?.length) {
      obj.applicationnameFilters = message.applicationnameFilters;
    }
    if (message.subsystemnameFilters?.length) {
      obj.subsystemnameFilters = message.subsystemnameFilters;
    }
    if (message.actionFilters?.length) {
      obj.actionFilters = message.actionFilters;
    }
    if (message.serviceFilters?.length) {
      obj.serviceFilters = message.serviceFilters;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SpansQuery>, I>>(base?: I): SpansQuery {
    return SpansQuery.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SpansQuery>, I>>(object: I): SpansQuery {
    const message = createBaseSpansQuery();
    message.lucene = object.lucene ?? undefined;
    message.applicationnameFilters = object.applicationnameFilters?.map((e) => e) || [];
    message.subsystemnameFilters = object.subsystemnameFilters?.map((e) => e) || [];
    message.actionFilters = object.actionFilters?.map((e) => e) || [];
    message.serviceFilters = object.serviceFilters?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
