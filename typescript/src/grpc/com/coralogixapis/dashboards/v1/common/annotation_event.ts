// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.176.1
//   protoc               v3.21.12
// source: com/coralogixapis/dashboards/v1/common/annotation_event.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { Struct } from "../../../../../google/protobuf/struct";
import { Timestamp } from "../../../../../google/protobuf/timestamp";

export const protobufPackage = "com.coralogixapis.dashboards.v1.common";

export interface AnnotationEvent {
  instant?: AnnotationEvent_Instant | undefined;
  range?: AnnotationEvent_Range | undefined;
}

export interface AnnotationEvent_Instant {
  timestamp: Date | undefined;
  labels: { [key: string]: string };
  payload: { [key: string]: any } | undefined;
}

export interface AnnotationEvent_Instant_LabelsEntry {
  key: string;
  value: string;
}

export interface AnnotationEvent_Range {
  start: Date | undefined;
  end: Date | undefined;
  labels: { [key: string]: string };
  payload: { [key: string]: any } | undefined;
}

export interface AnnotationEvent_Range_LabelsEntry {
  key: string;
  value: string;
}

function createBaseAnnotationEvent(): AnnotationEvent {
  return { instant: undefined, range: undefined };
}

export const AnnotationEvent = {
  encode(message: AnnotationEvent, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.instant !== undefined) {
      AnnotationEvent_Instant.encode(message.instant, writer.uint32(10).fork()).ldelim();
    }
    if (message.range !== undefined) {
      AnnotationEvent_Range.encode(message.range, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): AnnotationEvent {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAnnotationEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.instant = AnnotationEvent_Instant.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.range = AnnotationEvent_Range.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AnnotationEvent {
    return {
      instant: isSet(object.instant) ? AnnotationEvent_Instant.fromJSON(object.instant) : undefined,
      range: isSet(object.range) ? AnnotationEvent_Range.fromJSON(object.range) : undefined,
    };
  },

  toJSON(message: AnnotationEvent): unknown {
    const obj: any = {};
    if (message.instant !== undefined) {
      obj.instant = AnnotationEvent_Instant.toJSON(message.instant);
    }
    if (message.range !== undefined) {
      obj.range = AnnotationEvent_Range.toJSON(message.range);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AnnotationEvent>, I>>(base?: I): AnnotationEvent {
    return AnnotationEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AnnotationEvent>, I>>(object: I): AnnotationEvent {
    const message = createBaseAnnotationEvent();
    message.instant = (object.instant !== undefined && object.instant !== null)
      ? AnnotationEvent_Instant.fromPartial(object.instant)
      : undefined;
    message.range = (object.range !== undefined && object.range !== null)
      ? AnnotationEvent_Range.fromPartial(object.range)
      : undefined;
    return message;
  },
};

function createBaseAnnotationEvent_Instant(): AnnotationEvent_Instant {
  return { timestamp: undefined, labels: {}, payload: undefined };
}

export const AnnotationEvent_Instant = {
  encode(message: AnnotationEvent_Instant, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.timestamp !== undefined) {
      Timestamp.encode(toTimestamp(message.timestamp), writer.uint32(10).fork()).ldelim();
    }
    Object.entries(message.labels).forEach(([key, value]) => {
      AnnotationEvent_Instant_LabelsEntry.encode({ key: key as any, value }, writer.uint32(18).fork()).ldelim();
    });
    if (message.payload !== undefined) {
      Struct.encode(Struct.wrap(message.payload), writer.uint32(26).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): AnnotationEvent_Instant {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAnnotationEvent_Instant();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.timestamp = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          const entry2 = AnnotationEvent_Instant_LabelsEntry.decode(reader, reader.uint32());
          if (entry2.value !== undefined) {
            message.labels[entry2.key] = entry2.value;
          }
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.payload = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AnnotationEvent_Instant {
    return {
      timestamp: isSet(object.timestamp) ? fromJsonTimestamp(object.timestamp) : undefined,
      labels: isObject(object.labels)
        ? Object.entries(object.labels).reduce<{ [key: string]: string }>((acc, [key, value]) => {
          acc[key] = String(value);
          return acc;
        }, {})
        : {},
      payload: isObject(object.payload) ? object.payload : undefined,
    };
  },

  toJSON(message: AnnotationEvent_Instant): unknown {
    const obj: any = {};
    if (message.timestamp !== undefined) {
      obj.timestamp = message.timestamp.toISOString();
    }
    if (message.labels) {
      const entries = Object.entries(message.labels);
      if (entries.length > 0) {
        obj.labels = {};
        entries.forEach(([k, v]) => {
          obj.labels[k] = v;
        });
      }
    }
    if (message.payload !== undefined) {
      obj.payload = message.payload;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AnnotationEvent_Instant>, I>>(base?: I): AnnotationEvent_Instant {
    return AnnotationEvent_Instant.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AnnotationEvent_Instant>, I>>(object: I): AnnotationEvent_Instant {
    const message = createBaseAnnotationEvent_Instant();
    message.timestamp = object.timestamp ?? undefined;
    message.labels = Object.entries(object.labels ?? {}).reduce<{ [key: string]: string }>((acc, [key, value]) => {
      if (value !== undefined) {
        acc[key] = globalThis.String(value);
      }
      return acc;
    }, {});
    message.payload = object.payload ?? undefined;
    return message;
  },
};

function createBaseAnnotationEvent_Instant_LabelsEntry(): AnnotationEvent_Instant_LabelsEntry {
  return { key: "", value: "" };
}

export const AnnotationEvent_Instant_LabelsEntry = {
  encode(message: AnnotationEvent_Instant_LabelsEntry, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): AnnotationEvent_Instant_LabelsEntry {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAnnotationEvent_Instant_LabelsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AnnotationEvent_Instant_LabelsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: AnnotationEvent_Instant_LabelsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AnnotationEvent_Instant_LabelsEntry>, I>>(
    base?: I,
  ): AnnotationEvent_Instant_LabelsEntry {
    return AnnotationEvent_Instant_LabelsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AnnotationEvent_Instant_LabelsEntry>, I>>(
    object: I,
  ): AnnotationEvent_Instant_LabelsEntry {
    const message = createBaseAnnotationEvent_Instant_LabelsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseAnnotationEvent_Range(): AnnotationEvent_Range {
  return { start: undefined, end: undefined, labels: {}, payload: undefined };
}

export const AnnotationEvent_Range = {
  encode(message: AnnotationEvent_Range, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.start !== undefined) {
      Timestamp.encode(toTimestamp(message.start), writer.uint32(10).fork()).ldelim();
    }
    if (message.end !== undefined) {
      Timestamp.encode(toTimestamp(message.end), writer.uint32(18).fork()).ldelim();
    }
    Object.entries(message.labels).forEach(([key, value]) => {
      AnnotationEvent_Range_LabelsEntry.encode({ key: key as any, value }, writer.uint32(26).fork()).ldelim();
    });
    if (message.payload !== undefined) {
      Struct.encode(Struct.wrap(message.payload), writer.uint32(34).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): AnnotationEvent_Range {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAnnotationEvent_Range();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.start = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.end = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          const entry3 = AnnotationEvent_Range_LabelsEntry.decode(reader, reader.uint32());
          if (entry3.value !== undefined) {
            message.labels[entry3.key] = entry3.value;
          }
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.payload = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AnnotationEvent_Range {
    return {
      start: isSet(object.start) ? fromJsonTimestamp(object.start) : undefined,
      end: isSet(object.end) ? fromJsonTimestamp(object.end) : undefined,
      labels: isObject(object.labels)
        ? Object.entries(object.labels).reduce<{ [key: string]: string }>((acc, [key, value]) => {
          acc[key] = String(value);
          return acc;
        }, {})
        : {},
      payload: isObject(object.payload) ? object.payload : undefined,
    };
  },

  toJSON(message: AnnotationEvent_Range): unknown {
    const obj: any = {};
    if (message.start !== undefined) {
      obj.start = message.start.toISOString();
    }
    if (message.end !== undefined) {
      obj.end = message.end.toISOString();
    }
    if (message.labels) {
      const entries = Object.entries(message.labels);
      if (entries.length > 0) {
        obj.labels = {};
        entries.forEach(([k, v]) => {
          obj.labels[k] = v;
        });
      }
    }
    if (message.payload !== undefined) {
      obj.payload = message.payload;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AnnotationEvent_Range>, I>>(base?: I): AnnotationEvent_Range {
    return AnnotationEvent_Range.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AnnotationEvent_Range>, I>>(object: I): AnnotationEvent_Range {
    const message = createBaseAnnotationEvent_Range();
    message.start = object.start ?? undefined;
    message.end = object.end ?? undefined;
    message.labels = Object.entries(object.labels ?? {}).reduce<{ [key: string]: string }>((acc, [key, value]) => {
      if (value !== undefined) {
        acc[key] = globalThis.String(value);
      }
      return acc;
    }, {});
    message.payload = object.payload ?? undefined;
    return message;
  },
};

function createBaseAnnotationEvent_Range_LabelsEntry(): AnnotationEvent_Range_LabelsEntry {
  return { key: "", value: "" };
}

export const AnnotationEvent_Range_LabelsEntry = {
  encode(message: AnnotationEvent_Range_LabelsEntry, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): AnnotationEvent_Range_LabelsEntry {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAnnotationEvent_Range_LabelsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AnnotationEvent_Range_LabelsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: AnnotationEvent_Range_LabelsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AnnotationEvent_Range_LabelsEntry>, I>>(
    base?: I,
  ): AnnotationEvent_Range_LabelsEntry {
    return AnnotationEvent_Range_LabelsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AnnotationEvent_Range_LabelsEntry>, I>>(
    object: I,
  ): AnnotationEvent_Range_LabelsEntry {
    const message = createBaseAnnotationEvent_Range_LabelsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
