// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.176.1
//   protoc               v3.21.12
// source: com/coralogixapis/dashboards/v1/common/observation_field.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { StringValue } from "../../../../../google/protobuf/wrappers";

export const protobufPackage = "com.coralogixapis.dashboards.v1.common";

export enum DatasetScope {
  DATASET_SCOPE_UNSPECIFIED = 0,
  DATASET_SCOPE_USER_DATA = 1,
  DATASET_SCOPE_LABEL = 2,
  DATASET_SCOPE_METADATA = 3,
  UNRECOGNIZED = -1,
}

export function datasetScopeFromJSON(object: any): DatasetScope {
  switch (object) {
    case 0:
    case "DATASET_SCOPE_UNSPECIFIED":
      return DatasetScope.DATASET_SCOPE_UNSPECIFIED;
    case 1:
    case "DATASET_SCOPE_USER_DATA":
      return DatasetScope.DATASET_SCOPE_USER_DATA;
    case 2:
    case "DATASET_SCOPE_LABEL":
      return DatasetScope.DATASET_SCOPE_LABEL;
    case 3:
    case "DATASET_SCOPE_METADATA":
      return DatasetScope.DATASET_SCOPE_METADATA;
    case -1:
    case "UNRECOGNIZED":
    default:
      return DatasetScope.UNRECOGNIZED;
  }
}

export function datasetScopeToJSON(object: DatasetScope): string {
  switch (object) {
    case DatasetScope.DATASET_SCOPE_UNSPECIFIED:
      return "DATASET_SCOPE_UNSPECIFIED";
    case DatasetScope.DATASET_SCOPE_USER_DATA:
      return "DATASET_SCOPE_USER_DATA";
    case DatasetScope.DATASET_SCOPE_LABEL:
      return "DATASET_SCOPE_LABEL";
    case DatasetScope.DATASET_SCOPE_METADATA:
      return "DATASET_SCOPE_METADATA";
    case DatasetScope.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface ObservationField {
  keypath: string[];
  scope: DatasetScope;
}

function createBaseObservationField(): ObservationField {
  return { keypath: [], scope: 0 };
}

export const ObservationField = {
  encode(message: ObservationField, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.keypath) {
      StringValue.encode({ value: v!! }, writer.uint32(10).fork()).ldelim();
    }
    if (message.scope !== 0) {
      writer.uint32(16).int32(message.scope);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ObservationField {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseObservationField();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.keypath.push(StringValue.decode(reader, reader.uint32()).value);
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.scope = reader.int32() as any;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ObservationField {
    return {
      keypath: globalThis.Array.isArray(object?.keypath) ? object.keypath.map((e: any) => String(e)) : [],
      scope: isSet(object.scope) ? datasetScopeFromJSON(object.scope) : 0,
    };
  },

  toJSON(message: ObservationField): unknown {
    const obj: any = {};
    if (message.keypath?.length) {
      obj.keypath = message.keypath;
    }
    if (message.scope !== 0) {
      obj.scope = datasetScopeToJSON(message.scope);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ObservationField>, I>>(base?: I): ObservationField {
    return ObservationField.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ObservationField>, I>>(object: I): ObservationField {
    const message = createBaseObservationField();
    message.keypath = object.keypath?.map((e) => e) || [];
    message.scope = object.scope ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
