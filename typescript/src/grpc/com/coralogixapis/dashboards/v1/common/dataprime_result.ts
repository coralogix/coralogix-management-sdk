// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.176.1
//   protoc               v3.21.12
// source: com/coralogixapis/dashboards/v1/common/dataprime_result.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";

export const protobufPackage = "com.coralogixapis.dashboards.v1.common";

export interface DataprimeResult {
  metadata: DataprimeResult_KeyValue[];
  labels: DataprimeResult_KeyValue[];
  userData: string;
}

export interface DataprimeResult_KeyValue {
  key: string;
  value: string;
}

function createBaseDataprimeResult(): DataprimeResult {
  return { metadata: [], labels: [], userData: "" };
}

export const DataprimeResult = {
  encode(message: DataprimeResult, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.metadata) {
      DataprimeResult_KeyValue.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    for (const v of message.labels) {
      DataprimeResult_KeyValue.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    if (message.userData !== "") {
      writer.uint32(26).string(message.userData);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): DataprimeResult {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDataprimeResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.metadata.push(DataprimeResult_KeyValue.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.labels.push(DataprimeResult_KeyValue.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.userData = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DataprimeResult {
    return {
      metadata: globalThis.Array.isArray(object?.metadata)
        ? object.metadata.map((e: any) => DataprimeResult_KeyValue.fromJSON(e))
        : [],
      labels: globalThis.Array.isArray(object?.labels)
        ? object.labels.map((e: any) => DataprimeResult_KeyValue.fromJSON(e))
        : [],
      userData: isSet(object.userData) ? globalThis.String(object.userData) : "",
    };
  },

  toJSON(message: DataprimeResult): unknown {
    const obj: any = {};
    if (message.metadata?.length) {
      obj.metadata = message.metadata.map((e) => DataprimeResult_KeyValue.toJSON(e));
    }
    if (message.labels?.length) {
      obj.labels = message.labels.map((e) => DataprimeResult_KeyValue.toJSON(e));
    }
    if (message.userData !== "") {
      obj.userData = message.userData;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DataprimeResult>, I>>(base?: I): DataprimeResult {
    return DataprimeResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DataprimeResult>, I>>(object: I): DataprimeResult {
    const message = createBaseDataprimeResult();
    message.metadata = object.metadata?.map((e) => DataprimeResult_KeyValue.fromPartial(e)) || [];
    message.labels = object.labels?.map((e) => DataprimeResult_KeyValue.fromPartial(e)) || [];
    message.userData = object.userData ?? "";
    return message;
  },
};

function createBaseDataprimeResult_KeyValue(): DataprimeResult_KeyValue {
  return { key: "", value: "" };
}

export const DataprimeResult_KeyValue = {
  encode(message: DataprimeResult_KeyValue, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): DataprimeResult_KeyValue {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDataprimeResult_KeyValue();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DataprimeResult_KeyValue {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: DataprimeResult_KeyValue): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DataprimeResult_KeyValue>, I>>(base?: I): DataprimeResult_KeyValue {
    return DataprimeResult_KeyValue.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DataprimeResult_KeyValue>, I>>(object: I): DataprimeResult_KeyValue {
    const message = createBaseDataprimeResult_KeyValue();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
