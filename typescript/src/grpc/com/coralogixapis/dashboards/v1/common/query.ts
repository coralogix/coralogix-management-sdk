// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.176.1
//   protoc               v3.21.12
// source: com/coralogixapis/dashboards/v1/common/query.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { StringValue } from "../../../../../google/protobuf/wrappers";

export const protobufPackage = "com.coralogixapis.dashboards.v1.common";

export interface DataprimeQuery {
  text: string;
}

export interface SerializedDataprimeQuery {
  data: Uint8Array;
}

export interface FullDataprimeQuery {
  serialized: SerializedDataprimeQuery | undefined;
  raw: DataprimeQuery | undefined;
}

export interface PromQlQuery {
  value: string | undefined;
}

export interface LuceneQuery {
  value: string | undefined;
}

function createBaseDataprimeQuery(): DataprimeQuery {
  return { text: "" };
}

export const DataprimeQuery = {
  encode(message: DataprimeQuery, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.text !== "") {
      writer.uint32(10).string(message.text);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): DataprimeQuery {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDataprimeQuery();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.text = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DataprimeQuery {
    return { text: isSet(object.text) ? globalThis.String(object.text) : "" };
  },

  toJSON(message: DataprimeQuery): unknown {
    const obj: any = {};
    if (message.text !== "") {
      obj.text = message.text;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DataprimeQuery>, I>>(base?: I): DataprimeQuery {
    return DataprimeQuery.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DataprimeQuery>, I>>(object: I): DataprimeQuery {
    const message = createBaseDataprimeQuery();
    message.text = object.text ?? "";
    return message;
  },
};

function createBaseSerializedDataprimeQuery(): SerializedDataprimeQuery {
  return { data: new Uint8Array(0) };
}

export const SerializedDataprimeQuery = {
  encode(message: SerializedDataprimeQuery, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.data.length !== 0) {
      writer.uint32(10).bytes(message.data);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SerializedDataprimeQuery {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSerializedDataprimeQuery();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.data = reader.bytes();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SerializedDataprimeQuery {
    return { data: isSet(object.data) ? bytesFromBase64(object.data) : new Uint8Array(0) };
  },

  toJSON(message: SerializedDataprimeQuery): unknown {
    const obj: any = {};
    if (message.data.length !== 0) {
      obj.data = base64FromBytes(message.data);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SerializedDataprimeQuery>, I>>(base?: I): SerializedDataprimeQuery {
    return SerializedDataprimeQuery.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SerializedDataprimeQuery>, I>>(object: I): SerializedDataprimeQuery {
    const message = createBaseSerializedDataprimeQuery();
    message.data = object.data ?? new Uint8Array(0);
    return message;
  },
};

function createBaseFullDataprimeQuery(): FullDataprimeQuery {
  return { serialized: undefined, raw: undefined };
}

export const FullDataprimeQuery = {
  encode(message: FullDataprimeQuery, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.serialized !== undefined) {
      SerializedDataprimeQuery.encode(message.serialized, writer.uint32(10).fork()).ldelim();
    }
    if (message.raw !== undefined) {
      DataprimeQuery.encode(message.raw, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): FullDataprimeQuery {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFullDataprimeQuery();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.serialized = SerializedDataprimeQuery.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.raw = DataprimeQuery.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FullDataprimeQuery {
    return {
      serialized: isSet(object.serialized) ? SerializedDataprimeQuery.fromJSON(object.serialized) : undefined,
      raw: isSet(object.raw) ? DataprimeQuery.fromJSON(object.raw) : undefined,
    };
  },

  toJSON(message: FullDataprimeQuery): unknown {
    const obj: any = {};
    if (message.serialized !== undefined) {
      obj.serialized = SerializedDataprimeQuery.toJSON(message.serialized);
    }
    if (message.raw !== undefined) {
      obj.raw = DataprimeQuery.toJSON(message.raw);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FullDataprimeQuery>, I>>(base?: I): FullDataprimeQuery {
    return FullDataprimeQuery.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FullDataprimeQuery>, I>>(object: I): FullDataprimeQuery {
    const message = createBaseFullDataprimeQuery();
    message.serialized = (object.serialized !== undefined && object.serialized !== null)
      ? SerializedDataprimeQuery.fromPartial(object.serialized)
      : undefined;
    message.raw = (object.raw !== undefined && object.raw !== null)
      ? DataprimeQuery.fromPartial(object.raw)
      : undefined;
    return message;
  },
};

function createBasePromQlQuery(): PromQlQuery {
  return { value: undefined };
}

export const PromQlQuery = {
  encode(message: PromQlQuery, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.value !== undefined) {
      StringValue.encode({ value: message.value! }, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): PromQlQuery {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePromQlQuery();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.value = StringValue.decode(reader, reader.uint32()).value;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PromQlQuery {
    return { value: isSet(object.value) ? String(object.value) : undefined };
  },

  toJSON(message: PromQlQuery): unknown {
    const obj: any = {};
    if (message.value !== undefined) {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PromQlQuery>, I>>(base?: I): PromQlQuery {
    return PromQlQuery.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PromQlQuery>, I>>(object: I): PromQlQuery {
    const message = createBasePromQlQuery();
    message.value = object.value ?? undefined;
    return message;
  },
};

function createBaseLuceneQuery(): LuceneQuery {
  return { value: undefined };
}

export const LuceneQuery = {
  encode(message: LuceneQuery, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.value !== undefined) {
      StringValue.encode({ value: message.value! }, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): LuceneQuery {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLuceneQuery();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.value = StringValue.decode(reader, reader.uint32()).value;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LuceneQuery {
    return { value: isSet(object.value) ? String(object.value) : undefined };
  },

  toJSON(message: LuceneQuery): unknown {
    const obj: any = {};
    if (message.value !== undefined) {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LuceneQuery>, I>>(base?: I): LuceneQuery {
    return LuceneQuery.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LuceneQuery>, I>>(object: I): LuceneQuery {
    const message = createBaseLuceneQuery();
    message.value = object.value ?? undefined;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
