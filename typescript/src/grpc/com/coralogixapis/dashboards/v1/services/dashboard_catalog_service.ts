// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.176.1
//   protoc               v3.21.12
// source: com/coralogixapis/dashboards/v1/services/dashboard_catalog_service.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { Timestamp } from "../../../../../google/protobuf/timestamp";
import { BoolValue, StringValue } from "../../../../../google/protobuf/wrappers";
import { DashboardFolder } from "../common/folder";

export const protobufPackage = "com.coralogixapis.dashboards.v1.services";

export interface GetDashboardCatalogRequest {
}

export interface GetDashboardCatalogResponse {
  items: DashboardCatalogItem[];
}

export interface DashboardCatalogItem {
  id: string | undefined;
  name: string | undefined;
  description: string | undefined;
  isDefault: boolean | undefined;
  isPinned: boolean | undefined;
  createTime: Date | undefined;
  updateTime: Date | undefined;
  folder: DashboardFolder | undefined;
}

function createBaseGetDashboardCatalogRequest(): GetDashboardCatalogRequest {
  return {};
}

export const GetDashboardCatalogRequest = {
  encode(_: GetDashboardCatalogRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetDashboardCatalogRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetDashboardCatalogRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): GetDashboardCatalogRequest {
    return {};
  },

  toJSON(_: GetDashboardCatalogRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<GetDashboardCatalogRequest>, I>>(base?: I): GetDashboardCatalogRequest {
    return GetDashboardCatalogRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetDashboardCatalogRequest>, I>>(_: I): GetDashboardCatalogRequest {
    const message = createBaseGetDashboardCatalogRequest();
    return message;
  },
};

function createBaseGetDashboardCatalogResponse(): GetDashboardCatalogResponse {
  return { items: [] };
}

export const GetDashboardCatalogResponse = {
  encode(message: GetDashboardCatalogResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.items) {
      DashboardCatalogItem.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetDashboardCatalogResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetDashboardCatalogResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.items.push(DashboardCatalogItem.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetDashboardCatalogResponse {
    return {
      items: globalThis.Array.isArray(object?.items)
        ? object.items.map((e: any) => DashboardCatalogItem.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GetDashboardCatalogResponse): unknown {
    const obj: any = {};
    if (message.items?.length) {
      obj.items = message.items.map((e) => DashboardCatalogItem.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetDashboardCatalogResponse>, I>>(base?: I): GetDashboardCatalogResponse {
    return GetDashboardCatalogResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetDashboardCatalogResponse>, I>>(object: I): GetDashboardCatalogResponse {
    const message = createBaseGetDashboardCatalogResponse();
    message.items = object.items?.map((e) => DashboardCatalogItem.fromPartial(e)) || [];
    return message;
  },
};

function createBaseDashboardCatalogItem(): DashboardCatalogItem {
  return {
    id: undefined,
    name: undefined,
    description: undefined,
    isDefault: undefined,
    isPinned: undefined,
    createTime: undefined,
    updateTime: undefined,
    folder: undefined,
  };
}

export const DashboardCatalogItem = {
  encode(message: DashboardCatalogItem, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== undefined) {
      StringValue.encode({ value: message.id! }, writer.uint32(10).fork()).ldelim();
    }
    if (message.name !== undefined) {
      StringValue.encode({ value: message.name! }, writer.uint32(18).fork()).ldelim();
    }
    if (message.description !== undefined) {
      StringValue.encode({ value: message.description! }, writer.uint32(26).fork()).ldelim();
    }
    if (message.isDefault !== undefined) {
      BoolValue.encode({ value: message.isDefault! }, writer.uint32(34).fork()).ldelim();
    }
    if (message.isPinned !== undefined) {
      BoolValue.encode({ value: message.isPinned! }, writer.uint32(42).fork()).ldelim();
    }
    if (message.createTime !== undefined) {
      Timestamp.encode(toTimestamp(message.createTime), writer.uint32(50).fork()).ldelim();
    }
    if (message.updateTime !== undefined) {
      Timestamp.encode(toTimestamp(message.updateTime), writer.uint32(58).fork()).ldelim();
    }
    if (message.folder !== undefined) {
      DashboardFolder.encode(message.folder, writer.uint32(66).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): DashboardCatalogItem {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDashboardCatalogItem();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = StringValue.decode(reader, reader.uint32()).value;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.name = StringValue.decode(reader, reader.uint32()).value;
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.description = StringValue.decode(reader, reader.uint32()).value;
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.isDefault = BoolValue.decode(reader, reader.uint32()).value;
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.isPinned = BoolValue.decode(reader, reader.uint32()).value;
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.createTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.updateTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 8:
          if (tag !== 66) {
            break;
          }

          message.folder = DashboardFolder.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DashboardCatalogItem {
    return {
      id: isSet(object.id) ? String(object.id) : undefined,
      name: isSet(object.name) ? String(object.name) : undefined,
      description: isSet(object.description) ? String(object.description) : undefined,
      isDefault: isSet(object.isDefault) ? Boolean(object.isDefault) : undefined,
      isPinned: isSet(object.isPinned) ? Boolean(object.isPinned) : undefined,
      createTime: isSet(object.createTime) ? fromJsonTimestamp(object.createTime) : undefined,
      updateTime: isSet(object.updateTime) ? fromJsonTimestamp(object.updateTime) : undefined,
      folder: isSet(object.folder) ? DashboardFolder.fromJSON(object.folder) : undefined,
    };
  },

  toJSON(message: DashboardCatalogItem): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = message.id;
    }
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    if (message.description !== undefined) {
      obj.description = message.description;
    }
    if (message.isDefault !== undefined) {
      obj.isDefault = message.isDefault;
    }
    if (message.isPinned !== undefined) {
      obj.isPinned = message.isPinned;
    }
    if (message.createTime !== undefined) {
      obj.createTime = message.createTime.toISOString();
    }
    if (message.updateTime !== undefined) {
      obj.updateTime = message.updateTime.toISOString();
    }
    if (message.folder !== undefined) {
      obj.folder = DashboardFolder.toJSON(message.folder);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DashboardCatalogItem>, I>>(base?: I): DashboardCatalogItem {
    return DashboardCatalogItem.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DashboardCatalogItem>, I>>(object: I): DashboardCatalogItem {
    const message = createBaseDashboardCatalogItem();
    message.id = object.id ?? undefined;
    message.name = object.name ?? undefined;
    message.description = object.description ?? undefined;
    message.isDefault = object.isDefault ?? undefined;
    message.isPinned = object.isPinned ?? undefined;
    message.createTime = object.createTime ?? undefined;
    message.updateTime = object.updateTime ?? undefined;
    message.folder = (object.folder !== undefined && object.folder !== null)
      ? DashboardFolder.fromPartial(object.folder)
      : undefined;
    return message;
  },
};

export interface DashboardCatalogService {
  GetDashboardCatalog(request: GetDashboardCatalogRequest): Promise<GetDashboardCatalogResponse>;
}

export const DashboardCatalogServiceServiceName = "com.coralogixapis.dashboards.v1.services.DashboardCatalogService";
export class DashboardCatalogServiceClientImpl implements DashboardCatalogService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || DashboardCatalogServiceServiceName;
    this.rpc = rpc;
    this.GetDashboardCatalog = this.GetDashboardCatalog.bind(this);
  }
  GetDashboardCatalog(request: GetDashboardCatalogRequest): Promise<GetDashboardCatalogResponse> {
    const data = GetDashboardCatalogRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetDashboardCatalog", data);
    return promise.then((data) => GetDashboardCatalogResponse.decode(_m0.Reader.create(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
