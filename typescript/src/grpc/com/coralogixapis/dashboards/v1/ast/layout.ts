// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.176.1
//   protoc               v3.21.12
// source: com/coralogixapis/dashboards/v1/ast/layout.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { BoolValue, Int32Value, StringValue } from "../../../../../google/protobuf/wrappers";
import { UUID } from "../types";
import { Widget } from "./widget";

export const protobufPackage = "com.coralogixapis.dashboards.v1.ast";

export interface Layout {
  sections: Section[];
}

export interface Section {
  id: UUID | undefined;
  rows: Row[];
  options: SectionOptions | undefined;
}

export interface Row {
  id: UUID | undefined;
  appearance: Row_Appearance | undefined;
  widgets: Widget[];
}

export interface Row_Appearance {
  height: number | undefined;
}

export interface SectionOptions {
  internal?: InternalSectionOptions | undefined;
  custom?: CustomSectionOptions | undefined;
}

export interface InternalSectionOptions {
}

export interface CustomSectionOptions {
  name: string | undefined;
  description: string | undefined;
  collapsed: boolean | undefined;
}

function createBaseLayout(): Layout {
  return { sections: [] };
}

export const Layout = {
  encode(message: Layout, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.sections) {
      Section.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Layout {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLayout();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.sections.push(Section.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Layout {
    return {
      sections: globalThis.Array.isArray(object?.sections) ? object.sections.map((e: any) => Section.fromJSON(e)) : [],
    };
  },

  toJSON(message: Layout): unknown {
    const obj: any = {};
    if (message.sections?.length) {
      obj.sections = message.sections.map((e) => Section.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Layout>, I>>(base?: I): Layout {
    return Layout.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Layout>, I>>(object: I): Layout {
    const message = createBaseLayout();
    message.sections = object.sections?.map((e) => Section.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSection(): Section {
  return { id: undefined, rows: [], options: undefined };
}

export const Section = {
  encode(message: Section, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== undefined) {
      UUID.encode(message.id, writer.uint32(10).fork()).ldelim();
    }
    for (const v of message.rows) {
      Row.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    if (message.options !== undefined) {
      SectionOptions.encode(message.options, writer.uint32(26).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Section {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSection();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = UUID.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.rows.push(Row.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.options = SectionOptions.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Section {
    return {
      id: isSet(object.id) ? UUID.fromJSON(object.id) : undefined,
      rows: globalThis.Array.isArray(object?.rows) ? object.rows.map((e: any) => Row.fromJSON(e)) : [],
      options: isSet(object.options) ? SectionOptions.fromJSON(object.options) : undefined,
    };
  },

  toJSON(message: Section): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = UUID.toJSON(message.id);
    }
    if (message.rows?.length) {
      obj.rows = message.rows.map((e) => Row.toJSON(e));
    }
    if (message.options !== undefined) {
      obj.options = SectionOptions.toJSON(message.options);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Section>, I>>(base?: I): Section {
    return Section.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Section>, I>>(object: I): Section {
    const message = createBaseSection();
    message.id = (object.id !== undefined && object.id !== null) ? UUID.fromPartial(object.id) : undefined;
    message.rows = object.rows?.map((e) => Row.fromPartial(e)) || [];
    message.options = (object.options !== undefined && object.options !== null)
      ? SectionOptions.fromPartial(object.options)
      : undefined;
    return message;
  },
};

function createBaseRow(): Row {
  return { id: undefined, appearance: undefined, widgets: [] };
}

export const Row = {
  encode(message: Row, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== undefined) {
      UUID.encode(message.id, writer.uint32(10).fork()).ldelim();
    }
    if (message.appearance !== undefined) {
      Row_Appearance.encode(message.appearance, writer.uint32(18).fork()).ldelim();
    }
    for (const v of message.widgets) {
      Widget.encode(v!, writer.uint32(26).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Row {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRow();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = UUID.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.appearance = Row_Appearance.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.widgets.push(Widget.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Row {
    return {
      id: isSet(object.id) ? UUID.fromJSON(object.id) : undefined,
      appearance: isSet(object.appearance) ? Row_Appearance.fromJSON(object.appearance) : undefined,
      widgets: globalThis.Array.isArray(object?.widgets) ? object.widgets.map((e: any) => Widget.fromJSON(e)) : [],
    };
  },

  toJSON(message: Row): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = UUID.toJSON(message.id);
    }
    if (message.appearance !== undefined) {
      obj.appearance = Row_Appearance.toJSON(message.appearance);
    }
    if (message.widgets?.length) {
      obj.widgets = message.widgets.map((e) => Widget.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Row>, I>>(base?: I): Row {
    return Row.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Row>, I>>(object: I): Row {
    const message = createBaseRow();
    message.id = (object.id !== undefined && object.id !== null) ? UUID.fromPartial(object.id) : undefined;
    message.appearance = (object.appearance !== undefined && object.appearance !== null)
      ? Row_Appearance.fromPartial(object.appearance)
      : undefined;
    message.widgets = object.widgets?.map((e) => Widget.fromPartial(e)) || [];
    return message;
  },
};

function createBaseRow_Appearance(): Row_Appearance {
  return { height: undefined };
}

export const Row_Appearance = {
  encode(message: Row_Appearance, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.height !== undefined) {
      Int32Value.encode({ value: message.height! }, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Row_Appearance {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRow_Appearance();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.height = Int32Value.decode(reader, reader.uint32()).value;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Row_Appearance {
    return { height: isSet(object.height) ? Number(object.height) : undefined };
  },

  toJSON(message: Row_Appearance): unknown {
    const obj: any = {};
    if (message.height !== undefined) {
      obj.height = message.height;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Row_Appearance>, I>>(base?: I): Row_Appearance {
    return Row_Appearance.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Row_Appearance>, I>>(object: I): Row_Appearance {
    const message = createBaseRow_Appearance();
    message.height = object.height ?? undefined;
    return message;
  },
};

function createBaseSectionOptions(): SectionOptions {
  return { internal: undefined, custom: undefined };
}

export const SectionOptions = {
  encode(message: SectionOptions, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.internal !== undefined) {
      InternalSectionOptions.encode(message.internal, writer.uint32(10).fork()).ldelim();
    }
    if (message.custom !== undefined) {
      CustomSectionOptions.encode(message.custom, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SectionOptions {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSectionOptions();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.internal = InternalSectionOptions.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.custom = CustomSectionOptions.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SectionOptions {
    return {
      internal: isSet(object.internal) ? InternalSectionOptions.fromJSON(object.internal) : undefined,
      custom: isSet(object.custom) ? CustomSectionOptions.fromJSON(object.custom) : undefined,
    };
  },

  toJSON(message: SectionOptions): unknown {
    const obj: any = {};
    if (message.internal !== undefined) {
      obj.internal = InternalSectionOptions.toJSON(message.internal);
    }
    if (message.custom !== undefined) {
      obj.custom = CustomSectionOptions.toJSON(message.custom);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SectionOptions>, I>>(base?: I): SectionOptions {
    return SectionOptions.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SectionOptions>, I>>(object: I): SectionOptions {
    const message = createBaseSectionOptions();
    message.internal = (object.internal !== undefined && object.internal !== null)
      ? InternalSectionOptions.fromPartial(object.internal)
      : undefined;
    message.custom = (object.custom !== undefined && object.custom !== null)
      ? CustomSectionOptions.fromPartial(object.custom)
      : undefined;
    return message;
  },
};

function createBaseInternalSectionOptions(): InternalSectionOptions {
  return {};
}

export const InternalSectionOptions = {
  encode(_: InternalSectionOptions, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): InternalSectionOptions {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInternalSectionOptions();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): InternalSectionOptions {
    return {};
  },

  toJSON(_: InternalSectionOptions): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<InternalSectionOptions>, I>>(base?: I): InternalSectionOptions {
    return InternalSectionOptions.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InternalSectionOptions>, I>>(_: I): InternalSectionOptions {
    const message = createBaseInternalSectionOptions();
    return message;
  },
};

function createBaseCustomSectionOptions(): CustomSectionOptions {
  return { name: undefined, description: undefined, collapsed: undefined };
}

export const CustomSectionOptions = {
  encode(message: CustomSectionOptions, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.name !== undefined) {
      StringValue.encode({ value: message.name! }, writer.uint32(10).fork()).ldelim();
    }
    if (message.description !== undefined) {
      StringValue.encode({ value: message.description! }, writer.uint32(18).fork()).ldelim();
    }
    if (message.collapsed !== undefined) {
      BoolValue.encode({ value: message.collapsed! }, writer.uint32(26).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): CustomSectionOptions {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCustomSectionOptions();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.name = StringValue.decode(reader, reader.uint32()).value;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.description = StringValue.decode(reader, reader.uint32()).value;
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.collapsed = BoolValue.decode(reader, reader.uint32()).value;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CustomSectionOptions {
    return {
      name: isSet(object.name) ? String(object.name) : undefined,
      description: isSet(object.description) ? String(object.description) : undefined,
      collapsed: isSet(object.collapsed) ? Boolean(object.collapsed) : undefined,
    };
  },

  toJSON(message: CustomSectionOptions): unknown {
    const obj: any = {};
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    if (message.description !== undefined) {
      obj.description = message.description;
    }
    if (message.collapsed !== undefined) {
      obj.collapsed = message.collapsed;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CustomSectionOptions>, I>>(base?: I): CustomSectionOptions {
    return CustomSectionOptions.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CustomSectionOptions>, I>>(object: I): CustomSectionOptions {
    const message = createBaseCustomSectionOptions();
    message.name = object.name ?? undefined;
    message.description = object.description ?? undefined;
    message.collapsed = object.collapsed ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
