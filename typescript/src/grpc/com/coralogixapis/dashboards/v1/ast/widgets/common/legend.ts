// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.176.1
//   protoc               v3.21.12
// source: com/coralogixapis/dashboards/v1/ast/widgets/common/legend.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { BoolValue } from "../../../../../../../google/protobuf/wrappers";

export const protobufPackage = "com.coralogixapis.dashboards.v1.ast.widgets.common";

export interface Legend {
  isVisible: boolean | undefined;
  columns: Legend_LegendColumn[];
  groupByQuery: boolean | undefined;
}

export enum Legend_LegendColumn {
  LEGEND_COLUMN_UNSPECIFIED = 0,
  LEGEND_COLUMN_MIN = 1,
  LEGEND_COLUMN_MAX = 2,
  LEGEND_COLUMN_SUM = 3,
  LEGEND_COLUMN_AVG = 4,
  LEGEND_COLUMN_LAST = 5,
  LEGEND_COLUMN_NAME = 6,
  UNRECOGNIZED = -1,
}

export function legend_LegendColumnFromJSON(object: any): Legend_LegendColumn {
  switch (object) {
    case 0:
    case "LEGEND_COLUMN_UNSPECIFIED":
      return Legend_LegendColumn.LEGEND_COLUMN_UNSPECIFIED;
    case 1:
    case "LEGEND_COLUMN_MIN":
      return Legend_LegendColumn.LEGEND_COLUMN_MIN;
    case 2:
    case "LEGEND_COLUMN_MAX":
      return Legend_LegendColumn.LEGEND_COLUMN_MAX;
    case 3:
    case "LEGEND_COLUMN_SUM":
      return Legend_LegendColumn.LEGEND_COLUMN_SUM;
    case 4:
    case "LEGEND_COLUMN_AVG":
      return Legend_LegendColumn.LEGEND_COLUMN_AVG;
    case 5:
    case "LEGEND_COLUMN_LAST":
      return Legend_LegendColumn.LEGEND_COLUMN_LAST;
    case 6:
    case "LEGEND_COLUMN_NAME":
      return Legend_LegendColumn.LEGEND_COLUMN_NAME;
    case -1:
    case "UNRECOGNIZED":
    default:
      return Legend_LegendColumn.UNRECOGNIZED;
  }
}

export function legend_LegendColumnToJSON(object: Legend_LegendColumn): string {
  switch (object) {
    case Legend_LegendColumn.LEGEND_COLUMN_UNSPECIFIED:
      return "LEGEND_COLUMN_UNSPECIFIED";
    case Legend_LegendColumn.LEGEND_COLUMN_MIN:
      return "LEGEND_COLUMN_MIN";
    case Legend_LegendColumn.LEGEND_COLUMN_MAX:
      return "LEGEND_COLUMN_MAX";
    case Legend_LegendColumn.LEGEND_COLUMN_SUM:
      return "LEGEND_COLUMN_SUM";
    case Legend_LegendColumn.LEGEND_COLUMN_AVG:
      return "LEGEND_COLUMN_AVG";
    case Legend_LegendColumn.LEGEND_COLUMN_LAST:
      return "LEGEND_COLUMN_LAST";
    case Legend_LegendColumn.LEGEND_COLUMN_NAME:
      return "LEGEND_COLUMN_NAME";
    case Legend_LegendColumn.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

function createBaseLegend(): Legend {
  return { isVisible: undefined, columns: [], groupByQuery: undefined };
}

export const Legend = {
  encode(message: Legend, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.isVisible !== undefined) {
      BoolValue.encode({ value: message.isVisible! }, writer.uint32(10).fork()).ldelim();
    }
    writer.uint32(18).fork();
    for (const v of message.columns) {
      writer.int32(v);
    }
    writer.ldelim();
    if (message.groupByQuery !== undefined) {
      BoolValue.encode({ value: message.groupByQuery! }, writer.uint32(26).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Legend {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLegend();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.isVisible = BoolValue.decode(reader, reader.uint32()).value;
          continue;
        case 2:
          if (tag === 16) {
            message.columns.push(reader.int32() as any);

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.columns.push(reader.int32() as any);
            }

            continue;
          }

          break;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.groupByQuery = BoolValue.decode(reader, reader.uint32()).value;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Legend {
    return {
      isVisible: isSet(object.isVisible) ? Boolean(object.isVisible) : undefined,
      columns: globalThis.Array.isArray(object?.columns)
        ? object.columns.map((e: any) => legend_LegendColumnFromJSON(e))
        : [],
      groupByQuery: isSet(object.groupByQuery) ? Boolean(object.groupByQuery) : undefined,
    };
  },

  toJSON(message: Legend): unknown {
    const obj: any = {};
    if (message.isVisible !== undefined) {
      obj.isVisible = message.isVisible;
    }
    if (message.columns?.length) {
      obj.columns = message.columns.map((e) => legend_LegendColumnToJSON(e));
    }
    if (message.groupByQuery !== undefined) {
      obj.groupByQuery = message.groupByQuery;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Legend>, I>>(base?: I): Legend {
    return Legend.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Legend>, I>>(object: I): Legend {
    const message = createBaseLegend();
    message.isVisible = object.isVisible ?? undefined;
    message.columns = object.columns?.map((e) => e) || [];
    message.groupByQuery = object.groupByQuery ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
