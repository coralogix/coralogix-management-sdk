// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.176.1
//   protoc               v3.21.12
// source: com/coralogixapis/views/v1/time_selection.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { Timestamp } from "../../../../google/protobuf/timestamp";
import { StringValue } from "../../../../google/protobuf/wrappers";

export const protobufPackage = "com.coralogixapis.views.v1";

export interface TimeSelection {
  quickSelection?: QuickTimeSelection | undefined;
  customSelection?: CustomTimeSelection | undefined;
}

export interface QuickTimeSelection {
  caption: string | undefined;
  seconds: number;
}

export interface CustomTimeSelection {
  fromTime: Date | undefined;
  toTime: Date | undefined;
}

function createBaseTimeSelection(): TimeSelection {
  return { quickSelection: undefined, customSelection: undefined };
}

export const TimeSelection = {
  encode(message: TimeSelection, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.quickSelection !== undefined) {
      QuickTimeSelection.encode(message.quickSelection, writer.uint32(10).fork()).ldelim();
    }
    if (message.customSelection !== undefined) {
      CustomTimeSelection.encode(message.customSelection, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): TimeSelection {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTimeSelection();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.quickSelection = QuickTimeSelection.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.customSelection = CustomTimeSelection.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TimeSelection {
    return {
      quickSelection: isSet(object.quickSelection) ? QuickTimeSelection.fromJSON(object.quickSelection) : undefined,
      customSelection: isSet(object.customSelection) ? CustomTimeSelection.fromJSON(object.customSelection) : undefined,
    };
  },

  toJSON(message: TimeSelection): unknown {
    const obj: any = {};
    if (message.quickSelection !== undefined) {
      obj.quickSelection = QuickTimeSelection.toJSON(message.quickSelection);
    }
    if (message.customSelection !== undefined) {
      obj.customSelection = CustomTimeSelection.toJSON(message.customSelection);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TimeSelection>, I>>(base?: I): TimeSelection {
    return TimeSelection.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TimeSelection>, I>>(object: I): TimeSelection {
    const message = createBaseTimeSelection();
    message.quickSelection = (object.quickSelection !== undefined && object.quickSelection !== null)
      ? QuickTimeSelection.fromPartial(object.quickSelection)
      : undefined;
    message.customSelection = (object.customSelection !== undefined && object.customSelection !== null)
      ? CustomTimeSelection.fromPartial(object.customSelection)
      : undefined;
    return message;
  },
};

function createBaseQuickTimeSelection(): QuickTimeSelection {
  return { caption: undefined, seconds: 0 };
}

export const QuickTimeSelection = {
  encode(message: QuickTimeSelection, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.caption !== undefined) {
      StringValue.encode({ value: message.caption! }, writer.uint32(18).fork()).ldelim();
    }
    if (message.seconds !== 0) {
      writer.uint32(24).uint32(message.seconds);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QuickTimeSelection {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQuickTimeSelection();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 2:
          if (tag !== 18) {
            break;
          }

          message.caption = StringValue.decode(reader, reader.uint32()).value;
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.seconds = reader.uint32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QuickTimeSelection {
    return {
      caption: isSet(object.caption) ? String(object.caption) : undefined,
      seconds: isSet(object.seconds) ? globalThis.Number(object.seconds) : 0,
    };
  },

  toJSON(message: QuickTimeSelection): unknown {
    const obj: any = {};
    if (message.caption !== undefined) {
      obj.caption = message.caption;
    }
    if (message.seconds !== 0) {
      obj.seconds = Math.round(message.seconds);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QuickTimeSelection>, I>>(base?: I): QuickTimeSelection {
    return QuickTimeSelection.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QuickTimeSelection>, I>>(object: I): QuickTimeSelection {
    const message = createBaseQuickTimeSelection();
    message.caption = object.caption ?? undefined;
    message.seconds = object.seconds ?? 0;
    return message;
  },
};

function createBaseCustomTimeSelection(): CustomTimeSelection {
  return { fromTime: undefined, toTime: undefined };
}

export const CustomTimeSelection = {
  encode(message: CustomTimeSelection, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.fromTime !== undefined) {
      Timestamp.encode(toTimestamp(message.fromTime), writer.uint32(10).fork()).ldelim();
    }
    if (message.toTime !== undefined) {
      Timestamp.encode(toTimestamp(message.toTime), writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): CustomTimeSelection {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCustomTimeSelection();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.fromTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.toTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CustomTimeSelection {
    return {
      fromTime: isSet(object.fromTime) ? fromJsonTimestamp(object.fromTime) : undefined,
      toTime: isSet(object.toTime) ? fromJsonTimestamp(object.toTime) : undefined,
    };
  },

  toJSON(message: CustomTimeSelection): unknown {
    const obj: any = {};
    if (message.fromTime !== undefined) {
      obj.fromTime = message.fromTime.toISOString();
    }
    if (message.toTime !== undefined) {
      obj.toTime = message.toTime.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CustomTimeSelection>, I>>(base?: I): CustomTimeSelection {
    return CustomTimeSelection.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CustomTimeSelection>, I>>(object: I): CustomTimeSelection {
    const message = createBaseCustomTimeSelection();
    message.fromTime = object.fromTime ?? undefined;
    message.toTime = object.toTime ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
