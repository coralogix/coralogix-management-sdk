// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.176.1
//   protoc               v3.21.12
// source: com/coralogixapis/dataprime/v1/request.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { Timestamp } from "../../../../google/protobuf/timestamp";
import { StringValue } from "../../../../google/protobuf/wrappers";

export const protobufPackage = "com.coralogixapis.dataprime.v1";

/** dataprime text query request */
export interface QueryRequest {
  /** query for which you seek results */
  query:
    | string
    | undefined;
  /** configuration of query execution */
  metadata?: Metadata | undefined;
}

/** configuration of query execution */
export interface Metadata {
  /** beginning of the time range for the query, default: end - 15 min or current time - 15 min if end is not defined */
  startDate?:
    | Date
    | undefined;
  /** end of the time range for the query, default: start + 15 min or current time if start is not defined */
  endDate?:
    | Date
    | undefined;
  /** default value for source to be used when source is omitted in a query */
  defaultSource?:
    | string
    | undefined;
  /** tier on which query runs */
  tier?:
    | Metadata_Tier
    | undefined;
  /** which syntax query is written in */
  syntax?:
    | Metadata_QuerySyntax
    | undefined;
  /** limit number of results, default: 2000; max for TIER_FREQUENT_SEARCH: 12000;  max for TIER_ARCHIVE: 50000 */
  limit?:
    | number
    | undefined;
  /** prohibit using unknown fields, ones which were not detected in the ingested data, default = false */
  strictFieldsValidation?: boolean | undefined;
}

/** tier on which query runs, default: TIER_FREQUENT_SEARCH */
export enum Metadata_Tier {
  TIER_UNSPECIFIED = 0,
  TIER_ARCHIVE = 1,
  TIER_FREQUENT_SEARCH = 2,
  UNRECOGNIZED = -1,
}

export function metadata_TierFromJSON(object: any): Metadata_Tier {
  switch (object) {
    case 0:
    case "TIER_UNSPECIFIED":
      return Metadata_Tier.TIER_UNSPECIFIED;
    case 1:
    case "TIER_ARCHIVE":
      return Metadata_Tier.TIER_ARCHIVE;
    case 2:
    case "TIER_FREQUENT_SEARCH":
      return Metadata_Tier.TIER_FREQUENT_SEARCH;
    case -1:
    case "UNRECOGNIZED":
    default:
      return Metadata_Tier.UNRECOGNIZED;
  }
}

export function metadata_TierToJSON(object: Metadata_Tier): string {
  switch (object) {
    case Metadata_Tier.TIER_UNSPECIFIED:
      return "TIER_UNSPECIFIED";
    case Metadata_Tier.TIER_ARCHIVE:
      return "TIER_ARCHIVE";
    case Metadata_Tier.TIER_FREQUENT_SEARCH:
      return "TIER_FREQUENT_SEARCH";
    case Metadata_Tier.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** syntax of the query, default: QUERY_SYNTAX_DATAPRIME */
export enum Metadata_QuerySyntax {
  QUERY_SYNTAX_UNSPECIFIED = 0,
  QUERY_SYNTAX_LUCENE = 1,
  QUERY_SYNTAX_DATAPRIME = 2,
  UNRECOGNIZED = -1,
}

export function metadata_QuerySyntaxFromJSON(object: any): Metadata_QuerySyntax {
  switch (object) {
    case 0:
    case "QUERY_SYNTAX_UNSPECIFIED":
      return Metadata_QuerySyntax.QUERY_SYNTAX_UNSPECIFIED;
    case 1:
    case "QUERY_SYNTAX_LUCENE":
      return Metadata_QuerySyntax.QUERY_SYNTAX_LUCENE;
    case 2:
    case "QUERY_SYNTAX_DATAPRIME":
      return Metadata_QuerySyntax.QUERY_SYNTAX_DATAPRIME;
    case -1:
    case "UNRECOGNIZED":
    default:
      return Metadata_QuerySyntax.UNRECOGNIZED;
  }
}

export function metadata_QuerySyntaxToJSON(object: Metadata_QuerySyntax): string {
  switch (object) {
    case Metadata_QuerySyntax.QUERY_SYNTAX_UNSPECIFIED:
      return "QUERY_SYNTAX_UNSPECIFIED";
    case Metadata_QuerySyntax.QUERY_SYNTAX_LUCENE:
      return "QUERY_SYNTAX_LUCENE";
    case Metadata_QuerySyntax.QUERY_SYNTAX_DATAPRIME:
      return "QUERY_SYNTAX_DATAPRIME";
    case Metadata_QuerySyntax.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

function createBaseQueryRequest(): QueryRequest {
  return { query: undefined, metadata: undefined };
}

export const QueryRequest = {
  encode(message: QueryRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.query !== undefined) {
      StringValue.encode({ value: message.query! }, writer.uint32(10).fork()).ldelim();
    }
    if (message.metadata !== undefined) {
      Metadata.encode(message.metadata, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.query = StringValue.decode(reader, reader.uint32()).value;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.metadata = Metadata.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryRequest {
    return {
      query: isSet(object.query) ? String(object.query) : undefined,
      metadata: isSet(object.metadata) ? Metadata.fromJSON(object.metadata) : undefined,
    };
  },

  toJSON(message: QueryRequest): unknown {
    const obj: any = {};
    if (message.query !== undefined) {
      obj.query = message.query;
    }
    if (message.metadata !== undefined) {
      obj.metadata = Metadata.toJSON(message.metadata);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryRequest>, I>>(base?: I): QueryRequest {
    return QueryRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryRequest>, I>>(object: I): QueryRequest {
    const message = createBaseQueryRequest();
    message.query = object.query ?? undefined;
    message.metadata = (object.metadata !== undefined && object.metadata !== null)
      ? Metadata.fromPartial(object.metadata)
      : undefined;
    return message;
  },
};

function createBaseMetadata(): Metadata {
  return {
    startDate: undefined,
    endDate: undefined,
    defaultSource: undefined,
    tier: undefined,
    syntax: undefined,
    limit: undefined,
    strictFieldsValidation: undefined,
  };
}

export const Metadata = {
  encode(message: Metadata, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.startDate !== undefined) {
      Timestamp.encode(toTimestamp(message.startDate), writer.uint32(10).fork()).ldelim();
    }
    if (message.endDate !== undefined) {
      Timestamp.encode(toTimestamp(message.endDate), writer.uint32(18).fork()).ldelim();
    }
    if (message.defaultSource !== undefined) {
      StringValue.encode({ value: message.defaultSource! }, writer.uint32(26).fork()).ldelim();
    }
    if (message.tier !== undefined) {
      writer.uint32(32).int32(message.tier);
    }
    if (message.syntax !== undefined) {
      writer.uint32(40).int32(message.syntax);
    }
    if (message.limit !== undefined) {
      writer.uint32(48).int32(message.limit);
    }
    if (message.strictFieldsValidation !== undefined) {
      writer.uint32(56).bool(message.strictFieldsValidation);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Metadata {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMetadata();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.startDate = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.endDate = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.defaultSource = StringValue.decode(reader, reader.uint32()).value;
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.tier = reader.int32() as any;
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.syntax = reader.int32() as any;
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.limit = reader.int32();
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.strictFieldsValidation = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Metadata {
    return {
      startDate: isSet(object.startDate) ? fromJsonTimestamp(object.startDate) : undefined,
      endDate: isSet(object.endDate) ? fromJsonTimestamp(object.endDate) : undefined,
      defaultSource: isSet(object.defaultSource) ? String(object.defaultSource) : undefined,
      tier: isSet(object.tier) ? metadata_TierFromJSON(object.tier) : undefined,
      syntax: isSet(object.syntax) ? metadata_QuerySyntaxFromJSON(object.syntax) : undefined,
      limit: isSet(object.limit) ? globalThis.Number(object.limit) : undefined,
      strictFieldsValidation: isSet(object.strictFieldsValidation)
        ? globalThis.Boolean(object.strictFieldsValidation)
        : undefined,
    };
  },

  toJSON(message: Metadata): unknown {
    const obj: any = {};
    if (message.startDate !== undefined) {
      obj.startDate = message.startDate.toISOString();
    }
    if (message.endDate !== undefined) {
      obj.endDate = message.endDate.toISOString();
    }
    if (message.defaultSource !== undefined) {
      obj.defaultSource = message.defaultSource;
    }
    if (message.tier !== undefined) {
      obj.tier = metadata_TierToJSON(message.tier);
    }
    if (message.syntax !== undefined) {
      obj.syntax = metadata_QuerySyntaxToJSON(message.syntax);
    }
    if (message.limit !== undefined) {
      obj.limit = Math.round(message.limit);
    }
    if (message.strictFieldsValidation !== undefined) {
      obj.strictFieldsValidation = message.strictFieldsValidation;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Metadata>, I>>(base?: I): Metadata {
    return Metadata.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Metadata>, I>>(object: I): Metadata {
    const message = createBaseMetadata();
    message.startDate = object.startDate ?? undefined;
    message.endDate = object.endDate ?? undefined;
    message.defaultSource = object.defaultSource ?? undefined;
    message.tier = object.tier ?? undefined;
    message.syntax = object.syntax ?? undefined;
    message.limit = object.limit ?? undefined;
    message.strictFieldsValidation = object.strictFieldsValidation ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
