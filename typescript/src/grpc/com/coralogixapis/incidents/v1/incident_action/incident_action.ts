// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.176.1
//   protoc               v3.21.12
// source: com/coralogixapis/incidents/v1/incident_action/incident_action.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { Timestamp } from "../../../../../google/protobuf/timestamp";
import { UpsertIncidentState } from "./upsert_incident_state/upsert_incident_state";

export const protobufPackage = "com.coralogixapis.incidents.v1";

export enum AlertIncidentActionType {
  ALERT_INCIDENT_ACTION_TYPE_UNSPECIFIED = 0,
  ALERT_INCIDENT_ACTION_TYPE_CLOSE = 1,
  ALERT_INCIDENT_ACTION_TYPE_ACKNOWLEDGE = 2,
  ALERT_INCIDENT_ACTION_TYPE_ASSIGN = 3,
  UNRECOGNIZED = -1,
}

export function alertIncidentActionTypeFromJSON(object: any): AlertIncidentActionType {
  switch (object) {
    case 0:
    case "ALERT_INCIDENT_ACTION_TYPE_UNSPECIFIED":
      return AlertIncidentActionType.ALERT_INCIDENT_ACTION_TYPE_UNSPECIFIED;
    case 1:
    case "ALERT_INCIDENT_ACTION_TYPE_CLOSE":
      return AlertIncidentActionType.ALERT_INCIDENT_ACTION_TYPE_CLOSE;
    case 2:
    case "ALERT_INCIDENT_ACTION_TYPE_ACKNOWLEDGE":
      return AlertIncidentActionType.ALERT_INCIDENT_ACTION_TYPE_ACKNOWLEDGE;
    case 3:
    case "ALERT_INCIDENT_ACTION_TYPE_ASSIGN":
      return AlertIncidentActionType.ALERT_INCIDENT_ACTION_TYPE_ASSIGN;
    case -1:
    case "UNRECOGNIZED":
    default:
      return AlertIncidentActionType.UNRECOGNIZED;
  }
}

export function alertIncidentActionTypeToJSON(object: AlertIncidentActionType): string {
  switch (object) {
    case AlertIncidentActionType.ALERT_INCIDENT_ACTION_TYPE_UNSPECIFIED:
      return "ALERT_INCIDENT_ACTION_TYPE_UNSPECIFIED";
    case AlertIncidentActionType.ALERT_INCIDENT_ACTION_TYPE_CLOSE:
      return "ALERT_INCIDENT_ACTION_TYPE_CLOSE";
    case AlertIncidentActionType.ALERT_INCIDENT_ACTION_TYPE_ACKNOWLEDGE:
      return "ALERT_INCIDENT_ACTION_TYPE_ACKNOWLEDGE";
    case AlertIncidentActionType.ALERT_INCIDENT_ACTION_TYPE_ASSIGN:
      return "ALERT_INCIDENT_ACTION_TYPE_ASSIGN";
    case AlertIncidentActionType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum IncidentActionType {
  INCIDENT_ACTION_TYPE_UNSPECIFIED = 0,
  INCIDENT_ACTION_TYPE_CLOSE = 1,
  INCIDENT_ACTION_TYPE_ACKNOWLEDGE = 2,
  INCIDENT_ACTION_TYPE_ASSIGN = 3,
  INCIDENT_ACTION_TYPE_UPSERT_STATE = 4,
  UNRECOGNIZED = -1,
}

export function incidentActionTypeFromJSON(object: any): IncidentActionType {
  switch (object) {
    case 0:
    case "INCIDENT_ACTION_TYPE_UNSPECIFIED":
      return IncidentActionType.INCIDENT_ACTION_TYPE_UNSPECIFIED;
    case 1:
    case "INCIDENT_ACTION_TYPE_CLOSE":
      return IncidentActionType.INCIDENT_ACTION_TYPE_CLOSE;
    case 2:
    case "INCIDENT_ACTION_TYPE_ACKNOWLEDGE":
      return IncidentActionType.INCIDENT_ACTION_TYPE_ACKNOWLEDGE;
    case 3:
    case "INCIDENT_ACTION_TYPE_ASSIGN":
      return IncidentActionType.INCIDENT_ACTION_TYPE_ASSIGN;
    case 4:
    case "INCIDENT_ACTION_TYPE_UPSERT_STATE":
      return IncidentActionType.INCIDENT_ACTION_TYPE_UPSERT_STATE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return IncidentActionType.UNRECOGNIZED;
  }
}

export function incidentActionTypeToJSON(object: IncidentActionType): string {
  switch (object) {
    case IncidentActionType.INCIDENT_ACTION_TYPE_UNSPECIFIED:
      return "INCIDENT_ACTION_TYPE_UNSPECIFIED";
    case IncidentActionType.INCIDENT_ACTION_TYPE_CLOSE:
      return "INCIDENT_ACTION_TYPE_CLOSE";
    case IncidentActionType.INCIDENT_ACTION_TYPE_ACKNOWLEDGE:
      return "INCIDENT_ACTION_TYPE_ACKNOWLEDGE";
    case IncidentActionType.INCIDENT_ACTION_TYPE_ASSIGN:
      return "INCIDENT_ACTION_TYPE_ASSIGN";
    case IncidentActionType.INCIDENT_ACTION_TYPE_UPSERT_STATE:
      return "INCIDENT_ACTION_TYPE_UPSERT_STATE";
    case IncidentActionType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface IncidentAction {
  actionType: IncidentActionType;
  upsertState?: UpsertIncidentState | undefined;
  actionTimestamp: Date | undefined;
}

function createBaseIncidentAction(): IncidentAction {
  return { actionType: 0, upsertState: undefined, actionTimestamp: undefined };
}

export const IncidentAction = {
  encode(message: IncidentAction, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.actionType !== 0) {
      writer.uint32(8).int32(message.actionType);
    }
    if (message.upsertState !== undefined) {
      UpsertIncidentState.encode(message.upsertState, writer.uint32(834).fork()).ldelim();
    }
    if (message.actionTimestamp !== undefined) {
      Timestamp.encode(toTimestamp(message.actionTimestamp), writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): IncidentAction {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIncidentAction();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.actionType = reader.int32() as any;
          continue;
        case 104:
          if (tag !== 834) {
            break;
          }

          message.upsertState = UpsertIncidentState.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.actionTimestamp = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IncidentAction {
    return {
      actionType: isSet(object.actionType) ? incidentActionTypeFromJSON(object.actionType) : 0,
      upsertState: isSet(object.upsertState) ? UpsertIncidentState.fromJSON(object.upsertState) : undefined,
      actionTimestamp: isSet(object.actionTimestamp) ? fromJsonTimestamp(object.actionTimestamp) : undefined,
    };
  },

  toJSON(message: IncidentAction): unknown {
    const obj: any = {};
    if (message.actionType !== 0) {
      obj.actionType = incidentActionTypeToJSON(message.actionType);
    }
    if (message.upsertState !== undefined) {
      obj.upsertState = UpsertIncidentState.toJSON(message.upsertState);
    }
    if (message.actionTimestamp !== undefined) {
      obj.actionTimestamp = message.actionTimestamp.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<IncidentAction>, I>>(base?: I): IncidentAction {
    return IncidentAction.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IncidentAction>, I>>(object: I): IncidentAction {
    const message = createBaseIncidentAction();
    message.actionType = object.actionType ?? 0;
    message.upsertState = (object.upsertState !== undefined && object.upsertState !== null)
      ? UpsertIncidentState.fromPartial(object.upsertState)
      : undefined;
    message.actionTimestamp = object.actionTimestamp ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
