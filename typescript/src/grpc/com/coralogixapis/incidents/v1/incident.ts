// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.176.1
//   protoc               v3.21.12
// source: com/coralogixapis/incidents/v1/incident.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { Duration } from "../../../../google/protobuf/duration";
import { Timestamp } from "../../../../google/protobuf/timestamp";
import { BoolValue, StringValue, UInt32Value } from "../../../../google/protobuf/wrappers";
import { Assignment, UserDetails } from "./assignee";
import { IncidentEvent } from "./incident_event/incident_event";
import { IncidentSeverity, incidentSeverityFromJSON, incidentSeverityToJSON } from "./incident_severity";
import { IncidentState, incidentStateFromJSON, incidentStateToJSON } from "./incident_state";
import { IncidentStatus, incidentStatusFromJSON, incidentStatusToJSON } from "./incident_status";
import { MetaLabel } from "./meta_label";

export const protobufPackage = "com.coralogixapis.incidents.v1";

export enum IncidentFields {
  INCIDENTS_FIELDS_UNSPECIFIED = 0,
  INCIDENTS_FIELDS_ID = 1,
  INCIDENTS_FIELDS_SEVERITY = 2,
  INCIDENTS_FIELDS_NAME = 3,
  INCIDENTS_FIELDS_CREATED_TIME = 4,
  INCIDENTS_FIELDS_CLOSED_TIME = 5,
  INCIDENTS_FIELDS_STATE = 6,
  INCIDENTS_FIELDS_STATUS = 7,
  INCIDENTS_FIELDS_LAST_STATE_UPDATE_TIME = 8,
  INCIDENTS_FIELDS_APPLICATION_NAME = 9,
  INCIDENTS_FIELDS_SUBSYSTEM_NAME = 10,
  INCIDENTS_FIELDS_DURATION = 11,
  UNRECOGNIZED = -1,
}

export function incidentFieldsFromJSON(object: any): IncidentFields {
  switch (object) {
    case 0:
    case "INCIDENTS_FIELDS_UNSPECIFIED":
      return IncidentFields.INCIDENTS_FIELDS_UNSPECIFIED;
    case 1:
    case "INCIDENTS_FIELDS_ID":
      return IncidentFields.INCIDENTS_FIELDS_ID;
    case 2:
    case "INCIDENTS_FIELDS_SEVERITY":
      return IncidentFields.INCIDENTS_FIELDS_SEVERITY;
    case 3:
    case "INCIDENTS_FIELDS_NAME":
      return IncidentFields.INCIDENTS_FIELDS_NAME;
    case 4:
    case "INCIDENTS_FIELDS_CREATED_TIME":
      return IncidentFields.INCIDENTS_FIELDS_CREATED_TIME;
    case 5:
    case "INCIDENTS_FIELDS_CLOSED_TIME":
      return IncidentFields.INCIDENTS_FIELDS_CLOSED_TIME;
    case 6:
    case "INCIDENTS_FIELDS_STATE":
      return IncidentFields.INCIDENTS_FIELDS_STATE;
    case 7:
    case "INCIDENTS_FIELDS_STATUS":
      return IncidentFields.INCIDENTS_FIELDS_STATUS;
    case 8:
    case "INCIDENTS_FIELDS_LAST_STATE_UPDATE_TIME":
      return IncidentFields.INCIDENTS_FIELDS_LAST_STATE_UPDATE_TIME;
    case 9:
    case "INCIDENTS_FIELDS_APPLICATION_NAME":
      return IncidentFields.INCIDENTS_FIELDS_APPLICATION_NAME;
    case 10:
    case "INCIDENTS_FIELDS_SUBSYSTEM_NAME":
      return IncidentFields.INCIDENTS_FIELDS_SUBSYSTEM_NAME;
    case 11:
    case "INCIDENTS_FIELDS_DURATION":
      return IncidentFields.INCIDENTS_FIELDS_DURATION;
    case -1:
    case "UNRECOGNIZED":
    default:
      return IncidentFields.UNRECOGNIZED;
  }
}

export function incidentFieldsToJSON(object: IncidentFields): string {
  switch (object) {
    case IncidentFields.INCIDENTS_FIELDS_UNSPECIFIED:
      return "INCIDENTS_FIELDS_UNSPECIFIED";
    case IncidentFields.INCIDENTS_FIELDS_ID:
      return "INCIDENTS_FIELDS_ID";
    case IncidentFields.INCIDENTS_FIELDS_SEVERITY:
      return "INCIDENTS_FIELDS_SEVERITY";
    case IncidentFields.INCIDENTS_FIELDS_NAME:
      return "INCIDENTS_FIELDS_NAME";
    case IncidentFields.INCIDENTS_FIELDS_CREATED_TIME:
      return "INCIDENTS_FIELDS_CREATED_TIME";
    case IncidentFields.INCIDENTS_FIELDS_CLOSED_TIME:
      return "INCIDENTS_FIELDS_CLOSED_TIME";
    case IncidentFields.INCIDENTS_FIELDS_STATE:
      return "INCIDENTS_FIELDS_STATE";
    case IncidentFields.INCIDENTS_FIELDS_STATUS:
      return "INCIDENTS_FIELDS_STATUS";
    case IncidentFields.INCIDENTS_FIELDS_LAST_STATE_UPDATE_TIME:
      return "INCIDENTS_FIELDS_LAST_STATE_UPDATE_TIME";
    case IncidentFields.INCIDENTS_FIELDS_APPLICATION_NAME:
      return "INCIDENTS_FIELDS_APPLICATION_NAME";
    case IncidentFields.INCIDENTS_FIELDS_SUBSYSTEM_NAME:
      return "INCIDENTS_FIELDS_SUBSYSTEM_NAME";
    case IncidentFields.INCIDENTS_FIELDS_DURATION:
      return "INCIDENTS_FIELDS_DURATION";
    case IncidentFields.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Represents an Incident. */
export interface Incident {
  /** The Incident's id */
  id:
    | string
    | undefined;
  /** The Incident's name */
  name:
    | string
    | undefined;
  /** The Incident's current state, the internal state of the incident, usually is derived from the operational events of the incident */
  state: IncidentState;
  /** The Incident's current status, the status of the incident can be derived from state of the incident and/or administrative user operation (i.e acknowledging the incident) */
  status: IncidentStatus;
  /** The Incident's current assignments */
  assignments: Assignment[];
  /** The Incident's current description */
  description:
    | string
    | undefined;
  /** The Incident's current severity */
  severity: IncidentSeverity;
  /** The Incident's current contextual labels, contextual labels are dynamic labels that gives contextual information about the incident ( for example: alert_name label can appear in an incident created by an event originated from the alerting system ) */
  contextualLabels: { [key: string]: string };
  /** The Incident's current display labels, display labels are dynamic labels that gives more of an extension to the name of the incident, for identification and naming purposes */
  displayLabels: { [key: string]: string };
  /** The Incident's current events */
  events: IncidentEvent[];
  createdAt: Date | undefined;
  closedAt: Date | undefined;
  lastStateUpdateTime:
    | Date
    | undefined;
  /** The Incident's last event payload key that caused the incident to change state */
  lastStateUpdateKey:
    | string
    | undefined;
  /** Is the incident muted/suppressed */
  isMuted:
    | boolean
    | undefined;
  /** The Incident's current meta labels */
  metaLabels: MetaLabel[];
  /** The Incident duration between created at and close time */
  duration: Duration | undefined;
}

export interface Incident_ContextualLabelsEntry {
  key: string;
  value: string;
}

export interface Incident_DisplayLabelsEntry {
  key: string;
  value: string;
}

export interface IncidentFieldOneOf {
  id?: string | undefined;
  severity?: IncidentSeverity | undefined;
  name?: string | undefined;
  createdAt?: Date | undefined;
  closedAt?: Date | undefined;
  state?: IncidentState | undefined;
  status?: IncidentStatus | undefined;
  lastStateUpdateTime?: Date | undefined;
  applicationName?: string | undefined;
  subsystemName?: string | undefined;
  duration?: Duration | undefined;
}

export interface ContextualLabels {
  fieldName: string | undefined;
  fieldValue: string | undefined;
}

export interface GroupByValues {
  incidentField?: IncidentFieldOneOf | undefined;
  contextualLabels?: ContextualLabels | undefined;
}

/** Represents and Incident Aggregation (group by) */
export interface IncidentAggregation {
  /** The Aggregation's group by fields and values */
  groupBysValue: GroupByValues[];
  /** The Aggregation's count of states */
  aggStateCount: IncidentStateCount[];
  /** The Aggregation's count of statuses */
  aggStatusCount: IncidentStatusCount[];
  /** The Aggregation's count of severities */
  aggSeverityCount: IncidentSeverityCount[];
  /** The Aggregation's count of assignments */
  aggAssignmentsCount: IncidentAssignmentCount[];
  /** The Aggregation's first incident created time */
  firstCreatedAt:
    | Date
    | undefined;
  /** The Aggregation's last incident closed time */
  lastClosedAt:
    | Date
    | undefined;
  /** The Aggregation's count of incidents */
  allValuesCount:
    | number
    | undefined;
  /** The Aggregation's list of incident's id */
  listIncidentsId: string[];
  /** The Aggregation's last state update time */
  lastStateUpdateTime:
    | Date
    | undefined;
  /** The Aggregation's of meta labels */
  aggMetaLabelsCount: IncidentMetaLabelsCount[];
}

export interface IncidentMetaLabelsCount {
  metaLabel: MetaLabel | undefined;
  count: number | undefined;
}

export interface IncidentAssignmentCount {
  assignedTo: UserDetails | undefined;
  count: number | undefined;
}

export interface IncidentSeverityCount {
  severity: IncidentSeverity;
  count: number | undefined;
}

export interface IncidentStatusCount {
  status: IncidentStatus;
  count: number | undefined;
}

export interface IncidentStateCount {
  state: IncidentState;
  count: number | undefined;
}

function createBaseIncident(): Incident {
  return {
    id: undefined,
    name: undefined,
    state: 0,
    status: 0,
    assignments: [],
    description: undefined,
    severity: 0,
    contextualLabels: {},
    displayLabels: {},
    events: [],
    createdAt: undefined,
    closedAt: undefined,
    lastStateUpdateTime: undefined,
    lastStateUpdateKey: undefined,
    isMuted: undefined,
    metaLabels: [],
    duration: undefined,
  };
}

export const Incident = {
  encode(message: Incident, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== undefined) {
      StringValue.encode({ value: message.id! }, writer.uint32(10).fork()).ldelim();
    }
    if (message.name !== undefined) {
      StringValue.encode({ value: message.name! }, writer.uint32(18).fork()).ldelim();
    }
    if (message.state !== 0) {
      writer.uint32(24).int32(message.state);
    }
    if (message.status !== 0) {
      writer.uint32(32).int32(message.status);
    }
    for (const v of message.assignments) {
      Assignment.encode(v!, writer.uint32(42).fork()).ldelim();
    }
    if (message.description !== undefined) {
      StringValue.encode({ value: message.description! }, writer.uint32(50).fork()).ldelim();
    }
    if (message.severity !== 0) {
      writer.uint32(56).int32(message.severity);
    }
    Object.entries(message.contextualLabels).forEach(([key, value]) => {
      Incident_ContextualLabelsEntry.encode({ key: key as any, value }, writer.uint32(106).fork()).ldelim();
    });
    Object.entries(message.displayLabels).forEach(([key, value]) => {
      Incident_DisplayLabelsEntry.encode({ key: key as any, value }, writer.uint32(114).fork()).ldelim();
    });
    for (const v of message.events) {
      IncidentEvent.encode(v!, writer.uint32(82).fork()).ldelim();
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(90).fork()).ldelim();
    }
    if (message.closedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.closedAt), writer.uint32(98).fork()).ldelim();
    }
    if (message.lastStateUpdateTime !== undefined) {
      Timestamp.encode(toTimestamp(message.lastStateUpdateTime), writer.uint32(122).fork()).ldelim();
    }
    if (message.lastStateUpdateKey !== undefined) {
      StringValue.encode({ value: message.lastStateUpdateKey! }, writer.uint32(130).fork()).ldelim();
    }
    if (message.isMuted !== undefined) {
      BoolValue.encode({ value: message.isMuted! }, writer.uint32(138).fork()).ldelim();
    }
    for (const v of message.metaLabels) {
      MetaLabel.encode(v!, writer.uint32(146).fork()).ldelim();
    }
    if (message.duration !== undefined) {
      Duration.encode(message.duration, writer.uint32(154).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Incident {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIncident();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = StringValue.decode(reader, reader.uint32()).value;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.name = StringValue.decode(reader, reader.uint32()).value;
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.state = reader.int32() as any;
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.assignments.push(Assignment.decode(reader, reader.uint32()));
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.description = StringValue.decode(reader, reader.uint32()).value;
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.severity = reader.int32() as any;
          continue;
        case 13:
          if (tag !== 106) {
            break;
          }

          const entry13 = Incident_ContextualLabelsEntry.decode(reader, reader.uint32());
          if (entry13.value !== undefined) {
            message.contextualLabels[entry13.key] = entry13.value;
          }
          continue;
        case 14:
          if (tag !== 114) {
            break;
          }

          const entry14 = Incident_DisplayLabelsEntry.decode(reader, reader.uint32());
          if (entry14.value !== undefined) {
            message.displayLabels[entry14.key] = entry14.value;
          }
          continue;
        case 10:
          if (tag !== 82) {
            break;
          }

          message.events.push(IncidentEvent.decode(reader, reader.uint32()));
          continue;
        case 11:
          if (tag !== 90) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 12:
          if (tag !== 98) {
            break;
          }

          message.closedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 15:
          if (tag !== 122) {
            break;
          }

          message.lastStateUpdateTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 16:
          if (tag !== 130) {
            break;
          }

          message.lastStateUpdateKey = StringValue.decode(reader, reader.uint32()).value;
          continue;
        case 17:
          if (tag !== 138) {
            break;
          }

          message.isMuted = BoolValue.decode(reader, reader.uint32()).value;
          continue;
        case 18:
          if (tag !== 146) {
            break;
          }

          message.metaLabels.push(MetaLabel.decode(reader, reader.uint32()));
          continue;
        case 19:
          if (tag !== 154) {
            break;
          }

          message.duration = Duration.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Incident {
    return {
      id: isSet(object.id) ? String(object.id) : undefined,
      name: isSet(object.name) ? String(object.name) : undefined,
      state: isSet(object.state) ? incidentStateFromJSON(object.state) : 0,
      status: isSet(object.status) ? incidentStatusFromJSON(object.status) : 0,
      assignments: globalThis.Array.isArray(object?.assignments)
        ? object.assignments.map((e: any) => Assignment.fromJSON(e))
        : [],
      description: isSet(object.description) ? String(object.description) : undefined,
      severity: isSet(object.severity) ? incidentSeverityFromJSON(object.severity) : 0,
      contextualLabels: isObject(object.contextualLabels)
        ? Object.entries(object.contextualLabels).reduce<{ [key: string]: string }>((acc, [key, value]) => {
          acc[key] = String(value);
          return acc;
        }, {})
        : {},
      displayLabels: isObject(object.displayLabels)
        ? Object.entries(object.displayLabels).reduce<{ [key: string]: string }>((acc, [key, value]) => {
          acc[key] = String(value);
          return acc;
        }, {})
        : {},
      events: globalThis.Array.isArray(object?.events) ? object.events.map((e: any) => IncidentEvent.fromJSON(e)) : [],
      createdAt: isSet(object.createdAt) ? fromJsonTimestamp(object.createdAt) : undefined,
      closedAt: isSet(object.closedAt) ? fromJsonTimestamp(object.closedAt) : undefined,
      lastStateUpdateTime: isSet(object.lastStateUpdateTime)
        ? fromJsonTimestamp(object.lastStateUpdateTime)
        : undefined,
      lastStateUpdateKey: isSet(object.lastStateUpdateKey) ? String(object.lastStateUpdateKey) : undefined,
      isMuted: isSet(object.isMuted) ? Boolean(object.isMuted) : undefined,
      metaLabels: globalThis.Array.isArray(object?.metaLabels)
        ? object.metaLabels.map((e: any) => MetaLabel.fromJSON(e))
        : [],
      duration: isSet(object.duration) ? Duration.fromJSON(object.duration) : undefined,
    };
  },

  toJSON(message: Incident): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = message.id;
    }
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    if (message.state !== 0) {
      obj.state = incidentStateToJSON(message.state);
    }
    if (message.status !== 0) {
      obj.status = incidentStatusToJSON(message.status);
    }
    if (message.assignments?.length) {
      obj.assignments = message.assignments.map((e) => Assignment.toJSON(e));
    }
    if (message.description !== undefined) {
      obj.description = message.description;
    }
    if (message.severity !== 0) {
      obj.severity = incidentSeverityToJSON(message.severity);
    }
    if (message.contextualLabels) {
      const entries = Object.entries(message.contextualLabels);
      if (entries.length > 0) {
        obj.contextualLabels = {};
        entries.forEach(([k, v]) => {
          obj.contextualLabels[k] = v;
        });
      }
    }
    if (message.displayLabels) {
      const entries = Object.entries(message.displayLabels);
      if (entries.length > 0) {
        obj.displayLabels = {};
        entries.forEach(([k, v]) => {
          obj.displayLabels[k] = v;
        });
      }
    }
    if (message.events?.length) {
      obj.events = message.events.map((e) => IncidentEvent.toJSON(e));
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.closedAt !== undefined) {
      obj.closedAt = message.closedAt.toISOString();
    }
    if (message.lastStateUpdateTime !== undefined) {
      obj.lastStateUpdateTime = message.lastStateUpdateTime.toISOString();
    }
    if (message.lastStateUpdateKey !== undefined) {
      obj.lastStateUpdateKey = message.lastStateUpdateKey;
    }
    if (message.isMuted !== undefined) {
      obj.isMuted = message.isMuted;
    }
    if (message.metaLabels?.length) {
      obj.metaLabels = message.metaLabels.map((e) => MetaLabel.toJSON(e));
    }
    if (message.duration !== undefined) {
      obj.duration = Duration.toJSON(message.duration);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Incident>, I>>(base?: I): Incident {
    return Incident.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Incident>, I>>(object: I): Incident {
    const message = createBaseIncident();
    message.id = object.id ?? undefined;
    message.name = object.name ?? undefined;
    message.state = object.state ?? 0;
    message.status = object.status ?? 0;
    message.assignments = object.assignments?.map((e) => Assignment.fromPartial(e)) || [];
    message.description = object.description ?? undefined;
    message.severity = object.severity ?? 0;
    message.contextualLabels = Object.entries(object.contextualLabels ?? {}).reduce<{ [key: string]: string }>(
      (acc, [key, value]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.displayLabels = Object.entries(object.displayLabels ?? {}).reduce<{ [key: string]: string }>(
      (acc, [key, value]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.events = object.events?.map((e) => IncidentEvent.fromPartial(e)) || [];
    message.createdAt = object.createdAt ?? undefined;
    message.closedAt = object.closedAt ?? undefined;
    message.lastStateUpdateTime = object.lastStateUpdateTime ?? undefined;
    message.lastStateUpdateKey = object.lastStateUpdateKey ?? undefined;
    message.isMuted = object.isMuted ?? undefined;
    message.metaLabels = object.metaLabels?.map((e) => MetaLabel.fromPartial(e)) || [];
    message.duration = (object.duration !== undefined && object.duration !== null)
      ? Duration.fromPartial(object.duration)
      : undefined;
    return message;
  },
};

function createBaseIncident_ContextualLabelsEntry(): Incident_ContextualLabelsEntry {
  return { key: "", value: "" };
}

export const Incident_ContextualLabelsEntry = {
  encode(message: Incident_ContextualLabelsEntry, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Incident_ContextualLabelsEntry {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIncident_ContextualLabelsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Incident_ContextualLabelsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: Incident_ContextualLabelsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Incident_ContextualLabelsEntry>, I>>(base?: I): Incident_ContextualLabelsEntry {
    return Incident_ContextualLabelsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Incident_ContextualLabelsEntry>, I>>(
    object: I,
  ): Incident_ContextualLabelsEntry {
    const message = createBaseIncident_ContextualLabelsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseIncident_DisplayLabelsEntry(): Incident_DisplayLabelsEntry {
  return { key: "", value: "" };
}

export const Incident_DisplayLabelsEntry = {
  encode(message: Incident_DisplayLabelsEntry, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Incident_DisplayLabelsEntry {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIncident_DisplayLabelsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Incident_DisplayLabelsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: Incident_DisplayLabelsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Incident_DisplayLabelsEntry>, I>>(base?: I): Incident_DisplayLabelsEntry {
    return Incident_DisplayLabelsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Incident_DisplayLabelsEntry>, I>>(object: I): Incident_DisplayLabelsEntry {
    const message = createBaseIncident_DisplayLabelsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseIncidentFieldOneOf(): IncidentFieldOneOf {
  return {
    id: undefined,
    severity: undefined,
    name: undefined,
    createdAt: undefined,
    closedAt: undefined,
    state: undefined,
    status: undefined,
    lastStateUpdateTime: undefined,
    applicationName: undefined,
    subsystemName: undefined,
    duration: undefined,
  };
}

export const IncidentFieldOneOf = {
  encode(message: IncidentFieldOneOf, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== undefined) {
      StringValue.encode({ value: message.id! }, writer.uint32(10).fork()).ldelim();
    }
    if (message.severity !== undefined) {
      writer.uint32(16).int32(message.severity);
    }
    if (message.name !== undefined) {
      StringValue.encode({ value: message.name! }, writer.uint32(26).fork()).ldelim();
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(34).fork()).ldelim();
    }
    if (message.closedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.closedAt), writer.uint32(42).fork()).ldelim();
    }
    if (message.state !== undefined) {
      writer.uint32(48).int32(message.state);
    }
    if (message.status !== undefined) {
      writer.uint32(56).int32(message.status);
    }
    if (message.lastStateUpdateTime !== undefined) {
      Timestamp.encode(toTimestamp(message.lastStateUpdateTime), writer.uint32(66).fork()).ldelim();
    }
    if (message.applicationName !== undefined) {
      StringValue.encode({ value: message.applicationName! }, writer.uint32(74).fork()).ldelim();
    }
    if (message.subsystemName !== undefined) {
      StringValue.encode({ value: message.subsystemName! }, writer.uint32(82).fork()).ldelim();
    }
    if (message.duration !== undefined) {
      Duration.encode(message.duration, writer.uint32(90).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): IncidentFieldOneOf {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIncidentFieldOneOf();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = StringValue.decode(reader, reader.uint32()).value;
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.severity = reader.int32() as any;
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.name = StringValue.decode(reader, reader.uint32()).value;
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.closedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.state = reader.int32() as any;
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        case 8:
          if (tag !== 66) {
            break;
          }

          message.lastStateUpdateTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 9:
          if (tag !== 74) {
            break;
          }

          message.applicationName = StringValue.decode(reader, reader.uint32()).value;
          continue;
        case 10:
          if (tag !== 82) {
            break;
          }

          message.subsystemName = StringValue.decode(reader, reader.uint32()).value;
          continue;
        case 11:
          if (tag !== 90) {
            break;
          }

          message.duration = Duration.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IncidentFieldOneOf {
    return {
      id: isSet(object.id) ? String(object.id) : undefined,
      severity: isSet(object.severity) ? incidentSeverityFromJSON(object.severity) : undefined,
      name: isSet(object.name) ? String(object.name) : undefined,
      createdAt: isSet(object.createdAt) ? fromJsonTimestamp(object.createdAt) : undefined,
      closedAt: isSet(object.closedAt) ? fromJsonTimestamp(object.closedAt) : undefined,
      state: isSet(object.state) ? incidentStateFromJSON(object.state) : undefined,
      status: isSet(object.status) ? incidentStatusFromJSON(object.status) : undefined,
      lastStateUpdateTime: isSet(object.lastStateUpdateTime)
        ? fromJsonTimestamp(object.lastStateUpdateTime)
        : undefined,
      applicationName: isSet(object.applicationName) ? String(object.applicationName) : undefined,
      subsystemName: isSet(object.subsystemName) ? String(object.subsystemName) : undefined,
      duration: isSet(object.duration) ? Duration.fromJSON(object.duration) : undefined,
    };
  },

  toJSON(message: IncidentFieldOneOf): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = message.id;
    }
    if (message.severity !== undefined) {
      obj.severity = incidentSeverityToJSON(message.severity);
    }
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.closedAt !== undefined) {
      obj.closedAt = message.closedAt.toISOString();
    }
    if (message.state !== undefined) {
      obj.state = incidentStateToJSON(message.state);
    }
    if (message.status !== undefined) {
      obj.status = incidentStatusToJSON(message.status);
    }
    if (message.lastStateUpdateTime !== undefined) {
      obj.lastStateUpdateTime = message.lastStateUpdateTime.toISOString();
    }
    if (message.applicationName !== undefined) {
      obj.applicationName = message.applicationName;
    }
    if (message.subsystemName !== undefined) {
      obj.subsystemName = message.subsystemName;
    }
    if (message.duration !== undefined) {
      obj.duration = Duration.toJSON(message.duration);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<IncidentFieldOneOf>, I>>(base?: I): IncidentFieldOneOf {
    return IncidentFieldOneOf.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IncidentFieldOneOf>, I>>(object: I): IncidentFieldOneOf {
    const message = createBaseIncidentFieldOneOf();
    message.id = object.id ?? undefined;
    message.severity = object.severity ?? undefined;
    message.name = object.name ?? undefined;
    message.createdAt = object.createdAt ?? undefined;
    message.closedAt = object.closedAt ?? undefined;
    message.state = object.state ?? undefined;
    message.status = object.status ?? undefined;
    message.lastStateUpdateTime = object.lastStateUpdateTime ?? undefined;
    message.applicationName = object.applicationName ?? undefined;
    message.subsystemName = object.subsystemName ?? undefined;
    message.duration = (object.duration !== undefined && object.duration !== null)
      ? Duration.fromPartial(object.duration)
      : undefined;
    return message;
  },
};

function createBaseContextualLabels(): ContextualLabels {
  return { fieldName: undefined, fieldValue: undefined };
}

export const ContextualLabels = {
  encode(message: ContextualLabels, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.fieldName !== undefined) {
      StringValue.encode({ value: message.fieldName! }, writer.uint32(10).fork()).ldelim();
    }
    if (message.fieldValue !== undefined) {
      StringValue.encode({ value: message.fieldValue! }, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ContextualLabels {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseContextualLabels();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.fieldName = StringValue.decode(reader, reader.uint32()).value;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.fieldValue = StringValue.decode(reader, reader.uint32()).value;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ContextualLabels {
    return {
      fieldName: isSet(object.fieldName) ? String(object.fieldName) : undefined,
      fieldValue: isSet(object.fieldValue) ? String(object.fieldValue) : undefined,
    };
  },

  toJSON(message: ContextualLabels): unknown {
    const obj: any = {};
    if (message.fieldName !== undefined) {
      obj.fieldName = message.fieldName;
    }
    if (message.fieldValue !== undefined) {
      obj.fieldValue = message.fieldValue;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ContextualLabels>, I>>(base?: I): ContextualLabels {
    return ContextualLabels.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ContextualLabels>, I>>(object: I): ContextualLabels {
    const message = createBaseContextualLabels();
    message.fieldName = object.fieldName ?? undefined;
    message.fieldValue = object.fieldValue ?? undefined;
    return message;
  },
};

function createBaseGroupByValues(): GroupByValues {
  return { incidentField: undefined, contextualLabels: undefined };
}

export const GroupByValues = {
  encode(message: GroupByValues, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.incidentField !== undefined) {
      IncidentFieldOneOf.encode(message.incidentField, writer.uint32(10).fork()).ldelim();
    }
    if (message.contextualLabels !== undefined) {
      ContextualLabels.encode(message.contextualLabels, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GroupByValues {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGroupByValues();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.incidentField = IncidentFieldOneOf.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.contextualLabels = ContextualLabels.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GroupByValues {
    return {
      incidentField: isSet(object.incidentField) ? IncidentFieldOneOf.fromJSON(object.incidentField) : undefined,
      contextualLabels: isSet(object.contextualLabels) ? ContextualLabels.fromJSON(object.contextualLabels) : undefined,
    };
  },

  toJSON(message: GroupByValues): unknown {
    const obj: any = {};
    if (message.incidentField !== undefined) {
      obj.incidentField = IncidentFieldOneOf.toJSON(message.incidentField);
    }
    if (message.contextualLabels !== undefined) {
      obj.contextualLabels = ContextualLabels.toJSON(message.contextualLabels);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GroupByValues>, I>>(base?: I): GroupByValues {
    return GroupByValues.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GroupByValues>, I>>(object: I): GroupByValues {
    const message = createBaseGroupByValues();
    message.incidentField = (object.incidentField !== undefined && object.incidentField !== null)
      ? IncidentFieldOneOf.fromPartial(object.incidentField)
      : undefined;
    message.contextualLabels = (object.contextualLabels !== undefined && object.contextualLabels !== null)
      ? ContextualLabels.fromPartial(object.contextualLabels)
      : undefined;
    return message;
  },
};

function createBaseIncidentAggregation(): IncidentAggregation {
  return {
    groupBysValue: [],
    aggStateCount: [],
    aggStatusCount: [],
    aggSeverityCount: [],
    aggAssignmentsCount: [],
    firstCreatedAt: undefined,
    lastClosedAt: undefined,
    allValuesCount: undefined,
    listIncidentsId: [],
    lastStateUpdateTime: undefined,
    aggMetaLabelsCount: [],
  };
}

export const IncidentAggregation = {
  encode(message: IncidentAggregation, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.groupBysValue) {
      GroupByValues.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    for (const v of message.aggStateCount) {
      IncidentStateCount.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    for (const v of message.aggStatusCount) {
      IncidentStatusCount.encode(v!, writer.uint32(26).fork()).ldelim();
    }
    for (const v of message.aggSeverityCount) {
      IncidentSeverityCount.encode(v!, writer.uint32(34).fork()).ldelim();
    }
    for (const v of message.aggAssignmentsCount) {
      IncidentAssignmentCount.encode(v!, writer.uint32(42).fork()).ldelim();
    }
    if (message.firstCreatedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.firstCreatedAt), writer.uint32(50).fork()).ldelim();
    }
    if (message.lastClosedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.lastClosedAt), writer.uint32(58).fork()).ldelim();
    }
    if (message.allValuesCount !== undefined) {
      UInt32Value.encode({ value: message.allValuesCount! }, writer.uint32(66).fork()).ldelim();
    }
    for (const v of message.listIncidentsId) {
      StringValue.encode({ value: v!! }, writer.uint32(74).fork()).ldelim();
    }
    if (message.lastStateUpdateTime !== undefined) {
      Timestamp.encode(toTimestamp(message.lastStateUpdateTime), writer.uint32(82).fork()).ldelim();
    }
    for (const v of message.aggMetaLabelsCount) {
      IncidentMetaLabelsCount.encode(v!, writer.uint32(90).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): IncidentAggregation {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIncidentAggregation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.groupBysValue.push(GroupByValues.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.aggStateCount.push(IncidentStateCount.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.aggStatusCount.push(IncidentStatusCount.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.aggSeverityCount.push(IncidentSeverityCount.decode(reader, reader.uint32()));
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.aggAssignmentsCount.push(IncidentAssignmentCount.decode(reader, reader.uint32()));
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.firstCreatedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.lastClosedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 8:
          if (tag !== 66) {
            break;
          }

          message.allValuesCount = UInt32Value.decode(reader, reader.uint32()).value;
          continue;
        case 9:
          if (tag !== 74) {
            break;
          }

          message.listIncidentsId.push(StringValue.decode(reader, reader.uint32()).value);
          continue;
        case 10:
          if (tag !== 82) {
            break;
          }

          message.lastStateUpdateTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 11:
          if (tag !== 90) {
            break;
          }

          message.aggMetaLabelsCount.push(IncidentMetaLabelsCount.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IncidentAggregation {
    return {
      groupBysValue: globalThis.Array.isArray(object?.groupBysValue)
        ? object.groupBysValue.map((e: any) => GroupByValues.fromJSON(e))
        : [],
      aggStateCount: globalThis.Array.isArray(object?.aggStateCount)
        ? object.aggStateCount.map((e: any) => IncidentStateCount.fromJSON(e))
        : [],
      aggStatusCount: globalThis.Array.isArray(object?.aggStatusCount)
        ? object.aggStatusCount.map((e: any) => IncidentStatusCount.fromJSON(e))
        : [],
      aggSeverityCount: globalThis.Array.isArray(object?.aggSeverityCount)
        ? object.aggSeverityCount.map((e: any) => IncidentSeverityCount.fromJSON(e))
        : [],
      aggAssignmentsCount: globalThis.Array.isArray(object?.aggAssignmentsCount)
        ? object.aggAssignmentsCount.map((e: any) => IncidentAssignmentCount.fromJSON(e))
        : [],
      firstCreatedAt: isSet(object.firstCreatedAt) ? fromJsonTimestamp(object.firstCreatedAt) : undefined,
      lastClosedAt: isSet(object.lastClosedAt) ? fromJsonTimestamp(object.lastClosedAt) : undefined,
      allValuesCount: isSet(object.allValuesCount) ? Number(object.allValuesCount) : undefined,
      listIncidentsId: globalThis.Array.isArray(object?.listIncidentsId)
        ? object.listIncidentsId.map((e: any) => String(e))
        : [],
      lastStateUpdateTime: isSet(object.lastStateUpdateTime)
        ? fromJsonTimestamp(object.lastStateUpdateTime)
        : undefined,
      aggMetaLabelsCount: globalThis.Array.isArray(object?.aggMetaLabelsCount)
        ? object.aggMetaLabelsCount.map((e: any) => IncidentMetaLabelsCount.fromJSON(e))
        : [],
    };
  },

  toJSON(message: IncidentAggregation): unknown {
    const obj: any = {};
    if (message.groupBysValue?.length) {
      obj.groupBysValue = message.groupBysValue.map((e) => GroupByValues.toJSON(e));
    }
    if (message.aggStateCount?.length) {
      obj.aggStateCount = message.aggStateCount.map((e) => IncidentStateCount.toJSON(e));
    }
    if (message.aggStatusCount?.length) {
      obj.aggStatusCount = message.aggStatusCount.map((e) => IncidentStatusCount.toJSON(e));
    }
    if (message.aggSeverityCount?.length) {
      obj.aggSeverityCount = message.aggSeverityCount.map((e) => IncidentSeverityCount.toJSON(e));
    }
    if (message.aggAssignmentsCount?.length) {
      obj.aggAssignmentsCount = message.aggAssignmentsCount.map((e) => IncidentAssignmentCount.toJSON(e));
    }
    if (message.firstCreatedAt !== undefined) {
      obj.firstCreatedAt = message.firstCreatedAt.toISOString();
    }
    if (message.lastClosedAt !== undefined) {
      obj.lastClosedAt = message.lastClosedAt.toISOString();
    }
    if (message.allValuesCount !== undefined) {
      obj.allValuesCount = message.allValuesCount;
    }
    if (message.listIncidentsId?.length) {
      obj.listIncidentsId = message.listIncidentsId;
    }
    if (message.lastStateUpdateTime !== undefined) {
      obj.lastStateUpdateTime = message.lastStateUpdateTime.toISOString();
    }
    if (message.aggMetaLabelsCount?.length) {
      obj.aggMetaLabelsCount = message.aggMetaLabelsCount.map((e) => IncidentMetaLabelsCount.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<IncidentAggregation>, I>>(base?: I): IncidentAggregation {
    return IncidentAggregation.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IncidentAggregation>, I>>(object: I): IncidentAggregation {
    const message = createBaseIncidentAggregation();
    message.groupBysValue = object.groupBysValue?.map((e) => GroupByValues.fromPartial(e)) || [];
    message.aggStateCount = object.aggStateCount?.map((e) => IncidentStateCount.fromPartial(e)) || [];
    message.aggStatusCount = object.aggStatusCount?.map((e) => IncidentStatusCount.fromPartial(e)) || [];
    message.aggSeverityCount = object.aggSeverityCount?.map((e) => IncidentSeverityCount.fromPartial(e)) || [];
    message.aggAssignmentsCount = object.aggAssignmentsCount?.map((e) => IncidentAssignmentCount.fromPartial(e)) || [];
    message.firstCreatedAt = object.firstCreatedAt ?? undefined;
    message.lastClosedAt = object.lastClosedAt ?? undefined;
    message.allValuesCount = object.allValuesCount ?? undefined;
    message.listIncidentsId = object.listIncidentsId?.map((e) => e) || [];
    message.lastStateUpdateTime = object.lastStateUpdateTime ?? undefined;
    message.aggMetaLabelsCount = object.aggMetaLabelsCount?.map((e) => IncidentMetaLabelsCount.fromPartial(e)) || [];
    return message;
  },
};

function createBaseIncidentMetaLabelsCount(): IncidentMetaLabelsCount {
  return { metaLabel: undefined, count: undefined };
}

export const IncidentMetaLabelsCount = {
  encode(message: IncidentMetaLabelsCount, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.metaLabel !== undefined) {
      MetaLabel.encode(message.metaLabel, writer.uint32(10).fork()).ldelim();
    }
    if (message.count !== undefined) {
      UInt32Value.encode({ value: message.count! }, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): IncidentMetaLabelsCount {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIncidentMetaLabelsCount();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.metaLabel = MetaLabel.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.count = UInt32Value.decode(reader, reader.uint32()).value;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IncidentMetaLabelsCount {
    return {
      metaLabel: isSet(object.metaLabel) ? MetaLabel.fromJSON(object.metaLabel) : undefined,
      count: isSet(object.count) ? Number(object.count) : undefined,
    };
  },

  toJSON(message: IncidentMetaLabelsCount): unknown {
    const obj: any = {};
    if (message.metaLabel !== undefined) {
      obj.metaLabel = MetaLabel.toJSON(message.metaLabel);
    }
    if (message.count !== undefined) {
      obj.count = message.count;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<IncidentMetaLabelsCount>, I>>(base?: I): IncidentMetaLabelsCount {
    return IncidentMetaLabelsCount.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IncidentMetaLabelsCount>, I>>(object: I): IncidentMetaLabelsCount {
    const message = createBaseIncidentMetaLabelsCount();
    message.metaLabel = (object.metaLabel !== undefined && object.metaLabel !== null)
      ? MetaLabel.fromPartial(object.metaLabel)
      : undefined;
    message.count = object.count ?? undefined;
    return message;
  },
};

function createBaseIncidentAssignmentCount(): IncidentAssignmentCount {
  return { assignedTo: undefined, count: undefined };
}

export const IncidentAssignmentCount = {
  encode(message: IncidentAssignmentCount, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.assignedTo !== undefined) {
      UserDetails.encode(message.assignedTo, writer.uint32(10).fork()).ldelim();
    }
    if (message.count !== undefined) {
      UInt32Value.encode({ value: message.count! }, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): IncidentAssignmentCount {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIncidentAssignmentCount();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.assignedTo = UserDetails.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.count = UInt32Value.decode(reader, reader.uint32()).value;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IncidentAssignmentCount {
    return {
      assignedTo: isSet(object.assignedTo) ? UserDetails.fromJSON(object.assignedTo) : undefined,
      count: isSet(object.count) ? Number(object.count) : undefined,
    };
  },

  toJSON(message: IncidentAssignmentCount): unknown {
    const obj: any = {};
    if (message.assignedTo !== undefined) {
      obj.assignedTo = UserDetails.toJSON(message.assignedTo);
    }
    if (message.count !== undefined) {
      obj.count = message.count;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<IncidentAssignmentCount>, I>>(base?: I): IncidentAssignmentCount {
    return IncidentAssignmentCount.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IncidentAssignmentCount>, I>>(object: I): IncidentAssignmentCount {
    const message = createBaseIncidentAssignmentCount();
    message.assignedTo = (object.assignedTo !== undefined && object.assignedTo !== null)
      ? UserDetails.fromPartial(object.assignedTo)
      : undefined;
    message.count = object.count ?? undefined;
    return message;
  },
};

function createBaseIncidentSeverityCount(): IncidentSeverityCount {
  return { severity: 0, count: undefined };
}

export const IncidentSeverityCount = {
  encode(message: IncidentSeverityCount, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.severity !== 0) {
      writer.uint32(8).int32(message.severity);
    }
    if (message.count !== undefined) {
      UInt32Value.encode({ value: message.count! }, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): IncidentSeverityCount {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIncidentSeverityCount();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.severity = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.count = UInt32Value.decode(reader, reader.uint32()).value;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IncidentSeverityCount {
    return {
      severity: isSet(object.severity) ? incidentSeverityFromJSON(object.severity) : 0,
      count: isSet(object.count) ? Number(object.count) : undefined,
    };
  },

  toJSON(message: IncidentSeverityCount): unknown {
    const obj: any = {};
    if (message.severity !== 0) {
      obj.severity = incidentSeverityToJSON(message.severity);
    }
    if (message.count !== undefined) {
      obj.count = message.count;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<IncidentSeverityCount>, I>>(base?: I): IncidentSeverityCount {
    return IncidentSeverityCount.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IncidentSeverityCount>, I>>(object: I): IncidentSeverityCount {
    const message = createBaseIncidentSeverityCount();
    message.severity = object.severity ?? 0;
    message.count = object.count ?? undefined;
    return message;
  },
};

function createBaseIncidentStatusCount(): IncidentStatusCount {
  return { status: 0, count: undefined };
}

export const IncidentStatusCount = {
  encode(message: IncidentStatusCount, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.status !== 0) {
      writer.uint32(8).int32(message.status);
    }
    if (message.count !== undefined) {
      UInt32Value.encode({ value: message.count! }, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): IncidentStatusCount {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIncidentStatusCount();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.count = UInt32Value.decode(reader, reader.uint32()).value;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IncidentStatusCount {
    return {
      status: isSet(object.status) ? incidentStatusFromJSON(object.status) : 0,
      count: isSet(object.count) ? Number(object.count) : undefined,
    };
  },

  toJSON(message: IncidentStatusCount): unknown {
    const obj: any = {};
    if (message.status !== 0) {
      obj.status = incidentStatusToJSON(message.status);
    }
    if (message.count !== undefined) {
      obj.count = message.count;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<IncidentStatusCount>, I>>(base?: I): IncidentStatusCount {
    return IncidentStatusCount.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IncidentStatusCount>, I>>(object: I): IncidentStatusCount {
    const message = createBaseIncidentStatusCount();
    message.status = object.status ?? 0;
    message.count = object.count ?? undefined;
    return message;
  },
};

function createBaseIncidentStateCount(): IncidentStateCount {
  return { state: 0, count: undefined };
}

export const IncidentStateCount = {
  encode(message: IncidentStateCount, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.state !== 0) {
      writer.uint32(8).int32(message.state);
    }
    if (message.count !== undefined) {
      UInt32Value.encode({ value: message.count! }, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): IncidentStateCount {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIncidentStateCount();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.state = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.count = UInt32Value.decode(reader, reader.uint32()).value;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IncidentStateCount {
    return {
      state: isSet(object.state) ? incidentStateFromJSON(object.state) : 0,
      count: isSet(object.count) ? Number(object.count) : undefined,
    };
  },

  toJSON(message: IncidentStateCount): unknown {
    const obj: any = {};
    if (message.state !== 0) {
      obj.state = incidentStateToJSON(message.state);
    }
    if (message.count !== undefined) {
      obj.count = message.count;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<IncidentStateCount>, I>>(base?: I): IncidentStateCount {
    return IncidentStateCount.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IncidentStateCount>, I>>(object: I): IncidentStateCount {
    const message = createBaseIncidentStateCount();
    message.state = object.state ?? 0;
    message.count = object.count ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
