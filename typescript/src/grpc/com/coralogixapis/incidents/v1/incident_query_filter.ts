// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.176.1
//   protoc               v3.21.12
// source: com/coralogixapis/incidents/v1/incident_query_filter.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { Timestamp } from "../../../../google/protobuf/timestamp";
import { BoolValue, StringValue } from "../../../../google/protobuf/wrappers";
import { IncidentSearchQuery } from "./incident_query";
import { IncidentSeverity, incidentSeverityFromJSON, incidentSeverityToJSON } from "./incident_severity";
import { IncidentState, incidentStateFromJSON, incidentStateToJSON } from "./incident_state";
import { IncidentStatus, incidentStatusFromJSON, incidentStatusToJSON } from "./incident_status";
import { MetaLabel } from "./meta_label";

export const protobufPackage = "com.coralogixapis.incidents.v1";

export interface IncidentQueryFilter {
  assignee: string[];
  status: IncidentStatus[];
  state: IncidentState[];
  severity: IncidentSeverity[];
  contextualLabels: { [key: string]: ContextualLabelValues };
  /**
   * filters all incidents that were open in the given timeframe start time (deprecated, use incident_open_range instead)
   *
   * @deprecated
   */
  startTime:
    | Date
    | undefined;
  /**
   * filters all incidents that were open in the given timeframe end time (deprecated, use incident_open_range instead)
   *
   * @deprecated
   */
  endTime: Date | undefined;
  searchQuery: IncidentSearchQuery | undefined;
  applicationName: string[];
  subsystemName: string[];
  isMuted:
    | boolean
    | undefined;
  /** filters all incidents created at the given time range */
  createdAtRange:
    | TimeRange
    | undefined;
  /** filters all incidents open (alive) at the given time range */
  incidentDurationRange:
    | TimeRange
    | undefined;
  /** filters all incidents with the given meta labels */
  metaLabels: MetaLabel[];
}

export interface IncidentQueryFilter_ContextualLabelsEntry {
  key: string;
  value: ContextualLabelValues | undefined;
}

export interface TimeRange {
  startTime: Date | undefined;
  endTime: Date | undefined;
}

export interface ContextualLabelValues {
  contextualLabelValues: string[];
}

function createBaseIncidentQueryFilter(): IncidentQueryFilter {
  return {
    assignee: [],
    status: [],
    state: [],
    severity: [],
    contextualLabels: {},
    startTime: undefined,
    endTime: undefined,
    searchQuery: undefined,
    applicationName: [],
    subsystemName: [],
    isMuted: undefined,
    createdAtRange: undefined,
    incidentDurationRange: undefined,
    metaLabels: [],
  };
}

export const IncidentQueryFilter = {
  encode(message: IncidentQueryFilter, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.assignee) {
      StringValue.encode({ value: v!! }, writer.uint32(10).fork()).ldelim();
    }
    writer.uint32(18).fork();
    for (const v of message.status) {
      writer.int32(v);
    }
    writer.ldelim();
    writer.uint32(26).fork();
    for (const v of message.state) {
      writer.int32(v);
    }
    writer.ldelim();
    writer.uint32(34).fork();
    for (const v of message.severity) {
      writer.int32(v);
    }
    writer.ldelim();
    Object.entries(message.contextualLabels).forEach(([key, value]) => {
      IncidentQueryFilter_ContextualLabelsEntry.encode({ key: key as any, value }, writer.uint32(42).fork()).ldelim();
    });
    if (message.startTime !== undefined) {
      Timestamp.encode(toTimestamp(message.startTime), writer.uint32(50).fork()).ldelim();
    }
    if (message.endTime !== undefined) {
      Timestamp.encode(toTimestamp(message.endTime), writer.uint32(58).fork()).ldelim();
    }
    if (message.searchQuery !== undefined) {
      IncidentSearchQuery.encode(message.searchQuery, writer.uint32(66).fork()).ldelim();
    }
    for (const v of message.applicationName) {
      StringValue.encode({ value: v!! }, writer.uint32(74).fork()).ldelim();
    }
    for (const v of message.subsystemName) {
      StringValue.encode({ value: v!! }, writer.uint32(82).fork()).ldelim();
    }
    if (message.isMuted !== undefined) {
      BoolValue.encode({ value: message.isMuted! }, writer.uint32(90).fork()).ldelim();
    }
    if (message.createdAtRange !== undefined) {
      TimeRange.encode(message.createdAtRange, writer.uint32(98).fork()).ldelim();
    }
    if (message.incidentDurationRange !== undefined) {
      TimeRange.encode(message.incidentDurationRange, writer.uint32(106).fork()).ldelim();
    }
    for (const v of message.metaLabels) {
      MetaLabel.encode(v!, writer.uint32(114).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): IncidentQueryFilter {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIncidentQueryFilter();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.assignee.push(StringValue.decode(reader, reader.uint32()).value);
          continue;
        case 2:
          if (tag === 16) {
            message.status.push(reader.int32() as any);

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.status.push(reader.int32() as any);
            }

            continue;
          }

          break;
        case 3:
          if (tag === 24) {
            message.state.push(reader.int32() as any);

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.state.push(reader.int32() as any);
            }

            continue;
          }

          break;
        case 4:
          if (tag === 32) {
            message.severity.push(reader.int32() as any);

            continue;
          }

          if (tag === 34) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.severity.push(reader.int32() as any);
            }

            continue;
          }

          break;
        case 5:
          if (tag !== 42) {
            break;
          }

          const entry5 = IncidentQueryFilter_ContextualLabelsEntry.decode(reader, reader.uint32());
          if (entry5.value !== undefined) {
            message.contextualLabels[entry5.key] = entry5.value;
          }
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.startTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.endTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 8:
          if (tag !== 66) {
            break;
          }

          message.searchQuery = IncidentSearchQuery.decode(reader, reader.uint32());
          continue;
        case 9:
          if (tag !== 74) {
            break;
          }

          message.applicationName.push(StringValue.decode(reader, reader.uint32()).value);
          continue;
        case 10:
          if (tag !== 82) {
            break;
          }

          message.subsystemName.push(StringValue.decode(reader, reader.uint32()).value);
          continue;
        case 11:
          if (tag !== 90) {
            break;
          }

          message.isMuted = BoolValue.decode(reader, reader.uint32()).value;
          continue;
        case 12:
          if (tag !== 98) {
            break;
          }

          message.createdAtRange = TimeRange.decode(reader, reader.uint32());
          continue;
        case 13:
          if (tag !== 106) {
            break;
          }

          message.incidentDurationRange = TimeRange.decode(reader, reader.uint32());
          continue;
        case 14:
          if (tag !== 114) {
            break;
          }

          message.metaLabels.push(MetaLabel.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IncidentQueryFilter {
    return {
      assignee: globalThis.Array.isArray(object?.assignee) ? object.assignee.map((e: any) => String(e)) : [],
      status: globalThis.Array.isArray(object?.status) ? object.status.map((e: any) => incidentStatusFromJSON(e)) : [],
      state: globalThis.Array.isArray(object?.state) ? object.state.map((e: any) => incidentStateFromJSON(e)) : [],
      severity: globalThis.Array.isArray(object?.severity)
        ? object.severity.map((e: any) => incidentSeverityFromJSON(e))
        : [],
      contextualLabels: isObject(object.contextualLabels)
        ? Object.entries(object.contextualLabels).reduce<{ [key: string]: ContextualLabelValues }>(
          (acc, [key, value]) => {
            acc[key] = ContextualLabelValues.fromJSON(value);
            return acc;
          },
          {},
        )
        : {},
      startTime: isSet(object.startTime) ? fromJsonTimestamp(object.startTime) : undefined,
      endTime: isSet(object.endTime) ? fromJsonTimestamp(object.endTime) : undefined,
      searchQuery: isSet(object.searchQuery) ? IncidentSearchQuery.fromJSON(object.searchQuery) : undefined,
      applicationName: globalThis.Array.isArray(object?.applicationName)
        ? object.applicationName.map((e: any) => String(e))
        : [],
      subsystemName: globalThis.Array.isArray(object?.subsystemName)
        ? object.subsystemName.map((e: any) => String(e))
        : [],
      isMuted: isSet(object.isMuted) ? Boolean(object.isMuted) : undefined,
      createdAtRange: isSet(object.createdAtRange) ? TimeRange.fromJSON(object.createdAtRange) : undefined,
      incidentDurationRange: isSet(object.incidentDurationRange)
        ? TimeRange.fromJSON(object.incidentDurationRange)
        : undefined,
      metaLabels: globalThis.Array.isArray(object?.metaLabels)
        ? object.metaLabels.map((e: any) => MetaLabel.fromJSON(e))
        : [],
    };
  },

  toJSON(message: IncidentQueryFilter): unknown {
    const obj: any = {};
    if (message.assignee?.length) {
      obj.assignee = message.assignee;
    }
    if (message.status?.length) {
      obj.status = message.status.map((e) => incidentStatusToJSON(e));
    }
    if (message.state?.length) {
      obj.state = message.state.map((e) => incidentStateToJSON(e));
    }
    if (message.severity?.length) {
      obj.severity = message.severity.map((e) => incidentSeverityToJSON(e));
    }
    if (message.contextualLabels) {
      const entries = Object.entries(message.contextualLabels);
      if (entries.length > 0) {
        obj.contextualLabels = {};
        entries.forEach(([k, v]) => {
          obj.contextualLabels[k] = ContextualLabelValues.toJSON(v);
        });
      }
    }
    if (message.startTime !== undefined) {
      obj.startTime = message.startTime.toISOString();
    }
    if (message.endTime !== undefined) {
      obj.endTime = message.endTime.toISOString();
    }
    if (message.searchQuery !== undefined) {
      obj.searchQuery = IncidentSearchQuery.toJSON(message.searchQuery);
    }
    if (message.applicationName?.length) {
      obj.applicationName = message.applicationName;
    }
    if (message.subsystemName?.length) {
      obj.subsystemName = message.subsystemName;
    }
    if (message.isMuted !== undefined) {
      obj.isMuted = message.isMuted;
    }
    if (message.createdAtRange !== undefined) {
      obj.createdAtRange = TimeRange.toJSON(message.createdAtRange);
    }
    if (message.incidentDurationRange !== undefined) {
      obj.incidentDurationRange = TimeRange.toJSON(message.incidentDurationRange);
    }
    if (message.metaLabels?.length) {
      obj.metaLabels = message.metaLabels.map((e) => MetaLabel.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<IncidentQueryFilter>, I>>(base?: I): IncidentQueryFilter {
    return IncidentQueryFilter.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IncidentQueryFilter>, I>>(object: I): IncidentQueryFilter {
    const message = createBaseIncidentQueryFilter();
    message.assignee = object.assignee?.map((e) => e) || [];
    message.status = object.status?.map((e) => e) || [];
    message.state = object.state?.map((e) => e) || [];
    message.severity = object.severity?.map((e) => e) || [];
    message.contextualLabels = Object.entries(object.contextualLabels ?? {}).reduce<
      { [key: string]: ContextualLabelValues }
    >((acc, [key, value]) => {
      if (value !== undefined) {
        acc[key] = ContextualLabelValues.fromPartial(value);
      }
      return acc;
    }, {});
    message.startTime = object.startTime ?? undefined;
    message.endTime = object.endTime ?? undefined;
    message.searchQuery = (object.searchQuery !== undefined && object.searchQuery !== null)
      ? IncidentSearchQuery.fromPartial(object.searchQuery)
      : undefined;
    message.applicationName = object.applicationName?.map((e) => e) || [];
    message.subsystemName = object.subsystemName?.map((e) => e) || [];
    message.isMuted = object.isMuted ?? undefined;
    message.createdAtRange = (object.createdAtRange !== undefined && object.createdAtRange !== null)
      ? TimeRange.fromPartial(object.createdAtRange)
      : undefined;
    message.incidentDurationRange =
      (object.incidentDurationRange !== undefined && object.incidentDurationRange !== null)
        ? TimeRange.fromPartial(object.incidentDurationRange)
        : undefined;
    message.metaLabels = object.metaLabels?.map((e) => MetaLabel.fromPartial(e)) || [];
    return message;
  },
};

function createBaseIncidentQueryFilter_ContextualLabelsEntry(): IncidentQueryFilter_ContextualLabelsEntry {
  return { key: "", value: undefined };
}

export const IncidentQueryFilter_ContextualLabelsEntry = {
  encode(message: IncidentQueryFilter_ContextualLabelsEntry, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      ContextualLabelValues.encode(message.value, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): IncidentQueryFilter_ContextualLabelsEntry {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIncidentQueryFilter_ContextualLabelsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.value = ContextualLabelValues.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IncidentQueryFilter_ContextualLabelsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? ContextualLabelValues.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: IncidentQueryFilter_ContextualLabelsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = ContextualLabelValues.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<IncidentQueryFilter_ContextualLabelsEntry>, I>>(
    base?: I,
  ): IncidentQueryFilter_ContextualLabelsEntry {
    return IncidentQueryFilter_ContextualLabelsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IncidentQueryFilter_ContextualLabelsEntry>, I>>(
    object: I,
  ): IncidentQueryFilter_ContextualLabelsEntry {
    const message = createBaseIncidentQueryFilter_ContextualLabelsEntry();
    message.key = object.key ?? "";
    message.value = (object.value !== undefined && object.value !== null)
      ? ContextualLabelValues.fromPartial(object.value)
      : undefined;
    return message;
  },
};

function createBaseTimeRange(): TimeRange {
  return { startTime: undefined, endTime: undefined };
}

export const TimeRange = {
  encode(message: TimeRange, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.startTime !== undefined) {
      Timestamp.encode(toTimestamp(message.startTime), writer.uint32(10).fork()).ldelim();
    }
    if (message.endTime !== undefined) {
      Timestamp.encode(toTimestamp(message.endTime), writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): TimeRange {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTimeRange();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.startTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.endTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TimeRange {
    return {
      startTime: isSet(object.startTime) ? fromJsonTimestamp(object.startTime) : undefined,
      endTime: isSet(object.endTime) ? fromJsonTimestamp(object.endTime) : undefined,
    };
  },

  toJSON(message: TimeRange): unknown {
    const obj: any = {};
    if (message.startTime !== undefined) {
      obj.startTime = message.startTime.toISOString();
    }
    if (message.endTime !== undefined) {
      obj.endTime = message.endTime.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TimeRange>, I>>(base?: I): TimeRange {
    return TimeRange.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TimeRange>, I>>(object: I): TimeRange {
    const message = createBaseTimeRange();
    message.startTime = object.startTime ?? undefined;
    message.endTime = object.endTime ?? undefined;
    return message;
  },
};

function createBaseContextualLabelValues(): ContextualLabelValues {
  return { contextualLabelValues: [] };
}

export const ContextualLabelValues = {
  encode(message: ContextualLabelValues, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.contextualLabelValues) {
      StringValue.encode({ value: v!! }, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ContextualLabelValues {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseContextualLabelValues();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.contextualLabelValues.push(StringValue.decode(reader, reader.uint32()).value);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ContextualLabelValues {
    return {
      contextualLabelValues: globalThis.Array.isArray(object?.contextualLabelValues)
        ? object.contextualLabelValues.map((e: any) => String(e))
        : [],
    };
  },

  toJSON(message: ContextualLabelValues): unknown {
    const obj: any = {};
    if (message.contextualLabelValues?.length) {
      obj.contextualLabelValues = message.contextualLabelValues;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ContextualLabelValues>, I>>(base?: I): ContextualLabelValues {
    return ContextualLabelValues.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ContextualLabelValues>, I>>(object: I): ContextualLabelValues {
    const message = createBaseContextualLabelValues();
    message.contextualLabelValues = object.contextualLabelValues?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
