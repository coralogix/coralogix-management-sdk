// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.176.1
//   protoc               v3.21.12
// source: com/coralogixapis/incidents/v1/incident_query_filters_values.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { Int32Value, StringValue } from "../../../../google/protobuf/wrappers";
import { IncidentSeverity, incidentSeverityFromJSON, incidentSeverityToJSON } from "./incident_severity";
import { IncidentState, incidentStateFromJSON, incidentStateToJSON } from "./incident_state";
import { IncidentStatus, incidentStatusFromJSON, incidentStatusToJSON } from "./incident_status";
import { MetaLabel } from "./meta_label";

export const protobufPackage = "com.coralogixapis.incidents.v1";

export interface AssigneeWithCount {
  assignee: string | undefined;
  count: number | undefined;
}

export interface IncidentStatusWithCount {
  status: IncidentStatus;
  count: number | undefined;
}

export interface IncidentStateWithCount {
  state: IncidentState;
  count: number | undefined;
}

export interface IncidentSeverityWithCount {
  severity: IncidentSeverity;
  count: number | undefined;
}

export interface IncidentMetaLabelsWithCount {
  metaLabel: MetaLabel | undefined;
  count: number | undefined;
}

export interface IncidentQueryFiltersValues {
  assigneeWithCount: AssigneeWithCount[];
  statusWithCount: IncidentStatusWithCount[];
  stateWithCount: IncidentStateWithCount[];
  severityWithCount: IncidentSeverityWithCount[];
  contextualLabels: { [key: string]: ContextualLabelValuesWithCount };
  metaLabelsWithCount: IncidentMetaLabelsWithCount[];
}

export interface IncidentQueryFiltersValues_ContextualLabelsEntry {
  key: string;
  value: ContextualLabelValuesWithCount | undefined;
}

export interface ContextualLabelValuesWithCount {
  valuesWithCount: ContextualLabelValueWithCount[];
}

export interface ContextualLabelValueWithCount {
  contextualLabelValue: string | undefined;
  count: number | undefined;
}

function createBaseAssigneeWithCount(): AssigneeWithCount {
  return { assignee: undefined, count: undefined };
}

export const AssigneeWithCount = {
  encode(message: AssigneeWithCount, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.assignee !== undefined) {
      StringValue.encode({ value: message.assignee! }, writer.uint32(10).fork()).ldelim();
    }
    if (message.count !== undefined) {
      Int32Value.encode({ value: message.count! }, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): AssigneeWithCount {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAssigneeWithCount();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.assignee = StringValue.decode(reader, reader.uint32()).value;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.count = Int32Value.decode(reader, reader.uint32()).value;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AssigneeWithCount {
    return {
      assignee: isSet(object.assignee) ? String(object.assignee) : undefined,
      count: isSet(object.count) ? Number(object.count) : undefined,
    };
  },

  toJSON(message: AssigneeWithCount): unknown {
    const obj: any = {};
    if (message.assignee !== undefined) {
      obj.assignee = message.assignee;
    }
    if (message.count !== undefined) {
      obj.count = message.count;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AssigneeWithCount>, I>>(base?: I): AssigneeWithCount {
    return AssigneeWithCount.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AssigneeWithCount>, I>>(object: I): AssigneeWithCount {
    const message = createBaseAssigneeWithCount();
    message.assignee = object.assignee ?? undefined;
    message.count = object.count ?? undefined;
    return message;
  },
};

function createBaseIncidentStatusWithCount(): IncidentStatusWithCount {
  return { status: 0, count: undefined };
}

export const IncidentStatusWithCount = {
  encode(message: IncidentStatusWithCount, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.status !== 0) {
      writer.uint32(8).int32(message.status);
    }
    if (message.count !== undefined) {
      Int32Value.encode({ value: message.count! }, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): IncidentStatusWithCount {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIncidentStatusWithCount();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.count = Int32Value.decode(reader, reader.uint32()).value;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IncidentStatusWithCount {
    return {
      status: isSet(object.status) ? incidentStatusFromJSON(object.status) : 0,
      count: isSet(object.count) ? Number(object.count) : undefined,
    };
  },

  toJSON(message: IncidentStatusWithCount): unknown {
    const obj: any = {};
    if (message.status !== 0) {
      obj.status = incidentStatusToJSON(message.status);
    }
    if (message.count !== undefined) {
      obj.count = message.count;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<IncidentStatusWithCount>, I>>(base?: I): IncidentStatusWithCount {
    return IncidentStatusWithCount.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IncidentStatusWithCount>, I>>(object: I): IncidentStatusWithCount {
    const message = createBaseIncidentStatusWithCount();
    message.status = object.status ?? 0;
    message.count = object.count ?? undefined;
    return message;
  },
};

function createBaseIncidentStateWithCount(): IncidentStateWithCount {
  return { state: 0, count: undefined };
}

export const IncidentStateWithCount = {
  encode(message: IncidentStateWithCount, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.state !== 0) {
      writer.uint32(8).int32(message.state);
    }
    if (message.count !== undefined) {
      Int32Value.encode({ value: message.count! }, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): IncidentStateWithCount {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIncidentStateWithCount();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.state = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.count = Int32Value.decode(reader, reader.uint32()).value;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IncidentStateWithCount {
    return {
      state: isSet(object.state) ? incidentStateFromJSON(object.state) : 0,
      count: isSet(object.count) ? Number(object.count) : undefined,
    };
  },

  toJSON(message: IncidentStateWithCount): unknown {
    const obj: any = {};
    if (message.state !== 0) {
      obj.state = incidentStateToJSON(message.state);
    }
    if (message.count !== undefined) {
      obj.count = message.count;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<IncidentStateWithCount>, I>>(base?: I): IncidentStateWithCount {
    return IncidentStateWithCount.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IncidentStateWithCount>, I>>(object: I): IncidentStateWithCount {
    const message = createBaseIncidentStateWithCount();
    message.state = object.state ?? 0;
    message.count = object.count ?? undefined;
    return message;
  },
};

function createBaseIncidentSeverityWithCount(): IncidentSeverityWithCount {
  return { severity: 0, count: undefined };
}

export const IncidentSeverityWithCount = {
  encode(message: IncidentSeverityWithCount, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.severity !== 0) {
      writer.uint32(8).int32(message.severity);
    }
    if (message.count !== undefined) {
      Int32Value.encode({ value: message.count! }, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): IncidentSeverityWithCount {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIncidentSeverityWithCount();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.severity = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.count = Int32Value.decode(reader, reader.uint32()).value;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IncidentSeverityWithCount {
    return {
      severity: isSet(object.severity) ? incidentSeverityFromJSON(object.severity) : 0,
      count: isSet(object.count) ? Number(object.count) : undefined,
    };
  },

  toJSON(message: IncidentSeverityWithCount): unknown {
    const obj: any = {};
    if (message.severity !== 0) {
      obj.severity = incidentSeverityToJSON(message.severity);
    }
    if (message.count !== undefined) {
      obj.count = message.count;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<IncidentSeverityWithCount>, I>>(base?: I): IncidentSeverityWithCount {
    return IncidentSeverityWithCount.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IncidentSeverityWithCount>, I>>(object: I): IncidentSeverityWithCount {
    const message = createBaseIncidentSeverityWithCount();
    message.severity = object.severity ?? 0;
    message.count = object.count ?? undefined;
    return message;
  },
};

function createBaseIncidentMetaLabelsWithCount(): IncidentMetaLabelsWithCount {
  return { metaLabel: undefined, count: undefined };
}

export const IncidentMetaLabelsWithCount = {
  encode(message: IncidentMetaLabelsWithCount, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.metaLabel !== undefined) {
      MetaLabel.encode(message.metaLabel, writer.uint32(10).fork()).ldelim();
    }
    if (message.count !== undefined) {
      Int32Value.encode({ value: message.count! }, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): IncidentMetaLabelsWithCount {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIncidentMetaLabelsWithCount();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.metaLabel = MetaLabel.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.count = Int32Value.decode(reader, reader.uint32()).value;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IncidentMetaLabelsWithCount {
    return {
      metaLabel: isSet(object.metaLabel) ? MetaLabel.fromJSON(object.metaLabel) : undefined,
      count: isSet(object.count) ? Number(object.count) : undefined,
    };
  },

  toJSON(message: IncidentMetaLabelsWithCount): unknown {
    const obj: any = {};
    if (message.metaLabel !== undefined) {
      obj.metaLabel = MetaLabel.toJSON(message.metaLabel);
    }
    if (message.count !== undefined) {
      obj.count = message.count;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<IncidentMetaLabelsWithCount>, I>>(base?: I): IncidentMetaLabelsWithCount {
    return IncidentMetaLabelsWithCount.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IncidentMetaLabelsWithCount>, I>>(object: I): IncidentMetaLabelsWithCount {
    const message = createBaseIncidentMetaLabelsWithCount();
    message.metaLabel = (object.metaLabel !== undefined && object.metaLabel !== null)
      ? MetaLabel.fromPartial(object.metaLabel)
      : undefined;
    message.count = object.count ?? undefined;
    return message;
  },
};

function createBaseIncidentQueryFiltersValues(): IncidentQueryFiltersValues {
  return {
    assigneeWithCount: [],
    statusWithCount: [],
    stateWithCount: [],
    severityWithCount: [],
    contextualLabels: {},
    metaLabelsWithCount: [],
  };
}

export const IncidentQueryFiltersValues = {
  encode(message: IncidentQueryFiltersValues, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.assigneeWithCount) {
      AssigneeWithCount.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    for (const v of message.statusWithCount) {
      IncidentStatusWithCount.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    for (const v of message.stateWithCount) {
      IncidentStateWithCount.encode(v!, writer.uint32(26).fork()).ldelim();
    }
    for (const v of message.severityWithCount) {
      IncidentSeverityWithCount.encode(v!, writer.uint32(34).fork()).ldelim();
    }
    Object.entries(message.contextualLabels).forEach(([key, value]) => {
      IncidentQueryFiltersValues_ContextualLabelsEntry.encode({ key: key as any, value }, writer.uint32(42).fork())
        .ldelim();
    });
    for (const v of message.metaLabelsWithCount) {
      IncidentMetaLabelsWithCount.encode(v!, writer.uint32(50).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): IncidentQueryFiltersValues {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIncidentQueryFiltersValues();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.assigneeWithCount.push(AssigneeWithCount.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.statusWithCount.push(IncidentStatusWithCount.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.stateWithCount.push(IncidentStateWithCount.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.severityWithCount.push(IncidentSeverityWithCount.decode(reader, reader.uint32()));
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          const entry5 = IncidentQueryFiltersValues_ContextualLabelsEntry.decode(reader, reader.uint32());
          if (entry5.value !== undefined) {
            message.contextualLabels[entry5.key] = entry5.value;
          }
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.metaLabelsWithCount.push(IncidentMetaLabelsWithCount.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IncidentQueryFiltersValues {
    return {
      assigneeWithCount: globalThis.Array.isArray(object?.assigneeWithCount)
        ? object.assigneeWithCount.map((e: any) => AssigneeWithCount.fromJSON(e))
        : [],
      statusWithCount: globalThis.Array.isArray(object?.statusWithCount)
        ? object.statusWithCount.map((e: any) => IncidentStatusWithCount.fromJSON(e))
        : [],
      stateWithCount: globalThis.Array.isArray(object?.stateWithCount)
        ? object.stateWithCount.map((e: any) => IncidentStateWithCount.fromJSON(e))
        : [],
      severityWithCount: globalThis.Array.isArray(object?.severityWithCount)
        ? object.severityWithCount.map((e: any) => IncidentSeverityWithCount.fromJSON(e))
        : [],
      contextualLabels: isObject(object.contextualLabels)
        ? Object.entries(object.contextualLabels).reduce<{ [key: string]: ContextualLabelValuesWithCount }>(
          (acc, [key, value]) => {
            acc[key] = ContextualLabelValuesWithCount.fromJSON(value);
            return acc;
          },
          {},
        )
        : {},
      metaLabelsWithCount: globalThis.Array.isArray(object?.metaLabelsWithCount)
        ? object.metaLabelsWithCount.map((e: any) => IncidentMetaLabelsWithCount.fromJSON(e))
        : [],
    };
  },

  toJSON(message: IncidentQueryFiltersValues): unknown {
    const obj: any = {};
    if (message.assigneeWithCount?.length) {
      obj.assigneeWithCount = message.assigneeWithCount.map((e) => AssigneeWithCount.toJSON(e));
    }
    if (message.statusWithCount?.length) {
      obj.statusWithCount = message.statusWithCount.map((e) => IncidentStatusWithCount.toJSON(e));
    }
    if (message.stateWithCount?.length) {
      obj.stateWithCount = message.stateWithCount.map((e) => IncidentStateWithCount.toJSON(e));
    }
    if (message.severityWithCount?.length) {
      obj.severityWithCount = message.severityWithCount.map((e) => IncidentSeverityWithCount.toJSON(e));
    }
    if (message.contextualLabels) {
      const entries = Object.entries(message.contextualLabels);
      if (entries.length > 0) {
        obj.contextualLabels = {};
        entries.forEach(([k, v]) => {
          obj.contextualLabels[k] = ContextualLabelValuesWithCount.toJSON(v);
        });
      }
    }
    if (message.metaLabelsWithCount?.length) {
      obj.metaLabelsWithCount = message.metaLabelsWithCount.map((e) => IncidentMetaLabelsWithCount.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<IncidentQueryFiltersValues>, I>>(base?: I): IncidentQueryFiltersValues {
    return IncidentQueryFiltersValues.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IncidentQueryFiltersValues>, I>>(object: I): IncidentQueryFiltersValues {
    const message = createBaseIncidentQueryFiltersValues();
    message.assigneeWithCount = object.assigneeWithCount?.map((e) => AssigneeWithCount.fromPartial(e)) || [];
    message.statusWithCount = object.statusWithCount?.map((e) => IncidentStatusWithCount.fromPartial(e)) || [];
    message.stateWithCount = object.stateWithCount?.map((e) => IncidentStateWithCount.fromPartial(e)) || [];
    message.severityWithCount = object.severityWithCount?.map((e) => IncidentSeverityWithCount.fromPartial(e)) || [];
    message.contextualLabels = Object.entries(object.contextualLabels ?? {}).reduce<
      { [key: string]: ContextualLabelValuesWithCount }
    >((acc, [key, value]) => {
      if (value !== undefined) {
        acc[key] = ContextualLabelValuesWithCount.fromPartial(value);
      }
      return acc;
    }, {});
    message.metaLabelsWithCount = object.metaLabelsWithCount?.map((e) => IncidentMetaLabelsWithCount.fromPartial(e)) ||
      [];
    return message;
  },
};

function createBaseIncidentQueryFiltersValues_ContextualLabelsEntry(): IncidentQueryFiltersValues_ContextualLabelsEntry {
  return { key: "", value: undefined };
}

export const IncidentQueryFiltersValues_ContextualLabelsEntry = {
  encode(
    message: IncidentQueryFiltersValues_ContextualLabelsEntry,
    writer: _m0.Writer = _m0.Writer.create(),
  ): _m0.Writer {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      ContextualLabelValuesWithCount.encode(message.value, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): IncidentQueryFiltersValues_ContextualLabelsEntry {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIncidentQueryFiltersValues_ContextualLabelsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.value = ContextualLabelValuesWithCount.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IncidentQueryFiltersValues_ContextualLabelsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? ContextualLabelValuesWithCount.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: IncidentQueryFiltersValues_ContextualLabelsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = ContextualLabelValuesWithCount.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<IncidentQueryFiltersValues_ContextualLabelsEntry>, I>>(
    base?: I,
  ): IncidentQueryFiltersValues_ContextualLabelsEntry {
    return IncidentQueryFiltersValues_ContextualLabelsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IncidentQueryFiltersValues_ContextualLabelsEntry>, I>>(
    object: I,
  ): IncidentQueryFiltersValues_ContextualLabelsEntry {
    const message = createBaseIncidentQueryFiltersValues_ContextualLabelsEntry();
    message.key = object.key ?? "";
    message.value = (object.value !== undefined && object.value !== null)
      ? ContextualLabelValuesWithCount.fromPartial(object.value)
      : undefined;
    return message;
  },
};

function createBaseContextualLabelValuesWithCount(): ContextualLabelValuesWithCount {
  return { valuesWithCount: [] };
}

export const ContextualLabelValuesWithCount = {
  encode(message: ContextualLabelValuesWithCount, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.valuesWithCount) {
      ContextualLabelValueWithCount.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ContextualLabelValuesWithCount {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseContextualLabelValuesWithCount();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.valuesWithCount.push(ContextualLabelValueWithCount.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ContextualLabelValuesWithCount {
    return {
      valuesWithCount: globalThis.Array.isArray(object?.valuesWithCount)
        ? object.valuesWithCount.map((e: any) => ContextualLabelValueWithCount.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ContextualLabelValuesWithCount): unknown {
    const obj: any = {};
    if (message.valuesWithCount?.length) {
      obj.valuesWithCount = message.valuesWithCount.map((e) => ContextualLabelValueWithCount.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ContextualLabelValuesWithCount>, I>>(base?: I): ContextualLabelValuesWithCount {
    return ContextualLabelValuesWithCount.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ContextualLabelValuesWithCount>, I>>(
    object: I,
  ): ContextualLabelValuesWithCount {
    const message = createBaseContextualLabelValuesWithCount();
    message.valuesWithCount = object.valuesWithCount?.map((e) => ContextualLabelValueWithCount.fromPartial(e)) || [];
    return message;
  },
};

function createBaseContextualLabelValueWithCount(): ContextualLabelValueWithCount {
  return { contextualLabelValue: undefined, count: undefined };
}

export const ContextualLabelValueWithCount = {
  encode(message: ContextualLabelValueWithCount, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.contextualLabelValue !== undefined) {
      StringValue.encode({ value: message.contextualLabelValue! }, writer.uint32(10).fork()).ldelim();
    }
    if (message.count !== undefined) {
      Int32Value.encode({ value: message.count! }, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ContextualLabelValueWithCount {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseContextualLabelValueWithCount();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.contextualLabelValue = StringValue.decode(reader, reader.uint32()).value;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.count = Int32Value.decode(reader, reader.uint32()).value;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ContextualLabelValueWithCount {
    return {
      contextualLabelValue: isSet(object.contextualLabelValue) ? String(object.contextualLabelValue) : undefined,
      count: isSet(object.count) ? Number(object.count) : undefined,
    };
  },

  toJSON(message: ContextualLabelValueWithCount): unknown {
    const obj: any = {};
    if (message.contextualLabelValue !== undefined) {
      obj.contextualLabelValue = message.contextualLabelValue;
    }
    if (message.count !== undefined) {
      obj.count = message.count;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ContextualLabelValueWithCount>, I>>(base?: I): ContextualLabelValueWithCount {
    return ContextualLabelValueWithCount.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ContextualLabelValueWithCount>, I>>(
    object: I,
  ): ContextualLabelValueWithCount {
    const message = createBaseContextualLabelValueWithCount();
    message.contextualLabelValue = object.contextualLabelValue ?? undefined;
    message.count = object.count ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
