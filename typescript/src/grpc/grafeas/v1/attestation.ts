// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.176.1
//   protoc               v3.21.12
// source: grafeas/v1/attestation.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { Signature } from "./common";

export const protobufPackage = "grafeas.v1";

/**
 * Note kind that represents a logical attestation "role" or "authority". For
 * example, an organization might have one `Authority` for "QA" and one for
 * "build". This note is intended to act strictly as a grouping mechanism for
 * the attached occurrences (Attestations). This grouping mechanism also
 * provides a security boundary, since IAM ACLs gate the ability for a principle
 * to attach an occurrence to a given note. It also provides a single point of
 * lookup to find all attached attestation occurrences, even if they don't all
 * live in the same project.
 */
export interface AttestationNote {
  /** Hint hints at the purpose of the attestation authority. */
  hint: AttestationNote_Hint | undefined;
}

/**
 * This submessage provides human-readable hints about the purpose of the
 * authority. Because the name of a note acts as its resource reference, it is
 * important to disambiguate the canonical name of the Note (which might be a
 * UUID for security purposes) from "readable" names more suitable for debug
 * output. Note that these hints should not be used to look up authorities in
 * security sensitive contexts, such as when looking up attestations to
 * verify.
 */
export interface AttestationNote_Hint {
  /**
   * Required. The human readable name of this attestation authority, for
   * example "qa".
   */
  humanReadableName: string;
}

export interface Jwt {
  /**
   * The compact encoding of a JWS, which is always three base64 encoded strings
   * joined by periods. For details, see:
   * https://tools.ietf.org/html/rfc7515.html#section-3.1
   */
  compactJwt: string;
}

/**
 * Occurrence that represents a single "attestation". The authenticity of an
 * attestation can be verified using the attached signature. If the verifier
 * trusts the public key of the signer, then verifying the signature is
 * sufficient to establish trust. In this circumstance, the authority to which
 * this attestation is attached is primarily useful for lookup (how to find
 * this attestation if you already know the authority and artifact to be
 * verified) and intent (for which authority this attestation was intended to
 * sign.
 */
export interface AttestationOccurrence {
  /**
   * Required. The serialized payload that is verified by one or more
   * `signatures`.
   */
  serializedPayload: Uint8Array;
  /**
   * One or more signatures over `serialized_payload`.  Verifier implementations
   * should consider this attestation message verified if at least one
   * `signature` verifies `serialized_payload`.  See `Signature` in common.proto
   * for more details on signature structure and verification.
   */
  signatures: Signature[];
  /**
   * One or more JWTs encoding a self-contained attestation.
   * Each JWT encodes the payload that it verifies within the JWT itself.
   * Verifier implementation SHOULD ignore the `serialized_payload` field
   * when verifying these JWTs.
   * If only JWTs are present on this AttestationOccurrence, then the
   * `serialized_payload` SHOULD be left empty.
   * Each JWT SHOULD encode a claim specific to the `resource_uri` of this
   * Occurrence, but this is not validated by Grafeas metadata API
   * implementations.  The JWT itself is opaque to Grafeas.
   */
  jwts: Jwt[];
}

function createBaseAttestationNote(): AttestationNote {
  return { hint: undefined };
}

export const AttestationNote = {
  encode(message: AttestationNote, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.hint !== undefined) {
      AttestationNote_Hint.encode(message.hint, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): AttestationNote {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAttestationNote();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.hint = AttestationNote_Hint.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AttestationNote {
    return { hint: isSet(object.hint) ? AttestationNote_Hint.fromJSON(object.hint) : undefined };
  },

  toJSON(message: AttestationNote): unknown {
    const obj: any = {};
    if (message.hint !== undefined) {
      obj.hint = AttestationNote_Hint.toJSON(message.hint);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AttestationNote>, I>>(base?: I): AttestationNote {
    return AttestationNote.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AttestationNote>, I>>(object: I): AttestationNote {
    const message = createBaseAttestationNote();
    message.hint = (object.hint !== undefined && object.hint !== null)
      ? AttestationNote_Hint.fromPartial(object.hint)
      : undefined;
    return message;
  },
};

function createBaseAttestationNote_Hint(): AttestationNote_Hint {
  return { humanReadableName: "" };
}

export const AttestationNote_Hint = {
  encode(message: AttestationNote_Hint, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.humanReadableName !== "") {
      writer.uint32(10).string(message.humanReadableName);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): AttestationNote_Hint {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAttestationNote_Hint();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.humanReadableName = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AttestationNote_Hint {
    return { humanReadableName: isSet(object.humanReadableName) ? globalThis.String(object.humanReadableName) : "" };
  },

  toJSON(message: AttestationNote_Hint): unknown {
    const obj: any = {};
    if (message.humanReadableName !== "") {
      obj.humanReadableName = message.humanReadableName;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AttestationNote_Hint>, I>>(base?: I): AttestationNote_Hint {
    return AttestationNote_Hint.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AttestationNote_Hint>, I>>(object: I): AttestationNote_Hint {
    const message = createBaseAttestationNote_Hint();
    message.humanReadableName = object.humanReadableName ?? "";
    return message;
  },
};

function createBaseJwt(): Jwt {
  return { compactJwt: "" };
}

export const Jwt = {
  encode(message: Jwt, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.compactJwt !== "") {
      writer.uint32(10).string(message.compactJwt);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Jwt {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJwt();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.compactJwt = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Jwt {
    return { compactJwt: isSet(object.compactJwt) ? globalThis.String(object.compactJwt) : "" };
  },

  toJSON(message: Jwt): unknown {
    const obj: any = {};
    if (message.compactJwt !== "") {
      obj.compactJwt = message.compactJwt;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Jwt>, I>>(base?: I): Jwt {
    return Jwt.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Jwt>, I>>(object: I): Jwt {
    const message = createBaseJwt();
    message.compactJwt = object.compactJwt ?? "";
    return message;
  },
};

function createBaseAttestationOccurrence(): AttestationOccurrence {
  return { serializedPayload: new Uint8Array(0), signatures: [], jwts: [] };
}

export const AttestationOccurrence = {
  encode(message: AttestationOccurrence, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.serializedPayload.length !== 0) {
      writer.uint32(10).bytes(message.serializedPayload);
    }
    for (const v of message.signatures) {
      Signature.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    for (const v of message.jwts) {
      Jwt.encode(v!, writer.uint32(26).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): AttestationOccurrence {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAttestationOccurrence();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.serializedPayload = reader.bytes();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.signatures.push(Signature.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.jwts.push(Jwt.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AttestationOccurrence {
    return {
      serializedPayload: isSet(object.serializedPayload)
        ? bytesFromBase64(object.serializedPayload)
        : new Uint8Array(0),
      signatures: globalThis.Array.isArray(object?.signatures)
        ? object.signatures.map((e: any) => Signature.fromJSON(e))
        : [],
      jwts: globalThis.Array.isArray(object?.jwts) ? object.jwts.map((e: any) => Jwt.fromJSON(e)) : [],
    };
  },

  toJSON(message: AttestationOccurrence): unknown {
    const obj: any = {};
    if (message.serializedPayload.length !== 0) {
      obj.serializedPayload = base64FromBytes(message.serializedPayload);
    }
    if (message.signatures?.length) {
      obj.signatures = message.signatures.map((e) => Signature.toJSON(e));
    }
    if (message.jwts?.length) {
      obj.jwts = message.jwts.map((e) => Jwt.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AttestationOccurrence>, I>>(base?: I): AttestationOccurrence {
    return AttestationOccurrence.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AttestationOccurrence>, I>>(object: I): AttestationOccurrence {
    const message = createBaseAttestationOccurrence();
    message.serializedPayload = object.serializedPayload ?? new Uint8Array(0);
    message.signatures = object.signatures?.map((e) => Signature.fromPartial(e)) || [];
    message.jwts = object.jwts?.map((e) => Jwt.fromPartial(e)) || [];
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
