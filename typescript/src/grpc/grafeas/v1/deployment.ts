// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.176.1
//   protoc               v3.21.12
// source: grafeas/v1/deployment.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { Timestamp } from "../../google/protobuf/timestamp";

export const protobufPackage = "grafeas.v1";

/** An artifact that can be deployed in some runtime. */
export interface DeploymentNote {
  /** Required. Resource URI for the artifact being deployed. */
  resourceUri: string[];
}

/** The period during which some deployable was active in a runtime. */
export interface DeploymentOccurrence {
  /** Identity of the user that triggered this deployment. */
  userEmail: string;
  /** Required. Beginning of the lifetime of this deployment. */
  deployTime:
    | Date
    | undefined;
  /** End of the lifetime of this deployment. */
  undeployTime:
    | Date
    | undefined;
  /** Configuration used to create this deployment. */
  config: string;
  /** Address of the runtime element hosting this deployment. */
  address: string;
  /**
   * Output only. Resource URI for the artifact being deployed taken from
   * the deployable field with the same name.
   */
  resourceUri: string[];
  /** Platform hosting this deployment. */
  platform: DeploymentOccurrence_Platform;
}

/** Types of platforms. */
export enum DeploymentOccurrence_Platform {
  /** PLATFORM_UNSPECIFIED - Unknown. */
  PLATFORM_UNSPECIFIED = 0,
  /** GKE - Google Container Engine. */
  GKE = 1,
  /** FLEX - Google App Engine: Flexible Environment. */
  FLEX = 2,
  /** CUSTOM - Custom user-defined platform. */
  CUSTOM = 3,
  UNRECOGNIZED = -1,
}

export function deploymentOccurrence_PlatformFromJSON(object: any): DeploymentOccurrence_Platform {
  switch (object) {
    case 0:
    case "PLATFORM_UNSPECIFIED":
      return DeploymentOccurrence_Platform.PLATFORM_UNSPECIFIED;
    case 1:
    case "GKE":
      return DeploymentOccurrence_Platform.GKE;
    case 2:
    case "FLEX":
      return DeploymentOccurrence_Platform.FLEX;
    case 3:
    case "CUSTOM":
      return DeploymentOccurrence_Platform.CUSTOM;
    case -1:
    case "UNRECOGNIZED":
    default:
      return DeploymentOccurrence_Platform.UNRECOGNIZED;
  }
}

export function deploymentOccurrence_PlatformToJSON(object: DeploymentOccurrence_Platform): string {
  switch (object) {
    case DeploymentOccurrence_Platform.PLATFORM_UNSPECIFIED:
      return "PLATFORM_UNSPECIFIED";
    case DeploymentOccurrence_Platform.GKE:
      return "GKE";
    case DeploymentOccurrence_Platform.FLEX:
      return "FLEX";
    case DeploymentOccurrence_Platform.CUSTOM:
      return "CUSTOM";
    case DeploymentOccurrence_Platform.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

function createBaseDeploymentNote(): DeploymentNote {
  return { resourceUri: [] };
}

export const DeploymentNote = {
  encode(message: DeploymentNote, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.resourceUri) {
      writer.uint32(10).string(v!);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): DeploymentNote {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeploymentNote();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.resourceUri.push(reader.string());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeploymentNote {
    return {
      resourceUri: globalThis.Array.isArray(object?.resourceUri)
        ? object.resourceUri.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: DeploymentNote): unknown {
    const obj: any = {};
    if (message.resourceUri?.length) {
      obj.resourceUri = message.resourceUri;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeploymentNote>, I>>(base?: I): DeploymentNote {
    return DeploymentNote.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeploymentNote>, I>>(object: I): DeploymentNote {
    const message = createBaseDeploymentNote();
    message.resourceUri = object.resourceUri?.map((e) => e) || [];
    return message;
  },
};

function createBaseDeploymentOccurrence(): DeploymentOccurrence {
  return {
    userEmail: "",
    deployTime: undefined,
    undeployTime: undefined,
    config: "",
    address: "",
    resourceUri: [],
    platform: 0,
  };
}

export const DeploymentOccurrence = {
  encode(message: DeploymentOccurrence, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.userEmail !== "") {
      writer.uint32(10).string(message.userEmail);
    }
    if (message.deployTime !== undefined) {
      Timestamp.encode(toTimestamp(message.deployTime), writer.uint32(18).fork()).ldelim();
    }
    if (message.undeployTime !== undefined) {
      Timestamp.encode(toTimestamp(message.undeployTime), writer.uint32(26).fork()).ldelim();
    }
    if (message.config !== "") {
      writer.uint32(34).string(message.config);
    }
    if (message.address !== "") {
      writer.uint32(42).string(message.address);
    }
    for (const v of message.resourceUri) {
      writer.uint32(50).string(v!);
    }
    if (message.platform !== 0) {
      writer.uint32(56).int32(message.platform);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): DeploymentOccurrence {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeploymentOccurrence();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.userEmail = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.deployTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.undeployTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.config = reader.string();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.address = reader.string();
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.resourceUri.push(reader.string());
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.platform = reader.int32() as any;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeploymentOccurrence {
    return {
      userEmail: isSet(object.userEmail) ? globalThis.String(object.userEmail) : "",
      deployTime: isSet(object.deployTime) ? fromJsonTimestamp(object.deployTime) : undefined,
      undeployTime: isSet(object.undeployTime) ? fromJsonTimestamp(object.undeployTime) : undefined,
      config: isSet(object.config) ? globalThis.String(object.config) : "",
      address: isSet(object.address) ? globalThis.String(object.address) : "",
      resourceUri: globalThis.Array.isArray(object?.resourceUri)
        ? object.resourceUri.map((e: any) => globalThis.String(e))
        : [],
      platform: isSet(object.platform) ? deploymentOccurrence_PlatformFromJSON(object.platform) : 0,
    };
  },

  toJSON(message: DeploymentOccurrence): unknown {
    const obj: any = {};
    if (message.userEmail !== "") {
      obj.userEmail = message.userEmail;
    }
    if (message.deployTime !== undefined) {
      obj.deployTime = message.deployTime.toISOString();
    }
    if (message.undeployTime !== undefined) {
      obj.undeployTime = message.undeployTime.toISOString();
    }
    if (message.config !== "") {
      obj.config = message.config;
    }
    if (message.address !== "") {
      obj.address = message.address;
    }
    if (message.resourceUri?.length) {
      obj.resourceUri = message.resourceUri;
    }
    if (message.platform !== 0) {
      obj.platform = deploymentOccurrence_PlatformToJSON(message.platform);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeploymentOccurrence>, I>>(base?: I): DeploymentOccurrence {
    return DeploymentOccurrence.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeploymentOccurrence>, I>>(object: I): DeploymentOccurrence {
    const message = createBaseDeploymentOccurrence();
    message.userEmail = object.userEmail ?? "";
    message.deployTime = object.deployTime ?? undefined;
    message.undeployTime = object.undeployTime ?? undefined;
    message.config = object.config ?? "";
    message.address = object.address ?? "";
    message.resourceUri = object.resourceUri?.map((e) => e) || [];
    message.platform = object.platform ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
