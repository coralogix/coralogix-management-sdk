// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.176.1
//   protoc               v3.21.12
// source: grafeas/v1/grafeas.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { Empty } from "../../google/protobuf/empty";
import { FieldMask } from "../../google/protobuf/field_mask";
import { Timestamp } from "../../google/protobuf/timestamp";
import { AttestationNote, AttestationOccurrence } from "./attestation";
import { BuildNote, BuildOccurrence } from "./build";
import { Envelope, NoteKind, noteKindFromJSON, noteKindToJSON, RelatedUrl } from "./common";
import { ComplianceNote, ComplianceOccurrence } from "./compliance";
import { DeploymentNote, DeploymentOccurrence } from "./deployment";
import { DiscoveryNote, DiscoveryOccurrence } from "./discovery";
import { DSSEAttestationNote, DSSEAttestationOccurrence } from "./dsse_attestation";
import { ImageNote, ImageOccurrence } from "./image";
import { PackageNote, PackageOccurrence } from "./package";
import { SBOMReferenceNote, SBOMReferenceOccurrence } from "./sbom";
import { UpgradeNote, UpgradeOccurrence } from "./upgrade";
import { VulnerabilityAssessmentNote } from "./vex";
import { VulnerabilityNote, VulnerabilityOccurrence } from "./vulnerability";

export const protobufPackage = "grafeas.v1";

/** An instance of an analysis type that has been found on a resource. */
export interface Occurrence {
  /**
   * Output only. The name of the occurrence in the form of
   * `projects/[PROJECT_ID]/occurrences/[OCCURRENCE_ID]`.
   */
  name: string;
  /**
   * Required. Immutable. A URI that represents the resource for which the
   * occurrence applies. For example,
   * `https://gcr.io/project/image@sha256:123abc` for a Docker image.
   */
  resourceUri: string;
  /**
   * Required. Immutable. The analysis note associated with this occurrence, in
   * the form of `projects/[PROVIDER_ID]/notes/[NOTE_ID]`. This field can be
   * used as a filter in list requests.
   */
  noteName: string;
  /**
   * Output only. This explicitly denotes which of the occurrence details are
   * specified. This field can be used as a filter in list requests.
   */
  kind: NoteKind;
  /** A description of actions that can be taken to remedy the note. */
  remediation: string;
  /** Output only. The time this occurrence was created. */
  createTime:
    | Date
    | undefined;
  /** Output only. The time this occurrence was last updated. */
  updateTime:
    | Date
    | undefined;
  /** Describes a security vulnerability. */
  vulnerability?:
    | VulnerabilityOccurrence
    | undefined;
  /** Describes a verifiable build. */
  build?:
    | BuildOccurrence
    | undefined;
  /**
   * Describes how this resource derives from the basis in the associated
   * note.
   */
  image?:
    | ImageOccurrence
    | undefined;
  /** Describes the installation of a package on the linked resource. */
  package?:
    | PackageOccurrence
    | undefined;
  /** Describes the deployment of an artifact on a runtime. */
  deployment?:
    | DeploymentOccurrence
    | undefined;
  /** Describes when a resource was discovered. */
  discovery?:
    | DiscoveryOccurrence
    | undefined;
  /** Describes an attestation of an artifact. */
  attestation?:
    | AttestationOccurrence
    | undefined;
  /** Describes an available package upgrade on the linked resource. */
  upgrade?:
    | UpgradeOccurrence
    | undefined;
  /** Describes a compliance violation on a linked resource. */
  compliance?:
    | ComplianceOccurrence
    | undefined;
  /** Describes an attestation of an artifact using dsse. */
  dsseAttestation?:
    | DSSEAttestationOccurrence
    | undefined;
  /** Describes a specific SBOM reference occurrences. */
  sbomReference?:
    | SBOMReferenceOccurrence
    | undefined;
  /** https://github.com/secure-systems-lab/dsse */
  envelope: Envelope | undefined;
}

/** A type of analysis that can be done for a resource. */
export interface Note {
  /**
   * Output only. The name of the note in the form of
   * `projects/[PROVIDER_ID]/notes/[NOTE_ID]`.
   */
  name: string;
  /** A one sentence description of this note. */
  shortDescription: string;
  /** A detailed description of this note. */
  longDescription: string;
  /**
   * Output only. The type of analysis. This field can be used as a filter in
   * list requests.
   */
  kind: NoteKind;
  /** URLs associated with this note. */
  relatedUrl: RelatedUrl[];
  /** Time of expiration for this note. Empty if note does not expire. */
  expirationTime:
    | Date
    | undefined;
  /**
   * Output only. The time this note was created. This field can be used as a
   * filter in list requests.
   */
  createTime:
    | Date
    | undefined;
  /**
   * Output only. The time this note was last updated. This field can be used as
   * a filter in list requests.
   */
  updateTime:
    | Date
    | undefined;
  /** Other notes related to this note. */
  relatedNoteNames: string[];
  /** A note describing a package vulnerability. */
  vulnerability?:
    | VulnerabilityNote
    | undefined;
  /** A note describing build provenance for a verifiable build. */
  build?:
    | BuildNote
    | undefined;
  /** A note describing a base image. */
  image?:
    | ImageNote
    | undefined;
  /** A note describing a package hosted by various package managers. */
  package?:
    | PackageNote
    | undefined;
  /** A note describing something that can be deployed. */
  deployment?:
    | DeploymentNote
    | undefined;
  /** A note describing the initial analysis of a resource. */
  discovery?:
    | DiscoveryNote
    | undefined;
  /** A note describing an attestation role. */
  attestation?:
    | AttestationNote
    | undefined;
  /** A note describing available package upgrades. */
  upgrade?:
    | UpgradeNote
    | undefined;
  /** A note describing a compliance check. */
  compliance?:
    | ComplianceNote
    | undefined;
  /** A note describing a dsse attestation note. */
  dsseAttestation?:
    | DSSEAttestationNote
    | undefined;
  /** A note describing a vulnerability assessment. */
  vulnerabilityAssessment?:
    | VulnerabilityAssessmentNote
    | undefined;
  /** A note describing an SBOM reference. */
  sbomReference?: SBOMReferenceNote | undefined;
}

/** Request to get an occurrence. */
export interface GetOccurrenceRequest {
  /**
   * The name of the occurrence in the form of
   * `projects/[PROJECT_ID]/occurrences/[OCCURRENCE_ID]`.
   */
  name: string;
}

/** Request to list occurrences. */
export interface ListOccurrencesRequest {
  /**
   * The name of the project to list occurrences for in the form of
   * `projects/[PROJECT_ID]`.
   */
  parent: string;
  /** The filter expression. */
  filter: string;
  /**
   * Number of occurrences to return in the list. Must be positive. Max allowed
   * page size is 1000. If not specified, page size defaults to 20.
   */
  pageSize: number;
  /** Token to provide to skip to a particular spot in the list. */
  pageToken: string;
}

/** Response for listing occurrences. */
export interface ListOccurrencesResponse {
  /** The occurrences requested. */
  occurrences: Occurrence[];
  /**
   * The next pagination token in the list response. It should be used as
   * `page_token` for the following request. An empty value means no more
   * results.
   */
  nextPageToken: string;
}

/** Request to delete an occurrence. */
export interface DeleteOccurrenceRequest {
  /**
   * The name of the occurrence in the form of
   * `projects/[PROJECT_ID]/occurrences/[OCCURRENCE_ID]`.
   */
  name: string;
}

/** Request to create a new occurrence. */
export interface CreateOccurrenceRequest {
  /**
   * The name of the project in the form of `projects/[PROJECT_ID]`, under which
   * the occurrence is to be created.
   */
  parent: string;
  /** The occurrence to create. */
  occurrence: Occurrence | undefined;
}

/** Request to update an occurrence. */
export interface UpdateOccurrenceRequest {
  /**
   * The name of the occurrence in the form of
   * `projects/[PROJECT_ID]/occurrences/[OCCURRENCE_ID]`.
   */
  name: string;
  /** The updated occurrence. */
  occurrence:
    | Occurrence
    | undefined;
  /** The fields to update. */
  updateMask: string[] | undefined;
}

/** Request to get a note. */
export interface GetNoteRequest {
  /**
   * The name of the note in the form of
   * `projects/[PROVIDER_ID]/notes/[NOTE_ID]`.
   */
  name: string;
}

/** Request to get the note to which the specified occurrence is attached. */
export interface GetOccurrenceNoteRequest {
  /**
   * The name of the occurrence in the form of
   * `projects/[PROJECT_ID]/occurrences/[OCCURRENCE_ID]`.
   */
  name: string;
}

/** Request to list notes. */
export interface ListNotesRequest {
  /**
   * The name of the project to list notes for in the form of
   * `projects/[PROJECT_ID]`.
   */
  parent: string;
  /** The filter expression. */
  filter: string;
  /**
   * Number of notes to return in the list. Must be positive. Max allowed page
   * size is 1000. If not specified, page size defaults to 20.
   */
  pageSize: number;
  /** Token to provide to skip to a particular spot in the list. */
  pageToken: string;
}

/** Response for listing notes. */
export interface ListNotesResponse {
  /** The notes requested. */
  notes: Note[];
  /**
   * The next pagination token in the list response. It should be used as
   * `page_token` for the following request. An empty value means no more
   * results.
   */
  nextPageToken: string;
}

/** Request to delete a note. */
export interface DeleteNoteRequest {
  /**
   * The name of the note in the form of
   * `projects/[PROVIDER_ID]/notes/[NOTE_ID]`.
   */
  name: string;
}

/** Request to create a new note. */
export interface CreateNoteRequest {
  /**
   * The name of the project in the form of `projects/[PROJECT_ID]`, under which
   * the note is to be created.
   */
  parent: string;
  /** The ID to use for this note. */
  noteId: string;
  /** The note to create. */
  note: Note | undefined;
}

/** Request to update a note. */
export interface UpdateNoteRequest {
  /**
   * The name of the note in the form of
   * `projects/[PROVIDER_ID]/notes/[NOTE_ID]`.
   */
  name: string;
  /** The updated note. */
  note:
    | Note
    | undefined;
  /** The fields to update. */
  updateMask: string[] | undefined;
}

/** Request to list occurrences for a note. */
export interface ListNoteOccurrencesRequest {
  /**
   * The name of the note to list occurrences for in the form of
   * `projects/[PROVIDER_ID]/notes/[NOTE_ID]`.
   */
  name: string;
  /** The filter expression. */
  filter: string;
  /** Number of occurrences to return in the list. */
  pageSize: number;
  /** Token to provide to skip to a particular spot in the list. */
  pageToken: string;
}

/** Response for listing occurrences for a note. */
export interface ListNoteOccurrencesResponse {
  /** The occurrences attached to the specified note. */
  occurrences: Occurrence[];
  /** Token to provide to skip to a particular spot in the list. */
  nextPageToken: string;
}

/** Request to create notes in batch. */
export interface BatchCreateNotesRequest {
  /**
   * The name of the project in the form of `projects/[PROJECT_ID]`, under which
   * the notes are to be created.
   */
  parent: string;
  /** The notes to create. Max allowed length is 1000. */
  notes: { [key: string]: Note };
}

export interface BatchCreateNotesRequest_NotesEntry {
  key: string;
  value: Note | undefined;
}

/** Response for creating notes in batch. */
export interface BatchCreateNotesResponse {
  /** The notes that were created. */
  notes: Note[];
}

/** Request to create occurrences in batch. */
export interface BatchCreateOccurrencesRequest {
  /**
   * The name of the project in the form of `projects/[PROJECT_ID]`, under which
   * the occurrences are to be created.
   */
  parent: string;
  /** The occurrences to create. Max allowed length is 1000. */
  occurrences: Occurrence[];
}

/** Response for creating occurrences in batch. */
export interface BatchCreateOccurrencesResponse {
  /** The occurrences that were created. */
  occurrences: Occurrence[];
}

function createBaseOccurrence(): Occurrence {
  return {
    name: "",
    resourceUri: "",
    noteName: "",
    kind: 0,
    remediation: "",
    createTime: undefined,
    updateTime: undefined,
    vulnerability: undefined,
    build: undefined,
    image: undefined,
    package: undefined,
    deployment: undefined,
    discovery: undefined,
    attestation: undefined,
    upgrade: undefined,
    compliance: undefined,
    dsseAttestation: undefined,
    sbomReference: undefined,
    envelope: undefined,
  };
}

export const Occurrence = {
  encode(message: Occurrence, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.resourceUri !== "") {
      writer.uint32(18).string(message.resourceUri);
    }
    if (message.noteName !== "") {
      writer.uint32(26).string(message.noteName);
    }
    if (message.kind !== 0) {
      writer.uint32(32).int32(message.kind);
    }
    if (message.remediation !== "") {
      writer.uint32(42).string(message.remediation);
    }
    if (message.createTime !== undefined) {
      Timestamp.encode(toTimestamp(message.createTime), writer.uint32(50).fork()).ldelim();
    }
    if (message.updateTime !== undefined) {
      Timestamp.encode(toTimestamp(message.updateTime), writer.uint32(58).fork()).ldelim();
    }
    if (message.vulnerability !== undefined) {
      VulnerabilityOccurrence.encode(message.vulnerability, writer.uint32(66).fork()).ldelim();
    }
    if (message.build !== undefined) {
      BuildOccurrence.encode(message.build, writer.uint32(74).fork()).ldelim();
    }
    if (message.image !== undefined) {
      ImageOccurrence.encode(message.image, writer.uint32(82).fork()).ldelim();
    }
    if (message.package !== undefined) {
      PackageOccurrence.encode(message.package, writer.uint32(90).fork()).ldelim();
    }
    if (message.deployment !== undefined) {
      DeploymentOccurrence.encode(message.deployment, writer.uint32(98).fork()).ldelim();
    }
    if (message.discovery !== undefined) {
      DiscoveryOccurrence.encode(message.discovery, writer.uint32(106).fork()).ldelim();
    }
    if (message.attestation !== undefined) {
      AttestationOccurrence.encode(message.attestation, writer.uint32(114).fork()).ldelim();
    }
    if (message.upgrade !== undefined) {
      UpgradeOccurrence.encode(message.upgrade, writer.uint32(122).fork()).ldelim();
    }
    if (message.compliance !== undefined) {
      ComplianceOccurrence.encode(message.compliance, writer.uint32(130).fork()).ldelim();
    }
    if (message.dsseAttestation !== undefined) {
      DSSEAttestationOccurrence.encode(message.dsseAttestation, writer.uint32(138).fork()).ldelim();
    }
    if (message.sbomReference !== undefined) {
      SBOMReferenceOccurrence.encode(message.sbomReference, writer.uint32(154).fork()).ldelim();
    }
    if (message.envelope !== undefined) {
      Envelope.encode(message.envelope, writer.uint32(146).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Occurrence {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOccurrence();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.resourceUri = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.noteName = reader.string();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.kind = reader.int32() as any;
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.remediation = reader.string();
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.createTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.updateTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 8:
          if (tag !== 66) {
            break;
          }

          message.vulnerability = VulnerabilityOccurrence.decode(reader, reader.uint32());
          continue;
        case 9:
          if (tag !== 74) {
            break;
          }

          message.build = BuildOccurrence.decode(reader, reader.uint32());
          continue;
        case 10:
          if (tag !== 82) {
            break;
          }

          message.image = ImageOccurrence.decode(reader, reader.uint32());
          continue;
        case 11:
          if (tag !== 90) {
            break;
          }

          message.package = PackageOccurrence.decode(reader, reader.uint32());
          continue;
        case 12:
          if (tag !== 98) {
            break;
          }

          message.deployment = DeploymentOccurrence.decode(reader, reader.uint32());
          continue;
        case 13:
          if (tag !== 106) {
            break;
          }

          message.discovery = DiscoveryOccurrence.decode(reader, reader.uint32());
          continue;
        case 14:
          if (tag !== 114) {
            break;
          }

          message.attestation = AttestationOccurrence.decode(reader, reader.uint32());
          continue;
        case 15:
          if (tag !== 122) {
            break;
          }

          message.upgrade = UpgradeOccurrence.decode(reader, reader.uint32());
          continue;
        case 16:
          if (tag !== 130) {
            break;
          }

          message.compliance = ComplianceOccurrence.decode(reader, reader.uint32());
          continue;
        case 17:
          if (tag !== 138) {
            break;
          }

          message.dsseAttestation = DSSEAttestationOccurrence.decode(reader, reader.uint32());
          continue;
        case 19:
          if (tag !== 154) {
            break;
          }

          message.sbomReference = SBOMReferenceOccurrence.decode(reader, reader.uint32());
          continue;
        case 18:
          if (tag !== 146) {
            break;
          }

          message.envelope = Envelope.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Occurrence {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      resourceUri: isSet(object.resourceUri) ? globalThis.String(object.resourceUri) : "",
      noteName: isSet(object.noteName) ? globalThis.String(object.noteName) : "",
      kind: isSet(object.kind) ? noteKindFromJSON(object.kind) : 0,
      remediation: isSet(object.remediation) ? globalThis.String(object.remediation) : "",
      createTime: isSet(object.createTime) ? fromJsonTimestamp(object.createTime) : undefined,
      updateTime: isSet(object.updateTime) ? fromJsonTimestamp(object.updateTime) : undefined,
      vulnerability: isSet(object.vulnerability) ? VulnerabilityOccurrence.fromJSON(object.vulnerability) : undefined,
      build: isSet(object.build) ? BuildOccurrence.fromJSON(object.build) : undefined,
      image: isSet(object.image) ? ImageOccurrence.fromJSON(object.image) : undefined,
      package: isSet(object.package) ? PackageOccurrence.fromJSON(object.package) : undefined,
      deployment: isSet(object.deployment) ? DeploymentOccurrence.fromJSON(object.deployment) : undefined,
      discovery: isSet(object.discovery) ? DiscoveryOccurrence.fromJSON(object.discovery) : undefined,
      attestation: isSet(object.attestation) ? AttestationOccurrence.fromJSON(object.attestation) : undefined,
      upgrade: isSet(object.upgrade) ? UpgradeOccurrence.fromJSON(object.upgrade) : undefined,
      compliance: isSet(object.compliance) ? ComplianceOccurrence.fromJSON(object.compliance) : undefined,
      dsseAttestation: isSet(object.dsseAttestation)
        ? DSSEAttestationOccurrence.fromJSON(object.dsseAttestation)
        : undefined,
      sbomReference: isSet(object.sbomReference) ? SBOMReferenceOccurrence.fromJSON(object.sbomReference) : undefined,
      envelope: isSet(object.envelope) ? Envelope.fromJSON(object.envelope) : undefined,
    };
  },

  toJSON(message: Occurrence): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.resourceUri !== "") {
      obj.resourceUri = message.resourceUri;
    }
    if (message.noteName !== "") {
      obj.noteName = message.noteName;
    }
    if (message.kind !== 0) {
      obj.kind = noteKindToJSON(message.kind);
    }
    if (message.remediation !== "") {
      obj.remediation = message.remediation;
    }
    if (message.createTime !== undefined) {
      obj.createTime = message.createTime.toISOString();
    }
    if (message.updateTime !== undefined) {
      obj.updateTime = message.updateTime.toISOString();
    }
    if (message.vulnerability !== undefined) {
      obj.vulnerability = VulnerabilityOccurrence.toJSON(message.vulnerability);
    }
    if (message.build !== undefined) {
      obj.build = BuildOccurrence.toJSON(message.build);
    }
    if (message.image !== undefined) {
      obj.image = ImageOccurrence.toJSON(message.image);
    }
    if (message.package !== undefined) {
      obj.package = PackageOccurrence.toJSON(message.package);
    }
    if (message.deployment !== undefined) {
      obj.deployment = DeploymentOccurrence.toJSON(message.deployment);
    }
    if (message.discovery !== undefined) {
      obj.discovery = DiscoveryOccurrence.toJSON(message.discovery);
    }
    if (message.attestation !== undefined) {
      obj.attestation = AttestationOccurrence.toJSON(message.attestation);
    }
    if (message.upgrade !== undefined) {
      obj.upgrade = UpgradeOccurrence.toJSON(message.upgrade);
    }
    if (message.compliance !== undefined) {
      obj.compliance = ComplianceOccurrence.toJSON(message.compliance);
    }
    if (message.dsseAttestation !== undefined) {
      obj.dsseAttestation = DSSEAttestationOccurrence.toJSON(message.dsseAttestation);
    }
    if (message.sbomReference !== undefined) {
      obj.sbomReference = SBOMReferenceOccurrence.toJSON(message.sbomReference);
    }
    if (message.envelope !== undefined) {
      obj.envelope = Envelope.toJSON(message.envelope);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Occurrence>, I>>(base?: I): Occurrence {
    return Occurrence.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Occurrence>, I>>(object: I): Occurrence {
    const message = createBaseOccurrence();
    message.name = object.name ?? "";
    message.resourceUri = object.resourceUri ?? "";
    message.noteName = object.noteName ?? "";
    message.kind = object.kind ?? 0;
    message.remediation = object.remediation ?? "";
    message.createTime = object.createTime ?? undefined;
    message.updateTime = object.updateTime ?? undefined;
    message.vulnerability = (object.vulnerability !== undefined && object.vulnerability !== null)
      ? VulnerabilityOccurrence.fromPartial(object.vulnerability)
      : undefined;
    message.build = (object.build !== undefined && object.build !== null)
      ? BuildOccurrence.fromPartial(object.build)
      : undefined;
    message.image = (object.image !== undefined && object.image !== null)
      ? ImageOccurrence.fromPartial(object.image)
      : undefined;
    message.package = (object.package !== undefined && object.package !== null)
      ? PackageOccurrence.fromPartial(object.package)
      : undefined;
    message.deployment = (object.deployment !== undefined && object.deployment !== null)
      ? DeploymentOccurrence.fromPartial(object.deployment)
      : undefined;
    message.discovery = (object.discovery !== undefined && object.discovery !== null)
      ? DiscoveryOccurrence.fromPartial(object.discovery)
      : undefined;
    message.attestation = (object.attestation !== undefined && object.attestation !== null)
      ? AttestationOccurrence.fromPartial(object.attestation)
      : undefined;
    message.upgrade = (object.upgrade !== undefined && object.upgrade !== null)
      ? UpgradeOccurrence.fromPartial(object.upgrade)
      : undefined;
    message.compliance = (object.compliance !== undefined && object.compliance !== null)
      ? ComplianceOccurrence.fromPartial(object.compliance)
      : undefined;
    message.dsseAttestation = (object.dsseAttestation !== undefined && object.dsseAttestation !== null)
      ? DSSEAttestationOccurrence.fromPartial(object.dsseAttestation)
      : undefined;
    message.sbomReference = (object.sbomReference !== undefined && object.sbomReference !== null)
      ? SBOMReferenceOccurrence.fromPartial(object.sbomReference)
      : undefined;
    message.envelope = (object.envelope !== undefined && object.envelope !== null)
      ? Envelope.fromPartial(object.envelope)
      : undefined;
    return message;
  },
};

function createBaseNote(): Note {
  return {
    name: "",
    shortDescription: "",
    longDescription: "",
    kind: 0,
    relatedUrl: [],
    expirationTime: undefined,
    createTime: undefined,
    updateTime: undefined,
    relatedNoteNames: [],
    vulnerability: undefined,
    build: undefined,
    image: undefined,
    package: undefined,
    deployment: undefined,
    discovery: undefined,
    attestation: undefined,
    upgrade: undefined,
    compliance: undefined,
    dsseAttestation: undefined,
    vulnerabilityAssessment: undefined,
    sbomReference: undefined,
  };
}

export const Note = {
  encode(message: Note, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.shortDescription !== "") {
      writer.uint32(18).string(message.shortDescription);
    }
    if (message.longDescription !== "") {
      writer.uint32(26).string(message.longDescription);
    }
    if (message.kind !== 0) {
      writer.uint32(32).int32(message.kind);
    }
    for (const v of message.relatedUrl) {
      RelatedUrl.encode(v!, writer.uint32(42).fork()).ldelim();
    }
    if (message.expirationTime !== undefined) {
      Timestamp.encode(toTimestamp(message.expirationTime), writer.uint32(50).fork()).ldelim();
    }
    if (message.createTime !== undefined) {
      Timestamp.encode(toTimestamp(message.createTime), writer.uint32(58).fork()).ldelim();
    }
    if (message.updateTime !== undefined) {
      Timestamp.encode(toTimestamp(message.updateTime), writer.uint32(66).fork()).ldelim();
    }
    for (const v of message.relatedNoteNames) {
      writer.uint32(74).string(v!);
    }
    if (message.vulnerability !== undefined) {
      VulnerabilityNote.encode(message.vulnerability, writer.uint32(82).fork()).ldelim();
    }
    if (message.build !== undefined) {
      BuildNote.encode(message.build, writer.uint32(90).fork()).ldelim();
    }
    if (message.image !== undefined) {
      ImageNote.encode(message.image, writer.uint32(98).fork()).ldelim();
    }
    if (message.package !== undefined) {
      PackageNote.encode(message.package, writer.uint32(106).fork()).ldelim();
    }
    if (message.deployment !== undefined) {
      DeploymentNote.encode(message.deployment, writer.uint32(114).fork()).ldelim();
    }
    if (message.discovery !== undefined) {
      DiscoveryNote.encode(message.discovery, writer.uint32(122).fork()).ldelim();
    }
    if (message.attestation !== undefined) {
      AttestationNote.encode(message.attestation, writer.uint32(130).fork()).ldelim();
    }
    if (message.upgrade !== undefined) {
      UpgradeNote.encode(message.upgrade, writer.uint32(138).fork()).ldelim();
    }
    if (message.compliance !== undefined) {
      ComplianceNote.encode(message.compliance, writer.uint32(146).fork()).ldelim();
    }
    if (message.dsseAttestation !== undefined) {
      DSSEAttestationNote.encode(message.dsseAttestation, writer.uint32(154).fork()).ldelim();
    }
    if (message.vulnerabilityAssessment !== undefined) {
      VulnerabilityAssessmentNote.encode(message.vulnerabilityAssessment, writer.uint32(162).fork()).ldelim();
    }
    if (message.sbomReference !== undefined) {
      SBOMReferenceNote.encode(message.sbomReference, writer.uint32(170).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Note {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNote();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.shortDescription = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.longDescription = reader.string();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.kind = reader.int32() as any;
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.relatedUrl.push(RelatedUrl.decode(reader, reader.uint32()));
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.expirationTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.createTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 8:
          if (tag !== 66) {
            break;
          }

          message.updateTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 9:
          if (tag !== 74) {
            break;
          }

          message.relatedNoteNames.push(reader.string());
          continue;
        case 10:
          if (tag !== 82) {
            break;
          }

          message.vulnerability = VulnerabilityNote.decode(reader, reader.uint32());
          continue;
        case 11:
          if (tag !== 90) {
            break;
          }

          message.build = BuildNote.decode(reader, reader.uint32());
          continue;
        case 12:
          if (tag !== 98) {
            break;
          }

          message.image = ImageNote.decode(reader, reader.uint32());
          continue;
        case 13:
          if (tag !== 106) {
            break;
          }

          message.package = PackageNote.decode(reader, reader.uint32());
          continue;
        case 14:
          if (tag !== 114) {
            break;
          }

          message.deployment = DeploymentNote.decode(reader, reader.uint32());
          continue;
        case 15:
          if (tag !== 122) {
            break;
          }

          message.discovery = DiscoveryNote.decode(reader, reader.uint32());
          continue;
        case 16:
          if (tag !== 130) {
            break;
          }

          message.attestation = AttestationNote.decode(reader, reader.uint32());
          continue;
        case 17:
          if (tag !== 138) {
            break;
          }

          message.upgrade = UpgradeNote.decode(reader, reader.uint32());
          continue;
        case 18:
          if (tag !== 146) {
            break;
          }

          message.compliance = ComplianceNote.decode(reader, reader.uint32());
          continue;
        case 19:
          if (tag !== 154) {
            break;
          }

          message.dsseAttestation = DSSEAttestationNote.decode(reader, reader.uint32());
          continue;
        case 20:
          if (tag !== 162) {
            break;
          }

          message.vulnerabilityAssessment = VulnerabilityAssessmentNote.decode(reader, reader.uint32());
          continue;
        case 21:
          if (tag !== 170) {
            break;
          }

          message.sbomReference = SBOMReferenceNote.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Note {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      shortDescription: isSet(object.shortDescription) ? globalThis.String(object.shortDescription) : "",
      longDescription: isSet(object.longDescription) ? globalThis.String(object.longDescription) : "",
      kind: isSet(object.kind) ? noteKindFromJSON(object.kind) : 0,
      relatedUrl: globalThis.Array.isArray(object?.relatedUrl)
        ? object.relatedUrl.map((e: any) => RelatedUrl.fromJSON(e))
        : [],
      expirationTime: isSet(object.expirationTime) ? fromJsonTimestamp(object.expirationTime) : undefined,
      createTime: isSet(object.createTime) ? fromJsonTimestamp(object.createTime) : undefined,
      updateTime: isSet(object.updateTime) ? fromJsonTimestamp(object.updateTime) : undefined,
      relatedNoteNames: globalThis.Array.isArray(object?.relatedNoteNames)
        ? object.relatedNoteNames.map((e: any) => globalThis.String(e))
        : [],
      vulnerability: isSet(object.vulnerability) ? VulnerabilityNote.fromJSON(object.vulnerability) : undefined,
      build: isSet(object.build) ? BuildNote.fromJSON(object.build) : undefined,
      image: isSet(object.image) ? ImageNote.fromJSON(object.image) : undefined,
      package: isSet(object.package) ? PackageNote.fromJSON(object.package) : undefined,
      deployment: isSet(object.deployment) ? DeploymentNote.fromJSON(object.deployment) : undefined,
      discovery: isSet(object.discovery) ? DiscoveryNote.fromJSON(object.discovery) : undefined,
      attestation: isSet(object.attestation) ? AttestationNote.fromJSON(object.attestation) : undefined,
      upgrade: isSet(object.upgrade) ? UpgradeNote.fromJSON(object.upgrade) : undefined,
      compliance: isSet(object.compliance) ? ComplianceNote.fromJSON(object.compliance) : undefined,
      dsseAttestation: isSet(object.dsseAttestation) ? DSSEAttestationNote.fromJSON(object.dsseAttestation) : undefined,
      vulnerabilityAssessment: isSet(object.vulnerabilityAssessment)
        ? VulnerabilityAssessmentNote.fromJSON(object.vulnerabilityAssessment)
        : undefined,
      sbomReference: isSet(object.sbomReference) ? SBOMReferenceNote.fromJSON(object.sbomReference) : undefined,
    };
  },

  toJSON(message: Note): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.shortDescription !== "") {
      obj.shortDescription = message.shortDescription;
    }
    if (message.longDescription !== "") {
      obj.longDescription = message.longDescription;
    }
    if (message.kind !== 0) {
      obj.kind = noteKindToJSON(message.kind);
    }
    if (message.relatedUrl?.length) {
      obj.relatedUrl = message.relatedUrl.map((e) => RelatedUrl.toJSON(e));
    }
    if (message.expirationTime !== undefined) {
      obj.expirationTime = message.expirationTime.toISOString();
    }
    if (message.createTime !== undefined) {
      obj.createTime = message.createTime.toISOString();
    }
    if (message.updateTime !== undefined) {
      obj.updateTime = message.updateTime.toISOString();
    }
    if (message.relatedNoteNames?.length) {
      obj.relatedNoteNames = message.relatedNoteNames;
    }
    if (message.vulnerability !== undefined) {
      obj.vulnerability = VulnerabilityNote.toJSON(message.vulnerability);
    }
    if (message.build !== undefined) {
      obj.build = BuildNote.toJSON(message.build);
    }
    if (message.image !== undefined) {
      obj.image = ImageNote.toJSON(message.image);
    }
    if (message.package !== undefined) {
      obj.package = PackageNote.toJSON(message.package);
    }
    if (message.deployment !== undefined) {
      obj.deployment = DeploymentNote.toJSON(message.deployment);
    }
    if (message.discovery !== undefined) {
      obj.discovery = DiscoveryNote.toJSON(message.discovery);
    }
    if (message.attestation !== undefined) {
      obj.attestation = AttestationNote.toJSON(message.attestation);
    }
    if (message.upgrade !== undefined) {
      obj.upgrade = UpgradeNote.toJSON(message.upgrade);
    }
    if (message.compliance !== undefined) {
      obj.compliance = ComplianceNote.toJSON(message.compliance);
    }
    if (message.dsseAttestation !== undefined) {
      obj.dsseAttestation = DSSEAttestationNote.toJSON(message.dsseAttestation);
    }
    if (message.vulnerabilityAssessment !== undefined) {
      obj.vulnerabilityAssessment = VulnerabilityAssessmentNote.toJSON(message.vulnerabilityAssessment);
    }
    if (message.sbomReference !== undefined) {
      obj.sbomReference = SBOMReferenceNote.toJSON(message.sbomReference);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Note>, I>>(base?: I): Note {
    return Note.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Note>, I>>(object: I): Note {
    const message = createBaseNote();
    message.name = object.name ?? "";
    message.shortDescription = object.shortDescription ?? "";
    message.longDescription = object.longDescription ?? "";
    message.kind = object.kind ?? 0;
    message.relatedUrl = object.relatedUrl?.map((e) => RelatedUrl.fromPartial(e)) || [];
    message.expirationTime = object.expirationTime ?? undefined;
    message.createTime = object.createTime ?? undefined;
    message.updateTime = object.updateTime ?? undefined;
    message.relatedNoteNames = object.relatedNoteNames?.map((e) => e) || [];
    message.vulnerability = (object.vulnerability !== undefined && object.vulnerability !== null)
      ? VulnerabilityNote.fromPartial(object.vulnerability)
      : undefined;
    message.build = (object.build !== undefined && object.build !== null)
      ? BuildNote.fromPartial(object.build)
      : undefined;
    message.image = (object.image !== undefined && object.image !== null)
      ? ImageNote.fromPartial(object.image)
      : undefined;
    message.package = (object.package !== undefined && object.package !== null)
      ? PackageNote.fromPartial(object.package)
      : undefined;
    message.deployment = (object.deployment !== undefined && object.deployment !== null)
      ? DeploymentNote.fromPartial(object.deployment)
      : undefined;
    message.discovery = (object.discovery !== undefined && object.discovery !== null)
      ? DiscoveryNote.fromPartial(object.discovery)
      : undefined;
    message.attestation = (object.attestation !== undefined && object.attestation !== null)
      ? AttestationNote.fromPartial(object.attestation)
      : undefined;
    message.upgrade = (object.upgrade !== undefined && object.upgrade !== null)
      ? UpgradeNote.fromPartial(object.upgrade)
      : undefined;
    message.compliance = (object.compliance !== undefined && object.compliance !== null)
      ? ComplianceNote.fromPartial(object.compliance)
      : undefined;
    message.dsseAttestation = (object.dsseAttestation !== undefined && object.dsseAttestation !== null)
      ? DSSEAttestationNote.fromPartial(object.dsseAttestation)
      : undefined;
    message.vulnerabilityAssessment =
      (object.vulnerabilityAssessment !== undefined && object.vulnerabilityAssessment !== null)
        ? VulnerabilityAssessmentNote.fromPartial(object.vulnerabilityAssessment)
        : undefined;
    message.sbomReference = (object.sbomReference !== undefined && object.sbomReference !== null)
      ? SBOMReferenceNote.fromPartial(object.sbomReference)
      : undefined;
    return message;
  },
};

function createBaseGetOccurrenceRequest(): GetOccurrenceRequest {
  return { name: "" };
}

export const GetOccurrenceRequest = {
  encode(message: GetOccurrenceRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetOccurrenceRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetOccurrenceRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetOccurrenceRequest {
    return { name: isSet(object.name) ? globalThis.String(object.name) : "" };
  },

  toJSON(message: GetOccurrenceRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetOccurrenceRequest>, I>>(base?: I): GetOccurrenceRequest {
    return GetOccurrenceRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetOccurrenceRequest>, I>>(object: I): GetOccurrenceRequest {
    const message = createBaseGetOccurrenceRequest();
    message.name = object.name ?? "";
    return message;
  },
};

function createBaseListOccurrencesRequest(): ListOccurrencesRequest {
  return { parent: "", filter: "", pageSize: 0, pageToken: "" };
}

export const ListOccurrencesRequest = {
  encode(message: ListOccurrencesRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.parent !== "") {
      writer.uint32(10).string(message.parent);
    }
    if (message.filter !== "") {
      writer.uint32(18).string(message.filter);
    }
    if (message.pageSize !== 0) {
      writer.uint32(24).int32(message.pageSize);
    }
    if (message.pageToken !== "") {
      writer.uint32(34).string(message.pageToken);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ListOccurrencesRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListOccurrencesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.parent = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.filter = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.pageSize = reader.int32();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.pageToken = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListOccurrencesRequest {
    return {
      parent: isSet(object.parent) ? globalThis.String(object.parent) : "",
      filter: isSet(object.filter) ? globalThis.String(object.filter) : "",
      pageSize: isSet(object.pageSize) ? globalThis.Number(object.pageSize) : 0,
      pageToken: isSet(object.pageToken) ? globalThis.String(object.pageToken) : "",
    };
  },

  toJSON(message: ListOccurrencesRequest): unknown {
    const obj: any = {};
    if (message.parent !== "") {
      obj.parent = message.parent;
    }
    if (message.filter !== "") {
      obj.filter = message.filter;
    }
    if (message.pageSize !== 0) {
      obj.pageSize = Math.round(message.pageSize);
    }
    if (message.pageToken !== "") {
      obj.pageToken = message.pageToken;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListOccurrencesRequest>, I>>(base?: I): ListOccurrencesRequest {
    return ListOccurrencesRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListOccurrencesRequest>, I>>(object: I): ListOccurrencesRequest {
    const message = createBaseListOccurrencesRequest();
    message.parent = object.parent ?? "";
    message.filter = object.filter ?? "";
    message.pageSize = object.pageSize ?? 0;
    message.pageToken = object.pageToken ?? "";
    return message;
  },
};

function createBaseListOccurrencesResponse(): ListOccurrencesResponse {
  return { occurrences: [], nextPageToken: "" };
}

export const ListOccurrencesResponse = {
  encode(message: ListOccurrencesResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.occurrences) {
      Occurrence.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    if (message.nextPageToken !== "") {
      writer.uint32(18).string(message.nextPageToken);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ListOccurrencesResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListOccurrencesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.occurrences.push(Occurrence.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.nextPageToken = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListOccurrencesResponse {
    return {
      occurrences: globalThis.Array.isArray(object?.occurrences)
        ? object.occurrences.map((e: any) => Occurrence.fromJSON(e))
        : [],
      nextPageToken: isSet(object.nextPageToken) ? globalThis.String(object.nextPageToken) : "",
    };
  },

  toJSON(message: ListOccurrencesResponse): unknown {
    const obj: any = {};
    if (message.occurrences?.length) {
      obj.occurrences = message.occurrences.map((e) => Occurrence.toJSON(e));
    }
    if (message.nextPageToken !== "") {
      obj.nextPageToken = message.nextPageToken;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListOccurrencesResponse>, I>>(base?: I): ListOccurrencesResponse {
    return ListOccurrencesResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListOccurrencesResponse>, I>>(object: I): ListOccurrencesResponse {
    const message = createBaseListOccurrencesResponse();
    message.occurrences = object.occurrences?.map((e) => Occurrence.fromPartial(e)) || [];
    message.nextPageToken = object.nextPageToken ?? "";
    return message;
  },
};

function createBaseDeleteOccurrenceRequest(): DeleteOccurrenceRequest {
  return { name: "" };
}

export const DeleteOccurrenceRequest = {
  encode(message: DeleteOccurrenceRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): DeleteOccurrenceRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteOccurrenceRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteOccurrenceRequest {
    return { name: isSet(object.name) ? globalThis.String(object.name) : "" };
  },

  toJSON(message: DeleteOccurrenceRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteOccurrenceRequest>, I>>(base?: I): DeleteOccurrenceRequest {
    return DeleteOccurrenceRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteOccurrenceRequest>, I>>(object: I): DeleteOccurrenceRequest {
    const message = createBaseDeleteOccurrenceRequest();
    message.name = object.name ?? "";
    return message;
  },
};

function createBaseCreateOccurrenceRequest(): CreateOccurrenceRequest {
  return { parent: "", occurrence: undefined };
}

export const CreateOccurrenceRequest = {
  encode(message: CreateOccurrenceRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.parent !== "") {
      writer.uint32(10).string(message.parent);
    }
    if (message.occurrence !== undefined) {
      Occurrence.encode(message.occurrence, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): CreateOccurrenceRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateOccurrenceRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.parent = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.occurrence = Occurrence.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateOccurrenceRequest {
    return {
      parent: isSet(object.parent) ? globalThis.String(object.parent) : "",
      occurrence: isSet(object.occurrence) ? Occurrence.fromJSON(object.occurrence) : undefined,
    };
  },

  toJSON(message: CreateOccurrenceRequest): unknown {
    const obj: any = {};
    if (message.parent !== "") {
      obj.parent = message.parent;
    }
    if (message.occurrence !== undefined) {
      obj.occurrence = Occurrence.toJSON(message.occurrence);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateOccurrenceRequest>, I>>(base?: I): CreateOccurrenceRequest {
    return CreateOccurrenceRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateOccurrenceRequest>, I>>(object: I): CreateOccurrenceRequest {
    const message = createBaseCreateOccurrenceRequest();
    message.parent = object.parent ?? "";
    message.occurrence = (object.occurrence !== undefined && object.occurrence !== null)
      ? Occurrence.fromPartial(object.occurrence)
      : undefined;
    return message;
  },
};

function createBaseUpdateOccurrenceRequest(): UpdateOccurrenceRequest {
  return { name: "", occurrence: undefined, updateMask: undefined };
}

export const UpdateOccurrenceRequest = {
  encode(message: UpdateOccurrenceRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.occurrence !== undefined) {
      Occurrence.encode(message.occurrence, writer.uint32(18).fork()).ldelim();
    }
    if (message.updateMask !== undefined) {
      FieldMask.encode(FieldMask.wrap(message.updateMask), writer.uint32(26).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): UpdateOccurrenceRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateOccurrenceRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.occurrence = Occurrence.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.updateMask = FieldMask.unwrap(FieldMask.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateOccurrenceRequest {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      occurrence: isSet(object.occurrence) ? Occurrence.fromJSON(object.occurrence) : undefined,
      updateMask: isSet(object.updateMask) ? FieldMask.unwrap(FieldMask.fromJSON(object.updateMask)) : undefined,
    };
  },

  toJSON(message: UpdateOccurrenceRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.occurrence !== undefined) {
      obj.occurrence = Occurrence.toJSON(message.occurrence);
    }
    if (message.updateMask !== undefined) {
      obj.updateMask = FieldMask.toJSON(FieldMask.wrap(message.updateMask));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateOccurrenceRequest>, I>>(base?: I): UpdateOccurrenceRequest {
    return UpdateOccurrenceRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateOccurrenceRequest>, I>>(object: I): UpdateOccurrenceRequest {
    const message = createBaseUpdateOccurrenceRequest();
    message.name = object.name ?? "";
    message.occurrence = (object.occurrence !== undefined && object.occurrence !== null)
      ? Occurrence.fromPartial(object.occurrence)
      : undefined;
    message.updateMask = object.updateMask ?? undefined;
    return message;
  },
};

function createBaseGetNoteRequest(): GetNoteRequest {
  return { name: "" };
}

export const GetNoteRequest = {
  encode(message: GetNoteRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetNoteRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetNoteRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetNoteRequest {
    return { name: isSet(object.name) ? globalThis.String(object.name) : "" };
  },

  toJSON(message: GetNoteRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetNoteRequest>, I>>(base?: I): GetNoteRequest {
    return GetNoteRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetNoteRequest>, I>>(object: I): GetNoteRequest {
    const message = createBaseGetNoteRequest();
    message.name = object.name ?? "";
    return message;
  },
};

function createBaseGetOccurrenceNoteRequest(): GetOccurrenceNoteRequest {
  return { name: "" };
}

export const GetOccurrenceNoteRequest = {
  encode(message: GetOccurrenceNoteRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetOccurrenceNoteRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetOccurrenceNoteRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetOccurrenceNoteRequest {
    return { name: isSet(object.name) ? globalThis.String(object.name) : "" };
  },

  toJSON(message: GetOccurrenceNoteRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetOccurrenceNoteRequest>, I>>(base?: I): GetOccurrenceNoteRequest {
    return GetOccurrenceNoteRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetOccurrenceNoteRequest>, I>>(object: I): GetOccurrenceNoteRequest {
    const message = createBaseGetOccurrenceNoteRequest();
    message.name = object.name ?? "";
    return message;
  },
};

function createBaseListNotesRequest(): ListNotesRequest {
  return { parent: "", filter: "", pageSize: 0, pageToken: "" };
}

export const ListNotesRequest = {
  encode(message: ListNotesRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.parent !== "") {
      writer.uint32(10).string(message.parent);
    }
    if (message.filter !== "") {
      writer.uint32(18).string(message.filter);
    }
    if (message.pageSize !== 0) {
      writer.uint32(24).int32(message.pageSize);
    }
    if (message.pageToken !== "") {
      writer.uint32(34).string(message.pageToken);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ListNotesRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListNotesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.parent = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.filter = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.pageSize = reader.int32();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.pageToken = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListNotesRequest {
    return {
      parent: isSet(object.parent) ? globalThis.String(object.parent) : "",
      filter: isSet(object.filter) ? globalThis.String(object.filter) : "",
      pageSize: isSet(object.pageSize) ? globalThis.Number(object.pageSize) : 0,
      pageToken: isSet(object.pageToken) ? globalThis.String(object.pageToken) : "",
    };
  },

  toJSON(message: ListNotesRequest): unknown {
    const obj: any = {};
    if (message.parent !== "") {
      obj.parent = message.parent;
    }
    if (message.filter !== "") {
      obj.filter = message.filter;
    }
    if (message.pageSize !== 0) {
      obj.pageSize = Math.round(message.pageSize);
    }
    if (message.pageToken !== "") {
      obj.pageToken = message.pageToken;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListNotesRequest>, I>>(base?: I): ListNotesRequest {
    return ListNotesRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListNotesRequest>, I>>(object: I): ListNotesRequest {
    const message = createBaseListNotesRequest();
    message.parent = object.parent ?? "";
    message.filter = object.filter ?? "";
    message.pageSize = object.pageSize ?? 0;
    message.pageToken = object.pageToken ?? "";
    return message;
  },
};

function createBaseListNotesResponse(): ListNotesResponse {
  return { notes: [], nextPageToken: "" };
}

export const ListNotesResponse = {
  encode(message: ListNotesResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.notes) {
      Note.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    if (message.nextPageToken !== "") {
      writer.uint32(18).string(message.nextPageToken);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ListNotesResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListNotesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.notes.push(Note.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.nextPageToken = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListNotesResponse {
    return {
      notes: globalThis.Array.isArray(object?.notes) ? object.notes.map((e: any) => Note.fromJSON(e)) : [],
      nextPageToken: isSet(object.nextPageToken) ? globalThis.String(object.nextPageToken) : "",
    };
  },

  toJSON(message: ListNotesResponse): unknown {
    const obj: any = {};
    if (message.notes?.length) {
      obj.notes = message.notes.map((e) => Note.toJSON(e));
    }
    if (message.nextPageToken !== "") {
      obj.nextPageToken = message.nextPageToken;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListNotesResponse>, I>>(base?: I): ListNotesResponse {
    return ListNotesResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListNotesResponse>, I>>(object: I): ListNotesResponse {
    const message = createBaseListNotesResponse();
    message.notes = object.notes?.map((e) => Note.fromPartial(e)) || [];
    message.nextPageToken = object.nextPageToken ?? "";
    return message;
  },
};

function createBaseDeleteNoteRequest(): DeleteNoteRequest {
  return { name: "" };
}

export const DeleteNoteRequest = {
  encode(message: DeleteNoteRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): DeleteNoteRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteNoteRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteNoteRequest {
    return { name: isSet(object.name) ? globalThis.String(object.name) : "" };
  },

  toJSON(message: DeleteNoteRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteNoteRequest>, I>>(base?: I): DeleteNoteRequest {
    return DeleteNoteRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteNoteRequest>, I>>(object: I): DeleteNoteRequest {
    const message = createBaseDeleteNoteRequest();
    message.name = object.name ?? "";
    return message;
  },
};

function createBaseCreateNoteRequest(): CreateNoteRequest {
  return { parent: "", noteId: "", note: undefined };
}

export const CreateNoteRequest = {
  encode(message: CreateNoteRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.parent !== "") {
      writer.uint32(10).string(message.parent);
    }
    if (message.noteId !== "") {
      writer.uint32(18).string(message.noteId);
    }
    if (message.note !== undefined) {
      Note.encode(message.note, writer.uint32(26).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): CreateNoteRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateNoteRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.parent = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.noteId = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.note = Note.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateNoteRequest {
    return {
      parent: isSet(object.parent) ? globalThis.String(object.parent) : "",
      noteId: isSet(object.noteId) ? globalThis.String(object.noteId) : "",
      note: isSet(object.note) ? Note.fromJSON(object.note) : undefined,
    };
  },

  toJSON(message: CreateNoteRequest): unknown {
    const obj: any = {};
    if (message.parent !== "") {
      obj.parent = message.parent;
    }
    if (message.noteId !== "") {
      obj.noteId = message.noteId;
    }
    if (message.note !== undefined) {
      obj.note = Note.toJSON(message.note);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateNoteRequest>, I>>(base?: I): CreateNoteRequest {
    return CreateNoteRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateNoteRequest>, I>>(object: I): CreateNoteRequest {
    const message = createBaseCreateNoteRequest();
    message.parent = object.parent ?? "";
    message.noteId = object.noteId ?? "";
    message.note = (object.note !== undefined && object.note !== null) ? Note.fromPartial(object.note) : undefined;
    return message;
  },
};

function createBaseUpdateNoteRequest(): UpdateNoteRequest {
  return { name: "", note: undefined, updateMask: undefined };
}

export const UpdateNoteRequest = {
  encode(message: UpdateNoteRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.note !== undefined) {
      Note.encode(message.note, writer.uint32(18).fork()).ldelim();
    }
    if (message.updateMask !== undefined) {
      FieldMask.encode(FieldMask.wrap(message.updateMask), writer.uint32(26).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): UpdateNoteRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateNoteRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.note = Note.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.updateMask = FieldMask.unwrap(FieldMask.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateNoteRequest {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      note: isSet(object.note) ? Note.fromJSON(object.note) : undefined,
      updateMask: isSet(object.updateMask) ? FieldMask.unwrap(FieldMask.fromJSON(object.updateMask)) : undefined,
    };
  },

  toJSON(message: UpdateNoteRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.note !== undefined) {
      obj.note = Note.toJSON(message.note);
    }
    if (message.updateMask !== undefined) {
      obj.updateMask = FieldMask.toJSON(FieldMask.wrap(message.updateMask));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateNoteRequest>, I>>(base?: I): UpdateNoteRequest {
    return UpdateNoteRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateNoteRequest>, I>>(object: I): UpdateNoteRequest {
    const message = createBaseUpdateNoteRequest();
    message.name = object.name ?? "";
    message.note = (object.note !== undefined && object.note !== null) ? Note.fromPartial(object.note) : undefined;
    message.updateMask = object.updateMask ?? undefined;
    return message;
  },
};

function createBaseListNoteOccurrencesRequest(): ListNoteOccurrencesRequest {
  return { name: "", filter: "", pageSize: 0, pageToken: "" };
}

export const ListNoteOccurrencesRequest = {
  encode(message: ListNoteOccurrencesRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.filter !== "") {
      writer.uint32(18).string(message.filter);
    }
    if (message.pageSize !== 0) {
      writer.uint32(24).int32(message.pageSize);
    }
    if (message.pageToken !== "") {
      writer.uint32(34).string(message.pageToken);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ListNoteOccurrencesRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListNoteOccurrencesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.filter = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.pageSize = reader.int32();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.pageToken = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListNoteOccurrencesRequest {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      filter: isSet(object.filter) ? globalThis.String(object.filter) : "",
      pageSize: isSet(object.pageSize) ? globalThis.Number(object.pageSize) : 0,
      pageToken: isSet(object.pageToken) ? globalThis.String(object.pageToken) : "",
    };
  },

  toJSON(message: ListNoteOccurrencesRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.filter !== "") {
      obj.filter = message.filter;
    }
    if (message.pageSize !== 0) {
      obj.pageSize = Math.round(message.pageSize);
    }
    if (message.pageToken !== "") {
      obj.pageToken = message.pageToken;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListNoteOccurrencesRequest>, I>>(base?: I): ListNoteOccurrencesRequest {
    return ListNoteOccurrencesRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListNoteOccurrencesRequest>, I>>(object: I): ListNoteOccurrencesRequest {
    const message = createBaseListNoteOccurrencesRequest();
    message.name = object.name ?? "";
    message.filter = object.filter ?? "";
    message.pageSize = object.pageSize ?? 0;
    message.pageToken = object.pageToken ?? "";
    return message;
  },
};

function createBaseListNoteOccurrencesResponse(): ListNoteOccurrencesResponse {
  return { occurrences: [], nextPageToken: "" };
}

export const ListNoteOccurrencesResponse = {
  encode(message: ListNoteOccurrencesResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.occurrences) {
      Occurrence.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    if (message.nextPageToken !== "") {
      writer.uint32(18).string(message.nextPageToken);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ListNoteOccurrencesResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListNoteOccurrencesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.occurrences.push(Occurrence.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.nextPageToken = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListNoteOccurrencesResponse {
    return {
      occurrences: globalThis.Array.isArray(object?.occurrences)
        ? object.occurrences.map((e: any) => Occurrence.fromJSON(e))
        : [],
      nextPageToken: isSet(object.nextPageToken) ? globalThis.String(object.nextPageToken) : "",
    };
  },

  toJSON(message: ListNoteOccurrencesResponse): unknown {
    const obj: any = {};
    if (message.occurrences?.length) {
      obj.occurrences = message.occurrences.map((e) => Occurrence.toJSON(e));
    }
    if (message.nextPageToken !== "") {
      obj.nextPageToken = message.nextPageToken;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListNoteOccurrencesResponse>, I>>(base?: I): ListNoteOccurrencesResponse {
    return ListNoteOccurrencesResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListNoteOccurrencesResponse>, I>>(object: I): ListNoteOccurrencesResponse {
    const message = createBaseListNoteOccurrencesResponse();
    message.occurrences = object.occurrences?.map((e) => Occurrence.fromPartial(e)) || [];
    message.nextPageToken = object.nextPageToken ?? "";
    return message;
  },
};

function createBaseBatchCreateNotesRequest(): BatchCreateNotesRequest {
  return { parent: "", notes: {} };
}

export const BatchCreateNotesRequest = {
  encode(message: BatchCreateNotesRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.parent !== "") {
      writer.uint32(10).string(message.parent);
    }
    Object.entries(message.notes).forEach(([key, value]) => {
      BatchCreateNotesRequest_NotesEntry.encode({ key: key as any, value }, writer.uint32(18).fork()).ldelim();
    });
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): BatchCreateNotesRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBatchCreateNotesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.parent = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          const entry2 = BatchCreateNotesRequest_NotesEntry.decode(reader, reader.uint32());
          if (entry2.value !== undefined) {
            message.notes[entry2.key] = entry2.value;
          }
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BatchCreateNotesRequest {
    return {
      parent: isSet(object.parent) ? globalThis.String(object.parent) : "",
      notes: isObject(object.notes)
        ? Object.entries(object.notes).reduce<{ [key: string]: Note }>((acc, [key, value]) => {
          acc[key] = Note.fromJSON(value);
          return acc;
        }, {})
        : {},
    };
  },

  toJSON(message: BatchCreateNotesRequest): unknown {
    const obj: any = {};
    if (message.parent !== "") {
      obj.parent = message.parent;
    }
    if (message.notes) {
      const entries = Object.entries(message.notes);
      if (entries.length > 0) {
        obj.notes = {};
        entries.forEach(([k, v]) => {
          obj.notes[k] = Note.toJSON(v);
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BatchCreateNotesRequest>, I>>(base?: I): BatchCreateNotesRequest {
    return BatchCreateNotesRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BatchCreateNotesRequest>, I>>(object: I): BatchCreateNotesRequest {
    const message = createBaseBatchCreateNotesRequest();
    message.parent = object.parent ?? "";
    message.notes = Object.entries(object.notes ?? {}).reduce<{ [key: string]: Note }>((acc, [key, value]) => {
      if (value !== undefined) {
        acc[key] = Note.fromPartial(value);
      }
      return acc;
    }, {});
    return message;
  },
};

function createBaseBatchCreateNotesRequest_NotesEntry(): BatchCreateNotesRequest_NotesEntry {
  return { key: "", value: undefined };
}

export const BatchCreateNotesRequest_NotesEntry = {
  encode(message: BatchCreateNotesRequest_NotesEntry, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      Note.encode(message.value, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): BatchCreateNotesRequest_NotesEntry {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBatchCreateNotesRequest_NotesEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.value = Note.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BatchCreateNotesRequest_NotesEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? Note.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: BatchCreateNotesRequest_NotesEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = Note.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BatchCreateNotesRequest_NotesEntry>, I>>(
    base?: I,
  ): BatchCreateNotesRequest_NotesEntry {
    return BatchCreateNotesRequest_NotesEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BatchCreateNotesRequest_NotesEntry>, I>>(
    object: I,
  ): BatchCreateNotesRequest_NotesEntry {
    const message = createBaseBatchCreateNotesRequest_NotesEntry();
    message.key = object.key ?? "";
    message.value = (object.value !== undefined && object.value !== null) ? Note.fromPartial(object.value) : undefined;
    return message;
  },
};

function createBaseBatchCreateNotesResponse(): BatchCreateNotesResponse {
  return { notes: [] };
}

export const BatchCreateNotesResponse = {
  encode(message: BatchCreateNotesResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.notes) {
      Note.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): BatchCreateNotesResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBatchCreateNotesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.notes.push(Note.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BatchCreateNotesResponse {
    return { notes: globalThis.Array.isArray(object?.notes) ? object.notes.map((e: any) => Note.fromJSON(e)) : [] };
  },

  toJSON(message: BatchCreateNotesResponse): unknown {
    const obj: any = {};
    if (message.notes?.length) {
      obj.notes = message.notes.map((e) => Note.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BatchCreateNotesResponse>, I>>(base?: I): BatchCreateNotesResponse {
    return BatchCreateNotesResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BatchCreateNotesResponse>, I>>(object: I): BatchCreateNotesResponse {
    const message = createBaseBatchCreateNotesResponse();
    message.notes = object.notes?.map((e) => Note.fromPartial(e)) || [];
    return message;
  },
};

function createBaseBatchCreateOccurrencesRequest(): BatchCreateOccurrencesRequest {
  return { parent: "", occurrences: [] };
}

export const BatchCreateOccurrencesRequest = {
  encode(message: BatchCreateOccurrencesRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.parent !== "") {
      writer.uint32(10).string(message.parent);
    }
    for (const v of message.occurrences) {
      Occurrence.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): BatchCreateOccurrencesRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBatchCreateOccurrencesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.parent = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.occurrences.push(Occurrence.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BatchCreateOccurrencesRequest {
    return {
      parent: isSet(object.parent) ? globalThis.String(object.parent) : "",
      occurrences: globalThis.Array.isArray(object?.occurrences)
        ? object.occurrences.map((e: any) => Occurrence.fromJSON(e))
        : [],
    };
  },

  toJSON(message: BatchCreateOccurrencesRequest): unknown {
    const obj: any = {};
    if (message.parent !== "") {
      obj.parent = message.parent;
    }
    if (message.occurrences?.length) {
      obj.occurrences = message.occurrences.map((e) => Occurrence.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BatchCreateOccurrencesRequest>, I>>(base?: I): BatchCreateOccurrencesRequest {
    return BatchCreateOccurrencesRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BatchCreateOccurrencesRequest>, I>>(
    object: I,
  ): BatchCreateOccurrencesRequest {
    const message = createBaseBatchCreateOccurrencesRequest();
    message.parent = object.parent ?? "";
    message.occurrences = object.occurrences?.map((e) => Occurrence.fromPartial(e)) || [];
    return message;
  },
};

function createBaseBatchCreateOccurrencesResponse(): BatchCreateOccurrencesResponse {
  return { occurrences: [] };
}

export const BatchCreateOccurrencesResponse = {
  encode(message: BatchCreateOccurrencesResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.occurrences) {
      Occurrence.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): BatchCreateOccurrencesResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBatchCreateOccurrencesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.occurrences.push(Occurrence.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BatchCreateOccurrencesResponse {
    return {
      occurrences: globalThis.Array.isArray(object?.occurrences)
        ? object.occurrences.map((e: any) => Occurrence.fromJSON(e))
        : [],
    };
  },

  toJSON(message: BatchCreateOccurrencesResponse): unknown {
    const obj: any = {};
    if (message.occurrences?.length) {
      obj.occurrences = message.occurrences.map((e) => Occurrence.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BatchCreateOccurrencesResponse>, I>>(base?: I): BatchCreateOccurrencesResponse {
    return BatchCreateOccurrencesResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BatchCreateOccurrencesResponse>, I>>(
    object: I,
  ): BatchCreateOccurrencesResponse {
    const message = createBaseBatchCreateOccurrencesResponse();
    message.occurrences = object.occurrences?.map((e) => Occurrence.fromPartial(e)) || [];
    return message;
  },
};

/**
 * [Grafeas](https://grafeas.io) API.
 *
 * Retrieves analysis results of Cloud components such as Docker container
 * images.
 *
 * Analysis results are stored as a series of occurrences. An `Occurrence`
 * contains information about a specific analysis instance on a resource. An
 * occurrence refers to a `Note`. A note contains details describing the
 * analysis and is generally stored in a separate project, called a `Provider`.
 * Multiple occurrences can refer to the same note.
 *
 * For example, an SSL vulnerability could affect multiple images. In this case,
 * there would be one note for the vulnerability and an occurrence for each
 * image with the vulnerability referring to that note.
 */
export interface Grafeas {
  /** Gets the specified occurrence. */
  GetOccurrence(request: GetOccurrenceRequest): Promise<Occurrence>;
  /** Lists occurrences for the specified project. */
  ListOccurrences(request: ListOccurrencesRequest): Promise<ListOccurrencesResponse>;
  /**
   * Deletes the specified occurrence. For example, use this method to delete an
   * occurrence when the occurrence is no longer applicable for the given
   * resource.
   */
  DeleteOccurrence(request: DeleteOccurrenceRequest): Promise<Empty>;
  /** Creates a new occurrence. */
  CreateOccurrence(request: CreateOccurrenceRequest): Promise<Occurrence>;
  /** Creates new occurrences in batch. */
  BatchCreateOccurrences(request: BatchCreateOccurrencesRequest): Promise<BatchCreateOccurrencesResponse>;
  /** Updates the specified occurrence. */
  UpdateOccurrence(request: UpdateOccurrenceRequest): Promise<Occurrence>;
  /**
   * Gets the note attached to the specified occurrence. Consumer projects can
   * use this method to get a note that belongs to a provider project.
   */
  GetOccurrenceNote(request: GetOccurrenceNoteRequest): Promise<Note>;
  /** Gets the specified note. */
  GetNote(request: GetNoteRequest): Promise<Note>;
  /** Lists notes for the specified project. */
  ListNotes(request: ListNotesRequest): Promise<ListNotesResponse>;
  /** Deletes the specified note. */
  DeleteNote(request: DeleteNoteRequest): Promise<Empty>;
  /** Creates a new note. */
  CreateNote(request: CreateNoteRequest): Promise<Note>;
  /** Creates new notes in batch. */
  BatchCreateNotes(request: BatchCreateNotesRequest): Promise<BatchCreateNotesResponse>;
  /** Updates the specified note. */
  UpdateNote(request: UpdateNoteRequest): Promise<Note>;
  /**
   * Lists occurrences referencing the specified note. Provider projects can use
   * this method to get all occurrences across consumer projects referencing the
   * specified note.
   */
  ListNoteOccurrences(request: ListNoteOccurrencesRequest): Promise<ListNoteOccurrencesResponse>;
}

export const GrafeasServiceName = "grafeas.v1.Grafeas";
export class GrafeasClientImpl implements Grafeas {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || GrafeasServiceName;
    this.rpc = rpc;
    this.GetOccurrence = this.GetOccurrence.bind(this);
    this.ListOccurrences = this.ListOccurrences.bind(this);
    this.DeleteOccurrence = this.DeleteOccurrence.bind(this);
    this.CreateOccurrence = this.CreateOccurrence.bind(this);
    this.BatchCreateOccurrences = this.BatchCreateOccurrences.bind(this);
    this.UpdateOccurrence = this.UpdateOccurrence.bind(this);
    this.GetOccurrenceNote = this.GetOccurrenceNote.bind(this);
    this.GetNote = this.GetNote.bind(this);
    this.ListNotes = this.ListNotes.bind(this);
    this.DeleteNote = this.DeleteNote.bind(this);
    this.CreateNote = this.CreateNote.bind(this);
    this.BatchCreateNotes = this.BatchCreateNotes.bind(this);
    this.UpdateNote = this.UpdateNote.bind(this);
    this.ListNoteOccurrences = this.ListNoteOccurrences.bind(this);
  }
  GetOccurrence(request: GetOccurrenceRequest): Promise<Occurrence> {
    const data = GetOccurrenceRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetOccurrence", data);
    return promise.then((data) => Occurrence.decode(_m0.Reader.create(data)));
  }

  ListOccurrences(request: ListOccurrencesRequest): Promise<ListOccurrencesResponse> {
    const data = ListOccurrencesRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "ListOccurrences", data);
    return promise.then((data) => ListOccurrencesResponse.decode(_m0.Reader.create(data)));
  }

  DeleteOccurrence(request: DeleteOccurrenceRequest): Promise<Empty> {
    const data = DeleteOccurrenceRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "DeleteOccurrence", data);
    return promise.then((data) => Empty.decode(_m0.Reader.create(data)));
  }

  CreateOccurrence(request: CreateOccurrenceRequest): Promise<Occurrence> {
    const data = CreateOccurrenceRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "CreateOccurrence", data);
    return promise.then((data) => Occurrence.decode(_m0.Reader.create(data)));
  }

  BatchCreateOccurrences(request: BatchCreateOccurrencesRequest): Promise<BatchCreateOccurrencesResponse> {
    const data = BatchCreateOccurrencesRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "BatchCreateOccurrences", data);
    return promise.then((data) => BatchCreateOccurrencesResponse.decode(_m0.Reader.create(data)));
  }

  UpdateOccurrence(request: UpdateOccurrenceRequest): Promise<Occurrence> {
    const data = UpdateOccurrenceRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "UpdateOccurrence", data);
    return promise.then((data) => Occurrence.decode(_m0.Reader.create(data)));
  }

  GetOccurrenceNote(request: GetOccurrenceNoteRequest): Promise<Note> {
    const data = GetOccurrenceNoteRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetOccurrenceNote", data);
    return promise.then((data) => Note.decode(_m0.Reader.create(data)));
  }

  GetNote(request: GetNoteRequest): Promise<Note> {
    const data = GetNoteRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetNote", data);
    return promise.then((data) => Note.decode(_m0.Reader.create(data)));
  }

  ListNotes(request: ListNotesRequest): Promise<ListNotesResponse> {
    const data = ListNotesRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "ListNotes", data);
    return promise.then((data) => ListNotesResponse.decode(_m0.Reader.create(data)));
  }

  DeleteNote(request: DeleteNoteRequest): Promise<Empty> {
    const data = DeleteNoteRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "DeleteNote", data);
    return promise.then((data) => Empty.decode(_m0.Reader.create(data)));
  }

  CreateNote(request: CreateNoteRequest): Promise<Note> {
    const data = CreateNoteRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "CreateNote", data);
    return promise.then((data) => Note.decode(_m0.Reader.create(data)));
  }

  BatchCreateNotes(request: BatchCreateNotesRequest): Promise<BatchCreateNotesResponse> {
    const data = BatchCreateNotesRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "BatchCreateNotes", data);
    return promise.then((data) => BatchCreateNotesResponse.decode(_m0.Reader.create(data)));
  }

  UpdateNote(request: UpdateNoteRequest): Promise<Note> {
    const data = UpdateNoteRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "UpdateNote", data);
    return promise.then((data) => Note.decode(_m0.Reader.create(data)));
  }

  ListNoteOccurrences(request: ListNoteOccurrencesRequest): Promise<ListNoteOccurrencesResponse> {
    const data = ListNoteOccurrencesRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "ListNoteOccurrences", data);
    return promise.then((data) => ListNoteOccurrencesResponse.decode(_m0.Reader.create(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
