// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.176.1
//   protoc               v3.21.12
// source: grafeas/v1/package.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { Digest, License } from "./common";

export const protobufPackage = "grafeas.v1";

/** Instruction set architectures supported by various package managers. */
export enum Architecture {
  /** ARCHITECTURE_UNSPECIFIED - Unknown architecture. */
  ARCHITECTURE_UNSPECIFIED = 0,
  /** X86 - X86 architecture. */
  X86 = 1,
  /** X64 - X64 architecture. */
  X64 = 2,
  UNRECOGNIZED = -1,
}

export function architectureFromJSON(object: any): Architecture {
  switch (object) {
    case 0:
    case "ARCHITECTURE_UNSPECIFIED":
      return Architecture.ARCHITECTURE_UNSPECIFIED;
    case 1:
    case "X86":
      return Architecture.X86;
    case 2:
    case "X64":
      return Architecture.X64;
    case -1:
    case "UNRECOGNIZED":
    default:
      return Architecture.UNRECOGNIZED;
  }
}

export function architectureToJSON(object: Architecture): string {
  switch (object) {
    case Architecture.ARCHITECTURE_UNSPECIFIED:
      return "ARCHITECTURE_UNSPECIFIED";
    case Architecture.X86:
      return "X86";
    case Architecture.X64:
      return "X64";
    case Architecture.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * This represents a particular channel of distribution for a given package.
 * E.g., Debian's jessie-backports dpkg mirror.
 */
export interface Distribution {
  /**
   * The cpe_uri in [CPE format](https://cpe.mitre.org/specification/)
   * denoting the package manager version distributing a package.
   */
  cpeUri: string;
  /**
   * The CPU architecture for which packages in this distribution channel were
   * built.
   */
  architecture: Architecture;
  /** The latest available version of this package in this distribution channel. */
  latestVersion:
    | Version
    | undefined;
  /** A freeform string denoting the maintainer of this package. */
  maintainer: string;
  /** The distribution channel-specific homepage for this package. */
  url: string;
  /** The distribution channel-specific description of this package. */
  description: string;
}

/**
 * An occurrence of a particular package installation found within a system's
 * filesystem. E.g., glibc was found in `/var/lib/dpkg/status`.
 */
export interface Location {
  /**
   * Deprecated.
   * The CPE URI in [CPE format](https://cpe.mitre.org/specification/)
   */
  cpeUri: string;
  /**
   * Deprecated.
   * The version installed at this location.
   */
  version:
    | Version
    | undefined;
  /** The path from which we gathered that this package/version is installed. */
  path: string;
}

/** PackageNote represents a particular package version. */
export interface PackageNote {
  /** The name of the package. */
  name: string;
  /**
   * Deprecated.
   * The various channels by which a package is distributed.
   */
  distribution: Distribution[];
  /**
   * The type of package; whether native or non native (e.g., ruby gems,
   * node.js packages, etc.).
   */
  packageType: string;
  /**
   * The cpe_uri in [CPE format](https://cpe.mitre.org/specification/)
   * denoting the package manager version distributing a package.
   * The cpe_uri will be blank for language packages.
   */
  cpeUri: string;
  /**
   * The CPU architecture for which packages in this distribution channel were
   * built. Architecture will be blank for language packages.
   */
  architecture: Architecture;
  /** The version of the package. */
  version:
    | Version
    | undefined;
  /** A freeform text denoting the maintainer of this package. */
  maintainer: string;
  /** The homepage for this package. */
  url: string;
  /** The description of this package. */
  description: string;
  /** Licenses that have been declared by the authors of the package. */
  license:
    | License
    | undefined;
  /**
   * Hash value, typically a file digest, that allows unique
   * identification a specific package.
   */
  digest: Digest[];
}

/** Details on how a particular software package was installed on a system. */
export interface PackageOccurrence {
  /** The name of the installed package. */
  name: string;
  /**
   * All of the places within the filesystem versions of this package
   * have been found.
   */
  location: Location[];
  /**
   * The type of package; whether native or non native (e.g., ruby gems,
   * node.js packages, etc.).
   */
  packageType: string;
  /**
   * The cpe_uri in [CPE format](https://cpe.mitre.org/specification/)
   * denoting the package manager version distributing a package.
   * The cpe_uri will be blank for language packages.
   */
  cpeUri: string;
  /**
   * The CPU architecture for which packages in this distribution channel were
   * built. Architecture will be blank for language packages.
   */
  architecture: Architecture;
  /** Licenses that have been declared by the authors of the package. */
  license:
    | License
    | undefined;
  /** The version of the package. */
  version: Version | undefined;
}

/** Version contains structured information about the version of a package. */
export interface Version {
  /** Used to correct mistakes in the version numbering scheme. */
  epoch: number;
  /**
   * Required only when version kind is NORMAL. The main part of the version
   * name.
   */
  name: string;
  /** The iteration of the package build from the above version. */
  revision: string;
  /**
   * Whether this version is specifying part of an inclusive range. Grafeas
   * does not have the capability to specify version ranges; instead we have
   * fields that specify start version and end versions. At times this is
   * insufficient - we also need to specify whether the version is included in
   * the range or is excluded from the range. This boolean is expected to be set
   * to true when the version is included in a range.
   */
  inclusive: boolean;
  /**
   * Required. Distinguishes between sentinel MIN/MAX versions and normal
   * versions.
   */
  kind: Version_VersionKind;
  /**
   * Human readable version string. This string is of the form
   * <epoch>:<name>-<revision> and is only set when kind is NORMAL.
   */
  fullName: string;
}

/** Whether this is an ordinary package version or a sentinel MIN/MAX version. */
export enum Version_VersionKind {
  /** VERSION_KIND_UNSPECIFIED - Unknown. */
  VERSION_KIND_UNSPECIFIED = 0,
  /** NORMAL - A standard package version. */
  NORMAL = 1,
  /** MINIMUM - A special version representing negative infinity. */
  MINIMUM = 2,
  /** MAXIMUM - A special version representing positive infinity. */
  MAXIMUM = 3,
  UNRECOGNIZED = -1,
}

export function version_VersionKindFromJSON(object: any): Version_VersionKind {
  switch (object) {
    case 0:
    case "VERSION_KIND_UNSPECIFIED":
      return Version_VersionKind.VERSION_KIND_UNSPECIFIED;
    case 1:
    case "NORMAL":
      return Version_VersionKind.NORMAL;
    case 2:
    case "MINIMUM":
      return Version_VersionKind.MINIMUM;
    case 3:
    case "MAXIMUM":
      return Version_VersionKind.MAXIMUM;
    case -1:
    case "UNRECOGNIZED":
    default:
      return Version_VersionKind.UNRECOGNIZED;
  }
}

export function version_VersionKindToJSON(object: Version_VersionKind): string {
  switch (object) {
    case Version_VersionKind.VERSION_KIND_UNSPECIFIED:
      return "VERSION_KIND_UNSPECIFIED";
    case Version_VersionKind.NORMAL:
      return "NORMAL";
    case Version_VersionKind.MINIMUM:
      return "MINIMUM";
    case Version_VersionKind.MAXIMUM:
      return "MAXIMUM";
    case Version_VersionKind.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

function createBaseDistribution(): Distribution {
  return { cpeUri: "", architecture: 0, latestVersion: undefined, maintainer: "", url: "", description: "" };
}

export const Distribution = {
  encode(message: Distribution, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.cpeUri !== "") {
      writer.uint32(10).string(message.cpeUri);
    }
    if (message.architecture !== 0) {
      writer.uint32(16).int32(message.architecture);
    }
    if (message.latestVersion !== undefined) {
      Version.encode(message.latestVersion, writer.uint32(26).fork()).ldelim();
    }
    if (message.maintainer !== "") {
      writer.uint32(34).string(message.maintainer);
    }
    if (message.url !== "") {
      writer.uint32(42).string(message.url);
    }
    if (message.description !== "") {
      writer.uint32(50).string(message.description);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Distribution {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDistribution();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.cpeUri = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.architecture = reader.int32() as any;
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.latestVersion = Version.decode(reader, reader.uint32());
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.maintainer = reader.string();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.url = reader.string();
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.description = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Distribution {
    return {
      cpeUri: isSet(object.cpeUri) ? globalThis.String(object.cpeUri) : "",
      architecture: isSet(object.architecture) ? architectureFromJSON(object.architecture) : 0,
      latestVersion: isSet(object.latestVersion) ? Version.fromJSON(object.latestVersion) : undefined,
      maintainer: isSet(object.maintainer) ? globalThis.String(object.maintainer) : "",
      url: isSet(object.url) ? globalThis.String(object.url) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
    };
  },

  toJSON(message: Distribution): unknown {
    const obj: any = {};
    if (message.cpeUri !== "") {
      obj.cpeUri = message.cpeUri;
    }
    if (message.architecture !== 0) {
      obj.architecture = architectureToJSON(message.architecture);
    }
    if (message.latestVersion !== undefined) {
      obj.latestVersion = Version.toJSON(message.latestVersion);
    }
    if (message.maintainer !== "") {
      obj.maintainer = message.maintainer;
    }
    if (message.url !== "") {
      obj.url = message.url;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Distribution>, I>>(base?: I): Distribution {
    return Distribution.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Distribution>, I>>(object: I): Distribution {
    const message = createBaseDistribution();
    message.cpeUri = object.cpeUri ?? "";
    message.architecture = object.architecture ?? 0;
    message.latestVersion = (object.latestVersion !== undefined && object.latestVersion !== null)
      ? Version.fromPartial(object.latestVersion)
      : undefined;
    message.maintainer = object.maintainer ?? "";
    message.url = object.url ?? "";
    message.description = object.description ?? "";
    return message;
  },
};

function createBaseLocation(): Location {
  return { cpeUri: "", version: undefined, path: "" };
}

export const Location = {
  encode(message: Location, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.cpeUri !== "") {
      writer.uint32(10).string(message.cpeUri);
    }
    if (message.version !== undefined) {
      Version.encode(message.version, writer.uint32(18).fork()).ldelim();
    }
    if (message.path !== "") {
      writer.uint32(26).string(message.path);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Location {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLocation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.cpeUri = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.version = Version.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.path = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Location {
    return {
      cpeUri: isSet(object.cpeUri) ? globalThis.String(object.cpeUri) : "",
      version: isSet(object.version) ? Version.fromJSON(object.version) : undefined,
      path: isSet(object.path) ? globalThis.String(object.path) : "",
    };
  },

  toJSON(message: Location): unknown {
    const obj: any = {};
    if (message.cpeUri !== "") {
      obj.cpeUri = message.cpeUri;
    }
    if (message.version !== undefined) {
      obj.version = Version.toJSON(message.version);
    }
    if (message.path !== "") {
      obj.path = message.path;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Location>, I>>(base?: I): Location {
    return Location.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Location>, I>>(object: I): Location {
    const message = createBaseLocation();
    message.cpeUri = object.cpeUri ?? "";
    message.version = (object.version !== undefined && object.version !== null)
      ? Version.fromPartial(object.version)
      : undefined;
    message.path = object.path ?? "";
    return message;
  },
};

function createBasePackageNote(): PackageNote {
  return {
    name: "",
    distribution: [],
    packageType: "",
    cpeUri: "",
    architecture: 0,
    version: undefined,
    maintainer: "",
    url: "",
    description: "",
    license: undefined,
    digest: [],
  };
}

export const PackageNote = {
  encode(message: PackageNote, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    for (const v of message.distribution) {
      Distribution.encode(v!, writer.uint32(82).fork()).ldelim();
    }
    if (message.packageType !== "") {
      writer.uint32(90).string(message.packageType);
    }
    if (message.cpeUri !== "") {
      writer.uint32(98).string(message.cpeUri);
    }
    if (message.architecture !== 0) {
      writer.uint32(104).int32(message.architecture);
    }
    if (message.version !== undefined) {
      Version.encode(message.version, writer.uint32(114).fork()).ldelim();
    }
    if (message.maintainer !== "") {
      writer.uint32(122).string(message.maintainer);
    }
    if (message.url !== "") {
      writer.uint32(130).string(message.url);
    }
    if (message.description !== "") {
      writer.uint32(138).string(message.description);
    }
    if (message.license !== undefined) {
      License.encode(message.license, writer.uint32(146).fork()).ldelim();
    }
    for (const v of message.digest) {
      Digest.encode(v!, writer.uint32(154).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): PackageNote {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePackageNote();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        case 10:
          if (tag !== 82) {
            break;
          }

          message.distribution.push(Distribution.decode(reader, reader.uint32()));
          continue;
        case 11:
          if (tag !== 90) {
            break;
          }

          message.packageType = reader.string();
          continue;
        case 12:
          if (tag !== 98) {
            break;
          }

          message.cpeUri = reader.string();
          continue;
        case 13:
          if (tag !== 104) {
            break;
          }

          message.architecture = reader.int32() as any;
          continue;
        case 14:
          if (tag !== 114) {
            break;
          }

          message.version = Version.decode(reader, reader.uint32());
          continue;
        case 15:
          if (tag !== 122) {
            break;
          }

          message.maintainer = reader.string();
          continue;
        case 16:
          if (tag !== 130) {
            break;
          }

          message.url = reader.string();
          continue;
        case 17:
          if (tag !== 138) {
            break;
          }

          message.description = reader.string();
          continue;
        case 18:
          if (tag !== 146) {
            break;
          }

          message.license = License.decode(reader, reader.uint32());
          continue;
        case 19:
          if (tag !== 154) {
            break;
          }

          message.digest.push(Digest.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PackageNote {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      distribution: globalThis.Array.isArray(object?.distribution)
        ? object.distribution.map((e: any) => Distribution.fromJSON(e))
        : [],
      packageType: isSet(object.packageType) ? globalThis.String(object.packageType) : "",
      cpeUri: isSet(object.cpeUri) ? globalThis.String(object.cpeUri) : "",
      architecture: isSet(object.architecture) ? architectureFromJSON(object.architecture) : 0,
      version: isSet(object.version) ? Version.fromJSON(object.version) : undefined,
      maintainer: isSet(object.maintainer) ? globalThis.String(object.maintainer) : "",
      url: isSet(object.url) ? globalThis.String(object.url) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      license: isSet(object.license) ? License.fromJSON(object.license) : undefined,
      digest: globalThis.Array.isArray(object?.digest) ? object.digest.map((e: any) => Digest.fromJSON(e)) : [],
    };
  },

  toJSON(message: PackageNote): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.distribution?.length) {
      obj.distribution = message.distribution.map((e) => Distribution.toJSON(e));
    }
    if (message.packageType !== "") {
      obj.packageType = message.packageType;
    }
    if (message.cpeUri !== "") {
      obj.cpeUri = message.cpeUri;
    }
    if (message.architecture !== 0) {
      obj.architecture = architectureToJSON(message.architecture);
    }
    if (message.version !== undefined) {
      obj.version = Version.toJSON(message.version);
    }
    if (message.maintainer !== "") {
      obj.maintainer = message.maintainer;
    }
    if (message.url !== "") {
      obj.url = message.url;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.license !== undefined) {
      obj.license = License.toJSON(message.license);
    }
    if (message.digest?.length) {
      obj.digest = message.digest.map((e) => Digest.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PackageNote>, I>>(base?: I): PackageNote {
    return PackageNote.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PackageNote>, I>>(object: I): PackageNote {
    const message = createBasePackageNote();
    message.name = object.name ?? "";
    message.distribution = object.distribution?.map((e) => Distribution.fromPartial(e)) || [];
    message.packageType = object.packageType ?? "";
    message.cpeUri = object.cpeUri ?? "";
    message.architecture = object.architecture ?? 0;
    message.version = (object.version !== undefined && object.version !== null)
      ? Version.fromPartial(object.version)
      : undefined;
    message.maintainer = object.maintainer ?? "";
    message.url = object.url ?? "";
    message.description = object.description ?? "";
    message.license = (object.license !== undefined && object.license !== null)
      ? License.fromPartial(object.license)
      : undefined;
    message.digest = object.digest?.map((e) => Digest.fromPartial(e)) || [];
    return message;
  },
};

function createBasePackageOccurrence(): PackageOccurrence {
  return {
    name: "",
    location: [],
    packageType: "",
    cpeUri: "",
    architecture: 0,
    license: undefined,
    version: undefined,
  };
}

export const PackageOccurrence = {
  encode(message: PackageOccurrence, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    for (const v of message.location) {
      Location.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    if (message.packageType !== "") {
      writer.uint32(26).string(message.packageType);
    }
    if (message.cpeUri !== "") {
      writer.uint32(34).string(message.cpeUri);
    }
    if (message.architecture !== 0) {
      writer.uint32(40).int32(message.architecture);
    }
    if (message.license !== undefined) {
      License.encode(message.license, writer.uint32(50).fork()).ldelim();
    }
    if (message.version !== undefined) {
      Version.encode(message.version, writer.uint32(58).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): PackageOccurrence {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePackageOccurrence();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.location.push(Location.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.packageType = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.cpeUri = reader.string();
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.architecture = reader.int32() as any;
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.license = License.decode(reader, reader.uint32());
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.version = Version.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PackageOccurrence {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      location: globalThis.Array.isArray(object?.location) ? object.location.map((e: any) => Location.fromJSON(e)) : [],
      packageType: isSet(object.packageType) ? globalThis.String(object.packageType) : "",
      cpeUri: isSet(object.cpeUri) ? globalThis.String(object.cpeUri) : "",
      architecture: isSet(object.architecture) ? architectureFromJSON(object.architecture) : 0,
      license: isSet(object.license) ? License.fromJSON(object.license) : undefined,
      version: isSet(object.version) ? Version.fromJSON(object.version) : undefined,
    };
  },

  toJSON(message: PackageOccurrence): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.location?.length) {
      obj.location = message.location.map((e) => Location.toJSON(e));
    }
    if (message.packageType !== "") {
      obj.packageType = message.packageType;
    }
    if (message.cpeUri !== "") {
      obj.cpeUri = message.cpeUri;
    }
    if (message.architecture !== 0) {
      obj.architecture = architectureToJSON(message.architecture);
    }
    if (message.license !== undefined) {
      obj.license = License.toJSON(message.license);
    }
    if (message.version !== undefined) {
      obj.version = Version.toJSON(message.version);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PackageOccurrence>, I>>(base?: I): PackageOccurrence {
    return PackageOccurrence.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PackageOccurrence>, I>>(object: I): PackageOccurrence {
    const message = createBasePackageOccurrence();
    message.name = object.name ?? "";
    message.location = object.location?.map((e) => Location.fromPartial(e)) || [];
    message.packageType = object.packageType ?? "";
    message.cpeUri = object.cpeUri ?? "";
    message.architecture = object.architecture ?? 0;
    message.license = (object.license !== undefined && object.license !== null)
      ? License.fromPartial(object.license)
      : undefined;
    message.version = (object.version !== undefined && object.version !== null)
      ? Version.fromPartial(object.version)
      : undefined;
    return message;
  },
};

function createBaseVersion(): Version {
  return { epoch: 0, name: "", revision: "", inclusive: false, kind: 0, fullName: "" };
}

export const Version = {
  encode(message: Version, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.epoch !== 0) {
      writer.uint32(8).int32(message.epoch);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.revision !== "") {
      writer.uint32(26).string(message.revision);
    }
    if (message.inclusive !== false) {
      writer.uint32(48).bool(message.inclusive);
    }
    if (message.kind !== 0) {
      writer.uint32(32).int32(message.kind);
    }
    if (message.fullName !== "") {
      writer.uint32(42).string(message.fullName);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Version {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVersion();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.epoch = reader.int32();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.revision = reader.string();
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.inclusive = reader.bool();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.kind = reader.int32() as any;
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.fullName = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Version {
    return {
      epoch: isSet(object.epoch) ? globalThis.Number(object.epoch) : 0,
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      revision: isSet(object.revision) ? globalThis.String(object.revision) : "",
      inclusive: isSet(object.inclusive) ? globalThis.Boolean(object.inclusive) : false,
      kind: isSet(object.kind) ? version_VersionKindFromJSON(object.kind) : 0,
      fullName: isSet(object.fullName) ? globalThis.String(object.fullName) : "",
    };
  },

  toJSON(message: Version): unknown {
    const obj: any = {};
    if (message.epoch !== 0) {
      obj.epoch = Math.round(message.epoch);
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.revision !== "") {
      obj.revision = message.revision;
    }
    if (message.inclusive !== false) {
      obj.inclusive = message.inclusive;
    }
    if (message.kind !== 0) {
      obj.kind = version_VersionKindToJSON(message.kind);
    }
    if (message.fullName !== "") {
      obj.fullName = message.fullName;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Version>, I>>(base?: I): Version {
    return Version.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Version>, I>>(object: I): Version {
    const message = createBaseVersion();
    message.epoch = object.epoch ?? 0;
    message.name = object.name ?? "";
    message.revision = object.revision ?? "";
    message.inclusive = object.inclusive ?? false;
    message.kind = object.kind ?? 0;
    message.fullName = object.fullName ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
