version: "2"

linters:
  default: standard
  enable:
    - revive
  disable:
    - staticcheck
    - errcheck
    - ineffassign
  settings:
    revive:
      rules:
        - name: var-naming
          disabled: true
        - name: redefines-builtin-id
          disabled: true
