version: "2"

linters:
  default: standard
  enable:
    - revive
  disable:
    - staticcheck
  settings:
    errcheck:
      exclude-functions:
        - (*google.golang.org/grpc.ClientConn).Close
    revive:
      rules:
        - name: package-comments
          disabled: true
