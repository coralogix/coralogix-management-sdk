version: "2"

linters:
  default: standard
  enable:
    - revive
  disable:
    - staticcheck
    - errcheck
  settings:
    revive:
      rules:
        - name: package-comments
          disabled: true
