syntax = "proto3";

package com.coralogixapis.dashboards.v1.ast.widgets.common;

enum Aggregation {
  AGGREGATION_UNSPECIFIED = 0;
  AGGREGATION_LAST = 1;
  AGGREGATION_MIN = 2;
  AGGREGATION_MAX = 3;
  AGGREGATION_AVG = 4;
  AGGREGATION_SUM = 5;
}
